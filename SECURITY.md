# 安全政策 · Security Policy

## 支持的版本

我们只会为**最新发布版本**提供安全更新。请先升级到最新版再报告问题。

| 版本 | 是否支持 |
| --- | --- |
| v4.2（最新） | ✅ |
| v4.1 及更早 | ❌ |

## 报告安全漏洞

**🔒 请不要通过公开 Issue 报告安全漏洞。**

公开披露会让所有用户在修复前暴露于风险之中。

### 推荐方式：GitHub 私密漏洞报告

1. 打开仓库的 **Security** 标签页
2. 点击 **"Report a vulnerability"**
3. 填写漏洞详情

直达链接：<https://github.com/755489724-lgtm/GazeScroll/security/advisories/new>

### 备选方式

如果无法使用上述方式，可以开一个**不含任何技术细节**的 Issue，
只说明"我发现了一个安全问题，请提供私密联系方式"，维护者会与你取得联系。

## 请在报告中包含

- **漏洞类型**（如权限绕过、信息泄露、注入等）
- **受影响的版本**
- **复现步骤**（越详细越好）
- **影响评估**：攻击者能做什么？需要什么前提条件？
- **概念验证代码**（如果有）
- **建议的修复方案**（如果有想法）

## 我们承诺

| 阶段 | 时限 |
| --- | --- |
| 确认收到报告 | **48 小时内** |
| 初步评估与回复 | **7 天内** |
| 修复并发布 | 视严重程度，尽快 |

我们会在修复发布后，**在致谢中署名**（除非你希望匿名）。

## 本项目的安全设计边界

为了帮助你判断哪些问题属于"安全漏洞"，这里说明本项目的设计边界：

### 本项目会做的事情

- 使用**前置摄像头**进行本地人脸检测（ML Kit，**完全离线，不上传任何图像**）
- 通过 **AccessibilityService** 注入滑动手势
- 读取当前前台应用包名，用于判断是否在目标 App 内
- 通过 ADB 一次性授予 `WRITE_SECURE_SETTINGS`，用于自动开启无障碍服务

### 本项目明确不做的事情

- ❌ 不上传任何图像、人脸数据或使用记录
- ❌ 不联网（除 Gradle 构建期依赖下载外，运行时无网络请求）
- ❌ 不申请 root 权限
- ❌ 不使用系统签名
- ❌ 不做任何跨应用的数据读取

### 特别关注的漏洞类型

以下问题我们**非常欢迎**你报告：

- **隐私泄露**：任何导致摄像头图像、人脸数据被上传或写入外部存储的路径
- **权限提升**：绕过预期的权限模型，获取超出必要范围的权限
- **注入越界**：手势注入范围超出预期，导致在非目标 App 中触发操作
- **无障碍服务滥用**：被其他应用利用来触发本服务的注入能力
- **依赖漏洞**：CameraX / ML Kit / Kotlin 依赖中的已知 CVE

### 已知的、不属于漏洞的行为

以下行为是**预期设计**，不是漏洞：

- ✅ 需要 ADB 授权 `WRITE_SECURE_SETTINGS`（这是 Android 的权限模型决定的，见 README）
- ✅ 无障碍服务能够注入手势（这是本项目的核心功能）
- ✅ 读取前台应用包名（用于按需启停，不记录、不上传）
- ✅ 使用 debug 签名发布 APK（测试版，正式版会换）

## 依赖安全

本项目使用的主要依赖：

| 依赖 | 版本 |
| --- | --- |
| CameraX | 1.3.4 |
| ML Kit face-detection（bundled） | 16.1.7 |
| Kotlin | 2.0.21 |
| Android Gradle Plugin | 8.6.1 |

如果你发现上述依赖存在影响本项目的安全漏洞，也请一并告知。

---

感谢你帮助保护本项目及其用户的安全。🙏
