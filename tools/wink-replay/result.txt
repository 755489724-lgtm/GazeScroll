=== WinkDetector v5.31 离线回放验证（真实代码 + 打印版 Log 桩）===

[1] v5.30 误触发回放（俯视眯眼：一只眼慢慢读低，22:03:09→16 的形状）
    [LOG Wink] wink 左眼 保持 630ms 但起手不合格 → 不触发 （闭之前只明确睁了 2400ms，需要 ≥600ms；从睁到深闭用了 -，需要 ≤350ms；min=0.36 other=0.99 dist=near）
  PASS  缓慢下滑的单眼低读数不触发

[2] v5.30 误触发回放（22:03:48 那次：两只眼先闭 2.68 秒，左眼睁开后又低 1 秒）
    [LOG Wink] wink 左眼 保持 660ms 但起手不合格 → 不触发 （闭之前只明确睁了 710ms，需要 ≥600ms；从睁到深闭用了 2680ms，需要 ≤350ms；min=0.16 other=0.99 dist=near）
  PASS  长闭眼之后的眼皮残留不触发

[3] 有意单闭（0.6 秒档）：睁着 → 一帧合上 → 保持 0.7 秒
  PASS  触发一次
  PASS  方向是左眼
  PASS  保持时长在 600~800ms
  PASS  起手合格（睁得稳）
  PASS  起手合格（闭得快）

[4] 一直闭着不重复调档（一次单闭只调一组）
  PASS  只触发一次
  PASS  睁眼再闭可以再来一次

[5] 正常眨眼（两只眼同时闭）不触发
  PASS  三连眨不触发

[6] 另一只眼没睁着 → 不触发
    [LOG Wink] wink 左眼 保持 630ms 但另一只眼没睁着（other=0.60 需要 > 0.70）→ 不触发 dist=near
  PASS  另一只眼不明确睁着时不触发

[7] 保持时长档位（0.4 秒档：闭 450ms 就应触发）
  PASS  0.4 秒档能触发

=== 结果：全部通过 ===
