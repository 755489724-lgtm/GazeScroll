# 万能翻页 · GazeScroll

> 用**前置摄像头**检测眨眼和点头/仰头，自动触发上滑翻页 —— 刷视频不用手。

[![Release](https://img.shields.io/badge/release-v5.39-blue)](../../releases/tag/v5.39)
[![Platform](https://img.shields.io/badge/platform-Android%208.0%2B-green)]()
[![License](https://img.shields.io/badge/license-MIT-lightgrey)]()

---

## 这是什么

一个 Android 免手操作工具。它用前置摄像头实时检测你的**眼部与头部动作**，识别到指定动作后，通过无障碍服务模拟一次「向上滑动」，从而让抖音、B站等短视频信息流自动翻到下一个视频。

**适用场景**：吃饭、做饭、躺着、手上有东西、戴手套 —— 任何不方便碰屏幕的时候。

---

## ⚠️ 当前痛点（求社区大佬支招）

**目前必须借助一次 ADB 授权才能真正用起来。** 这是本项目最大的体验短板，也是我最希望社区帮忙解决的地方。

具体来说：

1. **自动开启无障碍服务需要 `WRITE_SECURE_SETTINGS` 权限。**
   这个权限是 `signature|privileged` 级别，普通 App 无法直接申请，只能通过 ADB 手动授予一次。
   代码里已经做了「授权一次后永久记住」的逻辑（`AccessibilityBootstrap.repairIfNeeded`），
   所以**只需要授权一次**，之后重启、升级都不用再来。

2. **用 `UsageStatsManager` 判断前台 App 也需要特殊授权。** 在 MIUI / HyperOS 上，
   `GET_USAGE_STATS` 的 appop 会被系统直接拒绝（日志里能看到 `ignore; rejectTime=…`），
   所以前台检测退化为「无障碍窗口列表 + 窗口标题」的方案。

3. **部分系统（如 HyperOS）会过滤第三方 App 的无障碍事件**，只有 launcher / systemui 的事件能收到。

### 如果你知道更好的方案，非常欢迎提 Issue 或 PR 🙏

我特别想知道：

- 有没有**免 ADB** 的方式让用户自己开启自定义无障碍服务？
- 有没有不依赖 `UsageStatsManager` 的可靠前台 App 检测方案？
- HyperOS / 澎湃 OS 上有没有绕过无障碍事件过滤的正当途径？

> 说明：本项目**不会**尝试任何 root、系统签名或跨应用越权方案。所有实现都基于公开的 Android 官方 API。

---

## 已实现功能

### 触发方式
- **眨眼翻页** —— 可配置连眨 1 / 2 / 3 次触发（默认 2 次）
  - 双眼开合度阈值 0.55（针对**戴眼镜**场景调过）
  - **必须两只眼都低于阈值才算闭眼**（v5.29 起）；单眼低不再计入眨眼
  - 两次眨眼间隔需在 200–1500ms 之间
  - 单次闭眼超过 900ms 不算眨眼（防困倦 / 低头眯眼误计数）
- **点头 / 仰头翻页** —— 头部俯仰角超过 **8°** 且持续 **150ms**
  - 动态基准线：最近 45 帧的**中位数**（避免短促动作把基准带偏）
  - 支持下拉选择灵敏度（6° / 8° / 12°）
- **触发延迟（v4.8 起，实测有效）**：用相邻两帧的角度差算角速度，超过门限
  （俯仰 0.02°/ms、偏航 0.03°/ms，约 20°/秒）就**立即触发**，不再等「保持时间」。
  噪声撑不起 onset→阈值 这段行程、更达不到速度门限，所以快通道天然只放行真实动作。
  实测仰头延迟从 211~329ms 降到 **71~207ms**
  （`tiltUp triggered ... latency=71ms (fast, v=0.042°/ms)`）。
  保持时间同时也改为 `min(holdMs, motionWindowMs)`，永远不比对动作本身的要求更严格。
- **防误触（v4.8 起，v4.9 增强）**
  - **遮挡抑制，三条判据**（命中任一即抑制）：
    - `face lost` —— **脸突然消失又回来**。手掌/拳头整个盖住脸时人脸检测会直接丢失，
      这正是 v4.8 漏掉的路径（它只判「有人脸但没嘴」）。短暂丢失抑制 800ms，手指级
      遮挡抑制 500ms。
    - `mouth missing` —— 有人脸但读不出嘴的关键点（手挡下半脸）。
    - `face size jump` —— 人脸框尺寸一帧内变化 >35%（有东西贴上镜头）。
    日志：`occlusion detected (reason=face lost, faceLostFor=320ms) -> suppress 800ms`
  - **静止锁定 + 距离自适应（v4.9 引入，v5.5 调整作用对象）**：0.5 秒内俯仰/偏航峰峰值
    小于门限 → 认定人没在动。**门限本身按离手机多远缩放**：脸高占画面 ≥0.55（约 30cm）
    门限 ×2，≥0.38（约 50cm）门限 ×1.35。近距离时同样的微晃折算出的角度更大，固定门限挡不住，
    这就是「50cm 不误触、30cm 一动不动也误触」的成因。设置页可关闭。
    - ⚠️ **v5.5 修正**：静止锁定原本乘在**幅度阈值**上（×1.5），结果静止时点头要 9°~12°
      而不是 6°，用户反馈"点头很费劲"。现在它**改作用于速度门限**——
      噪声有幅度但没有速度，速度门限才是正确的工具；放大幅度只会把真实动作一起挡掉。
      缓存效果：动作幅度手感完全不变，静止时那点抖动过不了速度这一关。
  - **脸丢失即作废基准线**：150ms 就作废（以前 3 秒），手掠过脸一下不再污染基准线。
  - **距离档只有一个真值（v5.9）**：之前「近距离」有**两个**门槛 —— 点头省力增益用
    `faceRatio >= 0.50`，速度门限/静止峰峰值/静止硬锁定/眨眼收紧都用 `>= 0.55`。
    而用户 30cm 俯视实测落在 **0.45~0.55**，正好卡在两者之间，于是变成
    「**灵敏度按近距离给、防护按中距离算**」，而且档位在 0.50 附近**逐帧乱跳**
    （日志里 `nodBoost` 在 0.68/1.00、`distMode` 在 near/far 之间反复切）。
    现在只保留一个 `nearDistance`：`NEAR_ENTER_RATIO=0.46` / `NEAR_EXIT_RATIO=0.40`
    回差 + `faceRatio` 的 EMA 平滑，所有按距离缩放的东西全读它。
    门槛取 0.46 略低于实测区间下沿，保证 30cm 稳定判为近距离；50cm（0.37~0.41）与
    40cm（约 0.42~0.45）回不到近距离档，**远距离手感不变**。
    日志新增 `frSm`（平滑后的脸占比）与 `distance tier: …` 切换行。
  - **触发确认窗口：「v5.9 试过、v5.10 已撤销」（保留记录，避免重蹈）**
    v5.9 曾要求「阈值必须在最近 3 帧里至少 2 帧被越过」才允许触发，意图挡掉单帧跳变。
    带阶段标记的实机复测证明**必须撤销**：静止 90 秒里它一次都没派上用场
    （那段时间头部路径越阈值次数是 **0**，误触全部来自眨眼），却在正常使用中挡掉了
    **37 次**越阈值 —— 用户反馈「点头要更大的角度」「仰头点头要等 0.5 秒」「不像之前那么丝滑」。
    轻快的点头往往只跨一帧阈值，被它整个吃掉。
    **这是第三次踩同一个坑**（v5.5 的幅度放大、v5.7 的增益方向、本条的确认窗口）：
    凡是会连带拖慢真实动作的手段，都不能用来防误触。正确顺序是
    「先确定误触来自哪个检测器 → 再只动那一条链路」。
  - **误触到底从哪来（v5.10 实测定位）**：30cm 俯视静止 90 秒的误触**全部是眨眼**
    （诊断行 `lastTrigger=blink`，`blinks` 计数 90 秒内从 35 涨到 49），
    而两个叠加原因都很具体：① 设置里 `blinkTriggerCount` 实际是 **2**（以为是 3），
    于是任意两次自然眨眼间隔落在 0.2~1.5 秒内就翻页；② 近距离眨眼收紧没有生效
    （`BlinkDetector` 自己用 `faceRatio >= 0.55` 判断近距离，而 30cm 实测 0.49~0.51）。
    本版先把 ① 改成 3（零风险）；② 的收紧涉及「4 帧 ≈ 要求闭眼 235ms」，
    有挡掉有意眨眼的风险，留待下一版并用数据决定。
  - **最低速度门限（v5.0 引入，v5.5 叠加静止锁定与距离）**：即使幅度超过阈值，也必须走得够快
    （俯仰 ≥0.012°/ms、偏航 ≥0.015°/ms，再乘距离与静止系数）才允许触发。
    静止噪声**有幅度但没有速度**，所以被干净地挡掉；实测真实点头/仰头是 0.037~0.136°/ms，
    比门限高 3~11 倍，对正常动作零影响。这比抬高幅度阈值安全得多。
  - **幅度阈值不再被任何系数放大（v5.5 俯仰 / v5.8 偏航）**：
    `俯仰阈值 = 用户设定值 × 俯视增益`、`偏航阈值 = 用户设定值`。
    你设 6° 就是 6°，设"中 20°"就是 20°。诊断行的 `pitchTh` / `pitchThUp` / `yawTh`
    会显示当前实际生效值。
    - ⚠️ **v5.8 才把偏航轴也统一过来**：偏航阈值一直乘着距离系数，近距离
      （`faceRatio >= 0.45`）时**翻倍**——「中 20°」在 30cm 处实际是 **40°**，
      而用户的扭头幅度就是 40~52°，**刚好卡在门上**。实机日志：
      `turn triggered yaw=40.5°` 与 `yawTh=40.0°` 出现在同一时刻。
      这和 v5.5 在俯仰轴上修掉的是同一个错误：**用幅度阈值挡噪声是错的工具**，
      该管"够不够快"的是速度门限、该管"有没有在动"的是静止锁定。
      现在两个轴统一：幅度阈值就是用户设定值，距离只作用于速度门限与静止门限。
  - **扭头动作窗口（v5.8）**：`turnMotionWindowMs` 默认 **900ms**（原 600ms）。
    这个窗口对扭头先天太紧——扭头起点常有一段几乎不动的准备（脖子先转、脸还没跟上），
    这段被算进 rise，于是正常扭头会超窗被 `slow-rise` 判死（实机里这是最多的一类
    扭头拒绝：`yaw=54.4° speed=0.1496°/ms reason=slow-rise`）。
    "够不够快"交给速度门限，不由这个窗口定义。
  - **方向仲裁（v5.8）**：俯视姿态下扭头会带出 **±3~±10°** 的俯仰分量，
    而近距离点头阈值只有 **4.1°**（灵敏度 6°），所以扭头必然先把俯仰判定打穿——
    表现为「想左右扭头，结果变成上下滑」。远距离俯仰阈值是 6.0°，同样的分量常常够不到，
    所以远距离正常。
    判据**只看角速度**：真正的扭头是绕 Y 轴转动，偏航角速度必然明显；
    真正的点头是绕 X 轴转动，偏航角速度几乎为零。
    `|偏航速度| >= 0.006°/ms`（实测真实扭头中位数 0.0126、p90 0.158）时，
    点头/仰头一律让位；反向也对称地做了。
    - ⚠️ **仲裁绝不能用幅度当判据**。第一版加了 `yaw >= 阈值×0.6` 兜底，结果**把合格的
      扭头挡掉了**（`yaw=30.7° speed=0.2481°/ms reason=pitch-dominant-arbitration`）：
      扭头时偏航幅度本来就大，"幅度大"在这里恰恰证明是扭头，不是俯仰更强。
    - ⚠️ 仲裁必须用**本帧**的偏航读数。俯仰先于偏航评估，读上一帧会让耦合检测失灵
      （第二版就是这么错的）。见 `yawVelocityWith`。
    - ⚠️ **v5.11 收紧：光有 0.006°/ms 这一条绝对门限不够。** 30cm 处偏航读数的正常抖动
      （±1~3°）在实测约 11fps 下折算出来就有 0.01~0.03°/ms，轻松越线，于是"我在扭头"被
      误判。实机日志里用户明确在点头的那几秒，**连续 7 次点头被拒**，最后要用 −17.3° 的
      大幅点头才成功；阶段④ 一共 **39 次**俯仰候选被这条规则拒掉（点头 19、仰头 18），
      是最大的一类拒绝。现在要求三条同时成立：**① 偏航在动**（原门限）、
      **② 偏航速度 ≥ 俯仰速度**（"占优"的字面含义）、**③ 偏航幅度 ≥ 扭头阈值×0.3**。
      ②③ 是**闸门**而非替代：真实扭头（23~45°、0.13~0.25°/ms）照样让位，
      姿态抖动（±1~3°、0.01~0.02°/ms）不再误伤点头。
    - ⚠️ **v5.28：上面三条判据全部依赖"有符号偏航"，而它会因为基准线重学而塌掉。**
      实机日志（20:47:37）同一帧里**扭头通道看到 29.5°、俯仰通道只看到 1.5°** ——
      用户正在往右扭头，俯仰侧却认为"没有扭头"而直接触发，于是**想要 RIGHT 却得到 UP**。
      现在增加一条**与基准线无关**的判据：**原始偏航**在 500ms 内的**摆幅 ≥ 12°**
      （实测真扭头摆 25~59°、真点头/仰头只摆 1.7~10.4°）→ 俯仰一律让位，理由
      `yaw-swing-arbitration`。它放在 onset 声明**之前**：扭头期间俯仰通道连动作都不开始记，
      否则扭头一结束，那个"已经越阈值"的残留姿势会立刻补一次上下滑。
    - ⚠️ **v5.28：扭头通道同样要给"强扭头"豁免闭眼门。** 实测 25 次扭头候选里 **12 次**
      死在 `eyes-unreliable` 上（扭头会把眼皮挤成半闭，日志里 `eyeL=0.18`），
      于是"在扭头"和"没反应"同时发生。与俯仰侧同一条规则：幅度 ≥ 阈值 + 2.0° 时不据此否决。
  - **回中锁定（v5.0 引入，v5.2 修复致命 bug）**：一次动作触发后，**反方向**要等头部回到
    中性区并稳定 150ms 才放行。修的是「仰头触发上滑后，把头放回正常位置的过程被判成点头，
    于是又下滑」——仰头时俯仰角为正，回正会经过 0 继续变负，天然长得像一次反向动作。
    - ⚠️ **v5.1 的这个锁会永久卡死**：中性区是相对基准线的 `±阈值×0.4`（默认 ±2.4°），
      而人的自然姿势常常长期偏离基准线（实测：俯仰角 -2.85°、基准线 3.69°，偏差 6.5°），
      **永远进不了中性区** → 锁永不解除 → 反向的点头被无限期屏蔽。
      用户反馈「仰头好用了，但点头全失效了」就是这个原因。
    - 修复：加 **400ms 最长锁定兜底**（回正动作本身只要几百毫秒），并让锁在头部静止时也能
      正常解除。日志会说明是哪条路径解除的。
    - ⚠️ **v5.27：这条 400ms 兜底本身又成了误触来源** —— "锁满 400ms 就**无条件**解锁"
      完全不看头在哪、也不看还在不在动：仰头触发后 400ms 头还在 16° 高位就已解锁，
      随后"把头放回来"的那次 18° 下行回摆穿过中性区时又把 150ms 计时刷满
      → 回摆末段被判成一次新的点头（实机 20:30:42.551 就是这样多翻了一页）。
      现在改成：解除必须先满足**不在动**（速度 < 当帧速度门限），再看
      「回到中性区并稳定 150ms」**或**「头就停在自己当前的姿势上（`|signed − settledPitch| ≤ 中性区`，
      姿势偏置的出口，替代原来那条兜底的职责）」，另加 **3 秒硬上限**（只需"停住"）保证不可能永久屏蔽。
  - **姿势偏置自动校正（v5.2）**：偏差越过死区、但始终没到阈值、且**持续 1.5 秒**时，
    判定为「换了姿势」而不是动作，把基准线渐进挪过去。
    这修的是 30cm 误触的**根本原因**：姿势缓慢变化（靠椅背、凑近屏幕）时滑动窗口中位数
    基准线跟不上，偏差被永久保留，白吃掉阈值余量——实测偏差 6.5° 正好贴在 ±6° 阈值上，
    静止噪声轻轻一推就越界。判据很干净：有意动作在 500ms 内就结束了，能维持 1.5 秒的是姿势。
  - **俯视姿态补偿（v5.4）**：大多数人是**俯视**看手机（手机在下方），俯仰角长期偏负。
    基准线若不跟着走，这个偏移会被当成"一直在低头"，于是往下动一点就误触、往上一动却
    不灵敏。现在相对基准线偏负 ≥3° 且**持续 2 秒**，就把基准线渐进迁到当前姿态，
    使后续点头/仰头以"当前俯视姿态"为中性点。
    判据基于**相对基准线**的偏移而非绝对角度，所以倒着拿手机、躺着看同样适用；
    只在俯仰轴生效，不碰扭头与眨眼。
    日志：`posture: looking down detected (held 2100ms, relative -6.2°), baseline gradually shifted to -14.8°`
  - **近距离点头增益（v5.6 引入，v5.7 收窄为单向，v5.12 增加俯视档）**：增益分三档，
    **仰头方向一律不动**：
    | 条件 | 系数 | 阈值（灵敏度设 6.0°） |
    |---|---|---|
    | 近距离 **+ 俯视** + 低头 | ×0.42 | **2.5°**（v5.12 引入，v5.14 再降一档并稳定） |
    | 近距离 + 低头 | ×0.68 | 4.1°（v5.6） |
    | 其他（含全部远距离、仰头方向） | ×1.00 | 6.0°（不变） |
    离手机近时（`faceRatio >= 0.50`，实测约 30~40cm）同样的颈部动作折算出的俯仰角更小，
    不补一点会明显更费力。**远距离完全不受影响** —— 新增档要求「近距 + 俯视」两条同时成立。
    - **v5.12 依据**：用户要的"轻点头"实测幅度是 **3.7~4.4°**，而 4.1° 的阈值卡在门上
      （`nodDown rejected: pitch=-3.7° threshold=4.1° reason=below-threshold`），
      所以"有时好使有时不好使"。压到 3.0° 后留出 0.7~1.4° 余量。
    - ⚠️ **v5.7 修正**：v5.6 把系数乘在了**共用的俯仰阈值**上，**仰头方向也一起吃到了**，
      于是近距离仰视被误触（灵敏度设 8° 时仰头只要 5.4°，设 6° 时只要 4.1°；
      实机日志里 45 次 tiltUp vs 14 次 nodDown，而被动仰视幅度正好是 4~7°）。
      现在改成**严格单向**：只在**低头方向**生效，抬头方向回到用户设定值。
      这正好实现「被动仰视不触发，主动仰头才触发」——被动仰视 4~7° 够不到 8°，
      主动仰头 9~11° 照样过。
      **不能用"抬高仰头阈值"来修**：那会把主动仰头一起挡掉。
    只压**幅度**阈值：速度门限、静止锁定、近距离静止硬锁定一律不动，
    所以"噪声有幅度没有速度"这道关卡照旧，**30cm 静止防误触逻辑未被触碰**。
    诊断行里 `pitchTh` 是**当前方向**的阈值、**`pitchThUp`** 是仰头方向的阈值：
    近距离看到 `pitchTh=5.4° pitchThUp=8.0°` 就说明单向增益生效。
    `nodBoost=0.68 nodBoostActive=true` 表示已生效（`nodBoost` 打的是**这一帧实际乘上去的
    系数**，与同一行的 `pitchTh` 永远自洽），触发日志会带 `boosted`。
    `distMode=near/far` 是增益的触发条件；别与同一行的 `dist=近/中/远`（握持距离档）混淆。
  - **晃动过滤：路径效率（v5.12）**：阈值压低之后必须挡住晃动（用户点名"地铁上近距离俯视
    晃动然后误触"）。判据是 `效率 = |净位移| / Σ|相邻差值|`：
    ```
    轻点头   0 → 1.0 → 2.5 → 3.5 → 4.2 → 4.2    路径 4.2   净位移 4.2   效率 1.00  ✅ 放行
    晃动   -1.5 → 1.2 → -1.0 → 2.8 → -1.2 → 3.2  路径 17.1  净位移 4.7   效率 0.27  ⛔ 拦下
    ```
    窗口 6 帧（约 0.5~0.7 秒），效率 <0.45 判为晃动。之所以不数"方向反转次数"：
    效率把幅度与次数合成一个量，少一个参数，对低频大幅晃动同样有效。
    效率值在诊断行 `shake=` 与候选拒绝日志里都能看到。
    - ⚠️ **它挡不住"急停/急刹"**：那一下是**单向**的，路径效率接近 1。
      v5.12 实测那次晃动误触就是 `tiltUp 8.1°`，效率判据没触发 —— 所以需要下面的加速度计。
  - **手机自身运动闸门（v5.13 加速度计，v5.14 加陀螺仪）**：`PhoneMotionMonitor` 读
    **陀螺仪 + 加速度计**（加速度优先 `TYPE_LINEAR_ACCELERATION`，没有就退回
    `TYPE_ACCELEROMETER` + 低通减重力），角速度 ≥ **0.6 rad/s** 或线性加速度 ≥ **2.5 m/s²**
    即判定"手机在动"，之后 **500ms** 内所有俯仰/偏航候选一律不接受（理由 `phone-motion`）。
    物理依据：**点头是绕脖子转头（手机几乎不动），急停/急刹/手晃是手机自己在动**。
    摄像头分不出这两者（角度信号几乎一样），传感器能。
    - ⚠️ **v5.14 为什么必须加陀螺仪**：v5.13 只用加速度计，实机复测证明**抓不到轻晃** ——
      整场会话线性加速度峰值最高只到 **2.3 m/s²**、一次都没触发。原因是**手晃手机主要是"转"
      而不是"平移"**：绕手腕转动时传感器处的直线加速度可以很小，角速度却很大。
      而这正好把两件事分开：点头时手机角速度 ≈ 0，手晃/地铁摇摆/急刹时 0.6~10 rad/s。
    - 门限依据：坐着刷 加速度 **0~0.5 m/s²** / 角速度 **0.05~0.2**；走路手持 **1.5~3** / **0.3~1.0**；
      急刹与手晃 **2.5~15** / **0.6~10**。
    传感器不可用时**失败开放**（`isMoving()` 恒 false），绝不因为读不到传感器就废掉手势。
    峰值在诊断行 `accel=` / `gyro=`，状态在 `phoneMotion=`，可用性在 `accelReady=` / `gyroReady=`。
  - **闭眼后的姿态不可信期（v5.14）**：`eyeDip`（本帧有眼睛跌破闭眼阈值，**含单帧浅闭**）
    之后 **120ms** 内不接受俯仰/偏航候选（理由 `eyes-unreliable`）。
    依据是用户报的那次误触的完整因果链：
    ```
    03:44:00.020  I/Blink: closure rejected: 63ms frames=1/2 minEye=0.98/0.14   ← 单帧浅闭
    03:44:00.127  I/HeadPose: nodDown triggered pitch=-3.5°  ← 一帧从 +0.1 跳到 -3.5
    ```
    ML Kit 的头部姿态靠**关键点回归**，眼睛闭上时眼部关键点消失，那一帧的姿态估计会跳。
    注意元凶是 `frames=1/2` 的一帧浅闭 —— 它永远不会被确认成"眨眼"，所以只盯 `eyesClosed`
    抓不到，必须用"本帧是否跌破阈值"这个口径。120ms ≈ 一帧多（实测帧间隔 63~116ms）。
    窗口在 v5.16 收到 **50ms**（只跳过闭眼那一帧），v5.27 起**只对边缘候选生效**：
    幅度越过"阈值 + 2.0°"的强候选不再被它否决 —— 因为**仰头本身就会把眼皮挤成半闭**
    （v5.26 实测连续 4 帧 `eyeDip`，一次 17.2° 的仰头被整整 1.3 秒拦住没触发）。
    安全性依据：已知最大的单帧跳变噪声是 **3.5°**（就是上面那次眨眼误触），
    而近距离俯视的强候选门槛是点头 4.5° / 仰头 5.8°，都在它之上。
    v5.28 起**扭头也适用同一条豁免**（强扭头 = 越阈值 + 2.0°）：实测 25 次扭头候选里
    12 次死在这条门上，而扭头本身就会把眼皮挤成半闭。
  - **突然性 / 位移判据（v5.28，仅近距离档）**：用户要求「点头和扭头的判定是**突然性**的，
    不是快速扭头点头的时候就不触发」，以及「晃动时产生的误触」。两条独立的判据：
    - `no-travel`：判定域里从**运动起点**（onset 那一帧的上一帧取值）到当前必须走过
      `0.5 × 当帧阈值`。清掉"姿势本来就压在阈值上、轻微晃动就触发"的一类 ——
      实测触发时的原始位移只有 **0.6° / 0.7° / 1.3°** 却都翻了页。
      位移必须在**同一个域**里比（轻通道 = 相对运动起点的位移，其余 = 相对基线量），
      域不一致时跳过判据（v5.17 的"域混用"把方向整体翻过一次，这里专门防呆）。
    - 动作窗口收紧：起点(0.4×阈值) → 第一次越阈值，俯仰 **300ms**、扭头 **400ms**
      （远处仍是用户设置的 500ms / 900ms）。实测真实动作的对应延迟是 53~249ms（俯仰）
      与 65~217ms（扭头），所以这两条只砍慢动作。
      ⚠️ **没有**拿"触发延迟"当判据：延迟里包含门控等待（远距离那两次 575/624ms
      其实是很快的仰头，只是被闭眼门等了 2~3 帧），拿它当"慢"的证据会误杀真实动作。
  - **轻点头速度通道（v5.13）**：近距离 + 俯视 + 低头时，速度门槛从
    `FAST_PITCH_VELOCITY`（0.02）降到 **0.013**，并要求**多撑过一帧**（60ms，
    实测帧间隔 63~116ms ⇒ 等价于"下一帧仍须在阈值之上"），所以单帧跳变依旧进不来。
    依据：用户轻点头实测 **0.014~0.018°/ms**，够不到 0.02 就落到"保持"通道，
    而那条通道要求速度 ≥ 近距离门限 `0.024`（比 0.02 还高）—— **实际上永远走不通**，
    所以"不够快的点头一律无效"，日志里表现为 `reason=hold-not-met` 连着刷。
    通道条件与 ×0.50 增益完全一致，远距离与仰头方向不受影响。
  - **俯视判据：基准俯仰角（v5.12）**：`baselineDeg`（静止时人脸相对摄像头的绝对俯仰中位数）
    实测 **30cm 俯视 7.8~14.4、50cm 平视 0~2**，分离干净，且不需要任何新传感器 ——
    它本来就是基线学习中位数的输出。`posture=down/flat` 现在反映它。
  - **为什么"俯视"没有接进灵敏度开关（v5.6，实测结论）**：原本设想「下巴占比大 = 俯视」，
    用 `|下巴Y - 眼中心Y| / 脸高`（`chinRatio`）作判据。实机标定（平视/俯视/近距离俯视各 20 秒）后：
    远距 `chinRatio 0.335~0.368`，近距 `0.368~0.432` —— **它主要在反映距离，而不是姿态**，
    区分度不足以驱动灵敏度开关（原定的 0.68 门限从未达到）。
    所以增益最终**只用距离驱动**，俯视几何只作诊断保留（`chinRatio` / `chinMed` / `posture=`），
    等拿到"同距离下只改姿态"的干净数据再考虑启用。
    **不把区分度不足的信号接到灵敏度开关上**，是本条最重要的决定。
  - **近距离静止硬锁定（v5.3）**：faceRatio ≥0.55 且连续 1.2 秒内俯仰、偏航、眼睛开合度
    都没有实质变化 → 判定"人没在动、画面在抖"，关掉全部翻页判定，直到出现明显动作。
    判据刻意不用"方差"而用"稳定时长"，语义直观、用户可验证。
    日志：`staticHardLock=true, reason=close-distance stillness`
  - **眨眼判定按距离收紧（v5.3 设计，v5.11 才真正生效）**：ML Kit 的 `eyeOpenProbability`
    是**绝对**读数、不随脸的大小变化，但离得越近，睫毛/镜片反光/轻微眯眼的遮挡越明显。
    所以近距离时同时**降低闭眼阈值**（默认 0.55 → 中距 0.41 → 近距 0.30）并**提高连续帧要求**。
    只做一件事不够：阈值降太多会漏掉真实眨眼，而真实眨眼在近距离下持续帧数也更长，
    所以提高帧数对真实眨眼几乎无损。
    - ⚠️ **v5.11 之前它一直在空转**：`BlinkDetector` 用的是**自己**的 `faceRatio >= 0.55`，
      而用户 30cm 实测只有 **0.49~0.51**，所以从未进入近距离档（诊断行
      `blinkBelow=0.41 blinkFrames=2`）。实测静止 90 秒里的误触**全部**是 `lastTrigger=blink`
      —— 这就是「什么都没做视频自己跳」的来源。现在它读 **v5.9 那个统一的距离档**。
    - 连续帧要求取 **3 帧**（不是 v5.3 设计的 4 帧）：实测帧间隔 63~116ms（约 11fps），
      4 帧 ≈ 要求连续闭眼 360ms 以上，可能连**有意**眨眼一起挡掉；3 帧 ≈ 270ms 留一档余量。
      如果觉得"有意眨眼也变难了"，第一个要退的就是这个值。
  - **逐次眨眼诊断（v5.11）**：每次被计数的眨眼都打一行
    `I/Blink: blink #12 closure=280ms minEye=0.04/0.07 below=0.30 needFrames=3 gap=940ms run=2/3`
    —— 闭眼持续多久、眼睛最深读到多少、当前阈值与帧数、与上次的间隔、累计到几次。
    这是区分「有意眨眼」和「不由自主的眨眼」唯一可靠的依据。
  - **上一次翻页是谁触发的（v5.3）**：诊断行新增 `lastTrigger=blink/nod/tilt/turnL/turnR`
    与 `blinkBelow` / `blinkFrames`（当前实际生效的阈值与帧数要求）。
    排查"静止疯狂误触"时这是第一个要看的东西——只看触发次数无法区分来源。
  - 诊断行会打印 `faceRatio` / `dist`（近/中/远）/ `staticRange` / `staticLock` /
    `staticHardLock` / `recenterLock` / `downGaze` / `biasRecenter` / `occl` / `suppressMs` /
    `mouthForced` / `mouthRejected` / `lastTrigger` / `blinkBelow` / `blinkFrames` /
    `pitchTh` / **`pitchThUp`**（当前方向 / 仰头方向的实际生效阈值，v5.7）/ `yawTh` / `speedGate` /
    **`yawNow`** / **`pitchNow`** / **`pitchYielded`**（实时读数与仲裁结果，v5.8）/
    `chinRatio` / `chinMed` / `posture`（俯视几何，v5.6）/ `nodBoost` / `nodBoostActive` / `distMode`。

### 恢复能力（「隔一会重开抖音不触发」的防线，v5.3 起逐版加固）

这套失效最难对付的地方在于：**整条恢复链都挂在 `targetActive` 这一个布尔值上**，
它一旦错成 `false`，相机、看门狗、liveness 探针会被同一个条件一起挡住，谁也不去纠正它。

- **主动存活探针（v5.3）**：不再依赖"前台包名变化"事件，每 800ms 检查一次
  "允许翻页、但流水线实际是死的"并强制重新武装。下拉状态栏之所以"有效"，
  是因为它人为制造了两次包名变化替我们触发通知——现在不等用户动手。
- **电源状态自校正（v5.4）**：每 800ms 用 `PowerManager.isInteractive` 比对
  `screenActive`。HyperOS 可能丢掉息屏/亮屏广播，一旦丢了这个标志会永久停在错值上，
  而 `shouldAnalyze()` 会因此恒为 false。
- **`wasStale` 强制重绑（v5.6）**：不再信任 `cameraBound` 标志位，
  只要"很久没有帧"就强制重绑。
- **前台复核 fail-open（v5.7）**：**不信任自己记的前台**，直接向无障碍服务再问一次
  "现在活动的窗口是谁"；若它明确是目标应用、我们却认为不是且持续 2 秒，就强制重算。
  这是唯一一条不经过 `targetActive` 的恢复路径。
- **持续无画面硬重建（v5.7）**：用**独立于重绑**的计时（只在真的收到帧时清零）量
  "到底多久没画面"，持续 12 秒就丢弃 CameraX provider 重新获取。
  单靠每秒重绑是升不了级的——它每次都会把证据清零。
- **无障碍强制重绑（v5.5 引入，v5.7 改为按持续时长升级）**：`isConnected()` 只是
  `instance != null`，**实例存在不等于连接可用**。开关开着却超过 10 秒拿不到实例就直接
  `forceRebind`（关掉再打开），等价于用户手动下拉状态栏的效果。
- **全链路自检（v5.5）**：每 30 秒 + 每次亮屏打一行 `GazeSelfCheck`，一行说清卡在哪一环。
  v5.7 起还带 `a11yEvents` / `a11yEventAgoMs`（**系统到底有没有把窗口事件发给我们**）、
  `hardResyncs` / `noFrameForMs`。
  这几个字段的关键在于能区分两个完全不同的病因：
  **`a11yEvents` 停涨 = 系统根本没发事件（只能靠轮询兜底）；
  计数在涨但前台判错 = 我们自己的判据有问题。** 只看 `foreground=` 是分不出来的——
  那是我们对事件的*理解*，不是事件本身。
- **日志标记**：`foreground change: A -> B (target=…)`、
  `rebind reason=user-present/app-switch/no-frame`、
  `pipeline resync (app-switch): forcing rebind`、
  `rebind reason=user-present -> ACTION_USER_PRESENT full restart`。
- **张嘴点击的健壮性（v5.0）**
  - **基准合理性检查**：手挡脸、半张脸出画会读出极小的比值（实测抓到 **0.114**，正常
    闭嘴是 0.205~0.23）。一旦它进了基准窗口，基准就被永久带偏 —— 之后所有正常读数都比
    它高出阈值，于是检测器**一直以为用户在张嘴**，表现为「功能要开关一次才能用」。
    现在不合理的样本直接不采纳（`mouthRejected` 计数可见）。
  - **卡死自愈**：连续判定"张嘴"超过 4 秒（没人能张这么久）就判定基准坏了，强制重学
    （`mouthForced` 计数可见）。
  - **遮挡后重学**：遮挡判定命中时连张嘴检测器一起复位。
  - **点击失败也计数**：以前只有注入成功才 +1，所以界面上「已点击 0 次」既可能是没识别
    到张嘴、也可能是识别到了但注入失败 —— 两种问题排查方向完全不同，现在分开显示并
    在通知里给出后端名。
  - 日志：`mouth open detected ... -> tap, rearm after mouth closes` /
    `mouth rearmed: closed again, ready for next tap`
- **张嘴 → 点击屏幕中央（v4.6 新增，默认开启）**
  - 张嘴一次 = 在屏幕中央注入一次单击。抖音这类全屏播放器收到单击就是**暂停 / 播放视频**
  - **不暂停本 App**：张嘴之后眨眼、点头、左右扭头照常翻页
  - 判定量是「嘴到鼻底的距离 ÷ 人脸高度」，并**按你本人自然闭嘴的水平自动校准**
  - 灵敏度三档 5% / 8% / 12% 脸高
  - **防连发**：必须「闭嘴之后再次张嘴」才算下一次，一直张着嘴只会点一下
- **歪头 → 音量加 / 减（v5.35 新增，取代 v5.30~v5.34 的「单眼闭眼」，默认开启）**
  - 把头往一边歪（像把耳朵压向肩膀），**歪到设定角度并坚持住** —— 音量调高或调低
    （**一次调 1（默认）/ 2 / 3 / 5 档**，1 档 = 按一次音量键）
  - **默认方向**：左歪头 = 调高，右歪头 = 调低；设置页有**两个独立的反方向开关**，
    左右两边可以各自反过来拨
  - **灵敏度 · 触发角度**可选 10 / **13（默认）** / 16 / 20°。倾斜角是**相对你本人平时的头姿**算的
    （最近几秒滚转角的中位数当基准线），所以天生歪着头看手机也不会误触发
  - **灵敏度 · 保持时间**可选 0.2 / **0.3（默认）** / 0.5 / 0.8 秒 —— 用户要求「判断方式可以久一点」，
    所以要求歪到角度后再坚持住，抖一下头、甩头发不会触发
  - **防连发 + 全局两秒（v5.36）**：一次歪头只调一次，必须**歪回中间并停稳 0.4 秒**才算下一次；而且**任何动作（翻页 / 调音量 / 张嘴点击）之后都强制等满 2 秒**，动作的**起手**必须晚于这个时刻 —— **没有提前量**（卡在 1.99 秒那一下不算，必须回正后重新歪）。反过来，调完音量后 2 秒内也不翻页
  - **歪头期间暂停翻页判定**：歪头会让俯仰读数串扰几度，而近距离俯视的点头阈值只有 2.5° ——
    不暂停就会「歪头顺带翻一页」（v5.34 日志里 22:38:54 / 22:38:56 两次 swipe UP 就是这么来的）；
    歪回中间后自动重新对齐头部基准线
  - 多档是**一步跳到目标值**（先量出系统一档等于几个索引），不会连着弹好几次音量面板
  - **控制指令**：不走防误触冷却（既不占用冷却、也不会被冷却挡住），
    也不需要无障碍 / Shizuku —— 音量由系统音频通道直接调
  - 为什么换掉「单眼闭眼」（四轮实机数据）：ML Kit 的睁眼概率在这台设备/这个姿势下**分不开**
    "有意单闭"与"低头眯眼" —— 绝对阈值试过 0.70/0.65/0.55，合眼快慢（onset）真单闭 55~1732ms
    与误触 1.6~3.2s 完全重叠；改用时间域判据（最近闭眼占比）后又只能挡住"反复读低"、
    挡不住低头刚开始那一下。用户因此决定换手势：**歪头（roll）** 是一条从未使用过、且
    与俯仰/偏航正交的轴。
- **上下滑动自适应（v4.5）+ 柔度可调（v4.6）**
  - 抖音是「一屏一个视频」，微博 / 小红书是连续列表，同样幅度的手势在前者刚好、后者会滚过好几屏
  - 按当前前台应用自动切换幅度与时长：
    | 应用 | 滑动距离 | 时长 |
    | --- | --- | --- |
    | 抖音 / 快手等整屏视频 | **39% 屏高** | **150ms** |
    | 微博 / 小红书 / 知乎 / B站等列表 | **可调**，默认 26% 屏高 | 420ms |
    | 其他 | 40% 屏高 | 300ms |

  - ⚠️ **抖音那一档是 v5.0/v5.1 实机逐组标定出来的**，不是"一屏一个视频就划一整屏"想当然。
    90%/130ms、60%/400ms、45%/350ms、50%/700ms 全部无效，**只有 39%/150ms 被接受**。
  - **手势路径必须有中间点（v5.1）**：`dispatchGesture` 的一条直线相当于"一个采样点从起点
    直接跳到终点"，很多 App 的滚动手势识别看不到足够的坐标更新就把它忽略了 —— 现象正是
    「日志 `swipe ok=true` 但页面纹丝不动」。现在纵向滑动会补 6 个均匀分布的中间点，
    让系统派发多次 MOVE 事件。已用截屏像素对比验证生效（设置页 ScrollView 被真实滚动）。
  - **自定义滑动柔度**：滑块调列表类的幅度（10%–50%，每档 2%），默认 26% 与 v4.5 手感一致，附「恢复默认」
  - 参数全部是**屏幕高度的比例**，不写死像素；左右扭头不参与自适应
- **左右扭头滑动（v4.4 新增，默认关闭）** —— 头部偏航角超过阈值并保持一下，向对应方向滑一次
  - 灵敏度三档：14° / 20° / 28°
  - 前后左右**共用同一个冷却**，所以不可能出现上下左右同时滑动
  - 前置摄像头画面是镜像的，符号已在检测器里翻正；方向反了可在设置里一键反转
- **防误触冷却（v4.3 新增，默认开启 1.5 秒 / 可调 0.5–5 秒）**
  - 摄像头逐帧（约 15fps）检测，一次真实动作会连续命中好几帧，点头时眼睛还会被连带判成眨眼
  - 眨眼和点头**共用同一个全局冷却计时器**：任意一个动作触发翻页后，冷却期内所有信号一律忽略
  - 触发点统一收敛到 `GazeCameraService.fireSwipe()`，两个检测器无法互相绕过

### 注视数据采集（v5.39，**测试功能，默认关闭**）

> 目标：给「**眼睛必须盯着屏幕，才允许触发翻页**」这道门准备真实数据。
> 本版**只记录、不判定**，也不改变任何一条现有触发规则。

- **它记录什么**：人脸检测的**每一帧原始几何量** —— 人脸框位置、十个关键点的归一化坐标
  （双眼、鼻底、嘴角、双耳、双颊）、三个欧拉角、睁眼概率、画面平均亮度、距离比例，
  逐帧写进 App 私有目录 `files/probe/probe-<日期>-<时间>.csv`（表头自带列名与规则说明，
  缺失一律写 `-1`，绝不把"没读到"当成 0）。
- **为什么需要它**：ML Kit 的人脸检测**没有虹膜**，"眼球转到哪"读不出来；能用的只有
  **头姿 + 关键点几何**（用户提的"鼻子辅助定位"正落在后者：鼻底在脸框内的相对位置对转头最敏感）。
  到底哪几个量能把「看屏幕」和「看别处」干净地分开，必须先用本人实测数据验证，
  而不是拍脑袋定阈值 —— 这与 v5.12/v5.22~v5.25 定头部判据的做法完全一致。
- **怎么开始 / 结束（全程不用碰屏幕，盖住前置摄像头就是开关）**：
  | 操作 | 结果 |
  | --- | --- |
  | 空闲时整只手盖住前置摄像头 **≥3 秒**，然后松手露脸 | 开始录制（`I/GazeProbe: REC START`） |
  | 录制中盖住 **1~3 秒** | 写一个分段标记（`PHASE n`），录制继续 |
  | 录制中盖住 **≥3 秒** | 结束录制，写汇总（`REC STOP … rows= faces= duration= phases=`） |
  | 单次超过 5 分钟 | 保险：自动结束 |
- **「盖住」的判据是两个条件**：画面里**没有人脸** 且 **画面平均亮度 < 32/255**
  （`GazeProbeRecorder.isCovered`）。只判"人脸消失"是不够的 —— 测试脚本里本来就有
  「眼睛/头离开屏幕」的段落，那样会把整场采集提前结束。亮度由新增的
  `FaceGazeAnalyzer.meanLuma()` 抽样读 YUV 的 Y 平面得到（每帧约 1000 次读，成本可忽略）；
  亮度读不到的机型退化成"只判人脸消失"。
- **零代价**：不开这个开关时，逐帧开销与 v5.36 **完全一致**（亮度与原始几何量都在
  `if (probeEnabled)` 里面才算）；开了也只是多写文件，不参与任何判定。
  诊断行新增 `probe=off|idle|armed|rec`，设置页能看到"在不在录、录到第几段、多少帧"。
- **离线可回归**：`GazeProbeRecorder` 本身没有任何 Android 依赖（落盘与打日志是注入的），
  所以 `tools/probe-replay/run.ps1` 能编译**真实代码**回放整套协议 —— 它当场抓出一个真 bug：
  开始录制那一帧没有清掉"盖住"计时，导致**段号一开录就变 2**（现已修）。
- **取数据**（开发侧，用户不用管）：
  `adb shell "run-as com.example.gazescroll cat /data/data/com.example.gazescroll/files/probe/<文件名>"`

### 功耗与运行策略
- **按需启停**：只有在你选定的目标 App 处于前台时才开摄像头，切走立刻释放
- **全局使用翻页（v4.7 新增，默认关闭）**：打开后白名单整个跳过，桌面 / 系统设置 / 任何 App
  都能翻页（代价是摄像头一直工作）。设置页实时显示当前前台应用。
- **切回目标应用必定恢复（v4.7 修复，v5.3 才真正彻底）**
  - v4.7 加过「强恢复」，但整条恢复链只在**前台包名发生变化**时才触发：
    `commitPending` 里有一句 `if (!force && packageName == foregroundPackage) return`。
    于是出现死角——状态里记的前台已经是抖音时（或这次切换没被观察到），**包名没变 →
    什么都不通知 → 相机不重绑 → 功能静默失效**。
  - 这解释了那个"老毛病"：**隔一会重新打开抖音必须下拉状态栏才生效**。下拉状态栏"有效"
    是因为它**人为制造了两次包名变化**（先变成 `com.android.systemui`，收起时再变回抖音），
    等于替我们触发了两次通知。
  - v5.3 改成**主动存活探针**：`AppStateManager` 每 800ms 轮询时直接问服务「你现在真的
    在跑吗」，不再依赖任何窗口事件。三级递进恢复：
    1. 应分析却无帧 → 强恢复（重绑相机）；
    2. 连续多次仍无帧 → 丢弃 provider 重新获取（相机被别的进程占着时必需）；
    3. 再不行 → 重建整条服务管线。
  - 服务实例不存在时探针会直接把它拉起来。日志：`liveness probe: ... -> recovering`。
- **息屏唤醒（v5.4 彻底修复）**
  - v5.3 的探针解决了"恢复没被触发"，但**恢复本身还可能被一个错误的标志挡住**。
    `screenActive` 只在收到息屏/亮屏广播时更新，而 HyperOS 省电策略下这些广播**可能被丢掉**：
    一旦丢了，标志永久停在错误值上 → `shouldAnalyze()` 永远返回 false →
    什么都不分析，而且探针 / 看门狗 / 强恢复**全被这一个条件挡住**。
  - **电源状态自校正**：每 800ms 用系统自己的 `PowerManager.isInteractive`（真相来源，
    不受广播影响）与标志比对，不一致就纠正并触发一次亮屏恢复。
    日志：`power state correction: screenActive=... but isInteractive=...`
  - **解锁事件**：除 `ACTION_SCREEN_ON` 外还监听 `ACTION_USER_PRESENT`——亮屏但锁屏时
    相机拍到的是锁屏画面，恢复动作放在解锁后更准。
  - 亮屏后做一次**完整重建**（而不是"检查后补齐"）：作废全部检测器与抑制状态 →
    丢弃 provider 重新获取 → 失败按 **500 / 1000 / 2000ms** 退避重试。
  - 顺带修掉一个隐私问题：息屏广播丢失时相机原本会在**屏幕已熄灭的情况下继续运行**，
    现在会被自校正纠正回来。
- **三级功耗模型**：
  | 状态 | 行为 |
  | --- | --- |
  | 运行 | 全速分析（约 15fps） |
  | 温机 | 保留相机、丢弃帧、ML Kit 空闲（30 秒窗口） |
  | 释放 | 完全关闭相机 |
- 息屏自动释放相机
- 前台服务通知实时显示「运行中 / 待机」

### 目标 App 管理
- 多选列表：抖音、抖音极速版、微博、小红书、B站
- 未安装的自动置灰
- 「全部不勾选 = 全局开启」逃生开关
- 「全局使用翻页」开关可以让桌面 / 任何应用都生效

### 其他
- **权限记住**：授权一次后不再弹窗
- 实时读数面板：人脸置信度、双眼开合度、俯仰角、当前基准线、帧率
- **手动重启按钮** + `ACTION_RESTART`（应对相机偶发卡死）
- 相机冷启动预热，进入目标 App 后约 **1.2 秒**开始响应

---

## 安装与使用

### 方式一：直接下载 APK（推荐）

1. 到 **[Releases](../../releases/latest)** 下载 `gazescroll-5.18-debug.apk`
2. 安装到手机（允许「安装未知来源应用」）
3. **按下面的 ADB 步骤授权**（不授权的话不会工作）

> APK 是 **debug 签名**，仅供测试使用。

### 方式二：自己编译

```bash
git clone https://github.com/755489724-lgtm/GazeScroll.git
cd GazeScroll
./gradlew assembleDebug
```

环境要求：JDK 17、Android SDK（compileSdk 34 / buildTools 34.0.0）、Gradle 8.9。

> 仓库里目前只有 `gradle/wrapper/gradle-wrapper.properties`，**没有 wrapper 脚本与 jar**。
> 用 Android Studio 打开会自动补齐；命令行编译请先执行一次 `gradle wrapper`。

### 🔑 必须的 ADB 授权步骤（只做一次）

手机开启「USB 调试」连上电脑后，执行：

```bash
# 1) 授予 WRITE_SECURE_SETTINGS —— 用于自动开启无障碍服务
adb shell pm grant com.example.gazescroll android.permission.WRITE_SECURE_SETTINGS

# 2) 允许后台弹出界面（部分机型需要）
adb shell appops set com.example.gazescroll SYSTEM_ALERT_WINDOW allow

# 3) 前台 App 检测（MIUI/HyperOS 上可能被系统拒绝，属于已知问题）
adb shell appops set com.example.gazescroll GET_USAGE_STATS allow

# 4) 手动把无障碍服务加进启用列表
adb shell settings put secure enabled_accessibility_services \
  com.example.gazescroll/com.example.gazescroll.GazeAccessibilityService
adb shell settings put secure accessibility_enabled 1

# 5) 启动
adb shell am start -n com.example.gazescroll/.MainActivity
```

**授权完成后就可以拔掉数据线了**，之后正常使用不需要再连电脑。

> 项目里附带了一个 `恢复翻页.bat`（Windows），会把上面这几步 + 重装 + 启动一次性做完，
> 适合手机重启后或服务掉线时一键恢复。

### 使用

1. 打开 App，勾选你要生效的 App（比如「抖音」）
2. 点「启动」
3. 切到抖音，开始刷 —— 眨眼、点头、仰头或扭头即可翻页
4. 灵敏度不合适就在 App 里调，面板上能看到实时数据

### 设置项一览

| 分组 | 设置项 | 说明 |
| --- | --- | --- |
| 触发方式 | 眨眼次数 | 连眨 1 / 2 / 3 次触发（默认 2） |
| | 点头 / 仰头开关 | 低头 = 上一个，仰头 = 下一个 |
| | 反向俯仰 | 点头仰头反了就打开 |
| | 静止防误触 | 人不动时自动抬高阈值 |
| 防误触冷却 | 启用冷却时间 | 眨眼与点头共用一个计时器 |
| | 冷却时长 | 0.5–5 秒滑块（默认 1.5 秒），实时显示倒计时 |
| 左右扭头 | 开关 + 灵敏度 | 14° / 20° / 28°，默认中 20° |
| | 反转左右方向 | 扭头方向反了就打开 |
| 张嘴点击 | 开关 + 灵敏度 | 5% / 8% / 12% 脸高，默认中 |
| 歪头控音量 | 开关 | 歪到一定角度并保持住 = 调音量（默认开启） |
| | 左歪头：调高音量 | 关掉则「左歪头 = 调低」（默认开） |
| | 右歪头：调高音量 | 关掉则「右歪头 = 调低」（默认关） |
| | 灵敏度 · 触发角度 | 10 / 13（默认）/ 16 / 20° |
| | 灵敏度 · 保持时间 | 0.2 / 0.3（默认）/ 0.5 / 0.8 秒 |
| | 每次调整多少音量 | 1（默认）/ 2 / 3 / 5 档（1 档 = 按一次音量键） |
| 自适应滑动 | 开关 | 按前台应用切换幅度与时长 |
| | 自定义滑动柔度 | 列表类 10%–50% 屏高，默认 26%，可恢复默认 |
| | 当前滑动配置 | 实时显示识别到的应用与生效参数 |
| 全局使用翻页 | 开关 | 桌面 / 任意应用都能触发 |
| 生效的应用 | 多选列表 | 节能白名单；全部不勾选 = 全局生效 |
| 注视数据采集（测试） | 开关 | 盖住前置摄像头 3 秒 = 开始录、盖 1 秒 = 分段、盖 3 秒 = 结束 |
| | 实时状态 | 显示「待机 / 已就绪 / 录制中（第几段、多少帧、几秒、文件名）」 |
| 实时数值 | 诊断面板 | 见下 |

**实时数值面板**能直接读到：人脸占比与距离档、俯仰/偏航/**滚转（歪头）角**与基准线、
张嘴比例与本人闭嘴基准、**当前歪头角度与已保持时长**（看着数字涨到设定时长就知道歪头判定生效了、
哪边正在歪）、当前滑动配置、冷却倒计时、静止/回中锁定状态。挑灵敏度就照着这里的真实数字选，不用猜。

---

## 版本历史

完整变更见 [CHANGELOG.md](CHANGELOG.md)。当前 **v5.39**（用户认可的锚点仍是 **v5.36**：
v5.39 = v5.36 + 「注视数据采集」这个默认关闭的测试功能，行为与 v5.36 一致）。

| 版本 | 主要内容 |
| --- | --- |
| **v5.39** | **新增「注视数据采集」测试功能**（用户要求：「加一个监测功能，用户的眼睛必须盯着屏幕才能触发万能翻页的全部功能……你先记录一下我的数据，先加一个测试功能」——本版**只采集、不判定**，为下一版的"注视门"定阈值）：打开开关后，每一帧额外算一组原始几何量（人脸框、十个关键点、三个欧拉角、睁眼概率、画面平均亮度）写进 `files/probe/probe-*.csv`，表头自带列名与规则、缺失写 `-1`。**起止完全由"盖住前置摄像头"控制，全程不用碰屏幕**：空闲时盖 ≥3 秒→就绪、露脸即开录；录制中盖 1~3 秒→分段标记；盖 ≥3 秒→结束并写 `SUMMARY`（5 分钟保险）。难点在"盖住"的判据：只判"人脸消失"会把**转头看别处**误判成结束（测试脚本本来就有"眼睛离开屏幕"的段落）→ 改成「**没有人脸 且 画面平均亮度 < 32/255**」（新增 `meanLuma()` 抽样读 Y 平面，每帧约 1000 次读；亮度读不到时退化成只判人脸消失）。默认**关闭**：不开时逐帧开销与 v5.36 完全一致，开了也不改任何触发行为；诊断行新增 `probe=`，设置页新增开关 + 实时状态。**为什么需要采集**：ML Kit 人脸检测没有虹膜，只能靠头姿 + 关键点几何（含用户提的"鼻子辅助定位"），哪几个量分得开必须用实测数据说话。离线回放（`tools/probe-replay/run.ps1`，编译真实 `GazeProbeRecorder.kt`）**54 项全通过**，并当场抓出一个真 bug：开录那一帧没清"盖住"计时 → **段号一开录就变成 2**。本版建立在 v5.36 之上（v5.37/v5.38 的失败实验保留在各自的 tag 里，未被继承）|
| **v5.38** | **回退 v5.37 的三条过严判据，只留不花灵敏度的修复**（用户报「真不如上一版灵敏，这一版还把我近距离俯视给仰头给弄死了」）：日志把两条症状都量化了 —— ① v5.37 的「连续歪着 >2.5 秒判为姿势并重锚基准线 + 之后自禁判 2 秒」在 25 秒内触发 5 次（`重锚到 -29.3° / -14.2° / 37.0° / 5.7° / 11.0°`），用户"歪住不动"的实际动作每次都命中，可用时间被吃掉大半；② 「起手前 1.2 秒内必须有中位带」在基准线一动时把合法手势也否掉（`起手之前没有中位（距上次中位 从未）`）；③ 与服务侧「歪头期间暂停翻页」叠加后，`\|tilt\| > 0.4×阈值` 的占比从 v5.36 的 **12%** 涨到 **35%** —— 翻页通道三分之一时间拿不到头部数据，近距离俯视仰头（3.8° 档、依赖轻通道+稳定基准线）因此"死"了。**修法**：删掉那三条判据；暂停翻页的门槛收回成 `\|tilt\| ≥ 阈值`（只在真做歪头手势时暂停）；**保留** `reset()` 不清基准线窗口（"回正音量不降反增"的真正根因修复，零灵敏度代价）、作废即闩锁、以及设置页的两秒倒计时。离线回放 **34 项全通过**。同时按要求做好备份：`backup\GazeScroll-v5.36`（用户认可的一版）+ `backup\GazeScroll-v5.38` |
| **v5.37** | **修"回正脖子时音量不降反增"**（用户报 23:00:50「没把握好提前量，音量不降反增」）：日志证明**不是提前量的问题** —— 基准线被锚在了歪头的姿势上（`23:00:19 基准线=3.8°` → `23:00:23 基准线=32.6°`，而本人真实头姿只有 +1~5°），于是"什么也没做"被读成"往左歪了 28°"、**触发升音量**。根因：服务侧 `reset()`（遮挡/硬锁定/换应用/重绑都会调）会**清空基准线窗口**，清空后紧接着的几帧（用户往往正在歪头或回正）就成了新基准线，而 v5.36 的"歪着时冻结"又让它永远错下去。修法（只动歪头通道）：① `reset()` **不再清基准线窗口**；② 连续歪着 >2.5 秒判定为"姿势"→ **重新锚定**基准线（不会被永久冻住）；③ 起手前 1.2 秒内必须被看到中位带；④ **起手前头必须停住过**（≥250ms 静止且刚结束）—— 回正是一段快速运动，靠这条挡住；⑤ 作废的那一次要**闩锁**（否则回正/甩到另一边会被当成新歪头、调成反方向）；⑥ 重锚后再自禁判 2 秒。设置页实时区新增**两秒间隔倒计时 + 最近一条作废原因**（用户说"判断不好时间"）。另修工具 bug：`capture-loop.ps1` 漏了 `Tilt:V` 标签（v5.35/v5.36 日志里没有触发明细）。离线回放 **34 项全通过**（含"reset 不丢基准线""歪 3 秒再回正不反方向触发且自愈""快速甩头不触发"）|
| **v5.36** | **歪头通道三处改进**（用户要求「再灵敏一点」「回正脖子不要误触」「加一个全局：上一秒做过动作就必须强制等两秒，而且不能有提前量」「加个灵敏度」）：① **修掉"回正脖子误触"的根因** —— `tilt = roll − 基准线`，而基准线是最近 45 帧中位数，歪着头停几秒会把基准线带走，回正时就变成"反方向的一大坨倾斜"；改为**只在头基本在中间（\|tilt\|≤阈值）时才更新基准线**（离线回放当场抓出第一版"窗口没填满就一律喂"的漏洞）；② **全局两秒 + 无提前量**：新增动作时钟（翻页/调音量/张嘴点击都记账），歪头的**起手帧**必须晚于"上一次动作 + 2 秒"，否则整段作废（不是顺延）——严格按"卡在 1.99 秒那一下不算"执行，日志写 `起手太早（还剩 Nms）`；反向也成立：调音量后 2 秒内不翻页（新增 `GlobalTriggerGate.extendCooldown()`，只延长不缩短）；③ **灵敏度整体调灵一档**：触发角度 12/15/18/22 → **10/13（默认）/16/20°**，保持时间 0.3/0.5/0.8/1.0 → **0.2/0.3（默认）/0.5/0.8 秒**，两项标题改成「灵敏度 · …」；回中位后的停稳时间 250→400ms。诊断行新增 `gapRemain=`。离线回放 **25 项全通过**（含"歪 6 秒再回正不触发且倾斜角回到 0 附近""甩到另一边两秒内不触发、两秒后重新歪才触发""起手早于两秒即使坚持过两秒也不触发"） |
| **v5.35** | **删掉「单眼闭眼控音量」，换成「歪头控音量」**（用户要求：「删了闭眼调整音量，改成歪头，左歪头上升，右歪头下降……判断方式可以久一点，仰头得到一定的角度才触发」）：新增采集 ML Kit 的 `headEulerAngleZ`（滚转 = 歪头）—— 这条轴 **v5.10~v5.34 从没采集过**，所以用户 22:38:50 之后歪的那几下，日志里只看到副作用：歪头让俯仰串扰几度，被近距离俯视档（阈值 2.5°）当成仰头，**顺带翻了两页**。新判据（纯逻辑 `TiltDetector`，可离线回放）：① 倾斜角 = 滚转 − 本人基准线（最近 45 帧中位数，天生歪头看手机也不误触发）；② 必须歪到设定角度；③ 并保持住；④ 一次歪头只调一组，必须歪回中位才能再来；⑤ 读数 >55°（躺下/侧卧）视为不可信。默认**左歪头 = 调高、右歪头 = 调低**，两边各自可反向；一次调 1（默认）/2/3/5 档。另新增**歪头期间暂停翻页判定**（歪回中位后 recalibrate 重新对齐基准线），避免「歪头顺带翻一页」。同时删除 `WinkDetector.kt` 与 `tools/wink-replay`。诊断行新增 `roll=` 与 `tilt=...` 实时状态 |
| **v5.34** | **按用户"在不灵敏，你自己想办法"的要求，把判据换成时间域判据**：v5.33 加的「合眼 ≤0.5 秒」经验证**根本不可用** —— 该场 16 次尝试里，成功的 onset 是 55~147ms，被拒的是 658 / 1216 / 1732ms，但**被拒那几条的两眼差高达 0.85~1.00**（一只眼 0.0x、另一只 0.9x，一眼就是有意单闭），只因为 ML Kit 的概率是逐帧软分类、真闭眼也会拖几帧甚至一秒多才掉到底；而 v5.30 那批误触的 onset 是 1.6~3.2 秒，**与真单闭重叠** → onset 判据废掉。换成看**"这只眼最近有多常被读成闭着"**（约 5 秒的指数滑动平均 duty）：有意单闭 ≈0.02~0.10，低头眯眼（同一只眼反复被读低）≈0.4~0.9，取 **≤0.30** 当判据。完整判据：两眼差 ≥0.30 + 另一只眼不闭着 + 保持设定时长且不超过 2.5 秒 + duty ≤0.30 + 一次闭合只触发一次（必须重新睁大）。日志把 `onset=` 换成 `duty=`。离线回放 **17 项全通过**（含"概率缓降 1.2 秒的真单闭"这条 v5.33 错杀案例、连续快闭 4 次、左右交替）；低头反复读低 8 次从 v5.30 的 8 次误调降到 ≤2 次。**已知残留**：低头刚开始时那一下仍可能调一格（时间域判据挡不了"第一次"），要再收就用头部通道的 `lookingDown`/`chinRatio` 加姿态条件 |
| **v5.33** | **按用户要求换算法：核心判据改成「两只眼的读数差」+ 修一个会吃掉右眼单闭的记账 bug**（用户报「为什么我眨眼可以触发调整音量？」）：① 用户提的「闭一只眼就读不到另一只眼」经全量日志验证**不成立**（v5.10 起 33 个日志、有脸画面 **3153 帧**里**没有一帧**只缺一只眼的读数 —— ML Kit 有脸就给两只眼的概率，闭的那只给的是 0.02~0.15 的低值），但用户的思路落在另一个量上正好成立：**22:25:56 的误触发是闭的 0.54 / 另一只 0.65（差 0.11），而当场 4 次真单闭是 0.30/0.77、0.13/0.95、0.43/0.95、0.24/0.98（差 0.47~0.82）** —— **绝对阈值分不开这两类**（0.70/0.65/0.55 三条线都试过，要么误触发要么漏触发），能分开的只有"两只眼差多少"，所以 v5.33 把 **差 ≥0.30** 作为核心判据（另一只眼不闭 + 合眼 ≤500ms + 保持设定时长三条保留）；② 修掉 v5.32 的一个记账 bug：`最后一次没闭着` 的时间戳被写在"另一只眼闭着就作废"的提前返回之后，于是左右眼交替单闭时右眼的时间戳从不更新、onset 算成 `9223372036854775807ms`（日志里真有这三行），一律判"合眼太慢" → 改成每只眼各自独立记账。触发行新增 `sep=`（两只眼读数差）。离线回放 **17 项全通过**：回放 22:25:56 的眨眼半闭**不触发**，回放 4 条真实成功 + v5.31 被错杀的 + 左右交替两次**全部触发** |
| **v5.32** | **修 v5.31 把真单闭挡掉的两条判据 + 查清"一波音量"的来源**（用户报「17.56-18.05 眨眼调音量一点动静没有」「19分40秒突然有一波调整音量」）：① **删掉**「闭之前必须连续明确睁着（>0.65）≥600ms」—— 它既冗余又有害：真正的判据是合眼快慢，而 30cm 俯视时**睁着的那只眼读数常在 0.55~0.70**（同场诊断 `eyeL=0.70/0.64/0.62`），这条线卡在真实读数中间，把用户连续 5 次有意单闭全否成"只明确睁了 0ms / 205ms"；② 「另一只眼」的门槛从 **>0.70 降到「不是闭着」（≥ 闭眼阈值）** —— 实测那只眼睁着时读到 0.59/0.65 也被旧门槛否掉，而眨眼时它只有 0.02~0.10，这条线照样分得开；③ onset 起点从"最后一次 >0.65"改成"**最后一次 ≥ 闭眼阈值**"，合眼窗口 350→**500ms**（实测真单闭 onset 63~432ms，被慢慢读低是 1.3~2.7 秒）；④ 默认保持时长 0.6s → **0.4s**；⑤ 顺手修掉"保持够了却因起手被拒时松开还打一行『只闭了 1200ms（<600ms）』"的自相矛盾日志。**"一波音量"= 一次真单闭（`held=616ms onset=278ms min=0.06 other=0.91`）× 当时默认的 2 档**：连调两次系统音量接口 → 音量面板连弹两次、音量从 0 直接跳到 20（此前已被 v5.30 的误触发打到 0），所以听着"突然一波"；现在多档**先空跑一档量出"1 档 = 几个索引"，再 `setStreamVolume` 一步跳到目标值、只弹一次面板**，默认档位也从 2 改回 **1 档**。离线回放验证（`tools/wink-replay`，编译真实 WinkDetector）**14 项全通过**：v5.30 的三种"慢慢读低"全部不触发，v5.31 被错杀的真单闭全部触发 |
| **v5.31** | **修 v5.30「单闭控音量」的误触发 + 两处新设置**（用户报 22:03:48「近距离俯视还是会误触」要求定位并解决）：日志定位到**既不是眨眼也不是头部动作**，而是新单闭通道把音量从 10 打到了 **0**。根因：**低头看屏幕时 ML Kit 会把一只眼"慢慢读低"、一读几十秒**（全场 11 次"单闭"全是这么来的，左眼 6 次 / 右眼 5 次；人的眼睛不可能单闭几十秒），它与有意单闭的唯一区别是**读数下降的快慢** → 判据加两道「起手」门：① 闭之前那只眼必须明确睁着（>0.65）**≥600ms**；② 从「最后一次明确睁着」到「第一次深闭（<0.30）」必须 **≤350ms**。回测 v5.30 那 11 次误触**全部被拒**，对有意单闭几乎无损（"睁得好好的→啪一下合上→保持"）。按用户要求新增：**保持时间可选 0.4 / 0.6（默认）/ 0.8 / 1.0 秒**（从 1 秒缩短，挡误触靠起手门而不是拖时间）、**每次调整音量可选 1 / 2（默认）/ 3 / 5 档**。另修一处**头部通道**的近距离俯视误触：22:05:32 脸消失 100 秒回来后原始俯仰只动了 0.7° 却触发下滑（基准线重建期一度算成 15.7°，`原始−基准` 凭空差 5°）→ 基准线被作废后攒够 **12 帧（≈1.1 秒）**才允许头部触发。顺手修掉 v5.30「右眼闭时 `eyeL/eyeR` 印反」的日志 bug。阈值表 / 方向仲裁 / 远距离 / 30cm 硬锁定 / 眨眼档位一字未改 |
| **v5.30** | **新增「单眼闭眼 1 秒 → 音量加 / 减」**（用户点名的新功能，其余功能一个字节未改）：一只眼读数 < 闭眼阈值、**另一只眼 > 睁眼阈值 0.70**、且**连续保持 ≥1000ms** 就调一档媒体音量。默认**右眼闭 = 调高、左眼闭 = 调低**，并按用户要求加了**两个互相独立的反方向开关**（左右眼各一个）。防误触：① 短于 1 秒不触发（实测有意眨眼单次只有 139~758ms，够不着）；② 一次单闭只调一档，必须睁眼之后重新闭才算下一次（一直闭着眼不会把音量推到底）；③ 两只眼同时闭整段作废，所以与眨眼翻页互斥。与「张嘴点击」同类属控制指令：不走全局冷却、不占用翻页冷却；不需要无障碍 / Shizuku（`AudioManager` 直调 `STREAM_MUSIC`，新增 normal 级 `MODIFY_AUDIO_SETTINGS`）。判定全部在新文件 `WinkDetector.kt`，`BlinkDetector`/`HeadPoseDetector` 一字未动。日志：`I/Wink: wink 右眼 held=1015ms eyeL=0.94 eyeR=0.03 min=0.03 other=0.94 thr=0.55 dist=mid/far -> volume UP 5->6/15 applied=true pitch=… chin=…`，诊断行新增 `winkVol / winkDir / wink=… / winkSteps`。**已知风险**：v5.28 实测过「低头看屏幕时 ML Kit 把一只眼读成闭着」（0.83/0.05 等，845~3103ms），形态与时长都和有意单闭重叠，需在真机上核对低头时会不会误调音量 |
| **v5.29** | **修两个"人没动却翻页"**（用户报 21:00:28，日志抓到两次、分属两个通道）：① **眨眼通道**——判定口径原是「任一只眼低于阈值就算闭眼」，而近距离俯视时 ML Kit 经常只把**一只**眼读成半闭（`eyeL=0.83 eyeR=0.05`），于是 7 秒内凑够 3 次假眨眼 → 上滑。改成**两只眼都低于阈值**才算闭眼（真眨眼 `0.02/0.03`、假的是 `0.90/0.17`，分得干净），并加**时长上限 900ms**（有意眨眼 139~758ms，误计数那批 845~3103ms）。② **俯仰通道**——闭眼会让姿态单帧跳 3.1°（静止两秒后），本该由跳变确认拦下，但**前一帧虽被 `eyes-unreliable` 拒掉却仍被记成"上一帧在动"的证据**，于是被判成渐进动作、零延迟放行；改为**闭眼帧不作证据**（不更新上一帧与速度样本），代价是真实动作最多多等一帧 |
| **v5.28** | **近距离俯视的左右扭头 + 晃动误触 + "只认突然动作"**：① 日志抓到同一帧里**扭头通道看到 29.5°、俯仰通道只看到 1.5°**（俯仰侧读的是"原始偏航 − 基准线"，那一刻基准线刚被重学）→ "让位给扭头"失效，用户往右扭头却得到 **UP**。改用**与基准线无关的原始偏航摆幅**（500ms 内 ≥12° 就让位；实测真扭头摆 25~59°、真点头只摆 1.7~10.4°），并放在 onset 之前，扭头期间俯仰连动作都不开始记；② 25 次扭头候选里 **12 次**死在 `eyes-unreliable` 上（扭头会把眼皮挤成半闭，实测 `eyeL=0.18`）→ 强扭头（越阈值+2°）豁免这条门，与俯仰侧一致；③ 新增 `no-travel` **位移判据**：判定域里从运动起点必须真的走过 0.5×阈值 —— 清掉"姿势本来就压在阈值上、轻微晃动就触发"的误触（实测 ctx 位移 0.6~1.3° 也触发过）；④ 近距离档的"突然性"窗口收紧为俯仰 **300ms** / 扭头 **400ms**（实测真实动作 53~249ms，只砍慢动作）。**远距离行为一个字未改** |
| **v5.27** | **只动"近距离俯视的点头/仰头"，一次解决延迟+误触+判反**：v5.26 日志里 2 分钟被拒候选 tiltUp `ref-veto-up` **89 次**、`eyes-unreliable` 58 次 —— 其中最刺眼的是 20:30:40.5~41.2 一次 **3.5°→17.2° 的连续仰头一帧都没放行**（起手帧被证人否决、随后 4 帧被判成"闭眼"），1.8 秒后回摆末段反而**又被判成点头多翻一页**。四改：① 证人否决改看**证人自己确认的结论**（`relVerdict`，只有它自己 `opt=ok` 才算有结论）而不再看"起手瞬间必然≈0 的原始位移"；② `eyes-unreliable` 只拦边缘候选（仰头本身会把眼皮挤成半闭），强候选（阈值+2°）豁免；③ 强候选免等保持时间/最低速度（晃动、手机运动、跳变确认三道门全部保留）；④ 回中锁定的"400ms **无条件**解锁"改成"**停住** + 已回中性区或已停在自己姿势上"，另加 3 秒硬上限 |
| **v5.26** | 用户给的解法是对的：**只认可快速动作**。① 轻点头速度门槛从 0.009 拉回 **0.020**（=快通道门槛）——慢点头（延迟 807/588/544ms 的那几次）不再触发，手势变成可学习的一句「快速点头=下滑、快速仰头=上滑」；② 轻通道加**位移上限 12°**（参考点过期造成的 29°"换姿势"再也冒充不了轻点头）；③ 证人否决**双向对称**（低头也要求证人没有明确反对）——挡住实测 4 次"仰头被判成下拉" |
| **v5.25** | 仰头慢的根因是**阈值太高**（不是识别慢）：起点 2.4° 慢慢抬到 6.0° 才放行 → 近距离俯视的仰头阈值开到 **3.8°**（增益 0.63）。逐关键点数据挑出仰头的干净判据：**鼻子相对眼睛**的位移（16 次真实仰头 14 次一致、**0 次相反**，且平移无关 —— 身体动只是整张脸平移，不改变它） |
| **v5.24** | 只做一件事：把"仰头"这一半的信号找出来（逐关键点统计数据，为 v5.25 的判据落地做准备） |
| **v5.23** | 实测确认用户的符号约定成立（点头=脸下移），但**绝对位移的噪声底与真实动作一样大** → 新增**平移无关**的第二条轨道（关键点相对眼睛），两条一起观测 |
| **v5.22** | 把用户提出的"参考点位移"算法完整实现成独立通道（`ReferencePointDetector` + 关键点归一化坐标），本版**只观测、不接管触发**，用真实数据判断它够不够格当判据 |
| **v5.21** | 按用户要求"把 5.19 的灵敏度抄回来"：撤销 v5.20 的回退，恢复**运动起点判向**（`settledPitch` + 同域相减），方向对应关系交给设置里的「反转俯仰方向」开关（拨一次两个方向一起反，不会各自为政） |
| **v5.20** | 回退到 v5.16（按用户明确要求），回退前先把 v5.17/v5.18 的证据固定进日志与文档，避免以后重复排查；眨眼 4 次档保留 |
| **v5.19** | 核对"方向是否真的判反"：7 次点头 7 次下滑、方向与灵敏度都对；全场唯一一次上滑其实是**不由自主连眨 3 次**触发的 → 新增**眨眼 4 次**档（设置页可选） |
| **v5.18** | **修 v5.17 的实现错误（一行）**：位移算成了 `pitchDeg - settledPitch` —— **域混用**（原始俯仰角 减 有符号值）。后果：方向整体翻一次（`invertPitch=true`）→ 轻点头全变上拉；同时叠上抖动的基准线偏移 → 静止时疯狂上拉。改为同域相减 `signedPitch - settledPitch`。触发日志新增 `light=yes(起点→终点)` 可直接核对方向 |
| **v5.17** | **修「轻点头变成上滑」**：根因是**滞后的基准线**（45 帧中位数，实测 2 秒内从 5.12 跳到 9.52），轻点头只有 5~8° 位移，起点落在基线哪一侧就决定方向 → 小幅度动作必然判反。轻通道改用**运动起点的稳定值** `settledPitch`（连 3 帧峰峰值 <1° 时更新）作参考点：方向 = 位移符号、幅度 = 位移大小，与基线漂到哪无关。**跳变确认加台阶限制**（相邻帧位移 >4° 不算渐进）：修 ⑥「抖动 + 单帧跳 5~10°」骗过确认的漏网 |
| **v5.16** | **修「走路/连续点头不触发」**：`eyes-unreliable`（闭眼不可信期）在 v5.14/5.15 里拒掉了 **126 次真实动作**（含 25.2°、17.5° 的大幅摆动），30cm 处"某只眼睛跌破阈值"几乎每秒都有，120ms 窗口把头部判定压制近三成时间——这就是「要等 0.5 秒」的真相；窗口收到 **50ms**（只跳过闭眼那一帧，其余交给跳变确认）。**晃动判据从「路径效率」换成「方向反转次数」**：走路（约 2Hz）6 帧内 ≤1 次反转、点头 0~1 次 → 放行；手抖（3~5Hz）3~6 次 → 拦下（旧判据在走路时任何窗口都含摆动，必然误杀点头，实测 19 次）。轻点头速度门槛 0.013 → 0.009 |
| **v5.15** | **修「v5.14 变差」**：新增**跳变确认**——越阈值时检查前一帧是否已在动：**渐进动作零延迟**（手感不变），只有**单帧跳变**才要求再撑过一帧（下一帧必然掉回阈值以下 → 永不触发）。依据是日志里两种形状一眼可分：真实轻点头前一帧已在动（0.3 → -2.0 → -2.8），误触是前一帧还在 +0.4 就一帧跳到 -3.1。**注意这不是 v5.9 那个被撤销的全局确认窗口**（那个要求跨越完整阈值 2/3 帧，把轻点头也抬高了门槛）。同时修诊断：recentPeak 窗口 1s < 诊断间隔 3s，导致 accel/gyro 整场读 0，v5.14 那轮数据白瞎；并补上 PhoneMotion 抓取标签 |
| **v5.14** | **修「不由自主眨眼导致误触」**：日志给出完整因果链——单帧浅闭（`frames=1/2 minEye=0.98/0.14`）后 **107ms**，俯仰从 +0.1° 一帧跳到 −3.5° 并触发；ML Kit 的姿态靠关键点回归，闭眼时眼部关键点消失会让姿态跳。新增 `eyeDip`（**含单帧浅闭**）+ 120ms 姿态不可信期。**修一个一直漏着的方向泄漏**：v5.4 的俯视增益乘在共用阈值上，把仰头阈值从 6.0° 偷偷压到 **4.5°**（实测 337 帧里 133 帧如此），且与近距离档叠乘使低头阈值在 2.25°/3.0° 之间跳；现在它只作用于低头且不叠乘。**点头再降一档到 2.5°**（稳定值）。**加陀螺仪**判"手机在摇"（v5.13 的加速度计抓不到轻晃：整场峰值仅 2.3 m/s²、一次没触发） |
| **v5.13** | **修「疯狂点头却没反应」**：轻点头实测速度 0.014~0.018°/ms，够不到快通道门槛 0.02，而"保持"通道要求速度 ≥ 近距离门限 0.024（比 0.02 还高）→ **实际永远走不通**。新增**轻点头通道**（近距+俯视+低头时门槛降到 0.013，并要求多撑过一帧以挡住单帧跳变）。**新增「急停/急刹」闸门**：`PhoneMotionMonitor` 判"手机被顿了一下"，点头是头在转（手机不动），急停是人和手机一起顿 |
| **v5.12** | 修「轻点头不好使」+「眨眼被弄坏」：**近距离俯视点头专用档**——俯视判据改用**基准俯仰角**（实测 30cm 俯视 7.8~14.4°、50cm 平视 0~2°，比 v5.6 试过的 chinRatio 干净得多），近距+俯视时低头阈值压到 **3.0°**（用户的轻点头实测 3.7~4.4°，原来 4.1° 卡在门上）；新增**晃动过滤**（路径效率 `|净位移|/Σ|相邻差值|` <0.45 判为地铁/手抖的来回晃） |
| **v5.11** | 修「点头要更大的角度才触发」：**方向仲裁第一次真的比较了两个轴的大小**——原判据只有 `0.006°/ms` 一条绝对门限，而 30cm 偏航抖动就有 0.01~0.03°/ms，实测连续 7 次点头被拒、阶段④ 共 39 次俯仰候选被误拒。现在要求「偏航在动 **且** 偏航速度 ≥ 俯仰速度 **且** 偏航幅度 ≥ 阈值×0.3」 |
| **v5.10** | **撤销 v5.9 的触发确认窗口**（实测它静止时一次没用上、正常使用却挡掉 37 次越阈值，导致「点头要更大角度 / 要等 0.5 秒 / 不丝滑」）；**定位误触真正来源是眨眼**——静止 90 秒的误触全部 `lastTrigger=blink`，且设备里 `blinkTriggerCount` 实际是 **2**（以为 3），已改回 3；保留 v5.9 的距离档统一（档位切换从逐帧乱跳降到 4 次/8 分钟，30cm 点头增益稳定生效） |
| **v5.9** | 修「30cm 俯视还是误触」：**距离档统一为一个带滞回的真值**（原 0.50/0.55 两个门槛让 0.45~0.55 变成"最灵敏、防护最弱"的空档，档位还逐帧乱跳）；**触发确认窗口**（已在 v5.10 撤销）。新增逐帧上下文日志（每次触发打印前 1.2 秒原始读数 `ctx …`）、`frSm`、`distance tier` 切换行、人脸返回 `MARK` 行 |
| **v5.8** | 修「近距离俯视扭头变成上下滑」：偏航阈值不再乘距离系数（近距离实际是 2 倍，20°→40°，而用户扭头只有 40~52°）；扭头动作窗口 600→900ms（`slow-rise` 曾把正常扭头判死）；新增**方向仲裁**——偏航在转动时点头/仰头一律让位，判据只看角速度（实机 13 次扭头全部触发、20 次俯仰候选被正确否决） |
| v5.7 | 近距离点头增益收窄为**单向**（仰头方向不再被压低）——修复「近距离仰视误触」；重开抖音不触发的三处死角：整条恢复链挂在同一个可能出错的布尔值上（新增前台复核 fail-open）、相机"绑着却没帧"可无限循环永不升级（新增独立的持续无画面计时 + 12 秒硬重建）、无障碍强制重绑只做一次（改为按持续时长升级）；新增 `a11yEvents`/`a11yEventAgoMs`/`hardResyncs`/`noFrameForMs`/`pitchThUp` 诊断 |
| v5.6 | 近距离（faceRatio ≥0.50）点头阈值压到 0.68 倍，远距离不动；实测证明「下巴占比」主要在反映距离而非姿态，故俯视几何只作诊断保留、不接灵敏度开关；修复常亮时偶尔失效（相机"有句柄但早已无帧"被反复确认成正常） |
| v5.5 | 摘掉幅度阈值上的两层放大系数（静止锁定 + 距离）——这是「点头费劲」的根因；俯视时点头阈值压到 0.75 倍；修复注入链上"实例在但连接失效"导致的息屏后需下拉状态栏；新增全链路自检日志 |
| v5.4 | 息屏唤醒彻底修复（电源状态自校正 + 解锁事件 + 退避重试）；扭头阈值也按距离放大；新增俯视姿态自动识别与补偿 |
| v5.3 | 修复「切后台后重开应用必须下拉状态栏才生效」的代码死角（改为主动存活探针）；30cm 近距离误触（眨眼判定按距离收紧 + 近距离静止硬锁定）；新增触发来源诊断 |
| v5.2 | 修复点头完全失效（回中锁定 400ms 兜底 + 速度采样错位）；姿势偏置自动校正；速度门限按距离缩放 |
| v5.1 | 删除头控音量；修复抖音忽略注入手势（滑动路径补中间点）；近距离防误触 |
| v5.0 | 抖音档实机标定为 39%/150ms（旧 80% 参数抖音不接受）；仰头回中锁定；最低速度门限；张嘴基准自愈 |
| v4.9 | 人脸丢失状态机；距离自适应；修复偏航延迟打印 |
| v4.8 | 速度驱动触发（仰头延迟 211~329ms → 71~207ms）；静止锁定；遮挡抑制 |
| v4.7 | 全局使用翻页开关；头控音量（v5.1 删除）；切回目标应用自动恢复（第一版） |
| v4.6 | 张嘴改为点击屏幕中央（不再冻结 App）；自定义滑动柔度滑块 |
| v4.5 | 上下滑动自适应；张嘴检测 |
| v4.4 | 左右扭头滑动；连续点头双击（后已移除） |
| v4.3 | 全局防误触冷却（眨眼与点头共用计时器） |

---

## 已知限制

- **必须 ADB 授权一次**（见上）
- MIUI / HyperOS 上 `GET_USAGE_STATS` 被系统拒绝，前台检测降级为窗口标题方案
- HyperOS 会过滤第三方 App 的无障碍事件，只放行 launcher / systemui 的事件
- `am force-stop` 会**清空** `enabled_accessibility_services`，服务需要重新拉起
  （用 `恢复翻页.bat` 或重新打开 App 即可）
- 部分机型需要同时开启「USB 调试(安全设置)」才能正常注入手势
- 强光 / 逆光 / 大幅晃动时检测率会下降

---

## 项目结构

```
app/src/main/java/com/example/gazescroll/
├── BlinkDetector.kt               眨眼状态机（纯逻辑，无 Android API）
├── TiltDetector.kt                歪头状态机（相对本人头姿基准线的滚转角 + 角度阈值 + 保持时长 → 调音量，v5.35）
├── GazeProbeRecorder.kt           注视数据采集状态机（纯逻辑、无 Android 依赖；盖住摄像头控制起止，逐帧写 CSV，v5.39 测试功能）
├── HeadPoseDetector.kt            头部姿态状态机（俯仰点头/仰头 + 偏航左右扭头，速度驱动、静止锁定、距离自适应）
├── MouthOpenDetector.kt           张嘴检测（嘴到鼻底距离 / 脸高，含本人闭嘴基准与防连发锁存）
├── AdaptiveSwipe.kt               按前台应用自适应的纵向滑动参数表 + 柔度档位换算
├── GlobalTriggerGate.kt           全局触发冷却闸门（各触发方式共用，防一次动作多触发）
├── GazeStateMachine.kt            早期「下看上扫」两区状态机（仅调试用）
├── FaceGazeAnalyzer.kt            CameraX ImageAnalysis + ML Kit → 分析结果
├── GazeCameraService.kt           前台服务（type=camera），持有整条流水线
├── GazeAccessibilityService.kt    无障碍服务，负责 dispatchGesture 注入上滑
├── AccessibilityBootstrap.kt      自动开启/修复无障碍服务（免手动去设置里点）
├── AppStateManager.kt             前台 App 检测 + 状态机启停
├── SwipeInjector.kt               手势注入抽象层（Shizuku 优先，无障碍兜底）
├── ShizukuSwipeDispatcher.kt      Shizuku 注入路径（shell 权限，最接近真实手指）
├── TargetApps.kt                  目标 App 列表与安装检测
├── AppPrefs.kt / GazeModel.kt     配置持久化与数据模型
└── MainActivity.kt                极简配置界面

app/src/main/res/xml/accessibility_service_config.xml
tools/                             实机测试脚本与配置样例（标定参数时用）
```

技术栈：**Kotlin** · CameraX 1.3.4 · ML Kit face-detection 16.1.7（bundled）· AccessibilityService · 原生 View

### 实机标定工具

`tools/` 下是标定时用的脚本，对 App 本身无影响：

- `verify-gesture.ps1` —— 用**截屏像素对比**验证注入手势是否真的生效（不依赖人眼看屏幕）
- `verify-slider.ps1` —— 验证设置项持久化链路
- `gz_prefs_test.xml` / `gz_prefs_slider.xml` —— 直接写入 SharedPreferences 的配置样例
- `tilt-replay/` —— 歪头判据的离线回放（编译**真实** `TiltDetector.kt`，只把 `android.util.Log`
  换成打印桩）；`probe-replay/` 同理用于采集状态机（那个类本来就没有 Android 依赖）。
  这套做法已经抓出过三个只有实机才会暴露的 bug（基准线被提前喂、1 档变 2 档、段号一开录就变 2）

触发参数可以用广播逐组实测（`BuildConfig.DEBUG` 限定）：

```bash
adb shell am broadcast -a com.example.gazescroll.action.TEST_SWIPE \
  -p com.example.gazescroll --ef from 0.70 --ef to 0.30 --el duration 150
```

抖音的 39%/150ms 就是用这个通道逐组试出来的（80%、90%、60%、45%、50% 全部无效）。

---

## 致谢

- [CameraX](https://developer.android.com/media/camera/camerax) —— 稳定的相机流水线
- [ML Kit Face Detection](https://developers.google.com/ml-kit/vision/face-detection) —— 人脸关键点与欧拉角
- [Shizuku](https://github.com/RikkaApps/Shizuku) —— 探索过的手势注入备选方案（`dev.rikka.shizuku`）

以及所有在 Issue 里帮忙测试、反馈机型和日志的朋友。

---

## 求助与反馈

**欢迎提 [Issue](../../issues)**，尤其欢迎这几类：

- 🐛 **Bug 反馈** —— 请附上机型和系统版本，最好带上 `adb logcat` 日志
- 💡 **免 ADB 方案** —— 这是我最想要的
- 📱 **机型适配** —— 你在别的机器上跑通了/跑不通，都欢迎告诉我
- 🔧 **灵敏度调参** —— 说说你戴眼镜/不戴眼镜时的实际效果，我可以把默认值调得更合理

提 Issue 时如果能带上这些信息，定位会快很多：

```
机型 / 系统版本：
是否戴眼镜：
触发的动作（眨眼 / 点头）：
现象描述：
logcat（过滤 gazescroll）：
```

---

## License

[MIT](LICENSE)
