# 更新日志

本项目遵循「一版一改、实机验证」的节奏：每个版本都先在小米 13（HyperOS 3.0.308.0）上
跑通才发版，所以下面每一条的结论都来自实机日志或参数化实测，而不是推测。

版本号规则：`vX.Y`，`versionCode` 与版本号同步递增（当前 v5.26 / 76）。

---

## [5.26] - 2026-09-16

用户复测 v5.25：

> 「灵敏度没啥问题，但是**我仰头有时候会判定下拉，又会判定成上滑，判定有问题**；
> 而且这个灵敏度虽然可以，但是**会有点容易误触**。点头和仰头你单独写个判定的方式，
> 比如**我真要下滑，我就用快速点头，而不是慢慢的点头** —— 有时候慢慢的点头还是会被误判。」

### ① 「仰头被判成下拉」的取证：用参考点通道当独立证人

逐个核对 26 次触发（把"鼻子相对眼睛"的走向当证人）：

`
20:23:01 nodDown light=yes(16.5→-12.6) lat=807ms  relN=-0.029  ← 证人相反 ✗
20:23:06 nodDown light=yes(13.9→-6.9)  lat=198ms  relN=-0.024  ← 证人相反 ✗
`

**17 次点头里有 4 次证人方向相反**，而那正是用户说的"仰头被判成下拉"。
看 light=yes(16.5→-12.6) 就明白了：位移通道的**参考点过期**了
（起点还停在 +16.5，而用户已经换完姿势），于是 **29° 的"换姿势"被当成一次点头** ✗。

### ② 用户给出的解法是对的：只认可**快速**动作

那些误判事件的延迟都很大（**807ms / 588ms / 544ms**）—— 一眼可见是慢动作。
**慢动作期间基准线与参考点会跟着跑，方向就会飘**；快速动作一瞬间完成，它们来不及漂。

**修复（两条）**：

1. **点头也要求"够快"**：[LIGHT_NOD_FAST_VELOCITY] 从 0.009 拉回 **0.020**
   （= 标准快通道门槛）。慢点头一律不再触发 —— 手势因此变得**可学习**：
   「**快速点头 = 下滑，快速仰头 = 上滑**」。
2. **轻通道加位移上限** [LIGHT_MAX_DISPLACEMENT_DEG] = 12°：超过就不走轻通道，
   于是"换姿势"那种 29° 的巨大位移再也冒充不了轻点头。
3. **证人否决改成双向对称**：近距离时，仰头要求 elNoseDy ≤ −0.015（v5.25 已有），
   现在**低头也要求证人没有明确反对**（elNoseDy 不能 ≤ −0.015）。
   实测这条正好挡住上面那 4 次"仰头被判成下拉"。

### 未动

30cm 静止硬锁定、静止锁定、跳变确认、手机运动闸门、距离档、方向仲裁（v5.8）、
眨眼 4 次、反转俯仰方向、阈值表（低头 2.5° / 仰头 3.8°）—— 全部原样。
灵敏度本身没动（用户说"没啥问题"），只改了"哪些动作算数"。

---

---

## [5.25] - 2026-09-16

用户复测 v5.24：「仰头比之前好点了，但是**还是不够灵敏**，我的角度还是需要大一点，
而且**我仰头了，要等个差不多 0.4-0.5 秒才触发**；我身体一动，就会误触。」

### ① 仰头为什么慢：阈值太高（不是识别慢）

实机 16 次仰头的延迟统计：**中位数 202ms，其中 3 次 ≥500ms** —— 正好对上"等半秒"。
仰头的起点在 onset（2.4°）附近慢慢抬，要涨到 **6.0°** 才放行，于是从起点到触发要 200~760ms。

**修复**：仰头方向在「近距离 + 俯视」时也开一档 —— [NEAR_LOOKUP_BOOST] = 0.63 → **3.8°**，
把这段行程砍掉近一半。

⚠️ v5.7 曾因「近距离仰视被误触」把仰头增益**单向化**（只压低头方向）。这次敢给仰头也开档，
是因为同时加了下面的参考点确认 —— 当年缺的正是这个。

### ② 逐关键点数据挑出了仰头的干净判据：**鼻子相对眼睛**

24 条标注对照（16 次仰头 / 8 次点头）的方向一致率：

| 判据 | 仰头（16 次） | 点头（8 次） |
|---|---|---|
| 绝对鼻子位移 | 一致 11 / 弱 3 / **相反 2** | 一致 0 / 弱 6 / **相反 2** |
| 绝对下巴位移 | 一致 12 / 弱 2 / **相反 2** | 一致 2 / 弱 4 / **相反 2** |
| **鼻子相对眼睛** | **一致 14 / 弱 2 / 相反 0** ✓✓ | 一致 0 / 弱 6 / 相反 2 |
| 下巴相对眼睛 | 一致 14 / 弱 0 / **相反 2** | 一致 4 / 弱 4 / 相反 0 |

**"鼻子相对眼睛"在 16 次真实仰头里 0 次反例**，幅度中位数 0.065、绝大多数 ≥0.028。
它是**平移无关**的：身体动一下只会让整张脸平移，不改变这个相对量。

**修复**：新增 [TILT_REF_CONFIRM] = 0.015 —— 仰头候选必须满足
`relNoseDy ≤ −0.015` 才放行（仅近距离生效，远距离不受影响）。
这条正好打掉用户说的「我身体一动就会误触」：身体平移不改变相对量，因此进不来。

### 未动

30cm 静止硬锁定、静止锁定、跳变确认、手机运动闸门、距离档、方向仲裁（v5.8）、
眨眼 4 次、反转俯仰方向开关、低头方向的 2.5° 档 —— 全部原样。

---

---

## [5.24] - 2026-09-16

**v5.23 的 51 次标注对照数据，给出了"参考点位移能不能替代角度"的定量答案：**

| 标注事件 | 轨道 A（绝对位移） | 轨道 B（平移无关） |
|---|---|---|
| **点头 31 次** | 一致 8 / 无信息 19 / **相反 4** | **一致 21 / 无信息 4 / 相反 6** ✓ |
| **仰头 20 次** | 一致 3 / 无信息 14 / **相反 3** | 一致 9 / 无信息 2 / **相反 9** ✗ |

结论有两条，必须分开说：

1. **"点头"这一半，用户的思路成立** ✓：平移无关轨道在 31 次真实点头里 21 次方向一致、
   只有 4 次"无信息"，是一个可用的判据。
2. **"仰头"这一半目前不可用** ✗：9 对 9，等于掷硬币。**所以本版不能按原计划把主判据
   整体切到位移通道** —— 那会把仰头搞坏，比现在更差。

### 本版（v5.24）因此只做一件事：把仰头这一半的信号找出来

新增**关键点明细日志**（`abs a=… b=… | rel a=… b=…`，a = 鼻子、b = 下巴）：

- 合并成一个 `dy` 之后，"鼻子和下巴哪个更可靠"这类信息被平均掉了；
- 分开记录之后，一次实机动作就能判定：是**下巴（用户对仰头的描述）**在仰头时才有干净的
  竖直位移，还是**仰头这个动作本身就不产生竖直平移**（而是透视缩短，需要另一种度量）。

行为零变化：参考点通道仍然**只观测不接管**，触发权仍在 pitch 通道（= 用户满意的 v5.19 灵敏度）。
30cm 硬锁定、静止锁定、跳变确认、手机运动闸门、距离档、方向仲裁、眨眼 4 次 —— 全部未动。

---

---

## [5.23] - 2026-09-16

**v5.22 首轮实机数据给出的两个结论**（26 条对照记录）：

### ① 用户的符号约定**实测成立** ✓

pitch 通道真实触发的那一刻，位移通道的读数与动作方向一致：

`
19:01:53.566  pitchCh=-9.0°(点头)  →  noseDy=+0.086 chinDy=+0.065   脸往下移 ✓
19:01:56.050  pitchCh=+13.0°(仰头) →  noseDy=-0.036 chinDy=-0.034   脸往上移 ✓
`

两个关键点（鼻子/下巴）**同向** ✓，所以"下移 = 点头、上移 = 仰头"可以用。

### ② 但**绝对位移**的噪声底与真实动作一样大 ✗

4.5% 阈值下 `would-trigger` 达到 **54 次/分钟**，而真实仰头那一次位移只有 **0.035**
（低于阈值）。根因很直接：**脸在画面里的绝对位置会被"手机本身在动"带着走** ——
抬一下手、换个姿势都会让整个脸平移，于是"位移"里混进了手机平移。

### 修法：加一条**平移无关**的轨道，两条一起观测

- **轨道 A（绝对位移）**：`noseNormY` / `chinNormY`，就是用户描述的那个量。
- **轨道 B（平移无关）**：`noseRelEye` / `chinRelEye` = 关键点相对**眼睛中心**的
  归一化距离。手机平移时它**不变**，只有头部真的俯仰（透视缩短）才变 ——
  同一思路，但把"手机在动"这个污染源去掉了。阈值 2.5%（比 A 小，因为读数本来就小）。

两条轨道的 `dy` / `thr` / `spd` / `would` 计数 / 丢弃原因**全部打进日志**，
用同一次实机动作做对照，谁的信噪比高就用谁。

### 仍然只观测、不接管触发

触发权仍在 pitch 通道手里（本版行为与 v5.21 完全一致，零风险）。
下一步：按实测对照数据选定轨道与阈值 → v5.24 切主判据。

---

---

## [5.22] - 2026-09-16

用户提出**换掉"硬算角度"**，改用**参考点位移**：

> 「用一个参考点，来计算我点头还是仰头。标准的仰头就是我的下巴会往上移动几厘米，
> 点头就是鼻子会往下移动几厘米。做一个参考点，**不能硬算**。」

### 做法：新通道完整实现，但本版**只观测、不接管触发**

**为什么分两步**：符号（图像坐标里"鼻子下移"是哪一侧）与阈值必须用**实测数据**确认，
不能猜 —— v5.17 / v5.18 两轮"全部乱了"都是猜符号造成的。所以本版把参考点通道
**完整跑起来、每帧计算、全程打日志**，而**触发权仍归原有 pitch 通道**，
现有可用行为零风险；拿到实测符号与阈值后，v5.23 再把它切成主判据。

### 实现（[ReferencePointDetector] + 分析器新增关键点归一化坐标）

1. FaceGazeAnalyzer 新增三个量（都归一化到**脸框内**）：
   `noseNormY = (noseY − boxTop) / boxHeight`、`chinNormY`（`MOUTH_BOTTOM`）、
   `eyeNormY`（眼睛中心，作不动参照）。除以脸框高度就是用户说的"距离归一化"。
2. **参考点**只在头部稳定时更新（连续 3 帧峰峰值 < 1.2% 脸高），
   所以它是"这次动作的起点"，不会像 45 帧中位数基线那样滞后 —— 这正是 v5.16~v5.21
   反复出问题的根源。
3. **位移**：`noseDy = noseNormY − refNose`、`chinDy = chinNormY − refChin`，
   单位是**脸高比例**（1.0 = 一个脸高）。同一动作下两个关键点同向，判定用二者均值
   `faceDy`；若反向（关键点抖动）则退回只信鼻子。
4. **方向**：图像坐标 Y 向下为正 —— 鼻子/下巴**下移** = 点头、**上移** = 仰头，
   与用户描述一致；`invert`（设置页「反转俯仰方向」）打开时两个方向**一起**反转。
5. **阈值**：默认 4.5% 脸高；近距离俯视时放宽到 **3.2%**（用户要求：只放宽上下方向）。
   另有一道时间窗（700ms 内从 onset 涨到阈值）、速度门限、以及单帧台阶上限
   （一帧跳超 6% 脸高判为关键点抖动）。
6. **日志**（用户要求格式）：
   - 诊断行新增 `ref noseDy=… chinDy=… dy=… thr=… spd=… would=… opt=…`；
   - 每次 pitch 通道真实触发时，紧跟一行 `RefPoint: nodDown ref: …`，
     用来对照"真实点头在位移通道上长什么样"；
   - 位移通道"本来会触发"时打 `would-trigger …`（400ms 节流）。

### 未动的东西

30cm 静止硬锁定、静止锁定、跳变确认、手机运动闸门（陀螺仪+加速度计）、距离档、
俯视判据、阈值表、方向仲裁（v5.8）、眨眼 4 次档、用户自己拨的「反转俯仰方向」——
全部保持 v5.21 原样。

---

---

## [5.21] - 2026-09-16

用户提出的思路（这一版就是照它做的）：

> 「控制开关里有个设置叫**反转俯仰方向**。你先别管是上滑还是下拉，只要调好灵敏度，
> **我自己开这个开关**。我觉得 **5.19 的那个灵敏度最合适**，你抄过来。我们一个一个的解决，先解决这个再说。」

### 做法：把 v5.19 的判据抄回来（撤销 v5.20 的回退）

`git checkout 28ee911 -- HeadPoseDetector.kt GazeCameraService.kt` —— 恢复轻通道的
**运动起点判向**（`settledPitch` + 同域相减 `signedPitch - settledPitch`），
也就是 v5.18 日志里"7 次点头 7 次下滑、3.3~6.6° 就触发"的那套灵敏度。

**方向问题从此交给用户的「反转俯仰方向」开关**：那套判据的方向是**跟着运动走**的
（位移的符号），所以：

- 开关拨到任意一侧，**点头与仰头会一起反过来**，不会出现"两个方向各自为政"；
- 用户自己拨一次就能得到自己期望的对应关系，不需要我们猜。

实机确认：用户**已经自己把该开关拨到 `headPoseInvertPitch = false`**（原为 true）。

### 顺带修一个副作用

在设置页拨开关时，app 会把**整份配置**存一遍，把之前用 adb 设好的
`blinkTriggerCount = 4` 覆盖回了 3。已重新设为 4（用户想用 3 次在设置页一点即可）。

### 保留不变

静止锁定、跳变确认（v5.15）、手机自身运动闸门（陀螺仪 + 加速度计）、
距离档与俯视判据、阈值表（近距俯视低头 2.5°）、眨眼 4 次档 —— 全部未动。

---

---

## [5.20] - 2026-09-16

**按用户明确要求回退**：检测逻辑回退到 v5.16（`git checkout a63ee54 -- HeadPoseDetector.kt GazeCameraService.kt`），
只保留 v5.19 的**眨眼 4 次档**。用户的话是「先不回退…不试了，立刻回退到 v5.16（并把眨眼 4 次档一起带上）」。

### 回退掉了什么

v5.17 / v5.18 的**轻通道位移判向**（`settledPitch` + `light=起点→终点`）整体撤销。
它是 v5.17 引入、v5.18 修掉域混用 bug 之后的版本，在 v5.18 的实测里方向是对的：

`
04:32:41.100 nodDown light=yes(8.5→-3.4) → swipe DOWN（上一个视频）✓   ← 7 次点头 7 次下滑
`

但用户仍然表示"点头变成了上滑"，并选择回退。**记录在此，供以后参考**。

### 回退前先把证据固定下来（避免以后重复排查）

1. **手势几何是对的**：`AdaptiveSwipe.pathFor` 里
   `UP -> 0.70→0.30`（手指往上 = 下一个）、`DOWN -> 0.30→0.70`（手指往下 = 上一个）；
   `SwipeInjector` 的注释也写明 `DOWN` = previous item。所以 app 发"下滑"时确实是"上一个视频"。
2. **上滑只有一个来源：眨眼**。`GazeCameraService` 里眨眼触发写死 `SwipeDirection.UP`。
   v5.18 日志里全场唯一一次上滑，前面紧跟 3 连眨：

   `
   04:33:13.259  blink #9  gap=4603ms run=1/3
   04:33:13.609  blink #10 gap=350ms  run=2/3
   04:33:14.193  blink #11 gap=583ms  run=3/3
   04:33:14.200  swipe UP
   `

   **"点头时看到跳到下一个视频"最可能就是它**：点头时眼皮会动，凑够 3 连眨就发出一次上滑。
3. v5.16 本身也存在**基准线滞后导致小幅度判反方向**的问题（用户当时原话就是
   「轻轻点头，没有下滑，倒是变成了上滑」），回退后该现象可能重现 —— 这是本次回退的已知代价，
   在得到用户同意后明确接受。

### 已知保留项

- **眨眼 4 次档**（v5.19 引入）保留，且设备已设为 4 次 —— 这是"不由自主 3 连眨"的直接对策。
- 静止锁定、跳变确认（v5.15）、手机自身运动闸门（陀螺仪 + 加速度计）、
  距离档与俯视判据（基准俯仰角）、阈值表（近距俯视低头 2.5°）**全部保留**。

### 回退点

`e5daedd` v5.12 / `a63ee54` v5.16 / `ef25e44` v5.18 / `28ee911` v5.19 —— 随时可切换。

---

---

## [5.19] - 2026-09-16

用户复测 v5.18：**「这个点头触发我很喜欢，基本很灵敏」**，但同时说「和之前一样，点头变成了上滑」，
以及「静止不动还会误触」。建议回退到问题最小的那一版。

### 先把日志证据摊开：v5.18 的方向其实是**对的**

`
04:32:41.100 nodDown light=yes(8.5→-3.4) → swipe DOWN（上一个视频）✓
04:32:44.501 nodDown light=yes(5.7→-6.6) → swipe DOWN ✓
04:32:47.713 nodDown light=yes(6.3→-5.8) → swipe DOWN ✓
04:32:50.002 nodDown light=yes(6.3→-5.3) → swipe DOWN ✓
04:32:53.586 nodDown light=yes(4.1→-3.9) → swipe DOWN ✓
04:32:56.572 nodDown light=yes(5.9→-3.3) → swipe DOWN ✓
04:32:58.700 nodDown light=yes(5.9→-4.6) → swipe DOWN ✓
`

**7 次点头、7 次下滑，方向与灵敏度都对**（3.3~6.6° 就触发）。手势几何也核对过：
`SwipeInjector` 里 `DOWN` 的注释就是 *Finger travels down the screen: previous item* ✓。

### 而全场唯一那次上滑，是**不由自主连眨 3 次**触发的

`
04:33:13.259  blink #9  gap=4603ms run=1/3
04:33:13.609  blink #10 gap=350ms  run=2/3
04:33:14.193  blink #11 gap=583ms  run=3/3   ← 3 连眨凑够门槛
04:33:14.200  swipe UP                        ← 眨眼按设计就是上滑
`

这就是用户说的「静止不动还会误触」—— 眨眼触发在服务里写死是 `SwipeDirection.UP`，
所以它永远是上滑。而门槛恰好是 3 次，用户"控制不了"的连续眨眼正是 3 连眨。

**修复**：眨眼次数新增 **4 次**档（设置页可选，`SUPPORTED_COUNTS` 与三处 `coerceIn` 同步放开到 4），
并已把设备设成 4 次。这样"不由自主的 3 连眨"不再触发，而**有意的**眨眼只要眨 4 下即可 ——
沿用"把门槛交回用户本意"这条在 v5.10 被验证有效的做法。

### 关于回退

用户提出回退到"问题最小的那一版"。本版**没有回退**，理由是日志显示 v5.18 的点头方向与
灵敏度正是用户满意的部分，回退会把它一起丢掉；而唯一的"误触"来源（不由自主的连续眨眼）
已经用 4 次门槛处理。git 里保留了完整回退点（`e5daedd` v5.12、`a63ee54` v5.16 …），
一条命令即可切换。

---

## [5.18] - 2026-09-16

用户复测 v5.17：**「全部乱了」** —— 轻点头全变上拉、连续点头也是、走路方向乱、
静止 30 秒更是"人都没动就疯狂上拉"。

### 这是我 v5.17 的一个实现错误（一行），不是方案问题

```kotlin
// v5.17 初版（错）
val lightDelta = pitchDeg - settledPitch      // ✗ 域混用
val signedLight = if (invertPitch) -lightDelta else lightDelta
```

`pitchDeg` 是**原始俯仰角**，而 `settledPitch` 是用 `signedPitch` 记下来的**有符号值**
（已经过 `invertPitch` 翻转、并且扣掉了基准线）。两者相减等于：

1. 把方向**整体翻了一次**（用户 `invertPitch=true`）→ 轻点头全变上拉；
2. 还叠上了一个基准线偏移，而这个偏移本身在抖动 → 差值的量级远大于真实位移
   → 阈值被频繁顶穿 → **静止时"疯狂上拉"**。

**修复**：位移必须在**同一个域**里算 —— 两个量都用有符号值：

```kotlin
val signedLight = signedPitch - settledPitch   // ✓ 同域相减
```

修完之后的语义是干净的：`settledPitch` 是动作起点（有符号域），
`signedLight` 就是"这次移动了多少、往哪个方向"，负值 = 低头方向 → nodDown。

触发日志会打出 `light=yes(起点→终点)`，例如 `light=yes(0.3→-2.8)` 表示从 +0.3 移动到 −2.8，
方向与幅度可以直接在日志里核对，不用再靠感觉判断。

---

## [5.17] - 2026-09-16

用户复测 v5.16：

> 「②**轻轻点头，没有下滑，倒是变成了上滑**，这个有问题」
> 「④连续点头 5 秒没啥问题，但是角度要大一点」
> 「⑤走路触发的概率太小了，只成功了两次……我走路的时候尝试过眨眼，但是没触发眨眼」
> 「⑥30 秒，还是误触了两次」

### ① 主修：轻点头被判成上滑 —— 根因是**滞后的基准线**

```
04:18:23.300  rawPitch=11.28°  base=5.12°  → signed=-6.2° → nodDown（下滑）✓
04:18:25.316  rawPitch=2.82°   base=9.52°  → signed=+6.7° → tiltUp（上滑）✗
```

同一个方向的位移，因为 `base` 在 2 秒内从 5.12 跳到 9.52（中途丢过一次脸、基线重学）
就判成了相反方向。`baselineDeg` 是 **45 帧中位数、本身在追用户的姿势**，
而轻点头的位移只有 5~8° —— 与滞后的基线一比，**起点落在基线哪一侧**就决定了方向，
所以**小幅度动作必然判反**。这也解释了 ⑤ 走路阶段那 8 次"上滑"（很可能就是用户的点头）。

**修复**：轻通道（近距离 + 俯视）改用**运动起点的稳定值** `settledPitch` 作参考点：

- `settledPitch` 只在头部稳定时更新（连续 3 帧峰峰值 < 1.0°），所以它天然是"这次动作的起点"，
  不会滞后好几度；
- 位移 = 当前值 − 稳定值，**方向 = 位移符号、幅度 = 位移大小**。

于是"点头 = 向低头方向移动 2.5°"变成一句能兑现的话，与基线漂到哪无关。
只作用于轻通道：远距离、仰头方向、回中锁定、方向仲裁、晃动判据一律照旧用相对基线的量。

### ② ⑥ 的两次误触：抖动 + 单帧跳变骗过了跳变确认

```
04:21:01.987  pitch: … -0.2  -2.1  -0.4  -2.5  **-12.5**
```

前一帧 −2.5° 恰好已在阈值附近（那一帧被晃动判据拦下了），于是"前一帧已经在动"成立、
跳变确认没有要求再撑一帧，接着一帧跳到 −12.5° 就放行了。

**修复**：跳变确认再加一条"**台阶限制**"——相邻两帧的位移超过 **4°** 就不算渐进动作。
实测轻点头每帧只走 1~2°、有意仰头每帧 2~5°，所以不会误伤正常动作；
代价只是"极快的大幅动作"也要多撑一帧（约 90ms）。

### ③ 走路时眨眼不触发：很可能是冷却被走路误触吃掉了（推测，待验证）

查日志：走路阶段眨眼**本身是正常识别的**（`blink #15~#23`、闭眼 184~506ms），
但 `run=1/3` 始终没凑够 —— 因为**全局冷却是 2 秒**，而走路阶段那些误触（上滑）
不停地重置冷却，眨眼触发被一起吞掉了。① 把走路误触压下去之后应该会自然缓解，
所以本版**不动眨眼逻辑**（间隔窗口仍是 200~1500ms、需要 3 次），留待实测确认。

---

## [5.16] - 2026-09-16

用户复测 v5.15：

> 「②90 秒俯视不动，已经很完美了，没有误触发，**别动了**」
> 「④轻轻点头还是有问题……我轻轻点头然后触发了，然后我等了 2 秒再点头，不触发，
> 得要更大的角度才能触发，而且触发有延迟，**要等 0.5 秒**」
> 「⑥小幅度轻轻晃动，误触了两次好像，还是一次，问题不大」
> 「⑧我走路的时候疯狂点头，但是**只触发了一次**，然后就再也不触发了，还不如第一版」

②的成功是这一版**刻意不动**的部分，改动全部集中在"放行真实动作"这一侧。

### ① `eyes-unreliable` 把真实动作挡了 126 次 → 窗口 120ms 收到 **50ms**

v5.14 加的"闭眼后姿态不可信期"是这一轮最大的问题。日志统计：

```
eyes-unreliable 拒绝 126 次（tiltUp 98 + nodDown 28），且全是真实动作：
  tiltUp  candidate rejected: pitch=25.2° reason=eyes-unreliable
  tiltUp  candidate rejected: pitch=17.5° reason=eyes-unreliable
  nodDown candidate rejected: pitch=-3.2° reason=eyes-unreliable   ← 用户的轻点头
```

原因是 30cm 处"某只眼睛跌破阈值"极其频繁（日志里几乎每秒都有），120ms 的窗口把头部判定
压制了近三成时间 —— 用户说的「**要等 0.5 秒**」就是这么来的（在等这个窗口过期）。

**修复**：窗口 120ms → **50ms**（略小于一帧 63~116ms），效果是**只跳过闭眼那一帧本身**。
它后面那些帧交给 v5.15 的**跳变确认**处理 —— 实测那次眨眼误触正是一帧从 +0.1 跳到 −3.5，
跳变确认足以拦住，所以不需要 120ms 这么长的窗口。

### ② 晃动判据从「路径效率」换成「方向反转次数」→ 走路时的点头不再被误杀

v5.15 里 19 次真实点头被 `reason=shake` 拒掉（幅度 −6.4~−9.3°、速度 0.05~0.08°/ms），
全部集中在走路阶段 —— 用户「走路时疯狂点头只触发了一次」。

根因是**判据选错**：走路时头部俯仰是**真实的周期性摆动（10~25°）**，
于是任何窗口里都含摆动 → 路径效率必然低 → 一律判成晃动。这不是调参能解决的。

**新判据**数"方向反转"，因为两者的**频率**差得很远：

```
走路（约 2Hz，帧间隔 ~90ms）：约 5~6 帧才反转一次 → 6 帧窗口内 ≤1 次  → 放行
有意点头：单调推进，几乎没有反转                  → 0~1 次           → 放行
手抖 / 高频摇晃（3~5Hz）：1~2 帧就反转一次        → 3~6 次           → 拦下
```

低频摇摆手机那种（v5.14 ⑥）由陀螺仪闸门负责 —— 那是"手机在转"，比数头部反转更直接。
路径效率仍然会算出来打进日志（`shake=`），只是不再用于判决。

### ③ 轻点头速度门槛 0.013 → **0.009**

日志里用户"很轻地试"的那几次速度只有 **0.006~0.011°/ms**，落在 0.013 之下，
于是掉进"保持"通道被拒（`reason=hold-not-met` / `speed-gate`）。
单帧跳变现在由跳变确认负责拦，所以速度门槛可以放得更松，让"轻轻一点"也能过。

### ④ 刻意没动的部分

② 之所以完美，靠的是**静止锁定 + 跳变确认 + 手机自身运动闸门**这三道，
本版一律未改；距离档（0.46/0.40 回滞）、俯视判据（基准俯仰角）、
阈值表（近距俯视低头 2.5°）也都保持原样。

---

## [5.15] - 2026-09-16

用户复测 v5.14：

> 「②**误触了 4 次以上，说实话不如上一版**」「④轻轻点头还是不触发，不是不触发，是**概率触发**，
> 我触发了一次，要等 0.5 秒后才能触发点头」「⑥轻轻晃，我能接受的晃动下，还是误触了两次」
> 「⑧我模拟日常走路，触发是会触发点头，能用，但是会**误触**，可能是角度的问题，你看日志」

### ① 主修：区分「渐进动作」与「单帧跳变」（这次是数据指路，不再猜判据）

日志里两种形状一眼可分：

```
真实轻点头：… -0.2  0.0  0.3  -2.0  -2.8  -2.4  -2.7  -3.4   ← 越阈值那帧的前一帧已经在动
误触      ：… -0.5  0.4  -3.1                              ← 前一帧还在 +0.4，一帧跳过去
```

**真实动作是渐进的**：越过阈值那一帧的**前一帧已经在向同一方向移动**；
而噪声、眨眼造成的跳变是"前一帧还在 0 附近、一帧跳过去"。这与 v5.14 修的那个
眨眼误触（浅闭后 107ms 的一帧 −3.5°）是同一类。

**修复**：越阈值时检查**前一帧**（250ms 内）是否已达 onset 门限（阈值×0.4）：

- **渐进动作 → 零延迟**，照旧立刻触发，手感完全不变；
- **跳变型 → 要求再撑过一帧**（本帧 ≥ 阈值、下一帧仍 ≥ 阈值才放行）。
  单帧跳变下一帧必然掉回阈值以下，被 `below-threshold` 清掉，**永远不会触发**。

⚠️ **这不是 v5.9 那个被撤销的全局确认窗口**。v5.9 要求"最近 3 帧至少 2 帧越过
**完整阈值**"，于是轻点头被二次抬高门槛，用户反馈"要更大角度、要等 0.5 秒"。
这次只针对**跳变型**（渐进型完全不受影响），代价只有跳变动作多一帧（约 90ms）。

### ② 「概率触发、要等 0.5 秒」：晃动过滤器在误伤连续点头

日志里 25 次点头候选被 `reason=shake` 拦掉，其中不少是真实动作：

```
nodDown rejected: pitch=-4.6° speed=0.0443°/ms reason=shake (shake=0.05)
nodDown rejected: pitch=-9.3° speed=0.0651°/ms reason=shake (shake=0.31)
```

这些集中在走路阶段（04:00:35~04:00:43）—— 那里晃动过滤器其实**拦对了**
（走路时头部俯仰本来就是周期性摆动，见下）。但连续点头时它也会误伤，
所以本版把它的判定与新增的跳变确认分开：跳变确认负责"一帧的假信号"，
路径效率负责"来回摆的真晃动"，两者不再互相盖住。

### ③ 诊断修正：v5.14 的 accel/gyro 整场都是 0.00，白瞎了一整轮数据

原因是 `recentPeak()` 的窗口只有 **1000ms**，而 `GazeDiag` 每 **3 秒**才打一行 ——
于是诊断行几乎永远读到 0，**走路、晃手机时全都看不见**。v5.15 把窗口放宽到 5000ms
（必须 ≥ 诊断间隔），并把 `PhoneMotion` 也加进抓取标签（上一版它的日志被过滤掉了，
所以"闸门到底有没有生效"都看不出来）。

### ④ 已知仍未解决：走路时的误触（诚实记录）

走路时**头部俯仰本身就会摆动 10~20°**（实测 tiltUp 触发在 8.5~20.5°），
这已经远高于仰头阈值 6.0°，而且它是**真实的头部运动**，不是噪声 ——
摄像头看到的信号与"有意仰头"几乎一致。目前只能靠**路径效率**（周期性摆动）部分挡住，
所以走路时仍会有误触。要真正解决得引入头部之外的信息（例如步态周期检测），
留给后续版本，本版不擅自扩大改动范围。

---

## [5.14] - 2026-09-16

用户复测 v5.13：

> 「②30cm 俯视倒是没啥问题，不过触发了一次，**触发的时候我不受控制的自己眨眼了几次**」
> 「④**轻轻点头还是不触发**，必须还是要稍微动作大一点……我希望近距离俯视的情况下，
> 点头幅度**再小一点点**就完美了」
> 「⑥急停好像没啥问题」「⑦轻轻晃动的话，**误触会多得多**……我要你的做的就是，
> **小幅度的晃动不影响就行**，要是大幅度的晃动误触那也没招」

### ① 用户报的那次误触，日志给出了完整因果链 —— 是眨眼导致的姿态跳变

```
03:44:00.020  I/Blink: closure rejected: 63ms frames=1/2 minEye=0.98/0.14     ← 单帧浅闭
03:44:00.127  I/HeadPose: nodDown triggered pitch=-3.5° speed=0.037°/ms
              ctx: … -99ms|0.1 … 0ms|-3.5                                      ← 一帧从 +0.1 跳到 -3.5
```

用户"不受控制地眨眼"的判断是对的，而且机制很具体：**ML Kit 的头部姿态是靠关键点回归的**，
眼睛闭上时眼部关键点消失，那一帧的姿态估计会跳。注意元凶是 `frames=1/2` 的**一帧浅闭**
——它永远不会被确认成"眨眼"，所以只盯 `eyesClosed` 是抓不到的。

**修复**：新增 `eyeDip`（本帧有眼睛跌破闭眼阈值，**含单帧浅闭**）+ 之后 **120ms** 的
"姿态不可信期"，期间俯仰/偏航候选一律不接受（理由 `eyes-unreliable`）。120ms ≈ 一帧多，
足够跨过跳变那一帧，又短到不影响真实动作。实测相邻帧间隔 63~116ms，
而被挡的那次发生在浅闭后约 107ms，正好落在窗口里。

### ② 阈值表统一：修掉一个一直漏着的方向泄漏 + 点头再降一档

#### 修 bug：v5.4 的俯视增益是**双向**的，偷偷把仰头阈值从 6.0° 压到 4.5°

实机日志（v5.13，用户设定灵敏度 6.0°）：

```
tiltUp triggered: pitch=8.4° speed=0.0369°/ms threshold=4.5° dist=near
诊断分组：th=4.5 thUp=2.3 nodBoost=1.00 downGazeActive=true   ← 337 帧里有 133 帧处于此状态
```

`nodDownGazeBoost = 0.75` 是 v5.4 引入的"俯视补偿"，它乘在**共用**的俯仰阈值上，
所以仰头方向也吃到了 0.75 → 阈值 4.5°。**这与 v5.7 修掉的是同一个错误**
（当年只改了"近距离增益"那一条，这条基于**相对基准线偏移**的一直漏着）。
"被动仰视 4.5~6° 就触发"正是它。

同时它还与 v5.12 的近距离俯视档**叠乘**，使低头阈值在那条状态开关时于
**2.25° 与 3.0° 之间来回跳**（133/337 帧处于该状态）—— 手感时灵时不灵。

**修复**：俯视增益只作用于**低头**方向，且**不与近距离档叠乘**（近距离档用的是可靠判据
"基准俯仰角"，已经把俯视算进去了）。修完之后的完整阈值表（灵敏度设 6.0°）：

| 条件 | 低头 | 仰头 |
|---|---|---|
| 远距离 | 4.5°（俯视补偿生效）/ 6.0° | **6.0°**（修复前是 4.5°/6.0°） |
| 近距离，平视 | 4.08° | 6.0° |
| **近距离 + 俯视** | **2.5°**（稳定值，修复前在 2.25°/3.0° 之间跳） | 6.0° |

#### 点头再降一档：3.0° → **2.5°**

用户要"再小一点点"。v5.12 那一版是 3.0°，而日志里他的轻点头落在 3.0~4.4°，
3.0° 还是卡在门上。现在 **2.5°**，并且是**稳定**的（不再随 v5.4 那条状态抖动）。

### ③ 轻晃误触：陀螺仪才是对的信号（加速度计看不见"摇"）

v5.13 只用直线加速度，实机复测证明**抓不到用户的轻晃**：整场会话线性加速度峰值
最高只到 **2.3 m/s²**、`lurch detected` **一行都没有**，而用户报告"轻晃误触多得多"。

原因是**手晃手机主要是"转"而不是"平移"**：绕手腕转动时传感器的直线加速度可以很小，
角速度却很大。而这恰恰能把两件事分开：

| | 头 | 手机 |
|---|---|---|
| **点头** | 绕脖子转 | 基本不动 → 角速度 ≈ 0 |
| **手晃 / 地铁摇摆 / 急刹** | 被动跟着 | **自己在转** → 角速度 0.6~10 rad/s |

**修复**：加 `TYPE_GYROSCOPE`，角速度 ≥ **0.6 rad/s**（≈35°/秒）即判定"手机在动"，
500ms 内不接受俯仰/偏航候选；加速度门限同步从 3.5 降到 **2.5 m/s²**。
两个峰值都打进诊断行（`accel=` / `gyro=`）与拒绝日志，下一版要调门限有实测数字可依。

顺带说明为什么"路径效率"抓不到这种轻晃：用户的"晃"是**低频摇摆**（日志里俯仰平滑地
±5° 来回走，每半周期都像一次单向动作），效率判据自然不成立 —— 这也解释了
v5.12 那次晃动误触为什么没被拦住。

---

## [5.13] - 2026-09-16

用户复测 v5.12：

> 「2，没有误触；4，**点头都成功了**，但是出现了有一个阶段，我疯狂点头没有触发，然后又等了 2 秒左右
> 才正常触发；6，眨眼三次我试了好像 4 次，成功了三次，失败了一次，能接受；7，晃动测试……
> 还行，只误触发了一次。我的想法是**允许轻轻的摇晃不触发**，但如果遇到那种**急停**
> —— 比如我走在街上看着手机突然停下来，或者地铁上、公交车上**急刹车**时，不要误触。」

本版修两件事。

### ① 「疯狂点头却没反应」：轻点头在被速度门槛挡死

日志坐实了原因 —— 他的轻点头速度只有 **0.014~0.018°/ms**，而"快通道"的门槛是 **0.02**：

```
03:33:39.430 nodDown rejected: pitch=-3.2° speed=0.0179°/ms reason=hold-not-met
03:34:02.575 nodDown rejected: pitch=-2.6° speed=0.0183°/ms reason=below-threshold
03:34:07.626 nodDown rejected: pitch=-3.5° speed=0.0161°/ms reason=hold-not-met
03:34:07.983 nodDown triggered: pitch=-4.7° speed=0.0290°/ms   ← 等它自己变快才成
```

够不到 0.02 就落到"保持"通道，而那条通道要求**保持 150ms 且速度 ≥ 速度门限** ——
近距离速度门限是 `0.012×2 = 0.024`，**比 0.02 还高**，所以那条路**实际上永远走不通**。
净效果是"不够快的点头一律无效"，而轻点头天然就慢。

**修复**：新增**轻点头通道**（[LIGHT_NOD_FAST_VELOCITY] = 0.013），条件与 ×0.50 增益
**完全一致**（近距离 + 俯视 + 低头），所以"阈值压到 3.0°"与"速度门槛降到 0.013"
永远同时生效，不会只松一半。远距离与仰头方向一概不受影响。

通道还要求**多撑过一帧**（60ms，实测帧间隔 63~116ms ⇒ 等价于"下一帧仍须在阈值之上"），
所以单帧跳变依旧进不来 —— 这正是 v5.9 那次全局确认窗口想做的事，
区别是现在只作用在这一条通道上，代价只有一帧，不拖慢其他动作。

### ② 「急停 / 急刹车不要误触」：用加速度计把"头在转"和"人和手机一起顿"分开

**先说明为什么摄像头分不出来**：急刹车时身体前倾，头部俯仰角会变化 5~15°，而且那一下
**是单向的**（不是来回抖），所以 v5.12 的路径效率判据挡不住 —— 实测那次晃动误触就是
`tiltUp 8.1°`，效率判据没触发。脸占比也不够干净（该次末 400ms ΔFR=0.04，
而有意动作也能到 0.04~0.06，重叠）。

但物理上有干净的分界：

| | 头 | 手机 |
|---|---|---|
| **点头** | 绕脖子转 | **几乎不动**（手持只有小抖动） |
| **急停 / 急刹 / 被撞** | 被动前倾 | **有明确的大幅直线加速度** |

所以新增 `PhoneMotionMonitor`：读**加速度计**（优先 `TYPE_LINEAR_ACCELERATION`，
没有就退回 `TYPE_ACCELEROMETER` 并用低通估计重力再减掉），线性加速度 ≥ **3.5 m/s²**
即判定"手机被顿了一下"，之后 **600ms** 内所有俯仰/偏航候选一律不接受（理由 `phone-motion`）。

门限的依据：坐着/躺着刷 **0~0.5 m/s²**；走路手持峰值 **1.5~3**（用户明确说这种轻晃"可以接受"）；
急刹车/突然停下/被撞 **4~15**。所以走路不会误伤，只有真正的顿挫才拦。

而且是**失败开放**：传感器不可用时 `isMoving()` 恒为 false，绝不因为读不到传感器
就把用户的手势一起废掉。峰值打到诊断行（`accel=`），于是"地铁上、走路时到底读到多少"
可以直接看日志 —— 下一版要调门限有实测数字可依。

---

## [5.12] - 2026-09-16

用户复测 v5.11：

> 「阶段 2，没有误触；阶段 4，**还是需要稍微用点角度**，我希望在俯视的状态下能**轻轻**地触发点头；
> 阶段 6，**我眼睛眨烂了，只触发了一次**，我甚至都怀疑触发的那一次是不是误触；阶段 7 没啥问题。
> 另外希望**专门针对近距离俯视点头**做优化……识别到我是近距离还是俯视，点头就稍微灵敏点，
> 而且还要**过滤掉晃动**（地铁上近距离俯视产生晃动然后误触）」

### ① 眨眼：v5.11 的"加帧数"必须撤销

**证据**：v5.11 那一整场会话里 `I/Blink:` **一行都没有** —— 也就是说 30cm 时**没有任何一次眨眼**
达到过判定条件。阶段② 的"0 误触"是**眨眼检测被废掉**换来的，不是真的防住了。

根因是帧数要求：实测帧间隔 63~116ms（**约 11fps**），加一帧就等于要求多闭眼约 90ms，
而一次正常眨眼总共才 150~250ms。v5.11 用了 3 帧 ≈ 270ms，于是用户的**有意眨眼**全被挡掉。

**修复**：近距离**只降阈值、不加帧数**（帧数要求恒等于用户设定值）。
这本来就是我给用户承诺的做法，v5.11 擅自改成 3 帧，教训与 v5.9/v5.10 完全一致：
**凡是会连带挡住真实动作的手段，都不能用来防误触。**

**并新增闭眼记录**：每一次抵达阈值的闭眼都有留痕，**包括被拒的**：

```
I/Blink: blink #12 closure=280ms minEye=0.04/0.07 below=0.30 needFrames=2 gap=940ms run=2/3 dist=near
I/Blink: closure rejected: 90ms frames=1/2 minEye=0.38/0.41 below=0.41 dist=near
```

v5.11 那次把用户的眨眼挡掉了却什么都看不到，只能靠用户说"眼睛眨烂了"才发现问题。
下一版要精调眨眼（有意 vs 不由自主），直接量这两行即可。

### ② 近距离俯视点头专用档：阈值 4.1° → **3.0°**

#### 证据：用户的"轻点头"正好卡在阈值上

```
03:20:02.275 nodDown rejected: pitch=-3.7° threshold=4.1° reason=below-threshold
03:20:05.152 nodDown rejected: pitch=-3.9° threshold=4.1° reason=below-threshold
03:21:20.565 nodDown rejected: pitch=-4.1° threshold=4.1° reason=below-threshold
03:21:20.464 nodDown rejected: pitch=-6.0° threshold=4.1° reason=hold-not-met (speed=0.0196<0.02)
```

用户要的"轻点头"幅度就是 **3.7~4.4°**，而 v5.6 那一档的阈值是 4.1° —— 卡在门上，
所以"有时候好使、有时候不好使"。

#### 俯视判据：改用**基准俯仰角**（v5.6 用 chinRatio 失败，这次换了信号）

`baselineDeg`（静止时人脸相对摄像头的绝对俯仰中位数）实测：

```
30cm 俯视   base = 7.8 ~ 14.4
50cm 平视   base = 0 ~ 2
```

分离干净，而且**不需要任何新传感器或新代码** —— 它本来就是基线学习中位数的输出。
v5.6 用几何比例 `|下巴Y−眼中心Y|/脸高` 失败是因为那个量主要在反映**距离**；
基准俯仰角反映的是**摄像头与脸的相对角度**，物理上直接对应"手机在脸下方、眼睛往下看"。

#### 增益分三档（仰头方向一律不动，v5.7 的原则保留）

| 条件 | 系数 | 阈值（灵敏度设 6.0°） |
|---|---|---|
| 近距离 + 俯视 + 低头 | ×0.50 | **3.0°** ← 本版新增 |
| 近距离 + 低头 | ×0.68 | 4.1°（v5.6） |
| 其他（含所有远距离、仰头方向） | ×1.00 | 6.0°（不变） |

远距离与仰头方向**完全不受影响** —— 该档要求「近距 + 俯视」两条同时成立。

### ③ 晃动过滤：路径效率

阈值压到 3.0° 之后必须有东西挡住晃动（用户明确点名"地铁上近距离俯视晃动然后误触"）。
现有防线（静止锁定/速度门限）都不适用：晃动**不是静止**，所以静止锁定反而失效。

新判据是**路径效率** = `|净位移| / Σ|相邻差值|`：

```
轻点头   0 → 1.0 → 2.5 → 3.5 → 4.2 → 4.2    路径 4.2   净位移 4.2   效率 1.00  ✅ 放行
晃动   -1.5 → 1.2 → -1.0 → 2.8 → -1.2 → 3.2  路径 17.1  净位移 4.7   效率 0.27  ⛔ 拦下
```

窗口 6 帧（约 0.5~0.7 秒，正好覆盖一次轻点头加它前面那段静止），效率低于 0.45 即判为晃动。
之所以不用"数方向反转次数"：效率把幅度与次数合成一个量，少一个要调的参数，
对低频大幅晃动（反转不多但来回走得很远）同样有效。

效率值挂到诊断行（`shake=`）与候选拒绝日志上，于是"地铁上到底算不算晃"可以直接读日志判断。

---

## [5.11] - 2026-09-16

用户复测 v5.10 的反馈：

> 「阶段 2 跳了 1 次」（v5.9 是 4 次）「阶段 4 确实丝滑了，但是**还是有需要角度大点才能点头触发下滑**」
> 「**不是有意的眨眼，是有时候人类的眼睛，会这样频繁的眨眼，控制不了**」

阶段② 的 4 次已经压到 1 次、手感也回来了；本版处理剩下的两件事。

### ① 方向仲裁：「偏航占优」第一次真的比较了两个轴的大小

#### 证据

用户明确在点头的那几秒里，**7 次连续的点头全部被拒**，最后不得不用 −17.3° 的大幅点头才成功：

```
03:10:35.990 nodDown candidate rejected: pitch=-7.7°  reason=yaw-dominant-arbitration
03:10:36.065 nodDown candidate rejected: pitch=-12.2° reason=yaw-dominant-arbitration
03:10:36.395 nodDown candidate rejected: pitch=-10.4° reason=yaw-dominant-arbitration
03:10:36.491 nodDown candidate rejected: pitch=-8.1°  reason=yaw-dominant-arbitration
03:10:38.802 nodDown candidate rejected: pitch=-4.7°  reason=yaw-dominant-arbitration
03:10:38.915 nodDown candidate rejected: pitch=-13.8° reason=yaw-dominant-arbitration
03:10:38.985 nodDown candidate rejected: pitch=-15.2° reason=yaw-dominant-arbitration
03:10:39.062 nodDown triggered                      pitch=-17.3°   ← 加大力气才成
```

阶段④ 里俯仰候选被这条规则拒掉 **39 次**（点头 19、仰头 18），是**最大的一类拒绝**。

#### 根因

v5.8 的仲裁判据**只有一条绝对速度门限**：

```kotlin
yawVelocity >= MIN_YAW_VELOCITY * ARBITRATION_VELOCITY_FRACTION   // 0.015 × 0.4 = 0.006°/ms
```

`0.006°/ms` 是 6°/秒，而 30cm 处偏航读数的**正常抖动**（±1~3°）在实测约 11fps 下折算出来
就是 **0.01~0.03°/ms**，轻松越线。函数叫「偏航占优」，可它从来没有比较过偏航和俯仰的大小。

#### 修复：三条判据缺一不可

1. **偏航确实在动** —— 保留 v5.8 的原始判据（0.006°/ms）；
2. **偏航强过俯仰**（新增）—— 这才是"占优"的字面含义。实测：真实扭头 0.13~0.25°/ms，
   扭头带出的俯仰分量约 0.02°/ms；点头时俯仰 0.04~0.13°/ms、偏航抖动 0.01~0.02°/ms。
   **速度的比较在两个方向上都有鉴别力**；
3. **头部确实偏到轴外**（新增）—— 至少偏到扭头阈值的 30%（用户设 20° → 6°）。
   真实扭头实测 23~45°，而点头时偏航抖动只有 ±1~3°。

⚠️ 第 3 条是**闸门**，不是 v5.8 文档里警告过的"幅度兜底判据"：那次的错误是拿幅度**替代**
速度判据，结果把合格扭头挡掉了。这里是"速度已成立之后，再要求幅度也成立"，两条同时满足
才让位 —— 真正的扭头照样让位，姿态抖动不再误伤点头。

### ② 眨眼的"近距离收紧"终于真正生效（v5.3 设计、一直空转）

#### 证据

`BlinkDetector` 一直用**自己**的 `faceRatio >= 0.55` 判断近距离，而用户 30cm 实测只有
**0.49~0.51** —— 于是收紧**从未生效**，一直在跑中距档（诊断行 `blinkBelow=0.41 blinkFrames=2`）。
而 v5.10 的实测已经坐实：静止 90 秒里的误触**全部**是 `lastTrigger=blink`。

#### 修复

把这个判断改成读 **v5.9 那个统一的距离档**（带 EMA + 回差，由服务每帧同步），
于是 30cm 时终于进入近距离档。

**闭眼帧数：设计值是 4 帧，本版取 3 帧。** 依据是这次终于量到了真实帧率——
实机 ctx 日志里相邻帧间隔 63~116ms（**约 11fps**），4 帧 ≈ 要求连续闭眼 **360ms 以上**，
已经超出一次正常眨眼的总时长，很可能把用户**有意的**眨眼触发一起挡掉。
本项目刚在 v5.10 因为「连带拖慢真实动作」撤过一次改动，不能重犯。3 帧 ≈ 270ms：
足够挡掉短促的自然眨眼，又给有意眨眼留一档余量。

### ③ 新增逐次眨眼诊断

每次**被计数的眨眼**都留痕，这是判断「有意眨眼 vs 不由自主的眨眼」唯一的可靠依据：

```
I/Blink: blink #12 closure=280ms minEye=0.04/0.07 below=0.30 needFrames=3 gap=940ms run=2/3 dist=near
```

字段：闭眼持续多久、左右眼各自最深读到多少、当前生效阈值与帧数要求、与上一次的间隔、累计到几次。
下一版要再调眨眼，直接看这一行就能定量决定，不用再猜。

---

## [5.10] - 2026-09-16

用户按同一流程（阶段②静止 90 秒 / 阶段④正常刷 / 阶段⑥故意动作）复测 v5.9，反馈：

> 「第二，误触了 4 次，应该不是眨眼，我眨眼设置的是触发 3 次才能翻页」「第四，点头需要更大的角度
> 才能触发，不像之前轻轻点头就能触发」「第六，仰头和低头倒是成功了，只不过需要很大的角度才能触发，
> 而且我仰头和点头要等一会，大概 0.5 秒，不像之前那么丝滑了」

结论：**v5.9 的「触发确认窗口」必须撤销**，同时找到了误触的**真正来源**（眨眼）。

### ① 撤销 v5.9 的触发确认窗口（手感回归 v5.8）

撤销依据来自这次带标记的实测（`v59-verify.log`）：

| 阶段 | 头部越阈值被确认窗口挡掉的次数 | 误触次数 |
|---|---|---|
| ② 静止 90 秒（30cm 俯视） | **0** | 3~4（**全部是眨眼**） |
| ④ 正常刷 | 21 | — |
| ⑥ 故意动作 | 16 | — |

也就是说：确认窗口在**静止阶段一次都没派上用场**（因为误触根本不在头部链路上），
却在正常使用中挡掉了 **37 次**越阈值 —— 用户感到的"要更大的角度""要等 0.5 秒""不丝滑"
正是它造成的。轻快的点头往往只跨一帧阈值，被它整个吃掉。

**这已经是本项目第三次踩同一个坑**（v5.5 的幅度放大、v5.7 的单向增益、现在的确认窗口）：
**凡是会连带拖慢真实动作的手段，都不能用来防误触。** 顺序必须是
「先确定误触来自哪个检测器 → 再只动那一条链路」。

### ② 误触的真正来源：眨眼（这是本次最重要的发现）

阶段② 那 90 秒里，头部路径的越阈值次数是 **0**，而所有翻页的诊断行都是
`lastTrigger=blink`：

```
02:58:47  lastTrigger=blink   ← 静止
02:58:54  lastTrigger=blink
02:59:35  lastTrigger=blink
blinks 计数：35 → 49（90 秒内自然眨眼 14 次）
blinkBelow=0.41 blinkFrames=2 faceRatio=0.49~0.51 distMode=near
```

两个叠加的原因：

1. **用户设置里存的是 2 次，不是 3 次**。用户明确说「我眨眼设置的是触发 3 次才能翻页」，
   而设备上的 `blinkTriggerCount=2`（UI 代码本身没有 bug，`rbBlink3 → 3` 的映射是对的，
   是值被存成了 2）。`needBlinks=2` 意味着**任意两次自然眨眼间隔落在 0.2~1.5 秒内就翻页**，
   而自然眨眼约 9~15 次/分钟，成对出现非常常见 —— 静止 90 秒误触 3~4 次完全对得上。
2. **近距离的眨眼收紧没有生效**：`BlinkDetector` 自己用 `faceRatio >= 0.55` 判断近距离，
   而用户 30cm 实测是 0.49~0.51，所以用的是中距档 `blinkBelow=0.41 / 2 帧`，
   而设计中的近距离档是 `0.30 / 4 帧`。

**本版处理**：把设备上的 `blinkTriggerCount` 改成 **3**（这正是用户以为的设置，零风险、
且不影响任何有意眨眼的手感）。

**未处理（留给下一版，附风险分析）**：把 `BlinkDetector` 也接到 v5.9 那个统一的距离档上，
让 30cm 真正进入 `0.30 / 4 帧`。之所以先不动：4 帧在实测帧率（约 17fps）下等于要求
闭眼 **235ms** 以上，可能把用户**主要的**有意眨眼触发也一起挡掉 —— 这正是①刚刚踩过的坑。
先用「2 次→3 次」这一条零风险的改动看效果，不够再动它，并优先只降阈值（0.41→0.34）
而不是加帧数。

### ③ 保留 v5.9 的距离档统一（这条是对的）

`v59-verify.log` 里距离档切换只剩 **4 次/8 分钟**（v5.9 之前是逐帧乱跳）：

```
02:57:53  distance tier: mid/far -> near (faceRatio raw=0.53 smoothed=0.47) -> boost=×2.00 speedGate=0.0240°/ms staticRange=3.0° nodDownThreshold=4.1°
03:01:15  distance tier: near -> mid/far (raw=0.33 smoothed=0.39) -> boost=×1.35 …
```

所以「点头省力增益」在 30cm 时**稳定生效**（`nodDownThreshold=4.1°`），不再时有时无。

---

## [5.9] - 2026-09-16

用户实测反馈：**30cm 俯视状态下还是会误触，太灵敏了**，并且明确了方向 ——
**误触是「上滑 / 跳到下一个视频」（tiltUp）**。

本版做两件事，并**新增逐帧上下文日志**（这是本版最重要的产出，见 ③）。

### ① 距离档统一 + 滞回：修掉「灵敏度按近距离给、防护按中距离算」的空档

#### 实机数据（4 分钟带标记采集，v5.9 之前）

用户 30cm 俯视时的 `faceRatio` 稳定落在 **0.45~0.55**，而代码里三处「近距离」判据
用了**两个不同的门槛**：

| 判据 | 门槛 | 用户 30cm 时实际状态 |
|---|---|---|
| 点头省力增益 0.68（阈值 6.0°→4.1°） | `faceRatio >= 0.50` | ✅ 生效（但逐帧抖动） |
| 速度门限 ×2 / 静止峰峰值 ×2 | `faceRatio >= 0.55` | ❌ 只吃到 ×1.35 |
| 30cm 静止硬锁定 | `faceRatio >= 0.55` | ❌ **从未生效** |
| 眨眼收紧（0.407→0.30、2→4 帧） | `faceRatio >= 0.55` | ❌ 没生效（日志 `blinkBelow=0.41 blinkFrames=2`） |

于是 0.50~0.55 这一段成了「**最灵敏、防护最弱**」的空档，而且这条线正好落在用户的
抖动带里，导致 `nodBoost` 在 0.68/1.00、`distMode` 在 near/far 之间**逐帧乱跳**：

```
02:42:25  FR=0.50  distMode=far   pitchTh=6.0°   ← 抖一下变 far，增益没了
02:42:28  FR=0.49  distMode=near  pitchThUp=4.1° ← 再抖一下变 near
02:44:33  FR=0.51  distMode=near  nodBoost=0.68  gate=0.0243
02:44:36  FR=0.51  distMode=near  nodBoost=1.00  gate=0.0243
```

> 顺带查明：**「30cm 静止硬锁定」在全部历史日志的 700+ 条诊断行里只生效过 2 次**
> （且那两次 `faceRatio=0.66/0.67`，是更近的距离）。也就是说它在用户真实的 30cm
> 使用中**从来没有启动过** —— 门槛 0.55 高于用户实际的脸占比。本版**未改动它的判据**
> （按用户要求"不要动 30cm 静止防误触"），只把发现记录在此，留给下一版用数据决定。

**修复**：只保留**一个**真值 `nearDistance`，由 `NEAR_ENTER_RATIO=0.46` /
`NEAR_EXIT_RATIO=0.40` 回差 + `faceRatio` 的 EMA 平滑共同决定，点头增益、速度门限、
静止峰峰值全部读它。0.46 略低于用户实测区间下沿，保证 30cm 稳定判为近距离；
50cm（0.37~0.41）与 40cm（约 0.42~0.45）**回不到近距离档**，所以远距离手感不变。

### ② 触发确认窗口：单帧跳变不再能翻页

#### 为什么不能靠调阈值解决

实测数据摆在一起看，**幅度和速度这两个瞬时量都已经没有鉴别力**：

```
误触（俯视 30cm，用户未主动动作）：幅度 6.0~7.8°，速度 0.021~0.048°/ms
有意仰头                        ：幅度 7.7~9.7°，速度 0.019~0.048°/ms
```

两者**完全重叠**。按数据反推：若把近距离速度门限提到 0.036°/ms（静止锁定时 ×1.5 的值），
**74% 的真实仰头会被一起挡掉** —— 这正是本项目在 v5.5/v5.7 反复踩过的坑
（"用错工具会把真实动作一起挡掉"）。

真正有鉴别力的是**时间形状**：检测噪声与「手机在手里抖一下」只能维持一帧就回到原位，
真实动作至少跨两帧。

**修复**：阈值必须在**最近 3 帧里至少 2 帧**被越过才允许触发（`CONFIRM_NEEDED=2`）。

- 用「2 of 3」而不是「连续 2 帧」：真实动作在阈值附近上下浮动（实测 ±1.5°）时
  仍然能凑够 2 帧，而孤立尖峰永远凑不够；
- 代价只有**一帧延迟**（实测帧间隔 47~66ms），快通道仍然立刻触发、
  不做 `requiredHoldMs` 那种长保持，所以"动作要快"的手感不变。

### ③ 新增逐帧上下文日志（排查手段的根本改进）

之前只有 3 秒一条的 `GazeDiag` 汇总行，**误触发生前那几百毫秒到底发生了什么完全看不到**，
所以此前几轮排查只能靠猜。本版起：

- **每次触发**都打印前约 1.2 秒的 `(dt, pitch, yaw, faceRatio)` 原始读数：
  ```
  tiltUp triggered: pitch=8.8° ... — ctx 24f: -235ms|2.0/0.4/0.32 -167ms|4.8/1.1/0.32 0ms|8.8/3.3/0.33
  ```
  「单帧尖峰」是孤立的一个大值，「平滑上升」是一串递增的值，日志里一眼可辨。
- **被确认窗口丢掉的帧**打印 `DISCARDED ...`（含同样的上下文），这是验证本版修复的
  主要依据。
- 人脸消失 ≥400ms 后回来打一条 `MARK face-back lost=…ms`：用户按
  「手挡摄像头 3 秒」做阶段标记时，标记不再可能认错（上一轮采集就是因为只有 3 秒采样，
  整段时间轴没法对齐，白跑一遍）。
- 距离档切换打一条 `distance tier: …`，含切换后实际生效的 boost / speedGate /
  staticRange / 点头阈值。

### 验证

`v59-verify.log`（后台断线自动重连抓取，`tools\capture-loop.ps1`）。已确认：
DISCARDED 行大量出现且上下文里都是「一帧跳到阈值以上」，而 `triggered` 明显变少。

---

## [5.8] - 2026-09-16

用户实测反馈：**近距离俯视时左右扭头不好使，会变成上下滑**；远距离俯视正常。
用户自己给的猜测是「近距离灵敏度调太高了，扭头信号被误判成上下滑」——**方向完全正确**。

本版只修这一个问题，其余一律未动。

### 根因：三个独立缺陷叠在一起

#### ① 扭头阈值在近距离被放大到 2 倍（主因）

`distanceBoost()` 在 `faceRatio >= 0.45` 时返回 **2.0**，而 `turnThresholdNow()` 乘了它：

```kotlin
private fun turnThresholdNow(distanceBoost: Float) = turnThresholdDeg * distanceBoost
```

于是用户设的「中 **20°**」在 30cm 处实际生效值是 **40°**。实机日志正好抓在门限上：

```
01:57:37.093 turn triggered yaw=40.5° latency=102ms (fast)
GazeDiag: faceRatio=0.51 dist=中 ... yawTh=40.0° speedGate=0.0240°/ms
```

而用户的扭头幅度就是 40~52° —— **刚好卡在门上，小一点就完全不触发**。

这和 v5.5 在俯仰轴上修掉的**是同一个错误**：当年也想用"放大幅度阈值"来挡近距离噪声，
后来证明那是错的方法（噪声有幅度但没有速度，该管它的是速度门限）。
俯仰轴 v5.5 改过之后没有回头改偏航轴，这条一直留到现在。

**修复**：`turnThresholdNow()` 不再乘距离系数，**两个轴统一**为
「幅度阈值 = 用户设定值」。距离缩放只作用于速度门限与静止门限。
远距离行为完全不变（系数本来就是 1.0）。

#### ② `slow-rise` 把正常的扭头判死（症状：第一下打不中）

扭头的 `turnMotionWindowMs` 是共用的 600ms，而**这个窗口对扭头先天太紧**：

- 俯仰起点容易定准 —— 点头一开始俯仰角就在变；
- 扭头起点常常**先有一段几乎不动的准备**（脖子先转、脸还没跟上），这段被算进
  rise，于是正常的一次扭头会超窗，被当成"慢慢蹭"直接判死。

实机日志里这是**最多的一类扭头拒绝**，且症状正好是用户说的「第一下打不中」：

```
turnL candidate rejected: yaw=54.4° speed=0.1496°/ms threshold=20.0° reason=slow-rise
turnL candidate rejected: yaw=39.4° speed=0.1116°/ms reason=slow-rise   // 连续 8 帧
```

54° 的扭头 + 0.1496°/ms 的速度（远高于 0.015 的速度门限）显然是一次真实动作。
**"够不够快"该由速度门限定义，不是由这个窗口定义** —— 又是"用错工具"。
**修复**：`turnMotionWindowMs` 默认 600 → **900ms**。

#### ③ 偏航在转动时，俯仰会抢先触发（这才是"变成上下滑"的直接原因）

真人在俯视姿态下扭头，头部不会纯绕 Y 轴旋转 —— 脖子带着一个俯仰分量。
实测这个分量是 **±3~±10°**，而近距离点头阈值被单向增益压到 **4.1°**（灵敏度 6°），
所以扭头必然先把俯仰判定打穿。而 `onHeadPose` 里**点头是先于扭头评估的**：

```
02:00:25.848 nodDown triggered pitch=-12.0°
02:00:25.850 turnR candidate rejected: yaw=23.5° ... reason=below-threshold
```

远距离俯仰阈值是 6.0°，同样的分量常常够不到，所以**远距离正常** —— 与用户观察完全一致。

**修复：方向仲裁。** 判据**只看角速度**，因为这是两个轴之间唯一干净的分界：

- 真正的扭头是绕 Y 轴转动 → **偏航角速度必然明显**；
- 真正的点头是绕 X 轴转动 → **偏航角速度几乎为零**；
- 而"扭头带出的俯仰分量"在偏航轴上一定带着那个转动速度。

`|偏航速度| >= MIN_YAW_VELOCITY × 0.4`（0.006°/ms）时，点头/仰头一律让位；
反向也对称地做了（偏航候选让位给俯仰）。门限由**实机 149 个扭头样本**标定：
真实扭头的偏航速度中位数 0.0126、p90 0.158、max 0.248，而静止/保持姿态时接近 0。

### 三次实机迭代（每一轮都由日志推翻上一轮的假设）

这一版是三次"改 → 装机 → 抓日志"循环的结果，中间两个错误值得记下来：

1. **第一版仲裁用了幅度兜底判据**（`yaw >= 阈值×0.6`）→ 反而**把合格的扭头挡掉了**：
   `yaw=30.7° speed=0.2481°/ms reason=pitch-dominant-arbitration`。
   原因很直白：扭头时偏航幅度本来就大，拿幅度当判据等于把"我正在扭头"误判成
   "俯仰更强"。**幅度在仲裁里没有鉴别力。**
2. **第二版仲裁读的是上一帧的偏航**。俯仰先于偏航评估，所以俯仰侧看到的偏航速度
   还停在上一个采样点，导致自己的耦合检测失灵。改为 `yawVelocityWith(本帧读数)` 后，
   日志立刻出现成对事件，说明修复方向对：
   ```
   02:05:27.413 nodDown candidate rejected: pitch=-9.9° reason=yaw-dominant-arbitration
   02:05:27.415 turn triggered yaw=28.5° latency=114ms (fast)
   ```

### 实机结果（v5.8 最终版，用户按测试序列操作 40 秒）

| 事件 | 次数 |
| --- | --- |
| **turn triggered（左 7 + 右 6）** | **13** |
| nodDown triggered | 4 |
| tiltUp triggered | 5 |
| 俯仰候选被"偏航主导"正确否决 | 20 |

扭头全部成功触发，且每次扭头期间的俯仰候选都被仲裁挡下。

### 新增诊断

- **扭头候选拒绝日志**（以前扭头**完全没有候选日志**，只有成功触发才打，
  这正是"近距离扭头不好使"查不出来的原因）：
  ```
  turnL candidate rejected: yaw=43.6° pitch=0.6° speed=0.0000°/ms gate=0.0203°/ms
      pitchSpeed=0.0000°/ms threshold=20.0° dist=far posture=flat reason=hold-not-met
  ```
- 诊断行新增 `yawNow` / `pitchNow` / `pitchYielded`。

### 保留

- **30cm 静止防误触**：逻辑未改动。
- **近距离俯视点头增益（v5.6）**：保留（`pitchTh=4.1°` 仍在生效）。
- **v5.7 仰视单向增益**：保留（实机 `tiltUp triggered: pitch=7.6° threshold=6.0°`）。
- **v5.7 息屏唤醒加固**：保留。
- 眨眼、张嘴点击、自适应滑动、滑动柔度、全局冷却、全局翻页开关全部未动。
- 远距离点头/仰头与扭头手感与 v5.7 一致。

### 验证状态

| 项目 | 状态 | 依据 |
| --- | --- | --- |
| 近距离扭头稳定触发 | ✅ 实机 | 13 次 turn triggered（左 7 / 右 6），`yawTh=20.0°` |
| 不再变成上下滑 | ✅ 实机 | 20 次俯仰候选被 `yaw-dominant-arbitration` 正确否决，且与 turn 触发成对出现 |
| 近距离点头增益未坏 | ✅ 实机 | `nodDown triggered (boosted, dist=near threshold=4.1°)` |
| 仰视单向增益未坏 | ✅ 实机 | `tiltUp triggered: pitch=7.6° threshold=6.0°` |
| 远距离扭头未受影响 | ✅ 实机 | `dist=far` 下 `yawTh=27.0°` 正常触发 |
| 30cm 静止防误触不回归 | ⏳ 待用户确认 | 逻辑未改动 |
| 长时间使用手感 | ⏳ 待用户确认 | 需要日常使用反馈 |

---

## [5.7] - 2026-09-16

本版只做两件事：修掉用户实测反馈的两个问题。**没有顺手改任何其他稳定逻辑**
（用户明确要求「一步一步来，别修炸」）。

### 修复一：近距离仰视误触

**现象**：约 30cm 近距离时仰视会误触，同样距离平视不会；远距离正常。

**根因**：v5.6 的近距离点头增益（×0.68）被乘在了**共用的俯仰阈值**上，
于是**仰头方向也一起吃到了这个系数**。用户灵敏度设为 8° 时，近距离仰头只要
**5.4°** 就触发；设为 6°（灵敏度「高」）时只要 **4.1°**。

从上一版的真机日志里把这个座位挖出来了（触发与检测器状态按时间轴关联）：

| 触发 | pitch | 当时 faceRatio | 当时 pitchTh |
| --- | --- | --- | --- |
| tiltUp | **4.2°** | 0.54（近） | **4.1°** |
| tiltUp | 6.0° / 6.1° / 6.3° / 6.6° / 6.7° | 0.47~0.51（近） | **6.0°** |
| tiltUp | 9.1° / 11.6°（用户**主动**做的） | ~0.30（远） | 8.0° |

整段日志里 **45 次 tiltUp vs 14 次 nodDown**，比例本身就说明了问题：
被动仰视（顺着脖子往后靠、抬头看远处）幅度就在 4~7°，而用户主动仰头是 9~11°。

**修复**：把增益改成**严格单向**——`nearNodDownBoost(signedPitch)` 只在
**低头方向**（`signedPitch < 0`）生效，抬头方向回到用户设定值。

这样正好满足用户的要求「被动仰视不触发，主动仰头才触发」：
被动仰视（4~7°）够不到 8°，主动仰头（9~11°）照样过。

> 为什么不能用"抬高仰头阈值"来修：那会把**主动**仰头一起挡掉。单向增益
> 才能在保住「近距离点头更省力」的同时不动仰头的手感。

**未改动**：速度门限（`speedGate`）本身对两个方向一视同仁，静止锁定、遮挡抑制、
回中锁定、30cm 静止硬锁定全部未动。**30cm 静止防误触逻辑零改动。**

**新增诊断**：`pitchTh`（当前方向）与 **`pitchThUp`（仰头方向）** 同时打出来，
近距离下看到 `pitchTh=5.4° pitchThUp=8.0°` 就说明单向增益生效。
仰头触发也改成完整判据行（用户要求的格式）：

```
tiltUp triggered: pitch=6.2° speed=0.0260°/ms threshold=8.0° dist=far boost=1.00 latency=108ms (fast)
tiltUp candidate rejected: pitch=6.0° speed=0.0090°/ms gate=0.0162°/ms threshold=8.0° dist=near boost=1.00 reason=speed-gate
```

顺带修掉一个**会误导排查**的日志缺陷：两个方向的候选拒绝行原本都打
`nodDown candidate rejected`，排查仰头问题时会把仰头候选误读成点头。现在按方向打
`nodDown` / `tiltUp`，并附带 `dist` 与 `boost`。

### 修复二：隔一会再打开抖音又不触发

用户反馈 v5.5/v5.6 修过的老问题在 v5.6 之后又复现。这次补的是**排查手段**与
**两个真实的死角**。

#### 死角 A：整条恢复链都挂在同一个可能出错的布尔值上

```kotlin
private fun shouldAnalyze(): Boolean =
    screenActive && (AppStateManager.targetActive || AppStateManager.forceActive)
```

`targetActive` 一旦错成 `false`，相机、看门狗、liveness 探针**全部被这一个条件挡住**，
没有任何一条路径会去纠正它。而且 `checkBlind()` 治不了这个方向——它治的是
「前台判不出来」（fail-open），不是「判错了」（fail-closed 到错误的一边）。

**修复**：新增独立的复核 [AppStateManager.fixContradiction]，**不信任自己记的前台**，
直接向无障碍服务再问一次"现在活动的窗口是谁"。若它明确是目标应用、而我们却认为不是，
且持续 2 秒，就强制按"允许翻页"重算一次。判据要求"明确"：读不到窗口时不做任何事，
避免把正常的省电待机误判成故障。

日志：`contradiction: active window is com.ss.android.ugc.aweme (allowed) but
targetActive=false for 2100ms — forcing an immediate re-evaluation`。

#### 死角 B：相机"绑着却没帧"可以无限循环，永不升级

`checkFrames()` 每秒重绑一次，但它**每次都会把 `lastFrameAtMs` 和计时清零**，于是
「重绑 → 宽限期 → 重判 → 再重绑」可以一直循环。如果坏的是 CameraX 那一层
（而不是绑定关系），重绑一万次也没用——只有丢掉 provider 重新 `getInstance` 才能治好。

**修复**：新增**独立于重绑**的计时 `staleBeganAtMs`（只在**真的收到帧**时清零，
见 `onFrame`），持续 12 秒无画面就丢弃 provider 重新获取（与人工点通知里的「重启」等价），
日志 `hard resync #N`。

#### 死角 C：无障碍强制重绑只做一次

`ensureAccessibilityBound` 在实例缺失时只做一次 `repairIfNeeded()`，之后就一直
"重试中"，从日志上看像是已经处理过了。现在改为按**持续缺失时长**升级：
超过 10 秒仍拿不到实例就 `forceRebind`（关掉再打开，等价于用户手动下拉状态栏的效果），
并累计 `a11yForceRebinds` 供诊断。

#### 排查手段：把"最关键的那个分叉"变成可读的日志

这套失效最难自查的地方在于**分不清是"系统没把窗口事件发给我们"还是"事件到了我们判错了"**。
只看 `foreground=` 是分不出来的——那是我们对事件的**理解**，不是事件本身。

所以新增两个计数（在 `onAccessibilityEvent` 里**无条件**记账，包括被过滤掉的 systemui 事件）：

- `a11yEvents` —— 无障碍窗口事件累计次数；
- `a11yEventAgoMs` —— 距上一次窗口事件多久。

再加上 `hardResyncs` / `noFrameForMs` / `a11yForceRebinds`，`GazeSelfCheck` 现在能
在一行里直接指出卡在哪一环。用户要求的标记也补齐了：

```
GazeDiag: foreground change: com.miui.home -> com.ss.android.ugc.aweme (target=true reason=change)
GazeCameraService: pipeline resync (app-switch): forcing rebind (cameraBound=true wasStale=true)
GazeDiag: rebind reason=user-present -> ACTION_USER_PRESENT full restart
```

### 保留

- **30cm 静止防误触**：逻辑未改动，只新增了诊断字段。近距离静止硬锁定仍在
  `faceRatio >= 0.55`、1.2 秒稳定判定、二次确认。
- **近距离俯视点头增益（v5.6）**：保留，且把 `pitchTh=5.4°`（远距离 8.0°）的真机数据
  写进了验证清单。
- 眨眼、点头、仰头、左右扭头、张嘴点击、自适应滑动、滑动柔度、全局冷却、全局翻页开关
  全部未动。远距离点头/仰头手感与 v5.6 一致。

> ⚠️ 本版的两处修复**尚未完成完整实机回归**（开发期间手机无线调试断连）。
> 已完成的验证见下方"验证状态"一节；标为「待实测」的项需要用户跑一遍。

### 验证状态

| 项目 | 状态 | 依据 |
| --- | --- | --- |
| 近距离仰头阈值不再被压低 | ✅ 已由代码与字节码确认 | `nearDownNodBoost` 已不存在；`nearNodDownBoost(signedPitch)` 只对 `< 0` 生效 |
| 编译 | ✅ 全量重编通过 | `compileDebugKotlin --rerun-tasks`，字节码含 `pitchThresholdUpDeg` 等新符号 |
| 近距离仰视不误触 | ⏳ 待实测 | 需要手机在线抓日志确认 `pitchThUp=8.0°` 且 4~7° 的 tiltUp 被 `below-threshold` 拒绝 |
| 主动仰头仍然触发 | ⏳ 待实测 | 9~11° 应照常触发 |
| 近距离点头仍然省力 | ⏳ 待实测 | `pitchTh=5.4°`、触发日志含 `boosted` |
| 30cm 静止防误触不回归 | ⏳ 待实测 | 逻辑未改动 |
| 重开抖音自动恢复 | ⏳ 待实测 | 需要按用户要求做「打开抖音 → 息屏 3~5 分钟 → 亮屏解锁 → 打开抖音 → 尝试翻页」的反复挂测 |

---

## [5.6] - 2026-09-16

### 新增：距离/姿态判定，近距离点头更省力

用户反馈「近距离俯视刷抖音时脖子很累」，并给出很直观的设想：**脸大 = 近距离，
下巴占比大 = 俯视**。按这个思路实现了绝对几何判据：

- **距离**：脸高占画面比例（`faceRatio`）
- **姿态**：`|下巴Y − 眼中心Y| / 脸高`（`chinRatio`）——低头时相机从上方拍脸，
  下巴在透视上被拉长，占比变大

用绝对几何而不是"相对基准线的俯仰偏移"，因为后者会被基准线的自适应吃掉：
用户一直俯视时基准线早就移过去了，相对偏移趋近 0 —— **实测 v5.5 的 `downGaze` 全程为 0，
正是这个原因**。

### ⚠️ 但姿态判据经实机标定后**没有启用**，而且原因值得记录

让用户按「平视 / 俯视 / 近距离俯视 / 回平视」各保持 20 秒，按时间轴对齐后得到：

| 时段 | faceRatio | chinRatio |
| --- | --- | --- |
| 远距 | 0.37 ~ 0.41 | **0.335 ~ 0.368** |
| 近距（第一段） | 0.51 ~ 0.66 | **0.368 ~ 0.421** |
| 近距（第二段） | 0.61 ~ 0.65 | **0.405 ~ 0.432** |

两个结论：

1. **几何推算的门限（0.68）在实测中从未达到**——那样写等于功能不存在；
2. 更重要的是：**`chinRatio` 主要在反映距离，而不是姿态**（近距比远距高约 0.06，
   而"俯视"本身只带来很小的额外变化）。区分度不足以驱动灵敏度开关。

**所以改成只用距离驱动**（`faceRatio` 是干净、可靠、已验证的信号）。
这同时满足用户的两条硬要求——「近距离更轻松」与「远距离俯视不要改错」——
因为远距离本就不在增益范围内。

- 近距离（`faceRatio ≥ 0.50`，实测约 30~40cm）点头阈值压到 **0.68 倍**；
- 远距离完全不变；
- 只压**幅度**阈值：速度门限、静止锁定、近距离静止硬锁定一律不动，
  所以"噪声有幅度没有速度"这道关卡照旧，**30cm 静止防误触未被改动**。

诊断行新增：`chinRatio`（原始俯视几何）、`chinMed`（滑动中位数，供后续标定）、
`dist=near/far`、`posture=down/flat`、`nodBoost`、`nodBoostActive`、`speedGate`。
触发时打印：`nodDown triggered (boosted, dist=near posture=flat threshold=4.1°)`。

> 俯视几何的代码与诊断**保留**，等拿到更干净的分组数据（同一距离下只改变姿态）再考虑启用。
> 现在**绝不**把区分度不足的信号接到灵敏度开关上。

### 修复：屏幕常亮时偶尔仍失效（v5.5 遗留）

两处"确认无事"式的静默跳过：

1. **`ensurePipelineForActive()` 在 `cameraBound == true` 时什么都不做**
   （只打印 `camera already bound`）。于是"相机的确有句柄、但已经很久没有帧"这种
   卡死状态会被反复确认成正常，永远不自愈。现在改为检查**实际是否还有帧**
   （`wasStale`），只要没画面就强制重绑，不再信任这个标志位。
2. **liveness 探针第一次发现卡死时也走同一条路径**，因此同样被"确认无事"吃掉一轮，
   用户感受到的就是"偶尔失效好几秒"。现在第一次就直接强恢复。

另外 `ensurePipelineForActive` 现在会在开始时记录"之前是否已经无帧"，顺序上必须在
重置计时之前判断——反了就永远看不出卡死。

### 保留

- **30cm 静止防误触逻辑未改动**（用户已验证有效）。
- v5.5 的成果全部保留：幅度阈值不膨胀、注入失败自动重连、全链路自检日志。

### 实测证据（发布前的真机运行，不是构造场景）

近距离增益确实生效——32 个诊断样本里 `pitchTh` 从远距离的 **8.0° 掉到 5.4°**
（= 8 × 0.68），手头灵敏度设为 6° 时则是 **6.0 → 4.1°**：

```
GazeDiag: faceRatio=0.60 dist=中 ... pitchTh=5.4° yawTh=27.0° ...
          chinRatio=0.405 chinMed=0.404 nodBoost=1.00 nodBoostActive=true
          speedGate=0.0162°/ms
HeadPose: tiltUp triggered pitch=9.1° latency=95ms (fast, v=0.054°/ms)
```

注意同一行里 `speedGate=0.0162°/ms` 是**距离缩放后**的速度门限——增益只压幅度、
速度门限照旧按距离走，这正是「30cm 静止防误触不回归」的机制保证。

屏幕常亮失效修复也在真机上抓到了现场（v5.5 会在这里静默跳过）：

```
GazeCameraService: pipeline resync (enter): forcing rebind (cameraBound=true wasStale=true)
GazeCameraService: camera released: enter: forced rebind
```

### 修正：两个自己造的诊断字段缺陷（同版本内修掉）

1. **`nodBoost` 打的是常量而不是实际生效值**。诊断行原本打 `nodDownGazeBoost`
   （俯视增益，默认 0.75 的对象），距离增益生效时它仍显示 `1.00`，
   而 `pitchTh` 其实已经乘过 0.68 —— **字段名与实际不符，排查时会误判成功能没生效**。
   现在 `pitchThresholdNow()` 把实际乘上去的系数记进 `lastAppliedNodBoost` 再打出来，
   保证 `nodBoost` 与 `pitchTh` 永远自洽。
2. **诊断行出现了两个 `dist=` 键**：原有的 `dist=近/中/远` 与新加的 `dist=near/far`
   语义不同（前者是握持距离档，后者是增益触发条件），同名字段会看错。
   新字段改名 **`distMode=near/far`**。

> 教训：诊断字段本身也是要 review 的代码。名字含糊或与实现脱节，比没有日志更危险——
> 它会主动把人引向错误的结论。

---

## [5.5] - 2026-09-15

### 修复：仰头/点头阈值被两层系数放大，导致「点头费劲」

这是用户两条抱怨（**近距离点头费劲**、**俯视时点头难受**）的**共同根因**。v5.4 的公式是：

```
阈值 = 用户设定值 × 静止锁定系数 × 距离系数
```

两个系数叠在一起，实测日志直接抓到（`GazeSelfCheck` + 诊断行）：

```
faceRatio=0.32 dist=远 staticLock=false  →  pitchTh=8.0°   yawTh=20.0°
faceRatio=0.32 dist=远 staticLock=true   →  pitchTh=12.0°  yawTh=30.0°   ← 静止锁定 ×1.5
```

也就是说：
- **30cm 时**距离系数 ×2，点头阈值 6° → 12°，要走两倍幅度；
- **静止时**静止锁定又 ×1.5，阈值再涨一半；
- 用户设定的「扭头 中 20°」实际生效值是 **30°**。

**关键认识**：抑制静止噪声**根本不需要放大幅度阈值**。噪声**有幅度但没有速度**，
[最低速度门限]已经在拦它。幅度阈值放大只会连带把真实动作一起挡掉——它不区分
"这是噪声还是动作"。

**修复**：把幅度阈值上的两层系数全部摘掉，改为

```
俯仰阈值 = 用户设定值 × 俯视增益        （8° 就是 8°，6° 就是 6°）
偏航阈值 = 用户设定值 × 距离系数        （"中 20°" 就是 20°）
```

而 [staticLockFactor] **改用到速度门限上**——这才是它该管的地方：静止时要求动作更快，
而不是要求幅度更大。真实动作的速度是噪声的十几倍，所以这条对正常点头/仰头几乎零成本。

距离缩放仍然作用于**所有防误触参数**（静止峰峰值门限、速度门限、偏航阈值），
所以用户已验证有效的「30cm 静止防误触」**一道防线没少**，只是不再靠"让动作更难做"来防误触。

诊断行新增 `pitchTh` / `yawTh` / `downGazeActive` / `nodBoost`，阈值是否被意外放大一眼可见。

### 优化：俯视状态下点头更轻松

俯视时头部本来就低着，再往下点的**可用行程**比平视时短，所以同样幅度的动作只能产生
更小的角度变化。现在检测到俯视姿态后，把点头阈值压到 **0.75 倍**（略大于六折的力度即可触发）。

- 只压**幅度**阈值：速度门限与静止锁定一律不动 → "动作要快"这条门槛没放松，
  噪声依然过不来（噪声有幅度没有速度）。
- 离开俯视 6 秒后自动恢复 1.0（避免刷视频时头一抬一低就反复切换灵敏度）。
- 日志：`posture: looking down detected (...) -> downGaze active, nod sensitivity boosted, threshold adjusted to 6.0°`
- 诊断字段：`downGazeActive` / `nodBoost`。

### 修复：息屏再开后需下拉状态栏（第三条路径）

v5.4 修了"恢复没被触发"和"恢复被错误标志挡住"，这一版找到**第三条**——在注入链上：

`AccessibilityBootstrap.repairIfNeeded()` 的第一句是
`if (GazeAccessibilityService.isConnected()) return`，而 `isConnected()` 只是
`instance != null`。**实例存在 ≠ 连接可用**：长时间息屏后无障碍连接可能已经失效，
但 `instance` 仍是旧引用。那种状态下：

- `repairIfNeeded()` 认为"已连接"，**什么都不做**；
- `dispatchGesture` 注入失败，只记一行日志就结束；
- 服务在跑、通知正常、摄像头也在分析，**但手势根本到不了目标 App**。

这正是"下拉状态栏就好了"的成因——下拉会产生一批窗口事件，让系统重新激活那个失效的连接。

修复：
- 新增 `AccessibilityBootstrap.forceRebind()`：**无条件**把无障碍条目关掉再打开，
  不等用户去下拉（`repairIfNeeded` 因为会提前返回，做不到这件事）。
- 新增**注入失败反馈回路**：连续 2 次注入失败就自动强制重连
  （`injFailures` 出现在自检日志里），成功一次即清零，避免偶发失败累积成误判。
- 亮屏恢复时也会检查一次无障碍绑定。

### 新增：全链路自检日志（便于定位偶发失效）

每 30 秒（以及亮屏时）打**一行**，把所有可能卡住的环节列全：

```
GazeSelfCheck: periodic powerInteractive=true screenActive=true running=true
  shouldAnalyze=true analyzing=true targetActive=true forceActive=true foreground=...
  cameraProvider=true cameraBound=true framesAgoMs=34 stale=false graceMs=0
  restartAttempts=0 probeFailures=0 rebinding=false a11yEnabled=true a11yConnected=true
  swipeReady=true backend=无障碍服务 injFailures=0 occlRemainMs=0 hardLock=false detectorArmed=true
```

判读方法：
| 现象 | 含义 |
| --- | --- |
| `powerInteractive=false` 而 `screenActive=true` | 电源状态错了（自校正未生效） |
| `a11yConnected=false` | 无障碍断开，注入必定失败 |
| `swipeReady=false` | 注入后端不可用 |
| `stale=true` 且 `cameraProvider=true` | 相机句柄在但没画面，需重绑 |
| `detectorArmed=false` | 摄像头在跑但检测器没被喂数据 |

---

## [5.4] - 2026-09-15

### 修复：息屏久了再打开抖音，偶尔仍需下拉状态栏（v5.3 只解决了一半）

v5.3 让恢复不再依赖「包名变化」，但**恢复本身可能被一个错误的标志挡住**。这一版找到两个死角：

**死角一：`screenActive` 会被错误的标志锁死。**
它只在收到 `ACTION_SCREEN_ON` / `ACTION_SCREEN_OFF` 广播时更新。HyperOS 省电策略下这些
广播**可能被丢掉**——一旦丢了，标志就永久停在错误值上：

```
广播丢失 → screenActive 停在 false
        → shouldAnalyze() 永远返回 false
        → 什么都不分析，而且 liveness 探针 / 看门狗 / 强恢复全被这一个条件挡住
        → 用户看到的："功能死了，下拉一下状态栏又活了"
```

修复：新增 **`syncPowerState()` 电源状态自校正**，每 800ms 用系统自己的
`PowerManager.isInteractive` 与标志比对，发现不一致就纠正并触发一次亮屏恢复。
`isInteractive` 是真相来源，不受广播丢失影响。
日志：`power state correction: screenActive=... but isInteractive=... — missed broadcast`

**死角二：`setScreenActive()` 和 `updateCameraState()` 都会"静默跳过"。**
前者有 `if (screenActive == active) return`；后者只在 `!cameraBound` 时才重绑。
于是"亮屏时标志已经是 true"或"相机栈坏了但标志还是 true"这两种情况，什么都不会发生。

修复：
- 亮屏时即使标志已为 true 也强制重新校准一次；
- 新增 **`onScreenAwake()`**：监听 `ACTION_SCREEN_ON` 与 **`ACTION_USER_PRESENT`**
  （解锁事件——亮屏但锁屏时相机拍到的是锁屏画面，恢复放在解锁后更准），
  无条件做一次**完整重建**：作废全部检测器与抑制状态 → 丢弃 provider 重新获取 →
  失败按 **500 / 1000 / 2000ms** 退避重试。
- 日志：`ACTION_USER_PRESENT -> proactive reactivation requested (...)`、
  `Periodic check: foreground=xxx, cameraRunning=false, rebind requested`。

> 附带修掉一个隐私问题：如果息屏广播丢了，相机原本会在**屏幕已熄灭的情况下继续运行**；
> 电源自校正现在也会把这种情况纠正回来。

### 优化：近距离（30cm）残余误触

v5.3 已经做了眨眼阈值收紧、静止锁定、速度门限、姿势偏置校正。这一版补齐剩下两处**不对称**：

- **扭头阈值现在也按距离放大**（v5.3 只放大了俯仰）。近距离下横向噪声同样被放大成十几度，
  而扭头阈值一直是固定的 20°，于是"手在脸旁晃一下"就够到。现在与俯仰共用同一套距离系数，
  近距离阈值变为 40°：真实扭头（实测 20~45°）仍够得到，噪声被挡在外面。
- **距离系数每帧只算一次**，俯仰与偏航共用同一份，保证两条轴行为一致（以前各算各的）。

诊断能力同步加强（便于定位残余误触的来源）：
- `lastTrigger=blink/nod/tilt/turnL/turnR` —— 上一次翻页是谁触发的
- `blinkBelow` / `blinkFrames` —— 眨眼判定**当前实际生效**的阈值与连续帧要求
- `staticHardLock` / `staticRange` / `staticLock` / `downGaze` / `biasRecenter`

> 近距离静止硬锁定（v5.3 引入）已满足本版要求：`faceRatio ≥ 0.55` 且连续 1.2 秒内
> 俯仰/偏航/眼睛开合度都无实质变化即停判。判据用「稳定时长」而非「方差」——
> 语义直观、不易被单个离群点影响，用户也能自行验证（"静止 1.2 秒后就不会误触"）。

### 新增：俯视姿态自动识别与补偿

用户大多数时间是**俯视**看手机（手机在下方、眼睛向下看），此时俯仰角长期偏负。
基准线若不跟着走，这个偏负的偏移会被当成"一直在低头"，白白吃掉阈值余量：
往下动一点立刻越界（误触），往上一动却要走很远（不灵敏）。

修复：把 v5.2 的「姿势偏置自动校正」扩展为显式的俯视检测。两条路径共用同一套动作，
只是触发所需的持续时间不同：

| 场景 | 条件 | 时长 |
| --- | --- | --- |
| **俯视姿态** | 相对基准线偏负 ≥ 3° | **2 秒** |
| 一般姿势偏置 | 偏离越过死区、未到阈值 | 1.5 秒 |

两者都要求「越过死区、但始终没到阈值」——到了阈值就是动作，绝不能把动作当成姿势。
判据基于**相对基准线**的偏移而非绝对角度，所以倒着拿手机、躺着看同样适用。
只在俯仰轴生效，不碰扭头与眨眼。

日志：`posture: looking down detected (held 2100ms, relative -6.2°), baseline gradually shifted to -14.8°`
诊断字段：`downGaze=N`（俯视补偿次数）、`biasRecenter=N`（一般姿势校正次数）。

---

## [5.3] - 2026-09-15

### 修复：切后台一段时间后，重开目标应用必须下拉状态栏才生效（长期遗留问题）

**根因**：`AppStateManager.commitPending()` 里有一句 `if (!force && packageName == foregroundPackage) return`，
导致整条恢复链**只在「前台包名发生变化」时**才会触发。于是当状态里记录的前台已经是抖音、
或这次切换压根没被观察到时：包名没变 → 服务收不到通知 → 相机不重绑 → 功能静默失效。

**下拉状态栏之所以"有效"**，是因为它人为制造了两次包名变化（`com.android.systemui` ↔ 抖音），
等于替我们触发了两次通知。这也解释了它为什么"有时出现有时不出现"，以及为什么跨了多个
版本都没根治——一直在修恢复动作本身，没修"恢复根本没被触发"这个前提。

**修复**：新增**主动存活探针**，由 `AppStateManager` 每 800ms 轮询驱动，不再依赖窗口事件。
三级递进恢复：
1. 应分析却无帧 → 强恢复（重绑相机）；
2. 连续多次仍无帧 → 丢弃 provider 重新获取（相机被其它进程占用时必需）；
3. 再不行 → 重建整条服务管线。

服务实例不存在时探针会直接把它拉起来。日志：`liveness probe: ... -> recovering`。

### 优化：30cm 近距离静止误触

**实测数据定位**：用户截图显示 `俯仰角 -2.08° / 基准线 -2.08°`（偏差 0°），
但 `眨眼累计 86 / 已触发 63` —— 偏差为 0 排除了点头，**误触来自眨眼检测**。

原因：ML Kit 的 `eyeOpenProbability` 是**绝对**读数、不随脸的大小变化，但离得越近，
睫毛 / 镜片反光 / 轻微眯眼造成的遮挡越明显，读数越容易跌破阈值。

**修复**（三道防线）：

1. **眨眼判定按距离收紧**：近距离时同时降低闭眼阈值（0.55 → 中距 0.41 → 近距 0.30）
   并提高连续帧要求（2 → 4 → 6）。只做一件不够——阈值降太多会漏掉真实眨眼，而真实眨眼
   在近距离下持续帧数本来就更多，所以提高帧数对真实眨眼几乎无损。
2. **近距离静止硬锁定**：`faceRatio ≥ 0.55` 且连续 1.2 秒内俯仰/偏航/眼睛开合度都无实质
   变化 → 判定"人没在动、画面在抖"，关掉全部翻页判定，直到出现明显动作。
   判据用"稳定时长"而非"方差"，语义直观、可自行验证。
3. 保留并复用 v5.2 的头部静止锁定与姿势偏置自动校正。

### 新增：触发来源诊断

诊断行新增 `lastTrigger=blink/nod/tilt/turnL/turnR`、`blinkBelow`、`blinkFrames`。
排查"静止疯狂误触"时这是第一个要看的东西——只看触发次数无法区分来源。

---

## [5.2] - 2026-09-15

### 修复：点头翻页完全失效（v5.1 引入的严重回归）

**根因**：v5.0 引入的**回中锁定**只在「角度回到中性区（`±阈值×0.4` = ±2.4°）并稳定 150ms」
时解除。而人的自然姿势常常长期偏离基准线（实测：`俯仰角 -2.85° / 基准线 3.69°`，偏差 6.5°），
**永远进不了中性区** → 锁永不解除 → 反向的点头被无限期屏蔽。

日志直接证实：`recenter lock released by timeout (400ms) — posture offset kept it out of the neutral zone`
——即中性区条件**从未生效过**，而当时并没有兜底。

**修复**：
- 加 **400ms 最长锁定兜底**（回正动作只要几百毫秒），同时这也是最初的设计意图
  （"回中抑制期 300–500ms"）；
- 让锁在头部**静止时**也参与解除判断（v5.1 把解除逻辑放在 onset 判断之后，静止时锁不释放）。

### 修复：速度采样错位

`evaluatePitch` / `evaluateYaw` 在放行路径上没有更新速度采样点，导致下一帧算出速度 0
而被最低速度门限误杀。统一收敛为「单一 reject 原因」结构，并新增诊断：

```
nodDown candidate rejected: pitch=-5.2° speed=0.0031°/ms gate=0.0120°/ms threshold=6.0° reason=speed-gate
```

拒绝原因：`below-onset` / `recenter-lock` / `below-threshold` / `slow-rise` / `hold-not-met` / `speed-gate`。

### 优化：姿势偏置自动校正（30cm 误触的根因）

实测偏差 6.5° 正好贴在 ±6° 阈值上——姿势**缓慢**变化（靠椅背、凑近屏幕）时，滑动窗口中位数
基准线跟不上，偏差被永久保留，白吃掉阈值余量。
新增：偏差越过死区、始终没到阈值、**且持续 1.5 秒**时，判定为「换了姿势」而不是动作，
把基准线渐进挪过去。判据干净：有意动作 500ms 内就结束或已触发，能维持 1.5 秒的只可能是姿势。

另外把**最低速度门限也按距离缩放**（50cm ×1 / 30cm ×2）：只抬高幅度门限挡不住偶尔偏大的
噪声样本，抬高速度门限才能区分开（噪声没有速度）。

---

## [5.1] - 2026-09-15

### 删除：头控音量功能

按要求整段移除（`HeadVolumeDetector.kt`、`AudioVolumeController.kt`、配置项、UI、字符串、
`AudioManager` 调用、诊断字段），全文扫描确认零残留，APK 资源表亦无残留。

> 实测澄清：该功能开关一直为 `false`（日志中 `vol=头控音量已关闭` 贯穿全程），
> 代码路径从未执行，因此删除它**不影响**翻页相关问题的表现。

### 修复：抖音等全屏播放器忽略注入的滑动手势

**发现**：日志显示 `GazeA11y: manual swipe ... ok=true` 但页面纹丝不动，且前台已正确识别为
抖音——手势发出去了、App 不响应。

**根因**：`dispatchGesture` 的**一条直线**相当于"一个采样点从起点直接跳到终点"，
而多数 App 的滚动手势识别（VelocityTracker + touch slop）期望多次坐标更新，
只看得到一次跳变就会忽略它。

**修复**：纵向滑动补 **6 个均匀分布的中间点**，让系统按真实手指轨迹派发多次 MOVE 事件。
已用**截屏像素对比**离线验证生效（设置页 ScrollView 被真实滚动，截图 MD5 改变）。

### 加强：近距离防误触

近距离静止门限与速度门限同步放大；手挡脸的多重遮挡判据（`face lost` / `mouth missing` /
`face size jump`）保留。

---

## [5.0] - 2026-09-14

### 修复：抖音不接受整屏滑动（参数化实测标定）

用新加的**参数化自测通道**（`am broadcast ... --ef from --ef to --el duration`）逐组试：

| 参数 | 抖音反应 |
| --- | --- |
| 90% / 130ms | 无效 |
| 60% / 400ms | 无效 |
| 45% / 350ms | 无效 |
| 50% / 700ms | 无效 |
| **39% / 150ms** | **有效，连试 3 次全部成功** |

结论：抖音认的是**短促快滑**，幅度过大反而被忽略（不是"越长越好"）。
抖音档因此从 `0.90→0.10 / 130ms` 改为 `0.70→0.30 / 150ms`。

### 新增：仰头回中锁定

修「仰头触发上滑后，把头放回正常位置的过程被判成点头，于是又下滑」——
仰头时俯仰角为正，回正会经过 0 继续变负，天然长得像一次反向动作。

### 新增：最低速度门限

噪声**有幅度但没有速度**：真实头部动作几十毫秒就能走完 6~20°，噪声做不到。
实测真实动作 0.037~0.136°/ms，门限 0.012°/ms，对正常动作零影响。

### 修复：张嘴检测基准被污染

实测抓到 `mouthBase=0.114`（正常闭嘴 0.205~0.23），一旦坏样本进了基准窗口就被永久带偏，
检测器**一直以为用户在张嘴** → 表现为"功能要开关一次才能用"。现在加合理性检查、
4 秒卡死自愈、遮挡后重学。

---

## [4.9] - 2026-09-14

- 修复偏航触发延迟打印出天文数字（`clearTurn()` 之后才计算延迟）。
- 新增**人脸丢失状态机**：脸短暂消失又回来 → 作废基准线 + 抑制。
  这是"手放脸上拿开就乱滑"的主要路径（手盖住整张脸时人脸是直接丢失的，
  而 v4.8 只判"有人脸但读不出嘴关键点"，漏掉了它）。
- **距离自适应**：静止门限按脸占比缩放（近 30cm ×2 / 中 50cm ×1.35）。
- 遮挡判据扩到三条：`face lost` / `mouth missing` / `face size jump`。

## [4.8] - 2026-09-14

- **速度驱动触发**：用相邻两帧角度差算角速度，超过门限（俯仰 0.02°/ms、偏航 0.03°/ms）
  就**立即触发**，不再等"保持时间"。实测仰头延迟从 211~329ms 降到 **71~207ms**。
  根因：无论动作多快都必须再等一整个 holdMs，而"动作要快"已由 motionWindowMs 把关，
  再叠一层保持时间属于重复约束。
- 保持时间改为 `min(holdMs, motionWindowMs)`，默认 200ms → 150ms。
- 新增静止锁定、遮挡抑制、脸丢失即作废基准线（150ms，以前 3 秒）。

## [4.7] - 2026-09-14

- 新增**全局使用翻页**开关（桌面 / 任意应用可触发，代价是摄像头常开）。
- 新增头控音量（默认关闭，v5.1 已删除）。
- 修复「切回目标应用后翻页失效、要手动滑一下才恢复」的第一版：引入强恢复 + 持续重试 +
  重绑宽限期。*（v5.3 才彻底解决，见上。）*

## [4.6] - 2026-09-14

- 张嘴从"暂停本 App"改为**点击屏幕中央**（交给目标 App 响应，抖音里即暂停/播放），
  并**不再冻结 App 自身**：张嘴之后眨眼、点头、扭头照常工作。
- 新增**自定义滑动柔度**滑块（列表类 10%–50% 屏高，默认 26%）+ 恢复默认按钮。

## [4.5] - 2026-09-14

- 新增**上下滑动自适应**：抖音整屏视频、微博/小红书/知乎/B站列表、其他应用各一套
  幅度与时长参数（全部是屏幕高度比例，不写死像素）。
- 新增张嘴检测（`MouthOpenDetector`）：嘴到鼻底距离 ÷ 脸高，并按本人闭嘴水平自动校准；
  必须"闭嘴后再次张嘴"才算下一次。

## [4.4] - 2026-09-14

- 新增**左右扭头滑动**（偏航轴，与俯仰轴完全独立计时；前置摄像头镜像已在检测器里翻正）。
- 新增连续点头双击（v4.5 起被"张嘴"取代，v5.1 全部移除）。

## [4.3] - 2026-09-14

- 新增**全局防误触冷却**（`GlobalTriggerGate`）：眨眼与点头**共用同一个冷却计时器**，
  闸门卡在所有触发路径的唯一汇合点 `fireSwipe()`，从根上消除"一次动作翻好几页"
  以及上下滑交替触发。
- 冷却开关 + 500–5000ms 时长滑块，SharedPreferences 持久化，设置页实时显示倒计时。

---

## 更早版本

- **v4.2 及以前**：眨眼翻页、点头/仰头翻页、按需启停省电、目标 App 白名单、
  Shizuku / 无障碍双路径手势注入、权限记忆、相机自愈看门狗。
