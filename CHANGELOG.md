# 更新日志

> **当前锚点版本：v5.36（用户认可）** —— v5.37/v5.38 的两轮改动经实测判定为"不如 v5.36"，
> 已按用户要求**把工作区代码回退到 v5.36**（`git checkout v5.36 -- app tools`，
> 见下方「回退到 v5.36」一节）。v5.36 的完整备份在 `backup\GazeScroll-v5.36`，
> APK 在 `apk\gazescroll-5.36-debug.apk`（与从 v5.36 源码重新构建的产物**逐字节一致**）。
> 后续只在 v5.36 的基础上做**小步修复**，并且先让用户把 v5.36 用熟、确认没有大问题再动。

> **v5.39（2026-09-17）是「v5.36 + 注视数据采集（测试功能）」**：功能默认关闭、
> 只记录不判定，不开时逐帧开销与 v5.36 完全一致 → **行为仍等同于 v5.36 锚点**。
> 版本号之所以是 5.39 而不是用户口述的「5.37」：**v5.37 / v5.38 已被上一轮那次失败实验占用**
> （tag、backup、apk 归档、本文件里的条目都在），用户在本轮确认改用 v5.39。
> 本版建立在 v5.36 之上，**不继承 v5.37/v5.38 的任何改动**。

> **v5.40（2026-09-17）修的是 v5.39 采集功能的一个致命判据错误**：v5.39 把"盖住摄像头"
> 等同于"画面变黑"，而前置摄像头的自动曝光会把被手掌盖住的画面提亮 —— 用户第一轮实测
> 整场没录到任何数据（4 分钟全是 `probe=idle`，人脸连续消失 50 秒都没进入就绪）。
> v5.40 把"盖住"改成三条独立信号并联（近距离传感器 / 画面纹理 / 亮度），并新增每秒一行的
> `cover-probe` 诊断。仍然是**默认关闭的测试功能，不改变任何触发行为**。

> **v5.41（2026-09-17）修两个实测出来的采集协议缺陷**：① 近距离传感器会**闩锁**在 NEAR
> （盖过一次之后两分半不回 far）→ 判据改成「NEAR **且** 环境光 < 15」两个都要；
> ② 用户按"盖 1 秒"分段实测是 **2.1~2.3 秒**，于是"结束"的 3 秒界线容错只有 800ms，
> 第一轮 11 段里有 6 段被这一次越界误结束 → **结束界线 3000 → 6000ms**。
> 仍然是**默认关闭的测试功能，不改变任何触发行为**。

> **v5.42（2026-09-17）给"分段"加了两个不依赖手部动作的兜底**：第三轮实测里用户盖摄像头时
> **手掌压到屏幕最上方、把通知栏拉下来了**（MIUI 日志 `StatusBar1 ACTION_DOWN 18:56:42.943
> → ACTION_UP 18:56:51.931`）→ 抖音失去前台、相机被系统释放 → **那 9 秒一帧都没有**，
> 分段标记与结束全部没发生。修法：**帧流中断 ≥1.5 秒也记一个分段边界**，再给一个
> **完全不用碰手机的标记方式 —— 录制中眨出一次翻页 = 换下一段**。

> **v5.43（2026-09-17）出「注视门」**：用户点名要的功能 —— **眼睛得盯着屏幕才允许触发**。
> 判据与阈值全部来自四轮实机采集（不是猜的）：脸在画面 + 脸够大 + 偏航 ±10° + 滚转 ±20°
> + **睁眼占比 ≥30%**。最关键的实测发现：**头不动、眼睛看别处时偏航/滚转完全看不出来
> （分布完全重叠），但"睁眼概率"从 50cm 的中位数 1.00 掉到 0.01** —— 眼睛离开屏幕是能测的，
> 靠的是眼皮不是头。**30cm 近距离这条不可用**（看屏幕时中位数只有 0.21），所以近距离档跳过它。
> 默认**「观察」模式**（只记录"本来会拦掉哪一次"，不真拦），先量代价 —— v5.37 的教训。

> **v5.44（2026-09-17）修「近距离仰头翻页失灵」**：用户实测反馈「前面两分钟仰头翻页失灵…
> 后面就好了」。日志抓到元凶 —— 近距离档（v5.28 加的）要求起手在 **300ms** 内完成，
> 而他那次真实仰头用了 **429ms**：`ignored slow lean: rise 429ms > 300ms` → 整段作废。
> 同一场这种"起手太慢"共 6 次（**336 / 429 / 429 / 563 / 684 / 839 ms**）。
> 把**近距离档的俯仰窗口 300 → 450ms**（远距离仍用用户设的 500ms，扭头窗口不动）。
> 离线回放（新增 `tools/headpose-replay`，编译真实 `HeadPoseDetector.kt`）**10 项全通过**，
> 并做了 A/B 对照：旧常量下 429ms 那次 **0 次触发**，新常量下能触发；563ms 以上两边都仍被挡。
> 顺带：**A 方案（近档点头阈值 2.5°→3.2°）经数据验算后没做** —— 详见下一节。

> **v5.45（2026-09-17）修「仰头好多次都没用」**：用户实测反馈后又问「能不能照抄 5.36 版本」。
> 先把 v5.36 那场的日志翻出来数了：**同样的遮挡误判在 v5.36 里一模一样**
> （`v536-anchor-verify.log` 21 次遮挡事件、其中 20 次是"丢脸 ≤300ms"、抑制总时长 **14.6 秒**）
> —— 这套逻辑是 **v4.9** 的代码，v5.36 之后一个字没改，**照抄 5.36 修不了**。
> 真凶：**大角度仰头时 ML Kit 会漏检 1~2 帧（92~214ms）**，每次都被当成"手挡脸"，
> 抑制 500~1000ms 并顺手 `recalibrate()`（再停 ~1.1 秒），那段窗口里头部检测器**完全不喂数据**
> —— 30 秒里零候选、零触发。修法：**人脸至少要消失 300ms 才算遮挡**（v5.45）。

> **v5.46（2026-09-17）修「遮挡时把头部基准线清空」**：用户又报「50cm 眼睛没看屏幕却触发了仰头，
> 30cm 还有一次误触」。日志显示 **11 次遮挡每次都无条件 `headPoseDetector.recalibrate()`**，
> 而基准线被清空后就在"他正抬头看别处"的那几帧上重建 —— 实测基准线 3 秒内
> 7.1 → 13.9 → 10.4 → 16.9 → 19.0 → **25.5** → 10.0，`signed` 因此翻号到 **−17.6°**，
> 直接凑出一次「点头（上一个视频）」误触。修法：**只有丢脸 ≥1 秒才清基准线**，
> 几百毫秒的丢失只清手势状态、保留基准线窗口。

> **v5.47（2026-09-17）界面重排（用户点名的「装修」）**：把「一页到底的设置清单」改成
> **首页功能卡片 + 右侧设置页**。① 10 张功能卡，一级只留「标题 + 一句话 + 开关」，
> 长解释与档位全部收进**三角**（二级详情），每张卡展开后显示**自己那条通道的实时读数**；
> ② 设置页**往左滑呼出**（右侧抽屉），依次是权限与状态 → 外观 → 测试与诊断 → 关于，
> 原来堆在页面底部的测试项（注视数据采集 / 实时数值 / 重启服务）全部收进去；
> ③ 主题三档 **白色（默认）/ 黑夜 / 跟随系统**，纯色背景、系统自带字体；
> ④ 打开 App **停在首页**，不再自动退到后台（用户 v5.47 明确选择）。
> **检测逻辑一行没动**：阈值、判据、方向仲裁、服务、无障碍注入全部原样，`git diff` 只出现在
> `res/`、`MainActivity.kt`（纯界面部分）和新增的 `UiPrefs.kt` / `SwipeToOpenLayout.kt`。
> 主题存在**独立的 `ui_prefs.xml`**，与用户的 `gaze_scroll_prefs.xml` 完全隔离 ——
> §7.1 那次「手改 prefs 把设置清空」的事故在结构上不可能再发生。

> **v5.48（2026-09-17）首页卡片可以自由拖动排序**（用户 v5.47 那条要求里的最后一项：
> 「剩下的全部功能，都可以自由移动排序」）：**长按卡片标题行**把卡片抬起来（放大 + 阴影 + 一下震动），
> 上下拖动经过别张卡的中线就换位，松手即保存；顺序按**资源名**存进 `ui_prefs.xml`，
> 设置页新增「首页卡片 → 恢复默认顺序」。**只动界面层**：没有 RecyclerView 可用（离线构建缓存里
> 没有这个依赖），所以是 `LinearLayout` + 手动换位，逐张卡按真实高度算位移、视觉上不跳。
> 检测逻辑照旧一行未动。

> **v5.50（2026-09-17）把 v5.48 的拖动排序修好**（装机实测抓出两处只有真机才会暴露的缺陷）：
> ① **外层 `ScrollView` 会抢走纵向手势** —— 长按抬起卡片后手指一动就变成滚页面，
> 子 View 只收到 `ACTION_CANCEL`；② **拖动中 `removeView()+addView()` 会当场取消触摸** ——
> 顺序刚换完位，`ACTION_CANCEL` 已经先跑进收尾函数（那时"已改序"标记还是 false），
> 于是**永远存不下去**，剩下的事件还落回 ScrollView 又滚一下。修法：
> 长按开始时 `requestDisallowInterceptTouchEvent(true)`；**拖动期间一个视图都不重排**
> （被拖的跟手指走、让位的用 `translationY` 做预览），松手后才做唯一的一次换位并落盘。
> **造不出拖动就只能靠用户的手指 —— 这两条都是"离线想不出来、装机一看就明白"的坑。**

---

## [5.50] - 2026-09-17

v5.48 的拖动排序在真机上完全不工作。本轮在**装机验证**里把它查清并修好 ——
两条缺陷都属于"编译能过、离线回放也过、只有真机手势才会暴露"的那一类。

### 缺陷 ①：外层 ScrollView 抢走纵向手势（v5.49 修）

现象（`backup\GazeScroll-v5.50\data\` 的抓拍）：长按标题行，卡片确实**抬起来了**
（放大 2% + 阴影 + 按下态）—— 说明长按识别没问题；但手指一纵向移动，**卡片不动、页面开始滚**。

根因：`ScrollView.onInterceptTouchEvent` 只要纵向超过 touchSlop 就拦截，子 View 只会收到
`ACTION_CANCEL`。标准做法是在拖动期间禁止父级拦截：

```kotlin
card.parent?.requestDisallowInterceptTouchEvent(true)   // 长按时
card.parent?.requestDisallowInterceptTouchEvent(false)  // 松手时
```

### 缺陷 ②：拖动中换位会把自己的触摸取消掉（v5.50 修）

修完 ① 之后，卡片能跟着手指走了、**松手也确实换了位**，但**顺序永远存不下来**：
`ui_prefs.xml` 里始终没有 `cardOrder`。把现象拆开看，三条线索指向同一个动作：

1. 顺序变了（左右扭头跑到了第一位）→ 说明换位那几行确实执行了；
2. 松手后卡片**没有**保持抬起状态 → 说明收尾函数跑过了；
3. 页面**又滚了一点** → 说明剩下的手势事件没人接，落回了 ScrollView。

根因：`ViewGroup.removeView()` 会当场把被移除视图的触摸**取消**（`dispatchDetachedFromWindow`
那一串），于是 `ACTION_CANCEL` 在 `dragCardTo()` 还没走到 `dragOrderChanged = true` 之前
就先跑进了 `endCardDrag()` —— 收尾函数看到"没改过序"，什么都不存；之后 `addView` 把卡片
插到新位置（所以顺序看起来变了），而取消掉的手势剩下的事件全部落回 ScrollView（所以又滚了）。

**修法：拖动期间一个视图都不重排。** 被拖的卡片只改 `translationY`（跟着手指），
让位的卡片也只改 `translationY`（`previewShift()` 做换位预览），
**松手之后**（手势已经结束，取消不取消都无所谓了）才做唯一的一次 `removeView + addView`，
紧接着 `saveCardOrder()`。顺带删掉了原来那套"边拖边换位 + `dragShift` 抵消格子位移"的算式
（不再需要，因为不再有中途换位）。

### 验证证据（同一条手势，修前修后各跑一遍）

用 `input swipe 400 407 400 700 10000` 复现真实拖动的手势形状：**前 800ms 只走 22px**
（不越过 touchSlop，所以长按能成立），之后才越过下一张卡的中线。

| | v5.48 | v5.50 |
| --- | --- | --- |
| 长按抬起卡片 | ✅ | ✅ |
| 卡片跟手 | ❌（变成滚页面） | ✅（`v550-drag-preview.png`） |
| 松手换位 | ✅（但只是碰巧） | ✅ |
| `ui_prefs.xml` 里的 `cardOrder` | ❌ 始终没有 | ✅ `cardTurn,cardHeadPose,cardTilt,…` |
| 重启后顺序 | — | ✅ 仍是左右扭头在第一位（`v550-order-persisted.png`） |
| 「恢复默认顺序」 | — | ✅ 键被删掉、顺序回到默认（`v550-order-reset.png`） |

- 离线构建：`versionCode=100 / versionName=5.50`，APK：`apk\gazescroll-5.50-debug.apk`。
- **用户设置一个字节没动**：升级前后 `gaze_scroll_prefs.xml` 逐行比对，47 个键、无重复键，
  唯一变化是 `headBaselineDeg`（App 自己学基准线写的）。
- 顺带确认：**「张嘴点击」的开关在这一轮里被关掉了**（`mouthTapEnabled: true → false`）。
  我的自动化只点过 `400,407`（卡片标题）/`992,219`（设置）/`303,2098`·`303,1986`（主题单选）/
  `374,491`（恢复默认顺序）这几处，都不在那一行的开关上，所以**不是我改的**；
  按 HANDOVER §7.1 的规矩我也没有代改回去。

---


## [5.48] - 2026-09-17

用户要求（v5.47 那轮的清单里的最后一项）：「把剩下的全部功能，都可以自由移动排序」。

### 怎么用

**长按**首页任意一张卡片的标题行 → 卡片抬起（放大 2% + 阴影 + 震动反馈）→ 上下拖 → 松手保存
（提示「顺序已保存」）。点标题仍然是展开 / 收起二级详情，两者不冲突（长按会吃掉那次点击）。
设置页 →「首页卡片 → 恢复默认顺序」可以一键回到默认排列。

### 实现要点（为什么不能用 RecyclerView）

离线构建（`--offline`）的 Gradle 缓存里**没有 `androidx.recyclerview`**，Material / CardView 也没有
（见 v5.47 那节），所以卡片列表就是 `ScrollView` 里的一个 `LinearLayout`，排序 = 直接重排它的子 View。

三个容易踩的点，都处理了：

1. **长按回调拿不到坐标** —— 拖动起点 `dragStartRawY` 靠触摸回调里记的 `lastTouchRawY`。
2. **卡片高度不一样**（展开的那些更高）—— 换位后「卡片所在格子」本身会挪一段，位移量 = **被让位的
   那些卡片的占位高度之和**（不是被拖动卡片自己的高度），按各自真实高度 + 上下 margin 现算，
   累加进 `dragShift` 抵消掉，卡片才会老老实实跟着手指、不跳。
3. **手势不打架** —— 触摸监听只在标题行上；短按 → 展开（`OnClickListener`），长按 → 拖动
   （`OnLongClickListener` 返回 true，那次点击就不会再触发）；卡片里的开关 / 单选 / 滑块照常工作。
   拖动期间 `translationY` 自己消费 MOVE，页面不会跟着滚（代价：一次拖不到屏幕外的位置，分两次拖即可）。

### 顺序怎么存

`UiPrefs.cardOrder`（独立文件 `ui_prefs.xml`，**与用户的 `gaze_scroll_prefs.xml` 无关**），
存的是**资源名**串（`cardHeadPose,cardTurn,…`）而不是数字 id —— 重新构建、加卡片都不会串位。
启动时 `applySavedCardOrder()` 校验：名字数量与当前卡片数不一致、或有认不出来的名字（版本升级删过卡片），
就**保持布局里的默认顺序**，绝不把用户的界面搞乱。

### 验证证据

- 离线构建通过：`versionCode=98 / versionName=5.48`，APK：`apk\gazescroll-5.48-debug.apk`。
- 装机截图：见 `backup\GazeScroll-v5.48\data\`（首页 / 展开态 / 设置页 / 首页卡片节 / 黑夜主题）。

> ⚠️ **本版的拖动排序在真机上是不工作的**（构建通过、长按能抬起卡片，但一拖就变成滚页面，
> 顺序也存不下来）。两处缺陷都是**只有装机才会暴露**的，已在本版装机的同一轮里定位并修掉，
> 见下面的 **[5.50]** —— 拖动排序请用 v5.50。

---


## [5.47] - 2026-09-17

用户原话：「这一版的体验勉强能用了，现在，来装修一下这个 APP 了，之前都是毛坯房，我的想法是，
打开后，把各种功能都精修一下，然后把注释都缩成三角，也就是二级界面，第一级界面简单的描述
这个功能是干嘛的，然后把剩下的全部功能，都可以自由移动排序，把最常用的几个，比如点头翻页，
左右扭头滑动，歪头调节音量，眨眼翻页，张嘴暂停等默认排第一排，然后把背景的光都设置成默认白色，
然后黑夜，然后跟随系统，然后背景用纯色背景，字体用系统的本地文字，然后把下面的测试啊，
什么的东西，都塞进设置里面，设置需要左滑一下呼出，然后依次是相应的权限……
不要动任何代码，只做好这一件事，就是修改 UI，排列 UI」。

### 动手前先问清楚的四件事（避免猜错返工）

| 问题 | 用户的选择 |
| --- | --- |
| 「不要动任何代码」的边界 | **只许改界面层代码**：`res/` + `MainActivity.kt` 里画界面的部分；检测器一行不碰 |
| 打开 App 后的行为 | **停在首页**，按返回键才退到后台（旧版是权限一齐就自动 `moveTaskToBack`） |
| 「左滑呼出设置」的形式 | **首页往左滑，设置页从右边滑出来**（右侧抽屉） |
| 「默认排第一排」的意思 | 竖排列表里**排最前**（不是顶部另做一排磁贴） |

### 1) 首页 = 功能卡片（一级 / 二级）

默认顺序（用户点名的 5 个在最前）：**点头/仰头翻页 → 左右扭头滑动 → 歪头调音量 → 眨眼翻页
→ 张嘴点击** → 注视门 → 防误触（冷却 + 静止锁定）→ 自适应滑动幅度 → 生效的应用 → 全局使用翻页。

每张卡：
- **一级**：状态圆点（开=绿 / 关=灰）+ 标题 + 一句话说明 + 开关；没有开关的通道显示一个小标签
  （眨眼卡「2 连眨」、注视门「拦截中 / 观察中 / 已关闭」、生效的应用「3 个」或「全部界面」）。
- **二级（三角展开）**：原来那些档位 / 反转 / 滑块，加上**该通道自己的实时读数**
  （眨眼卡显示左右眼睁开度与逐帧判定、点头卡显示俯仰角与基准线、歪头卡显示当前倾斜角与已保持时长……）。
  用户要的「注释缩成三角」= 原来那些长解释（扭头 / 张嘴 / 冷却 / 自适应 / 注视门 / 采集说明）
  全部从一级界面移进二级。

### 2) 设置页（往左滑呼出，右侧抽屉）

`DrawerLayout` + `layout_gravity="end"`。顺序就是用户要的「依次是相应的权限」：

1. **权限与状态**：相机 / 通知 / 手势注入（Shizuku）/ 无障碍备用通道 / 使用情况访问 / 前台检测服务 ——
   每行右侧显示实时状态（已授予 / 已连接 / 已允许 / 运行中……），**点一下就去处理对应权限**，
   复用原来已有的入口，没有新增任何权限申请路径；下面保留原来那块完整状态文字（点它一键补齐）。
2. **外观**：主题三档。
3. **测试与诊断**：注视数据采集、实时数值、重启检测服务 —— 用户说的「下面的测试什么的都塞进设置里」。
4. **关于**：功能说明 + 版本号。

另外：DrawerLayout 自带的边缘手势只认「从屏幕最右边那条边往里拖」，用户在卡片中间随手往左划
是没反应的 → 新增 `SwipeToOpenLayout`（自定义 `FrameLayout`，只在 `onInterceptTouchEvent` 里
判断横向位移，纵向照常滚页面），**页面任意位置向左滑 56dp 就能呼出设置**；顶栏另有一个「设置」按钮。

### 3) 主题三档 + 纯色背景 + 系统字体

- `values/colors.xml` 换成**亮色**（默认，纯白背景 `#FFFFFF` + 浅灰卡片），
  新增 `values-night/colors.xml` **放原来那套深色**（用户已经看惯的 `#101418`）。
- `values/themes.xml` / `values-night/themes.xml`：白底配深色状态栏图标，深色底配浅色图标。
- 主题存在 **`UiPrefs`（独立文件 `ui_prefs.xml`）**，用 `AppCompatDelegate.setDefaultNightMode` 切换；
  **绝不写进 `gaze_scroll_prefs.xml`**，从结构上杜绝 §7.1 那次「清空用户设置」的事故。
- 删掉全部 `android:fontFamily="monospace"`，数字也用系统自带字体。

### 没有改的（一个字节都没动）

阈值表、方向仲裁（v5.8 那套）、远距离行为、30cm 静止硬锁定、眨眼档位、注视门判据与阈值、
采集状态机、`GazeCameraService` / `BlinkDetector` / `HeadPoseDetector` / `TiltDetector` /
`MouthOpenDetector` / `GazeGate` / `GazeProbeRecorder` / `AppPrefs` / `GazeRuntime` 全部原样。
**用户的设置项一个没碰**（这一版没有任何一处读写 `gaze_scroll_prefs.xml`）。

### 一个已知的「没加」：眨眼通道没有总开关

别的通道都有 `xxxEnabled`，只有眨眼翻页一直是常开的（配置里没有这个字段）。要给眨眼卡也加个
开关就必须动 `GazeConfig` / `AppPrefs` / 判据，属于「改行为」，本版按用户要求不做。

### 验证证据

- **离线构建通过**：`gradlew -p GazeScroll --console=plain --offline assembleDebug` → `BUILD SUCCESSFUL`，
  `versionCode=97 / versionName=5.47`，APK：`apk\gazescroll-5.47-debug.apk`。
- **装机截图**：见 `backup\GazeScroll-v5.47\data\`（首页 / 展开态 / 设置页 / 三档主题各一张）。
  验证手段说明：界面改动的「可核对证据」是**截图**，不是日志 —— 本轮检测逻辑没动，
  日志里也不该出现任何新字段或新行为。

---


## [5.46] - 2026-09-17

用户原话：「38分40秒左右，手机离我差不多50cm，然后我眼睛没盯着屏幕，但是还是触发了仰头上滑，
然后我拿手机到30cm左右，眼睛没看屏幕，好一点了，但是还是有一次误触，还是要优化一下，
**保留这一版，然后继续优化**」。

### 取证：两次误触的机制**不一样**

**① 19:39:19 @30cm（本次修掉的）—— 遮挡把头部基准线清空 → 基准线被锚在"抬头看别处"的姿势上**

```
19:38:38  raw 11.1  base 13.9  signed  -2.8
19:38:44  raw 17.3  base 10.4  signed  +6.9   ← 仰头触发
19:39:12  raw  7.9  base 25.5  signed -17.6   ← 基准线被锚到 25.5°
19:39:19  nodDown triggered  signed=-5.0°（阈值 2.5°）  ← 用户报的那次误触
```

3 秒内基准线 7.1 → 13.9 → 10.4 → 16.9 → 19.0 → **25.5** → 10.0 —— 正常 45 帧中位数不可能这么跳，
只有"被清空重学"做得到。清它的是 `handleOcclusion` 里那句**无条件**的
`headPoseDetector?.recalibrate()`（本场 11 次遮挡 = 11 次清空）。
这与 v5.36/v5.37 那次「基准线被锚到 32.6° → 回正脖子音量反增」是**同一类事故**
（§6.2 的三个问题），只是触发源换成了遮挡逻辑。

**② 19:38:40 @50cm（本版修不了，如实记录）—— 真实的抬头动作**

```
-391ms|-1.2  -303ms|2.7  -205ms|8.4  -99ms|10.5  0ms|13.9   （signed pitch，390ms 内走 15°）
```

原始俯仰到 **28°**、偏航几乎不动、`eyeDuty=1.00`（眼睛一直睁着）、
`gyro=0.00 accel=0.0 phoneMotion=false`（手机没动）—— **就是一次真实的抬头**。
"抬头看别处"与"抬头翻页"在现有的全部信号（幅度/速度/眼睛开合/头姿）上完全重合，
任何阈值都分不开。这类只能靠注视门：同一场 19:38:33 那次它标了
`head-turned yaw=+10.4°`，拨到「拦截」就会被挡下；纯抬头它看不到。

### 修法

- 新增 `OCCLUSION_RECALIBRATE_MIN_MS = 1000L`：`handleOcclusion` 只在 `lostFor >= 1000ms`
  时才 `recalibrate()`；几百毫秒的丢失**保留基准线窗口**（窗口里本来就只有丢脸之前的样本，
  中位数仍然有效），只清手势状态。
- 遮挡行新增可核对字段：
  `occlusion detected (... faceLostFor=597ms) -> suppress 800ms (#12) headBaseline=kept(597ms)`
  —— `kept(NNNms)` / `reset` 直接说明这次有没有动基准线。
- **没动**：抑制时长、抖动下限（v5.45 的 300ms）、阈值表、注视门判据。

### ⚠️ 事故记录（工具链，写给下一个接手的人）

把注视门从「观察」拨到「拦截」时，我**手改了 `/data/data/.../shared_prefs/gaze_scroll_prefs.xml`**，
结果**把用户全部设置清空了**（App 回落到出厂默认）。根因：

```xml
<!-- 我自己"追加"进去的那份，gazeGateMode 有两行 -->
<string  name="gazeGateMode">OBSERVE</string>      <!-- App 自己写的（v5.43 起 saveConfig 每次都写） -->
<boolean name="gazeGateMode" value="ENFORCE" />    <!-- 我追加的 -->
```

**重复键 → XML 解析失败 → SharedPreferences 认为是空配置 → `setSetupComplete` 把空配置写回磁盘。**
我的核对只查了"旧键没丢 + 新键在"，**没查重复**。已从设备内备份
（`files/prefs-before-enforce.xml`）完整恢复（`blinkBelow=0.55 cooldown=2000ms tiltThr=13°` 全部核对过）。

**教训**：手改 SharedPreferences 必须①替换同名键而不是追加；②写完查重复；
③改之前先 `cp` 一份到 App 私有目录；④**不要以为 `am force-stop` 能长时间停住 App**
（它带无障碍服务绑定，系统随时会把它拉起来，读-改-写会互相踩）。
**设置项一律让用户在界面上拨**，不要代劳。

### 本版已验证 / 未验证

- ✅ 构建 + 装机（`versionCode=96 / versionName=5.46`）；APK 内确认含 `headBaseline=` 字段。
- ✅ 用户设置已从备份恢复并核对（`blinkBelow=0.55 blinkFrames=2 cooldown=2000ms
  pitchTh=6.0° tiltThr=13°`）。
- ⏳ 未验证：遮挡不再清基准线之后，30cm 那次"点头误触"是否消失（需要用户实测一轮）。
- ⏳ 待用户确认：注视门拨到「拦截」（需他在设置页点一下，我不再代劳）。

---

---

## [5.45] - 2026-09-17

用户原话：「29分30秒开始，我仰头了好多次都没用，你看看」；
随后又问：「**能不能照抄 5.36 版本啊，也就是第一个版本**，如果不行的话，就 C2」。

### 先回答那个问题：照抄 v5.36 修不了这个毛病（有数据）

把三场历史日志里"同一个模式"数出来（`occlusion detected (reason=face lost…)`）：

| 日志（版本） | 遮挡事件 | 其中"丢脸 ≤300ms" | 抑制总时长 |
| --- | --- | --- | --- |
| `v536-anchor-verify.log`（**用户认可的 v5.36**） | 21 | **20** | **14600ms** |
| `v536-verify.log` | 16 | 15 | 11300ms |
| `v529-verify.log` | 58 | 52 | 41500ms |

```
09-16 23:30:17 I/GazeDiag: occlusion detected (reason=face lost, faceLostFor=60ms) -> suppress 500ms (#1)
09-16 23:30:35 I/GazeDiag: occlusion detected (reason=face lost, faceLostFor=100ms) -> suppress 500ms (#2)
```

这套「手挡脸」逻辑是 **v4.9** 引入的，v5.36 → v5.45 之间**没有改动过**。
所以回退到 v5.36 只会丢掉「注视门」等新东西，**不会修掉仰头失灵**。

### 取证：19:29 那 30 秒是一场遮挡风暴，而画面根本没被挡

```
19:29:33.386 occlusion detected (reason=face lost, faceLostFor=93ms)  -> suppress 1000ms (#56)
19:29:34.423 ... faceLostFor=127ms -> suppress 1000ms   (#57)
19:29:36.019 ... 123ms -> 1000ms                        (#58)
19:29:37.086 ... 122ms -> 1000ms                        (#59)
19:29:39.516 ... 92ms  -> 1000ms                        (#60)
19:29:41.620 ... 123ms -> 1000ms                        (#62)
19:29:47.085 ... 214ms -> 800ms                         (#63)
（19:28:30 之后共 30 次）
```

代价是**双重**的：抑制窗口内 `headPoseDetector` 完全不喂数据（`if (!gate && !tilting)`），
而且遮挡时会 `recalibrate()` → `baselineSettling` 再停 ~1.1 秒。
每 1.5 秒一次的漏检就把头部通道永久占满 → 用户那 30 秒里
`tiltUp candidate rejected = 0`、`tiltUp triggered = 0`（连候选都没产生）。

同一时刻画面是**正常**的：

```
19:30:19 I/GazeProbe: cover-probe faceLostFor=29345ms luma=80.6 tex=11.6 prox=far lux=30 covered=0 why=-
```

`luma=80.6 tex=11.6 prox=far` —— 离"被挡住"差得很远。原因是大角度仰头时脸的透视压缩，
**ML Kit 会间歇性漏检 1~2 帧**（92~214ms），这套为"手掌整个盖住脸"设计的逻辑把它当成了遮挡。

### 修法（C2，一处判据加下限）

- 新增 `FACELOST_OCCLUSION_MIN_MS = 300L`：`brieflyLost` 由 `lostFor in 1..1200` 改成
  **`lostFor >= 300`** 才算遮挡；300ms 以内的丢脸只当"检测抖动"，不抑制、不 recalibrate。
- 300ms 的依据：那 30 次的丢失全是 **92~214ms**；真用手盖住镜头是以秒计的
  （用户自己盖摄像头时 1~8 秒），v4.9 要防的"手掌/拳头整个盖住脸"也一样。
- 新增可核对字段（限流 2 秒一行）：
  `I/GazeDiag: face flicker ignored: faceLostFor=93ms < 300ms — 不当遮挡、不抑制（v5.45）`
  —— 下一场直接数这行就知道抖动的频率，也就知道这条改动有没有生效。
- **没动**：抑制时长（500/800/1600ms）、`FACELOST_OCCLUSION_MAX_MS`(1200)、
  重复判定窗口(2500ms)、以及真实遮挡（≥300ms）的全部行为。

### 本版已验证 / 未验证

- ✅ 构建 + 装机（`versionCode=95 / versionName=5.45`）；APK 内确认含 `face flicker ignored`。
- ✅ **离线核对**：把三场历史日志按新判据重算，**丢失时长呈明显双峰，300ms 正好落在空档里**：

  | 日志 | 遮挡事件 | 丢失时长分布 | 新判据下 |
  | --- | --- | --- | --- |
  | `v544-verify.log`（用户抱怨那场） | 47 | **77~218ms ×43** … 423/424/553/606ms ×4 | 43 次不再抑制，4 次仍算遮挡 |
  | `v536-anchor-verify.log`（v5.36 锚点） | 21 | **60~199ms ×20** … 432ms ×1 | 20 次不再抑制，1 次仍算遮挡 |
  | `v536-verify.log` | 16 | **85~140ms ×15** … 335ms ×1 | 15 次不再抑制，1 次仍算遮挡 |

  "检测抖动"（60~218ms）与"真的被挡住"（335~606ms）之间**没有任何重叠**，
  300ms 站在中间 —— 这不是拍脑袋的数。
- ⏳ 未验证：用户贴到 30cm 仰头是否恢复正常（需要他实测一轮）。

---

---

## [5.44] - 2026-09-17

用户原话：「刷了几分钟，你看日志，**前面两分钟仰头翻页失灵**，后面就好了，前面两分钟还有
误触的情况……后面就好了，误触的情况都少了点」。

### 取证 1：仰头失灵 = 近距离档的「突然性窗口」只有 300ms

```
19:16:23.675 I/HeadPose: pitch 13.78° base 7.76° delta 6.0°
19:16:23.675 I/HeadPose: ignored slow lean: rise 429ms > 300ms        ← 元凶
19:16:23.676 I/HeadPose: tiltUp candidate rejected: pitch=6.0° speed=0.0569°/ms
             gate=0.0240°/ms threshold=3.8° dist=near ... reason=slow-rise
```

`NEAR_SUDDEN_RISE_MS`（v5.28 引入）要求"从 onset（0.4×阈值）到第一次越阈值"在 **300ms** 内完成。
v5.28 的依据是"实测真实动作延迟都在 249ms 以内"，**这一次被推翻**：用户这次仰头用了 429ms。
全场这种"起手太慢"共 6 次，实测时长 **336 / 429 / 429 / 563 / 684 / 839 ms**。

前两分钟（19:14:45~19:16:45，近档）候选被拒 **100 次**（slow-rise 26 / recenter-lock 16 /
no-travel 11 / jump-confirm 8），而 19:16 那一分钟就 **71 次**（其中 45 次是仰头）。

### 取证 2：为什么"后面就好了" —— 他把手机拿远了

| | 前两分钟 | 之后 |
| --- | --- | --- |
| `frSm` 均值 | **0.491**（近档） | **0.389**（远档） |
| 阈值 | 点头 **2.5°** / 仰头 **3.8°**，窗口 300ms | 点头/仰头 **6.0°**，窗口 500ms |
| 仰头成功触发 | 10 | 20（3 分钟） |
| 下滑注入 | 5 | 3（3 分钟） |

远档下他仰头 10~14° 轻松超过 6° 阈值，所以"后面就好了"；点头阈值回到 6°，
所以 5° 级的小动作不再误触发 →"误触少了点"。**这两件事都与注视门无关**
（注视门当时是「观察」模式，整场真拦 0 次）。

### A 方案为什么没做（本来打算做的那条）

原本准备把近档点头阈值 2.5° → 3.2°（`NEAR_LOOKDOWN_NOD_BOOST` 0.42→0.53）来压误触。
动手前先用数据验算了一遍，**它挡不住用户报的那几次**：

| 近档点头触发幅度 | 次数 | A（阈值 3.2°）能否挡住 |
| --- | --- | --- |
| 2.5~3.2° | 1 | ✅（就 1 次） |
| 3.2~4.9° | 0 | ❌ |
| **≥4.9°** | **5** | ❌ **用户抱怨的那 5 次全在这里** |

而且那 5 次从 `ctx 24f` 逐帧原始读数看**都是真实头部动作**（100~400ms 内原始俯仰真的动了 5~8°）。
要把它们挡掉阈值得抬到 5° 以上，而用户**有意的轻点头**恰好是 3.0~4.4°（v5.12 的记录：
他当时反馈"轻轻点头还是不触发"，所以才从 3.0° 降到 2.5°）→ **两者完全重叠，任何阈值都分不开**
（与当年"单眼闭眼"同一类困局）。所以这条不做，改成 B。

### B 方案（本版）：近距离档俯仰窗口 300 → 450ms

- 只动 `NEAR_SUDDEN_RISE_MS`（**远距离仍用用户设置的 500ms，扭头窗口 400ms 不动**）；
- 新增 `actWin=` 字段打在每一次候选拒绝行里（**与判定用的是同一个函数**，日志与实际永远自洽）；
- 新增离线回放 `tools/headpose-replay/`（编译真实 `HeadPoseDetector.kt`），A/B 对照见
  `tools/headpose-replay/ab-old-300ms.txt`：

```
旧常量 300ms:  FAIL 起手 429ms → 应触发   实际触发 0 次     ← 用户那次
新常量 450ms:  PASS 起手 429ms → 应触发
两边都:       PASS 起手 563/684/839ms → 仍被挡（慢晃）
两边都:       PASS 远档 400ms 触发 / 700ms 被挡、近档点头 200ms 触发、边缘慢晃不触发
```

### 本版已验证 / 未验证

- ✅ 离线回放 **10 项全通过**，并做了 300ms vs 450ms 的 A/B 对照（上面）。
- ✅ 构建 + 装机（`versionCode=94 / versionName=5.44`）；APK 内确认含 `actWin=`；
  实机拒绝行已带 `actWin=`（远档显示 500ms，符合"远距离不变"）。
- ⏳ 未验证：用户**贴到 30cm** 时仰头是否真的不再失灵（需要他实测一轮）。
  预期：近档拒绝行会显示 `actWin=450ms`，`rise` ≤450ms 的仰头不再被作废。

---

## [5.43] - 2026-09-17

用户的原始要求：

> 「加一个监测功能，就是用户的眼睛，必须盯着屏幕，才能触发万能翻页的全部功能，这样避免
> 有时候的误触……这个得设置一个强设置，必须监测眼睛有没有盯着屏幕，如果觉得不准确，
> 加一个鼻子的辅助定位。」

### 数据（第四轮采集成功：4 段 / 689 帧 / 92% 有脸）

分位数（p5 / 中位 / p95），人眼盯着屏幕 vs 不盯：

| 量 | 段1 50cm 看屏幕 | 段2 **50cm 头不动、眼睛看别处** | 段3 30cm 看屏幕 | 段4 **30cm 眼睛看别处** |
| --- | --- | --- | --- | --- |
| `eY` 偏航 | −18.8 / **−0.1** / 1.3 | −16.1 / **−2.2** / 0.9 | −0.7 / **0.1** / 0.7 | −20.9 / **−0.4** / 0.6 |
| `eX` 俯仰 | 1.3 / **2.7** / 8.0 | −6.2 / **−4.3** / 7.2 | −0.5 / **0.9** / 2.8 | −11.8 / **−9.4** / 15.1 |
| `openL` 睁眼 | 0.99 / **1.00** / 1.00 | 0.00 / **0.01** / 1.00 | 0.06 / **0.21** / 0.90 | 0.01 / **0.03** / 0.95 |
| `openR` 睁眼 | 0.22 / **0.99** / 1.00 | 0.01 / **0.01** / 0.99 | 0.02 / **0.12** / 0.69 | 0.01 / **0.02** / 0.99 |

**两条硬结论**（都写进了 `GazeGate` 的注释）：

1. **头没动的时候，偏航/滚转一点都看不出来** —— 段1 与段2 的 `eY` 中位数是 −0.1 与 −2.2，
   分布完全重叠。这符合预期：ML Kit 的人脸检测**没有虹膜**，眼球转向它读不到。
2. **但"睁眼概率"读得出来**：50cm 盯着屏幕时两只眼的中位数是 **1.00**，眼睛往下看别处时是
   **0.01** —— 眼皮遮住眼球，ML Kit 的软分类跟着掉。**"眼睛离开屏幕"不是测不出来，只是靠的是眼皮。**
   ⚠️ 这条**只在 ≥45cm 可用**：30cm 下"看屏幕"的中位数只有 **0.21**（近距离 ML Kit 本来就读得低，
   HANDOVER 里 `blinkBelow` 分远/中/近三档就是这个原因），与"看别处"的 0.03 大面积重叠
   （两边 p5~p95 都是 0.01~0.95）→ **近距离档必须跳过这条判据**，否则会把近距离正常使用整段拦掉。

### 判据（`GazeGate`）

任一不成立就算"没在看着屏幕"（全部 fail-open：读数缺失一律放行）：

| # | 判据 | 阈值 | 挡住的误触 |
| --- | --- | --- | --- |
| ① | 脸在画面里 | — | 手机放桌上 / 人走开 |
| ② | 脸够大 | `faceRatio ≥ 0.20` | 离得太远（手机架在远处） |
| ③ | 偏航相对本人基准线 | `≤ ±10°` | 头转开看别处（实测转开是 ±11~13°） |
| ④ | 滚转相对本人基准线 | `≤ ±20°` | 躺下、侧脸 |
| ⑤ | **睁眼占比** | `≥ 30%`（最近 1.5 秒） | **眼睛离开屏幕**（只在 `faceRatio < 0.45` 时生效） |

**为什么⑤用"占比"而不是瞬时值**：眨眼本身要经过"闭眼"那一帧（`BlinkDetector` 的触发点就在闭眼上），
用瞬时值当门会把眨眼通道自己拦死。取最近 1.5 秒里"两只眼平均睁着"的帧占比：一次眨眼只占
2~6 帧（约 0.2 秒），占比仍有 ~0.9；而"眼睛真的离开屏幕"是持续几秒的 → 掉到 0.1 以下。
**用时间域换掉瞬时判定** —— 与 v5.34 学到的教训一致（软分类输出不能当物理量）。

**每通道豁免**：左右扭头本身就是偏航、歪头调音量本身就是滚转 —— 那两个通道分别豁免③和④，
否则等于把这两条通道直接关掉。

### 默认是「观察」，不是「拦截」

三档：`关 / 观察（默认）/ 拦截`。观察模式把"本来会拦掉哪一次触发"写进日志
（`I/GazeGate: gate would-block trigger=blink reason=head-turned …`），**一次都不拦**。
理由写在配置注释里：① v5.37 就是"加严过头"被整版退回的；② 判据⑤在 30cm 下不可用，
这道门天然挡不住近距离误触 —— **先量代价，再决定要不要默认拦**。
用户也可以自己在设置页拨到「拦截」。

### 其它

- 诊断行新增 `gazeGate= / gateOk= / gateWhy= / gateDetail= / eyeDuty=`；
  设置页实时显示"现在算不算盯着屏幕 + 睁眼占比"。
- `tools/capture-loop.ps1` 抓取标签补 `GazeGate:V`（**加新 tag 第一件事就是补它**）。
- 离线回放 **96 项全通过**（新增 14 项：no-face / too-far / head-turned / head-tilted /
  两个通道豁免 / fail-open / 眨眼不自杀 / 近距离跳过 / 丢脸清窗口）。

### 本版已验证 / 未验证

- ✅ 构建 + 装机（`versionCode=93 / versionName=5.43`）；诊断行实测
  `gazeGate=OBSERVE gateOk=false gateWhy=no-face`。
- ✅ 离线回放 96 项全通过。
- ⏳ 未验证：真实使用中"观察"模式量出来的代价（会拦掉多少次真实动作）——**需要用户正常用几天**。
- ⏳ 未验证：`拦截` 模式的实际手感（用户自己拨过去就能试）。

---

## [5.42] - 2026-09-17

第四轮准备：第三轮实测（18:56:24~18:57:10）**只录到 2 段**，根因是物理层面的，不是判据。

### 取证：手掌压到屏幕把通知栏拉下来了

```
18:56:42.943 I/MIUIInput: ViewRootImpl windowName 'StatusBar1', { action=ACTION_DOWN ... }
18:56:51.931 I/MIUIInput: ...[MotionEvent] 'StatusBar1', { action=ACTION_UP ... moveCount:369 }
```

同一时刻的 CSV（`probe-20260917-185631.csv`）：

```
idx 121  t=11.5s  face=0 cover=1  luma=152.8 tex=4.30   ← 手掌刚盖上来，判据成立
idx 130  t=12.3s  face=0 cover=1  luma=124.0 tex=4.16   lux 从 30.3 掉到 2.9（手盖住了环境光）
idx 131~143      face=0 cover=0  luma≈140  tex=7.5~8.5  ← 通知栏已经弹出来，画面在变
idx 144  t=21.1s  face=1 …                              ← 中间 **7.8 秒一帧都没有**
```

- 通知栏一弹出，抖音就不是前台 → 相机被系统释放 → **帧流直接断掉 7.8 秒**；
- 我的状态机只在"收到帧"时才知道"盖住了"，**没有帧 = 什么都看不见** →
  那 9 秒的盖既没记成分段、也没结束那一段，用户等于白盖；
- 于是 7 段只录到 2 段（50cm 看中间 eX≈−3°、50cm 看上方 eX≈+5.5°）。

### 修法：三个分段判据并存（任何一个成立都算换段）

| 判据 | 覆盖的场景 | 出处 |
| --- | --- | --- |
| 盖住 1~6 秒 | 手干净地盖住镜头、没碰到屏幕 | v5.41 |
| **帧流中断 ≥1.5 秒** | **盖的时候压到屏幕 / 通知栏弹出 / 切了应用 / 相机被系统抢走** | 新增 `FRAME_GAP_MARKER_MS` |
| **录制中眨出一次翻页** | **完全不想动手** —— 连眨两次眼（用户平时的翻页动作）即可换段 | 新增 `onMarker()`，服务侧挂在眨眼回调上 |

"帧流中断"这条特别重要：**录制被打断**这件事本身就是最强的分段信号，不管打断它的是手掌、
通知栏还是系统 —— 而 v5.41 之前我只认"盖住"，于是最容易出问题的场景恰恰没有判据。

眨眼标记挂在 `BlinkDetector` 的回调上（`probeRecorder?.onMarker("blink:…")` 紧挨着
`fireSwipe`），**只观测、不影响触发**；只在录制中计数。

### 本版已验证 / 未验证

- ✅ 离线回放 **82 项全通过**（新增：帧流断 8 秒 → 记边界、只断 300ms 不算、
  眨眼标记换段且不写多余数据行、待机时的眨眼不算）。
- ✅ 装机 `versionCode=92 / versionName=5.42`；APK 内已确认含新字符串
  （`frame gap` / `marker `）。
- ⏳ 未验证：修正后能一次跑完 4 段（下一轮）。
- ⏳ 未验证：**头不动、只用眼睛看别处**（"注视门"上限的关键），以及 30cm 俯视的两段。

---


## [5.41] - 2026-09-17

第二次实测（18:47~18:49）**数据本身很干净**，但暴露两个我留下的缺陷。两个都不是用户的问题。

### 缺陷 ①：近距离传感器会闩锁在 NEAR

```
18:47:21  prox -> NEAR(0.0)  face=false lux=8    ← 用户盖住摄像头（真盖住）
18:47:24  prox -> NEAR(0.0)  face=true  lux=22   ← 手已拿开，脸在画面里，传感器却还是 NEAR
18:48:35  prox -> NEAR(0.0)  face=true  lux=29   ← 一直 NEAR
18:49:35  prox -> NEAR(0.0)  face=false lux=55   ← 卡了 2 分半，画面是亮的、根本没人盖
```

这颗 Xiaomi 的 proximity 是 on-change 的**虚拟**传感器（`XiaoMi(V1.6)`），实测在盖住之后
不会自己回到 far。只信它的话，**任何一次"脸离开画面"都会被当成盖住** → 误结束。

**修法**：近距离传感器要**加上环境光旁证**才成立 —— `prox=NEAR` **且** `lux < 15`。
同一场实测的两个分布分得很开：手掌盖住时 `lux = 0~9`，没盖住时 `lux = 21~55`。
没有环境光的机型（`lux` 读不到）才退化成"只信近距离传感器"。

### 缺陷 ②：分段与结束之间只有 800ms 容错，人做不到

用户按脚本"盖 1 秒"分段，实测时长是 **2153 / 2109 / 2299ms** —— 他对"1 秒"的感觉偏长。
于是第 4 段之后那一次盖到 **3036ms**，越过了 3000ms 的结束界线，**整场录制被提前结束**：
11 段只录到 5 段（session #2 四段 + session #3 一段）。这是典型的"过严的门自己打死自己"
（§6.2）—— 判据没错，是**留给人的容错太小**。

**修法**：结束界线 **3000 → 6000ms**。现在：分段 = 盖 1~6 秒（说"盖 2 秒"）、
结束 = 盖 ≥6 秒（说"盖 8 秒"），两边各留 3 秒以上余量。离线回放专门加了这条用例：
「盖 3.04 秒 = 仍然只是分段（**这正是第一轮把整场录制止住的那一下**）」。

### 顺带定下来的阈值（第一次有了实测分布）

| 量 | 手掌盖住镜头 | 没盖住（无脸） | 有脸 | 阈值 |
| --- | --- | --- | --- | --- |
| `tex` 纹理 | **2.4 ~ 4.5**（刚盖上头 1 秒 7~13） | **8.1 ~ 11.4** | 15 ~ 22 | `< 5.0`（v5.40 是 4.0） |
| `lux` 环境光 | **0 ~ 9** | 21 ~ 55 | — | `< 15`（近距离传感器的旁证） |
| `luma` 亮度 | 45 ~ **157**（AEC 提亮，不可用） | 80 ~ 140 | 66 ~ 102 | `< 32`（保留为兜底） |

`cover-probe` 行新增 `why=` 字段，直接说明这一帧是哪一条判据命中的（`prox+lux` / `tex` /
`luma` / `-`），下一轮核对不用再猜。

### 本轮数据里的第一个正面结论（对"注视门"最关键）

session #2 的 4 段（用户按顺序做的前 4 段）与校准场的对照：

| 量 | 校准场（50cm 盯着屏幕） | 头往一边转的那两段 |
| --- | --- | --- |
| `eY` 偏航 | 2.6 ± 0.6° | **+10.8 ± 1.8° / −12.8 ± 2.0°** |
| `eyeNoseDx`（鼻底相对眼线，脸宽为单位） | 0.016 ± 0.003 | **+0.033 ± 0.005 / −0.038 ± 0.007** |

两者都是"头有没有朝着手机"的干净判据，且**与距离无关**（50cm / 30cm 都能用）。
真正还没验证的是**头不动、只用眼睛看别处**那一段（用户第一轮没录上）—— 这正是下一轮
要专门补的数据，也是决定"注视门"能做到什么程度的关键。

### 本版已验证 / 未验证

- ✅ 构建 + 装机（`versionCode=91 / versionName=5.41`）；诊断行 `prox=far(5.0) luma=… tex=… lux=39`。
- ✅ 离线回放 **71 项全通过**（含闩锁场景、盖 2.2 秒 / 3.04 秒分段、6.5 秒结束三条新用例）。
- ⏳ 未验证：修正后的采集协议能一次跑完全部段（需要用户按新协议再跑一轮）。
- ⏳ 未验证：**头不动、只用眼睛离开屏幕**这一段的可分性（"注视门"能做到什么程度的关键）。

---

## [5.40] - 2026-09-17

用户第一轮实测（18:33:32 打开采集 → 18:37 结束整套脚本）**整场没有录到任何数据**。
这是第一次实机使用采集功能，也是本版存在的唯一原因。

### 取证：不是用户的操作问题，是"盖住"的判据错了

```
18:33:31  I/GazeDiag: … probe=off
18:33:32  I/GazeProbe: probe ON — 整只手盖住前置摄像头 3 秒 = 开始采集 …
（此后 4 分钟，每 3 秒一行）
          I/GazeDiag: … probe=idle sawFace=true
18:37:01~18:37:50   face=false 连续 50 秒（用户按脚本盖住摄像头收尾），仍然 probe=idle
```

- `files/probe/` 目录**根本没被创建** → 一次 `REC START` 都没有；
- `sawFace=true` → 状态机在跑、开关是开的、帧在来；
- 人脸连续消失 **50 秒**（远超 3 秒门槛）却没进入"已就绪" → 只能是
  **「画面平均亮度 < 32」这一条不成立**。

根因很直白：**前置摄像头的自动曝光/增益会把被手掌盖住的画面提亮**（AEC 的目标是让画面
"曝光正常"，它不看画面里是什么）。v5.39 把"盖住"等同于"画面黑"，这个前提本身是错的 ——
与被亲手推翻过的"绝对阈值不如相对量"是同一条教训（§6.2）。

### 修法：三条互相独立的信号并联（任意一条成立即算盖住）

| # | 信号 | 为什么它能成立 | 出处 |
| --- | --- | --- | --- |
| ① | **近距离传感器** `prox=NEAR` | 手掌贴住手机顶部时它直接翻"近"，与曝光/光照/画面内容完全无关（小米 13 实测有 `android.sensor.proximity`） | 新增 `ProximityMonitor.kt` |
| ② | **纹理量** `tex < 4` | 抽样网格（32×32）上**相邻采样点的平均亮度差**：手掌贴镜头 = 完全离焦 = 没有边缘，**AEC 只能整体提亮，变不出边缘来** | `FaceGazeAnalyzer.lumaStats()` |
| ③ | **亮度** `luma < 32` | v5.39 的原判据，AEC 还没补偿上来时仍然有效 | 保留 |

三条**全部读不到**（老机型既没有近距离传感器、画面也读不出来）时才退化成"只看人脸消失" ——
退化路径有风险（采集脚本里就有"转头看别处"的段落），所以只在两条画面信号都拿不到时才走。

### 新增诊断：`cover-probe`（人脸消失期间每秒一行）

```
I/GazeProbe: cover-probe faceLostFor=1240ms luma=101.2 tex=10.8 prox=far(-1.0) lux=-1 covered=0 state=idle
```

这次能一轮定位就靠它 —— 上一版只记了亮度，而亮度正好是被 AEC 带走的那一项。
诊断行（`GazeDiag`）同时新增 `luma= / tex= / prox= / lux=`；CSV 新增 `tex / prox / lux` 三列。

### 其它

- `ProximityMonitor` 只在**采集开关打开时**注册（`setEnabled`），关掉即注销 ——
  不开测试功能时仍然是零开销。
- 离线回放 **61 项全通过**，含两条新用例：
  「自动曝光把画面提亮、但纹理低 → 算盖住 ★」与
  「回放 v5.39 那次真实失败场景（盖住 4 秒 + luma=88 + tex=1.5 + prox NEAR）→ 现在是 ARMED」。

### 本版已验证 / 未验证

- ✅ 构建 + 装机（`versionCode=90 / versionName=5.40`）。
- ✅ 新诊断实机在跑：`cover-probe … luma=101.2 tex=10.8 prox=far(-1.0) covered=0 state=idle`；
  诊断行同时出现 `luma= / tex= / prox= / lux=`，无 crash。
- ⏳ **未验证**：三条信号在"手掌真的盖住"时的读数（近距离传感器是否收到事件、纹理掉到多少）。
  需要用户做一次 20 秒校准（盖 10 秒 → 拿开 10 秒 → 盖 10 秒），拿到数字后再定阈值。
- ⏳ 未验证：一次完整采集（REC START → CSV → REC STOP）。

---

## [5.39] - 2026-09-17

用户任务（原话）：

> 「新任务，保留 5.36 版本，开发 5.37 版本……我的要求是，加一个监测功能，就是用户的眼睛，
> 必须盯着屏幕，才能触发万能翻页的全部功能，这样避免有时候的误触。……不管是远距离≈50cm
> 看手机，还是近距离俯视≈30cm，眼睛都会盯着屏幕中间位置或者屏幕中间位置朝上，你只需要测算，
> 我的眼睛在不在盯着屏幕就行……这个得设置一个强设置，必须监测眼睛有没有盯着屏幕，
> 如果觉得不准确，加一个鼻子的辅助定位，一般来说，眼睛如果盯着屏幕，鼻子肯定也会有一个很准的位置。
> 这样，你可以先在万能翻页里面，记录我的数据，你先记录一下我的数据，比如先加一个测试功能，
> 然后测试条件给我，然后你说挡住屏幕摄像头 3 秒后，开始测试，然后结束后，再挡住屏幕摄像头。」

### 本版做了什么：**只采集数据，不做任何判定**（"注视门"要等这批数据出来再定）

1. **新增采集器 `GazeProbeRecorder.kt`**（纯逻辑，无 Android 依赖，可离线回放）。
2. **`FaceGazeAnalyzer` 新增 `ProbeFrame`**：只在开关打开时，把一帧的**原始几何量**算出来 ——
   人脸框位置（按摆正后的画面归一化）、十个关键点坐标（双眼 / 鼻底 / 嘴角 / 双耳 / 双颊）、
   三个欧拉角、睁眼概率、张嘴比例、俯视几何、跟踪 ID。
3. **新增 `meanLuma()`**：抽样读 YUV 的 Y 平面（约 32×32 个点）得到画面平均亮度。
4. **落盘**：`files/probe/probe-<yyyyMMdd-HHmmss>.csv`，表头自带列名与规则说明；
   缺失一律写 `-1`（不把"没读到"当成 0）；收尾时写 `# SUMMARY` + 每段的 `# PHASE n rows= faces=`。
5. **诊断行新增 `probe=`**（`off / idle / armed / rec ph= rows= faces=`），设置页新增开关 + 实时状态 +
   `I/GazeProbe` 里程碑日志（`probe ON` / `ARMED` / `REC START` / `PHASE n` / `REC STOP`）+
   `tools/capture-loop.ps1` 抓取标签补 `GazeProbe:V`（**加新 tag 第一件事就是补它**，v5.35/v5.36 漏过一次）。

### 采集协议（用户口述，全部围绕"3 秒"这一条界线）

| 状态 | 操作 | 结果 |
| --- | --- | --- |
| 空闲（已见过人脸） | 整只手盖住前置摄像头 **≥3 秒** | 进入"已就绪" |
| 已就绪 | 松手露脸 | **开始录制**（`REC START #n`） |
| 录制中 | 盖住 **1~3 秒** | 写分段标记（`PHASE n`），录制继续 |
| 录制中 | 盖住 **≥3 秒** | **结束**（`REC STOP … rows= faces= duration= phases=`） |
| 录制中 | 单次超过 5 分钟 | 保险自动结束 |

### 关键设计决定：「盖住」不能只判"人脸消失"

测试脚本里本来就有「眼睛/头离开屏幕」的段落（人还在、只是不看手机）。如果"盖住"= 人脸消失，
那些段落会被当成结束，**整场采集直接废掉**。所以判据是**两个条件同时成立**：

```
covered = 没有人脸  且  画面平均亮度 < 32/255      （亮度读不到时退化成只判人脸消失）
```

实测依据：手掌/手指盖住前置摄像头时画面是个位数到十几；正常举着手机看屏幕时整幅画面 ≥40。
CSV 里逐帧记了 `luma`，这条线合不合适可以直接用采到的数据复核、再调。

### 离线回放当场抓出一个真 bug（本项目第 3 次）

`tools/probe-replay/run.ps1` 编译**真实的** `GazeProbeRecorder.kt` 回放 12 组场景（**54 项全通过**）：

```
FAIL  第 1 段  phase=2      ← 开始录制那一帧没有清掉"盖住"计时，
                              于是第一帧就把"刚开始的那次盖住"算成"刚结束一段遮挡"
```

修法：`startSession()` 里显式 `coverStartMs = 0L`。同一次回放还验证了：
「只露脸不会自己接上一次录制，必须重新盖满 3 秒」「录制中转头看别处 6 秒既不分段也不结束（★）」
「盖 0.5 秒的手晃忽略」「重复关闭开关幂等」「5 分钟保险」等。

### 本版**已验证 / 未验证**（照实写）

- ✅ 构建通过、装机通过（`versionCode=89 / versionName=5.39`，`dumpsys package` 已核对）。
- ✅ 新增代码路径实机在跑：诊断行出现 `probe=idle sawFace=true`，无 crash、无异常，
  原有触发通道照常工作（同一场日志里 tilt / turnR 都还在正常触发）。
- ✅ 离线回放 54/54。
- ⏳ **未验证**：真正的一段采集（`REC START` → CSV → `REC STOP`）。它需要用户按测试脚本
  做完整套动作（盖 3 秒 → 分段动作 → 盖 3 秒），我无法代替。
- ⏳ 未验证：CSV 落盘链路在真机上的一次完整往返（文件已建目录但还没被写过）。

### 版本号 / 回退点

- 版本号：用户口述"开发 5.37"，但 `v5.37`/`v5.38` 已被上一轮的失败实验占用
  （tag、`backup\GazeScroll-v5.37`/`-v5.38`、`apk\gazescroll-5.37/-5.38-debug.apk`、
  本文件里的条目），**已问过用户，确认改用 v5.39**。
- 锚点不变：**v5.36 仍是用户认可的锚点**；要回到锚点用 `tools/install-v536.ps1`。
- 本版回退点：`git tag v5.39`、`backup\GazeScroll-v5.39`、`apk\gazescroll-5.39-debug.apk`
  （SHA256 见 `apk\v539-verify.log` 或本目录说明）。

---

## [回退] 2026-09-16 —— 工作区回到 v5.36（锚点）

用户要求（原话）：「**退回 5.36 吧，你来安装，然后以 5.36 以锚点**，我先多体验一下 5.36，
确认都没问题了再说，确认 5.36 不会出大问题再说，小问题再慢慢修」

- 判定依据：v5.37（以及建立在它之上的 v5.38）虽然修掉了「回正脖子音量不降反增」，
  但引入了**更糟的灵敏度回退**（用户的判断：不如 v5.36）。
  v5.37 的两条量化证据见下一节「v5.38」；结论是**先把 v5.36 当锚点用**。
- 代码回退方式：`git checkout v5.36 -- app/src/main/java/... app/src/main/res
  app/src/main/AndroidManifest.xml app/build.gradle.kts tools`
  —— 因此 `versionCode/versionName` 又回到 **86 / 5.36**。
- **保留的两处非行为改动**（都不影响设备行为，只为可核对性）：
  1. `tools/capture-loop.ps1` 的抓取标签含 `Tilt:V` —— v5.36 那版缺这个标签，
     日志里没有 `I/Tilt` 明细（v5.35/v5.36 的验证日志都因此缺了这一块）。
  2. `tools/install-v536.ps1`（新增）—— 一键把手机装回 v5.36 并重启抓取。
- 回退点/备份：`git tag v5.36`（= 用户认可的一版）、`backup\GazeScroll-v5.36`（源码 + APK + ROLLBACK，
  开头已标「用户认可的一版」）、`apk\gazescroll-5.36-debug.apk`。
- v5.37 / v5.38 的代码与 APK **没有删除**，需要时可以再取：
  `git tag v5.37` / `git tag v5.38`、`apk\gazescroll-5.37-debug.apk`、`apk\gazescroll-5.38-debug.apk`。

**v5.36 已知的、留着以后慢慢修的小问题**（用户批准后才动）：

1. **回正脖子时偶尔"音量不降反增"**（用户 23:00:50 报的那次）。
   根因已定位：45 帧基准线窗口被服务侧 `reset()`（遮挡 / 静止硬锁定 / 换应用 / 重绑）
   清空后，在"用户正歪着头"的那几帧上重建 → 基准线锚到 32.6° → 回正被读成反方向歪头。
   最小修法（v5.37 里已经验证过、无灵敏度代价的那一条）：**`reset()` 不清基准线窗口**。
   注意：v5.37 同时加的那两条（"连续歪 >2.5 秒重锚 + 自禁判 2 秒"、"起手前必须有中位"）
   经实测过严，**不能一起带回来**。
2. **歪头方向如果和实际相反**：设置页两个开关各自拨一下即可（日志 `roll=` / `tilt=` 可核对）。
3. 近距离俯视 + 往下看几度就触发下滑（阈值 2.5°，属灵敏度取舍）；
   `travel=-` 的残留（判定域切换时位移判据跳过）。

---

本项目遵循「一版一改、实机验证」的节奏：每个版本都先在小米 13（HyperOS 3.0.308.0）上
跑通才发版，所以下面每一条的结论都来自实机日志或参数化实测，而不是推测。

版本号规则：`vX.Y`，`versionCode` 与版本号同步递增（当前 v5.45 / 95）。

---

## [5.38] - 2026-09-16

用户反馈（原话）：「看到了吗 **真不如上一版灵敏**，这一版还把我**近距离俯视给仰头给弄死了**，
都不灵敏了，你修一修，修不好的话，退回上一版，上一版记得做个备份，我觉得上一版好像没啥问题」

### 结论：v5.37 我加的三条判据是错的，**全部删除**；只保留不花灵敏度的修复

用户说的两个症状都能用日志量化，根因是我自己的三条新判据：

**① 「连续歪着 >2.5 秒就重锚基准线 + 之后自禁判 2 秒」→ 重锚风暴**

```
23:08:42 重锚到 -29.3°   23:08:50 重锚到 -14.2°   23:09:00 重锚到 37.0°
23:09:03 重锚到 5.7°     23:09:06 重锚到 11.0°     ← 25 秒内 5 次，每次前后各废掉一段时间
```

用户的实际动作是"歪 30° 左右、保持住"，正好每次都触发"判定为姿势 → 重锚 → 自禁判 2 秒"，
可用时间被吃掉大半 → **"不如上一版灵敏"**。

**② 「起手前 1.2 秒内必须出现过中位带」→ 自己把自己否掉**

```
23:09:10 左歪头 起手之前没有中位（距上次中位 从未）→ 这一段不算
```

基准线一动，头就"永远不在中位附近"，这条门把合法手势也挡了。

**③ 与服务侧"歪头期间暂停翻页"叠加 → 把近距离俯视的仰头弄钝**

暂停的门槛原先是 `|tilt| > 0.4×阈值`（5.2°）。诊断行统计：

```
v5.36：|tilt| > 5.2° 的样本占 12%      v5.37：占 35%
```

也就是说 v5.37 里**翻页通道有三分之一的时间拿不到头部数据**（`onHeadPose` 被跳过 +
每次歪头切换还会让头部基准线重学），近距俯视仰头（阈值 3.8°、依赖轻通道与稳定基准线）
自然就"死"了。

### 修法（v5.38 = v5.36 的灵敏度 + 只留不花灵敏度的两条修复）

**保留**：
1. **`reset()` 不清基准线窗口**（v5.37 引入，是"回正脖子音量不降反增"的真正根因修复：
   基准线 = 这个人的头姿，不能被遮挡/换应用的重置丢掉）。**零灵敏度代价。**
2. **作废即闩锁**：起手早于两秒的那一段作废后占住这一段，避免回正/甩到另一边被当成新歪头
   调成反方向（用户报的"不降反增"）。只在"已经作废"之后才生效，不影响正常手势。
3. 设置页的**两秒间隔倒计时 + 作废原因**（v5.37 加的，纯显示，帮用户判断时间）。

**删除**（三条 v5.37 判据）：
- ~~连续歪着 >2.5 秒重锚基准线~~（重锚风暴）
- ~~起手前必须有中位带~~（自己否自己）
- ~~起手前头必须停住过~~（多余，正常手势之前本来就是停住的）

**收回**：暂停翻页的门槛从 `0.4×阈值`（5.2°）改回 **`|tilt| ≥ 阈值`** —— 只在真的歪出阈值
（也就是真在做歪头手势）时才暂停翻页，不再霸占中间那一段。

### 离线回放验证（`tools/tilt-replay/run.ps1`，编译真实 TiltDetector）**34 项全通过**

包含：基本触发 / 反方向 / 本人平时歪着头 / 档位 / 抖动 150ms 不算 / 角度不够 / 单帧尖峰 /
基准线不足 8 帧 / 读数 70° / 丢脸 / 一直歪着只触发一次 / **回正不触发且倾斜角回到 0 附近** /
**reset 前后基准线一致（事故根因）** / **reset 后正常歪头方向正确** / 快速甩头不触发 /
两秒规则（起手早于两秒即使坚持过两秒也不触发、两秒后重新歪才触发）。

### 备份（用户明确要求）

- **`backup\GazeScroll-v5.36`**（用户认可的上一版）：完整源码 + `gazescroll-5.36-debug.apk`
  + ROLLBACK.txt，已在 `git tag v5.36` 上，随时可回。
- `backup\GazeScroll-v5.38`：本版源码 + APK + ROLLBACK.txt。
- `apk\gazescroll-5.36-debug.apk` / `gazescroll-5.37-debug.apk` / `gazescroll-5.38-debug.apk` 都在。

### 未改动

阈值表 / 方向仲裁 / 远距离行为 / 30cm 静止硬锁定 / 眨眼档位 / 张嘴点击 / 两秒规则语义
（无提前量，不顺延）/ 灵敏度档位 / 其余设置项。

---

## [5.37] - 2026-09-16

用户反馈（原话）：「没啥问题，就是有时候，我判断不好时间，你看 **11 点 00 分 50 秒左右**，
我想歪头，但是没把握好提前量，所以**音量不降反增**，这个是个问题，其他的不动，把这个问题给
解决掉吧」

### 取证：不是"提前量"的问题，是**基准线被锚在了歪头的姿势上**（日志铁证）

用户报的时刻附近，`I/Tilt` 行**一条都没有**（而且 v5.36 的抓取标签里我漏了 `Tilt:V`，
所以那一段的触发明细根本没进日志 —— 这次一并修好，见文末）。但诊断行把机制写得很清楚：

```
23:00:19.977  roll=+3.1°   基准线=3.8°    tilt=-0.9°    fired=12   ← 一切正常
23:00:23.042  roll=+0.6°   基准线=32.6°   tilt=-28.5°   fired=12   ← 基准线突然变成 32.6°
23:00:26.090  roll=+5.4°   基准线=32.7°   tilt=-22.4°   fired=13   last=左歪头 32.0°  ← 触发"左歪头"= 升
```

本人真实头姿只有 +1~5°，基准线却被锚在 **+32.6°**，于是"什么也没做"被读成"往左歪了 22~28°"，
**触发的是升音量** —— 正是用户说的"不降反增"。

基准线怎么会跑到 32.6°？因为它所在的 45 帧窗口在**"用户正歪着头"的那几帧上被重建**了：
v5.36 的实现里，服务侧的 `reset()`（遮挡 / 静止硬锁定 / 换应用 / 重绑都会调）会把整个
基准线窗口清空 —— 清空之后紧接着的几帧（用户此时往往正歪着头或正在回正）就成了新基准线。
而 v5.36 的"歪着时冻结基准线"又让它**永远错下去**（头永远不在它附近 → 再也学不回来）。

### 修法（只动歪头通道本身）

1. **`reset()` 不再清基准线窗口**（v5.37 核心）：基准线是"这个人的头姿"，遮挡一下、切个应用
   不该把它丢掉。只清手势状态（进行中的那一段、闩锁、静止跟踪）。
2. **基准线不会被"永远冻住"**：连续歪着超过 **2.5 秒** = 那是姿势、不是手势 → 把基准线
   **重新锚定**到当前读数（此后自动回到 0），并记一条日志/界面提示。
   2.5 秒足够区分手势（0.2~0.8 秒）与姿势。
3. **起手必须有中位在前**：起手那一帧之前 1.2 秒内必须被看到处于中位带 —— 基准线万一锚错，
   头就永远不在它附近，于是**不可能凭空造出一次（方向相反的）歪头**。
4. **起手之前头必须停住过**（≥250ms 静止，且静止段刚刚结束）：有意手势都是"先停住 → 再歪"，
   而**回正脖子是一段快速运动**，靠这条挡住（这是"回正误触"的直接对策）。
5. **被作废的那一次要"占住这一段"**（闩锁）：以前作废后紧接着回正/甩到另一边的动作会被当成
   一次**新的**歪头、调成反方向；现在作废即闩锁，必须回中位并停稳 400ms 才能重来。
6. **重锚之后自己再禁判 2 秒**（重锚把"歪着的姿势"当成了新中位，紧接着的回正运动最容易被
   误读成反方向歪头）。
7. 读数异常（>55°，躺下/侧卧/脸转走的野值）时**丢弃基准线窗口重学**（野值既不进窗口，
   也不会被当成"姿势"锚下来）。

### 界面 / 日志：让"时间"看得见（用户说"我判断不好时间"）

- 设置页「实时数值」新增一行：**两秒动作间隔：还差 N ms（现在歪会作废）/ 已就绪（可以歪）**，
  外加最近一条作废原因（例如「上一次动作后还差 560ms 满两秒：这次歪头已作废，回正后重新歪」）。
- 诊断行保留 `gapRemain=`。
- **修工具 bug**：`tools/capture-loop.ps1` 的抓取标签漏了 `Tilt:V`（v5.35/v5.36 的日志里
  因此没有触发明细）→ 加上 `Tilt:V`、去掉已废弃的 `Wink:V`。

### 离线回放验证（`tools/tilt-replay/run.ps1`，编译真实 TiltDetector）**34 项全通过**

新增/重点项：

- **服务 reset 不能丢基准线**：reset 前后基准线一致（0° 附近）、reset 后正常歪头方向正确、
  回正不触发反方向。
- **歪 3 秒再回正**：歪着时只触发一次、**回正不触发（尤其不反方向）**、停稳后倾斜角自愈回 0、
  自愈后重新歪照常触发。
- **快速甩头**（运动中经过中位，模拟 11fps 下的回正）：不触发；停稳 + 等满两秒后重新歪才触发。
- 原有各项（基本触发 / 反方向 / 本人平时歪着头 / 档位 / 抖动不算 / 角度不够 / 单帧尖峰 /
  基准线不足 / 读数 70° / 丢脸 / 一直歪着只触发一次）全部保持通过。

### 未改动

阈值表 / 方向仲裁 / 远距离行为 / 30cm 静止硬锁定 / 眨眼档位 / 张嘴点击 / 两秒规则的语义
（**仍然没有提前量**：起手早于两秒就作废，不顺延）/ 灵敏度档位（10/13/16/20°、0.2/0.3/0.5/0.8 秒）
/ 其余设置项。

### 残留（如实记录）

- 连续歪着**超过约 4.5 秒**再回正，理论上仍可能触发一次反方向（重锚把那个姿势当成了中位，
  而 2 秒的自加禁判期已经走完）。用户的实际动作是"歪 0.3 秒 → 停一会 → 回正"，落在 2 秒以内，
  所以这条路走不到。
- 若仍遇到「不降反增」，日志现在有 `I/Tilt` 明细 + `tilt=`/`roll=`/`gapRemain=` 四项，
  一眼可以看出是"起手太早 / 没有先回中位 / 起手时头还在动"哪一种。

---

## [5.36] - 2026-09-16

用户反馈（原话）：「做的不错，但是可以**左右歪头判断再灵敏一点**，另外，你要解决，**歪头我回正脖子
的时候，不要误触**，或者就是，**加一个全局**，就是上一秒你做了什么动作，**必须强制等两秒**，
才能触发，而且**不能提前量**，比如我卡一个 1.99 秒，然后刚好到 2 秒，然后触发，然后造成了延迟
这种，**必须是 2 秒后作出的动作，才能判定**，加个灵敏度吧」

### ① 「回正脖子误触」的根因：基准线被歪头本身带走了（已修）

`tilt = roll − baseline`，而 baseline 是最近 45 帧（≈4 秒）滚转角的**中位数**。你歪着头停几秒，
中位数就跟着歪过去 —— 等你回正时，"相对基准线"反倒变成**反方向的一大坨倾斜**，于是回正的动作
被判成反方向歪头、又调一次音量。

**修法**：只有"头基本在中间"（`|tilt| ≤ 阈值`）时才把这一帧喂进基准线窗口；歪着的时候不喂。
这样基准线只跟着"平时的头姿"走，不会被故意的歪头带走。
（**离线回放把它抓了出来**：第一版我写成了"窗口没填满就一律喂"，于是刚启动时歪着头照样把基准线
带走 —— 回放第 5 项当场判 FAIL，改掉后才过。）

### ② 全局「两秒 + 不能有提前量」（用户要求，已实现）

- 新增动作时钟：**翻页 / 调音量 / 张嘴点击**任一发生，都记下时刻。
- **歪头动作的"起手"（第一次超过阈值的那一帧）必须晚于"上一次动作 + 2 秒"**，
  否则这一段**整段作废**（不是等两秒后放行）—— 严格按"不能有提前量"执行：
  卡在 1.99 秒那一下不算，必须回到中位、等满两秒之后**重新歪**才判定。日志会写
  `起手太早（还剩 Nms 才满两秒）→ 这一段不算，回正后重新歪`。
- 反过来，**调音量之后 2 秒内也不翻页**：新增 `GlobalTriggerGate.extendCooldown()`，
  调音量时把翻页闸门一起推到 2 秒后（只延长、不缩短；用户关掉全局冷却时尊重开关）。
- 诊断行新增 `gapRemain=……ms`（还剩多久才允许下一次动作），可直接核对。

### ③ 灵敏度：整体调灵一档（含"加个灵敏度"的设置）

- 触发角度：原来 12/15/**18**/22 → 现在 **10 / 13（默认）/ 16 / 20°**。
- 保持时间：原来 0.3/**0.5**/0.8/1.0 → 现在 **0.2 / 0.3（默认）/ 0.5 / 0.8 秒**。
- 两个设置项的标题也改成「**灵敏度 · 触发角度**」「**灵敏度 · 保持时间**」，一眼知道调的是灵敏度。
- 回到中位后重新武装的停稳时间：250 → **400ms**（顺手挡住"回正时甩到另一边"）。

### 日志（这一版的可核对字段）

```
I/Tilt: tilt 左歪头 held=320ms tilt=-14.2° peak=15.8° thr=13° base=-0.4° neutralBefore=1200ms
        dist=near -> volume UP 1 档 (1档=10) 40->50/150 applied=true roll=-14.2° pitch=8.0° ...
I/Tilt: 右歪头 起手太早（还剩 560ms 才满两秒）→ 这一段不算，回正后重新歪
GazeDiag: ... roll=-14.2 ... tilt=tilt=-14.2° base=-0.4° thr=13° held=320ms peak=15.8° fired=1 ... gapRemain=1680ms
```

### 离线回放验证（`tools/tilt-replay/run.ps1`，编译真实 TiltDetector）**25 项全通过**

- 必须触发：+14° 保持 0.4s（新默认）/ −14° 反向 / 平时歪着 −12° 再歪 14° / 0.8 秒档坚持 0.9s /
  甩到另一边后**等满两秒重新歪**
- **回正相关（本版重点）**：歪着头停 6 秒再回正 → **回正不触发、且倾斜角回到 0 附近**
  （证明基准线没被带走）/ 甩到另一边在 2 秒内不触发、两秒后重新歪才触发 /
  起手卡在 2 秒内 → **即使坚持过两秒也不触发** / 两秒内连歪两次只认第二次
- 必须不触发：抖一下 150ms / 只歪 8° 保持 3s / 单帧尖峰 / 基准线不足 8 帧 / 读数 70° / 丢脸 /
  一直歪着只触发一次

### 未改动

阈值表 / 方向仲裁 / 远距离行为 / 30cm 静止硬锁定 / 眨眼档位 / 张嘴点击的锁存 / 其余设置项。
新增的只有「基准线冻结」「全局两秒动作间隔」「灵敏度档位整体下调一档」这三处。

### 需要你实测确认

- **两秒规则的代价**：起手早于两秒的歪头**不会被顺延**（按你要求的"不能提前量"），
  所以那种情况下要**回正再重新歪一次**才算。如果觉得"有时候白歪了一下"，可以告诉我，
  我可以改成"顺延"版本（等满两秒自动放行）—— 但那正是你不要的提前量，所以默认按你的原话做。
- 灵敏度defaults（13°/0.3s）是按"更灵敏"的先验调的；若仍嫌不够，直接选 10°/0.2s。

---

## [5.35] - 2026-09-16

用户要求（原话）：「看到了吧，那么换个判定方式吧，看到我 38 分 50 多秒后的左右歪头吗？
**删了闭眼调整音量，改成歪头，左歪头上升，右歪头下降**，当然，也给用户自己选择左歪头降低还是
增加，还是右歪头降低还是增加，不过这个判断方式可以久一点，意思是仰头得到一定的角度，才会触发
降低增加音量」

### 先说一件事：日志里**看不到**你的歪头 —— 因为这条轴以前根本没采集

`headEulerAngleZ`（滚转 = 歪头/压耳朵）**从 v5.10 到 v5.34 一个字都没有**：`AnalyzedFrame`
只带了 X（俯仰）和 Y（偏航）。所以 22:38:50 之后你歪的那几下，日志里只能看到副作用 ——
歪头让俯仰读数串扰了几个度，被近距离俯视档（阈值 2.5°）当成仰头，**顺带翻了两页**：

```
22:38:54.382 tiltUp triggered: pitch=6.1° ... → swipe UP
22:38:56.677 tiltUp triggered: pitch=6.1° ... → swipe UP
```

v5.35 起采集并打印原始滚转角（诊断行新增 `roll=`），所以你这一次的实测数据会直接进日志，
阈值就照它来定。

### 改动：删掉「单眼闭眼控音量」，换成「歪头控音量」

- **删除**：`WinkDetector.kt` 整个文件、服务里的喂帧/回调/复位/日志、`wink*` 配置与设置项、
  离线回放工具 `tools/wink-replay`（连同 v5.30~v5.34 的四轮判据一起作废）。
  （四轮都没做稳的根因写在 [TiltDetector] 与 v5.34 的条目里：ML Kit 的睁眼概率在这台设备上
  分不开"有意单闭"和"低头眯眼"。）
- **新增** [TiltDetector]（纯逻辑，可离线回放）：
  1. **相对本人基准线的倾斜**：`tilt = roll − baseline`，baseline 是最近 45 帧滚转角的**中位数**
     —— 天生歪着头看手机的人不会被误触发，换姿势也会自动跟随；
  2. **必须歪到设定角度**：12 / 15 / **18（默认）** / 22°（用户原话"仰头得到一定的角度"）；
  3. **并且保持住**：0.3 / **0.5（默认）** / 0.8 / 1.0 秒（"这个判断方式可以久一点"）；
  4. **一次歪头只调一组档位**：必须**歪回中位**并保持 0.25 秒才允许下一次（一直歪着不会把
     音量一路推到底）；
  5. 读数太歪（>55°，例如躺下/侧卧）视为不可信，重置不判定。
- **方向**：默认**左歪头 = 调高、右歪头 = 调低**；设置页两个开关可以各自反向（用户要求）。
- **每次调几档**：1（默认）/ 2 / 3 / 5 档（1 档 = 按一次音量键），多档一次跳到位、只弹一次面板。
- **歪头期间暂停翻页判定**（新增）：歪头会让俯仰串扰几度，而近距离俯视的点头阈值只有 2.5°
  —— 不暂停就会"歪头顺带翻一页"（上面那两行 swipe UP 就是）。歪回中位后 `recalibrate()`
  重新对齐头部基准线（`baselineSettling` 那条门会顺带挡住重建期的误判）。

### 日志（这一版的可核对字段）

```
I/GazeDiag: ... pitch=8.2 ... yaw=-1.5 roll=-19.4+ ... tiltVol=on tiltDir=L=up/R=down
            tilt=tilt=-19.4° base=-0.3° thr=18° held=520ms peak=21.7° fired=1 last=左歪头 21.7° 520ms tiltSteps=1 ...
I/Tilt   : tilt 左歪头 held=520ms tilt=-19.4° peak=21.7° thr=18° base=-0.3° neutralBefore=1200ms
            dist=near -> volume UP 1 档 (1档=10) 40->50/150 applied=true roll=-19.4° pitch=8.2° ...
```

`roll=` 是原始滚转角，`tilt=` 是相对倾斜角 —— 一次实测就能确认正负号（哪边是"左歪头"），
若和你要的相反，把设置页两个开关都拨一下即可。

### 离线回放验证（`tools/tilt-replay/run.ps1`，编译真实 TiltDetector）**17 项全通过**

- 必须触发：+20° 保持 0.6s（阈值 18°/保持 0.5s）/ −20° 反方向 / 1.0 秒档坚持 1.1s /
  **本人平时就歪着 −12°（基准线自动吸收）再歪 20°** / 回正后再歪第二次
- 必须不触发：抖一下（20° 只 200ms）/ 角度不够（10° 保持 3s）/ 单帧尖峰 /
  刚有脸基准线没攒够 / 读数 70°（躺下）/ 歪到一半丢脸 / 一直歪着只触发一次

### 未改动

阈值表 / 方向仲裁 / 远距离行为 / 30cm 静止硬锁定 / 眨眼档位 / 张嘴点击 / 基准线重建期禁触发 /
其余设置项：一个字节未改。新增的只有「滚转采集 + 歪头通道 + 歪头期间暂停翻页」。
（`AppPrefs` 里旧的 `wink*` 键不再读写，留在 SharedPreferences 里无害。）

---

## [5.34] - 2026-09-16

用户反馈（原话）：「抓到了吗？看到问题了吗？**还是不灵敏**。按照你的思路，来解决这个问题吧」

### 取证：v5.33 把真单闭挡掉的唯一原因，是「合眼快慢」这条判据本身不可用

v5.33 那一场的 16 次单闭尝试逐条对比（`sep` = 两只眼读数差，`onset` = 从"最后一次没闭着"
到"深闭"的用时）：

```
成功  held=406ms sep=0.88 onset=88ms        被拒  held=466ms sep=0.93 onset=658ms
成功  held=475ms sep=0.84 onset=100ms       被拒  held=449ms sep=0.99 onset=1216ms
成功  held=410ms sep=0.50 onset=147ms       被拒  held=480ms sep=1.00 onset=1732ms
成功  held=444ms sep=0.72 onset=55ms        被拒  held=485ms sep=0.85 onset=（本段之前从没被读到没闭着）
成功  held=477ms sep=0.83 onset=84ms
成功  held=484ms sep=0.87 onset=62ms
成功  held=465ms sep=0.76 onset=105ms
被拒 还有 3 次只是"没保持够"（340/372/382ms < 400ms）
```

被拒的那几条 `sep` 高达 **0.85~1.00** —— 一只眼 0.0x、另一只眼 0.9x，一眼就是有意单闭，
却因为"**概率下降慢**"被判成误触。原因：ML Kit 的 `eyeOpenProbability` 是逐帧软分类输出，
眼皮实际合上之后概率还会在 0.5~0.3 之间拖几帧甚至一秒多（真闭眼也这样）。而 v5.30 那批
误触的 onset 是 1.6~3.2 秒 —— **和真单闭重叠**，这条判据根本分不开两类。**结论：onset 判据废掉。**

### 修法：换成时间域判据 —— 「这只眼最近有多常被读成闭着」

不看"这一下多快"，看"**这只眼最近是不是老被读成闭着**"（约 5 秒的指数滑动平均 `duty`）：

- **有意单闭** = 偶尔闭一下：用户实机 duty ≈ **0.02~0.10**（每 5 秒闭 0.4 秒 ≈ 0.08）。
- **低头眯眼** = 同一只眼**反复**被读低（v5.30 那 90 秒里右眼连续低了 30 秒、左眼接着低）：
  实测 duty ≈ **0.4~0.9**。
- 判据：**闭合开始时的 duty ≤ 0.30**。对单帧噪声不敏感，也不会因为"某一下闭得慢"误杀。

v5.34 的完整判据（全部要满足）：
1. 两只眼的读数**差 ≥ 0.30**（v5.33 加的，负责挡"眨眼半闭"这类两只眼一起低的情况）；
2. 另一只眼**不是闭着**（挡眨眼）；
3. 保持用户设定的时长（默认 0.4 秒），且这次闭合**不超过 2.5 秒**（闭太久是休息/眯眼）；
4. **duty ≤ 0.30**（这只眼最近不常被读成闭着）；
5. 一次闭合只调一组档位，**必须重新睁大（>0.65）**才能再来一次。

### 日志（这一版的可核对字段）

触发行把 `onset=` 换成了 `duty=`（默认档位、`sep=` 保留）：

```
I/Wink: wink 右眼 held=406ms eyeL=0.95 eyeR=0.07 min=0.02 other=0.95 sep=0.88 duty=0.05
        thr=0.55 dist=near -> volume UP 1 档 (1档=10) 50->60/150 applied=true ...
```

被拒时写明是哪一条：`两只眼读数差不多（差=0.15 需要 ≥0.30）` / `最近这只眼老被读成闭着（duty=0.43 需要 ≤0.30）` /
`闭了 2680ms 太久（>2500ms）= 休息 / 眯眼` / `只闭了 340ms（< 400ms）`。

### 离线回放验证（`tools/wink-replay/run.ps1`，编译真实 WinkDetector）**17 项全通过**

- 必须不触发：**低头反复读低 8 次 → 最多只触发 2 次（v5.30 是 8 次，日志里 duty 从 0.33 一路到 0.61 全被挡）** /
  长闭眼后的眼皮残留 / 眨眼半闭（sep 0.15）/ 两只眼一起半闭 / 三连眨 / 闭着不松
- 必须触发：**概率缓降 1.2 秒的真单闭（v5.33 正是被这条错杀）** / 回放 v5.33 日志里那 6 次真实成功 /
  连续快闭 4 次（连点音量）/ 左右眼交替 / 一次闭合只触发一次

### 已知残留（如实记录）

时间域判据只能挡住"**反复**被读低"，挡不住**一次低头刚开始时的那一下**：如果用户刚低下头、
那只眼第一下就被读低满 0.4 秒，v5.34 仍可能调一格（日志里 `duty` 会很小、`sep` 很大）。
v5.30 是 90 秒误调 11 格（音量被打到 0），v5.34 退化成"刚开始低头时最多一两格"。
若实测仍嫌多，下一步就用头部通道已有的 `lookingDown`/`chinRatio` 加一条姿态条件（有数据再定阈值）。

### 未改动

阈值表 / 方向仲裁 / 远距离行为 / 30cm 静止硬锁定 / 眨眼档位 / 基准线重建期禁触发 /
设置项与默认值：一个字节未改。

---

## [5.33] - 2026-09-16

用户反馈（原话）：「为什么我眨眼可以触发调整音量？你看日志，**你换个算法啊**，闭单眼的时候，
你是采集不到另外一只眼睛的数据啊，你采集不到另外一个眼睛的数据，那就说明要调整音量了，
这个就很简单了啊。」

### 先验证用户的假设：**"闭一只眼就读不到另一只眼"在本设备上不成立**

翻遍所有历史验证日志（v5.10 → v5.32，共 33 个日志文件），逐帧统计诊断行里的左右眼读数：

```
两眼都有读数 = 3153 帧      只缺左眼 = 0 帧      只缺右眼 = 0 帧      没脸 = 4029 帧
```

原因是代码本身就是直通的（`FaceGazeAnalyzer`：`leftEyeOpenProbability = face?.leftEyeOpenProbability`），
而 ML Kit 只要检测到人脸，就**会同时给出两只眼的概率** —— 闭上那只眼给的是 **0.02~0.15 的低值**，
不是"没有数据"。所以"读不到另一只眼"这条判据在这台设备上一次都不会出现，不能用它。

**但用户的思路本身是对的，只是落在了另一个量上**：把两只眼**放在一起比**。日志恰好证明只有这个量
能分开两类 —— 这就是 v5.33 的核心。

### 修法①：核心判据换成「两只眼的读数必须差 ≥ 0.30」

22:25:56 那次误触发（用户说的"眨眼也能调音量"）与当场的 4 次真单闭放一起看：

```
22:25:56 误触发（眨眼/半闭）  闭的那只=0.54  另一只=0.65  → 差 0.11   ✗
22:26:12 真单闭              闭的那只=0.30  另一只=0.77  → 差 0.47   ✓
22:26:17 真单闭              闭的那只=0.13  另一只=0.95  → 差 0.82   ✓
22:26:25 真单闭              闭的那只=0.43  另一只=0.95  → 差 0.52   ✓
22:26:27 真单闭              闭的那只=0.24  另一只=0.98  → 差 0.74   ✓
```

**绝对阈值分不开这两类**：眨眼时两只眼会一起落在 0.5~0.7（v5.30 用 0.70、v5.31 用 0.65、v5.32 用 0.55
都试过，每换一次线都要么误触发要么漏触发）；真单闭时闭的那只也能停在 0.54。能分开的只有
**两只眼差多少** —— 误触发 0.11，真单闭 0.47~0.82，**0.30 卡在中间**。

v5.33 的判据因此是（三条，全部要满足）：
1. **两只眼读数差 ≥ 0.30**（一只明显闭着、另一只明显睁着）；
2. 另一只眼不是闭着（≥ 用户闭眼阈值）；
3. 合眼要快（onset ≤ 500ms）+ 保持用户设定的时长（默认 0.4 秒）。

### 修法②：修掉一个会"吃掉右眼单闭"的记账 bug

v5.32 的日志里有三行明显不对（同一段里左右眼交替单闭时）：

```
22:26:06 wink 右眼 保持 496ms 但合眼太慢（从最后一次没闭着到深闭用了 9223372036854775807ms）
22:26:15 wink 右眼 保持 437ms 但合眼太慢（... 9223372036854775807ms）
22:26:28 wink 右眼 保持 461ms 但合眼太慢（... 9223372036854775807ms）
```

`Long.MAX_VALUE` 的意思是"**这只眼在本段之前从没被读到不闭着**"。原因是 v5.32 把"最后一次没闭着"
的记账放在了 `handle()` 里、且在"另一只眼闭着就整段作废"的提前返回**之后** —— 于是用左眼单闭时，
右眼（睁着、读数 0.95）的时间戳一直不更新；轮到右眼单闭时，onset 算成天文数字，一律判"合眼太慢"。
**修法**：每只眼的"最后一次没闭着"改成在 `onEyeProbabilities()` 里**各自独立记录**，与另一只眼无关。

### 日志（这一版的可核对字段）

触发行新增 `sep=`（两只眼的读数差，就是这个核心判据）：

```
I/Wink: wink 右眼 held=487ms eyeL=0.77 eyeR=0.30 min=0.03 other=0.77 sep=0.47 thr=0.55
        onset=91ms dist=near -> volume UP 1 档 (1档=10) 10->20/150 applied=true pitch=7.1° ...
```

被拒时也有明确原因：`两只眼读数差不多 → 不算单闭（这只眼=0.54 另一只=0.65 差=0.11 需要 ≥0.30）`
或 `合眼太慢`。

### 离线回放验证（`tools/wink-replay/run.ps1`，编译真实 WinkDetector）**17 项全通过**

- 必须不触发：2.7 秒缓降 / 长闭眼后的眼皮残留 / **眨眼半闭（回放 22:25:56 那条：0.52→0.15→0.50，另一只 0.65）** /
  两只眼一起半闭 / 630ms 慢眯 / 三连眨
- 必须触发：回放 v5.32 那 4 条真实成功（0.30/0.77、0.13/0.95、0.43/0.95、0.24/0.98）/
  v5.31 被错杀的（另一只眼 0.59、0.65）/ **左右眼交替单闭两次都触发（即上面的记账 bug）** /
  22:19:36 那次真实成功 / 一次单闭只触发一次

### 未改动

阈值表 / 方向仲裁 / 远距离行为 / 30cm 静止硬锁定 / 眨眼档位 / 基准线重建期禁触发 /
设置项与默认值：一个字节未改（本次只动了单闭通道内部的判据与记账）。

---

## [5.32] - 2026-09-16

用户反馈（原话）：「17.56-18.05 这期间，我眨眼调音量一点动静没有，你自己看日志，先把这个闭眼
调整音量给弄好。19分40秒突然有一波调整音量，你看看是什么问题？」

### 先取证①：22:17:56~22:18:05 为什么"一点动静没有"

**是 v5.31 新加的两道「起手」门把真单闭也挡掉了 —— 其中一条判据本身是错的。**

```
22:17:56.245 起手不合格（闭之前只明确睁了 0ms，需要 ≥600ms；从睁到深闭用了 294ms；min=0.03 other=0.99）
22:18:01.677 起手不合格（闭之前只明确睁了 205ms，需要 ≥600ms；从睁到深闭用了 432ms；min=0.06 other=1.00）
22:18:03.063 另一只眼没睁着（other=0.65，门槛却是 >0.70）→ 不触发
22:17:56.532 另一只眼没睁着（other=0.59，门槛却是 >0.70）→ 不触发
```

两条门各有问题：

1. **「闭之前必须连续明确睁着 ≥600ms」（阈值 0.65）是冗余且有害的**：真正的"是不是有意动作"
   由「合眼快不快」保证就够了（这些真单闭的 onset 是 294 / 432ms，本来就合格）。而 30cm
   俯视时**同一只眼的"睁着"读数经常只有 0.55~0.70**（同场诊断行：`eyeL=0.70 eyeR=0.13`、
   `eyeL=0.64`、`eyeL=0.62`、`eyeL=0.77`），这条 0.65 的线卡在真实读数中间，于是"只明确睁了
   0ms / 205ms"——把用户连续 5 次有意单闭全部否掉。
2. **「另一只眼 > 0.70」这条线也定高了**：实测那只"睁着的眼"读到 **0.59 / 0.65** 就被否掉了。

### 修法①：判据重定为两条（v5.32）

- **另一只眼不能是闭的**（读数 ≥ 用户的闭眼阈值，默认 0.55）—— 这条线同时用于"算不算眨眼"，
  两只眼同时闭（眨眼时另一只眼只有 0.02~0.10）照样被排掉。
- **合眼要快**：从「最后一次读到没闭着」到「第一次闭到 0.30 以下」必须 **≤ 500ms**。
  onset 的起点从"最后一次 >0.65"改成"**最后一次 ≥ 闭眼阈值**"，这样起手读数落在 0.55~0.70
  时也算得出真正的合眼用时。
- 删掉「闭之前明确睁着 ≥600ms」那条（冗余，见上）。
- 默认保持时长 0.6s → **0.4s**（实测用户有意单闭大多 400~620ms，600ms 常常差一点）。
- 顺手修掉一条自相矛盾的日志（保持够了却因起手被拒时，松开后还会再打一行
  "只闭了 1200ms（< 600ms）"）。

**离线回放验证**（`tools/wink-replay/run.ps1`，编译真实 WinkDetector 跑）：14 项全通过 ——
v5.30 的"慢慢读低"三种形态（2.7 秒缓降 / 长闭眼后的眼皮残留 / 630ms 慢眯）**全部不触发**；
v5.31 被错杀的那批真单闭（起手读数 0.62、另一只眼 0.65、onset 360ms、以及 22:19:36 那次
真实成功的 held=616ms/onset=278ms）**全部触发**。

### 先取证②：22:19:40 那"一波调整音量"是什么

日志里那一刻**只有一次**单闭事件，而且是"教科书级"的一次：

```
22:19:34.889 wink 右眼 只闭了 480ms（< 600ms）→ 不触发          ← 第一次闭得不够久
22:19:36.821 wink 右眼 held=616ms eyeL=0.91 eyeR=0.19 min=0.06 other=0.91
   openRun=1058ms onset=278ms -> volume UP 2 档 0->20/150
```

也就是：**一次真单闭 × 当时的默认 2 档 = 音量从 0（静音）一步跳到 20**。此前音量已经被
v5.30 的误触发打到 0，所以这一下听起来就是"突然一波"。它不是误触发 —— 门控数字
（睁着 1058ms、278ms 合到底、另一只眼 0.91、闭到 0.06）与有意单闭一致。

**修法②**：多档不再"一档一档连着调"。原来 2 档 = 连调两次系统音量接口，系统音量面板会连着
弹两次（这就是"一波"的直接来源）。现在：**1 档就直接调一次**（和按一次音量键完全一样）；
**多档则先空跑一档量出"一档 = 几个索引"**（小米 13 上是 10），再用 `setStreamVolume`
**一步跳到目标值、只弹一次面板**；日志也把 `1档=N` 打出来。
另外：**默认档位从 2 档改回 1 档**（= 和按一次音量键完全一样，最不容易"吓一跳"），
想要一大步在设置里选 2/3/5。

### 未改动

阈值表 / 方向仲裁（v5.8）/ 远距离行为 / 30cm 静止硬锁定 / 眨眼档位 / 上一版的
「基准线重建期禁触发」/ 其余设置项：一个字节未改。

---

## [5.31] - 2026-09-16

用户反馈（原话）：「03分48秒左右，触发了误触，你看，**近距离俯视还是会误触**，你看看是眨眼误触的，
还是我动了误触的，给解决掉，而且**单闭眼的触发时间太长了**，而且我希望把**调整音量一次调整多少**
也做出来，让用户自己选择。」

### 先取证：22:03:48 那次到底是谁触发的

**不是眨眼、也不是你动了 —— 是 v5.30 新增的「单闭控音量」通道。** 日志里那一刻只有这一条事件：

```
22:03:47.342 I/Blink: closure ignored: 2681ms > 900ms minEye=0.06/0.02 dist=near   ← 两只眼闭了 2.7s，眨眼通道按规则丢弃
22:03:48.571 I/Wink : wink 左眼 held=1065ms eyeL=0.29 eyeR=0.99 min=0.11 ... dist=near -> volume DOWN 10->0/150
```

- 同刻**没有** `blink #N`（眨眼通道没计数），**也没有** `HeadPose ... triggered`（头部通道没动）；
  距它最近的一次头部触发是 22:03:44.974（早 3.6 秒，见文末）。
- 那一下把音量从 10 打到了 **0**（静音），所以你会立刻察觉。

根因 —— v5.30 全场日志给出的答案：**低头看屏幕时 ML Kit 会把一只眼"慢慢读低"，一读就是几十秒。**
这跟 v5.28 抓到的俯视眯眼是同一件事；v5.29 把它从**眨眼**里清掉了，v5.30 的单闭判据
（「一只眼 < 0.55 + 另一只眼 > 0.70 + 保持 1 秒」）又把它当成"有意单闭"收了进来：

```
22:02:20 eyeL=0.99 eyeR=0.02      ← 这一段"低"的是右眼
22:02:54 eyeL=0.08 eyeR=0.19
22:03:04 wink 左眼 held=1051ms eyeL=0.06 eyeR=0.86   ← 换成左眼低
22:03:45 eyeL=0.16 eyeR=0.03      ← 两只都低（上面那条 2681ms 闭眼）
22:03:48 wink 左眼 held=1065ms eyeL=0.29 eyeR=0.99   ← 又算一次"单闭"，音量 10→0
```

**全场 11 次"单闭"全是这么来的**（左眼 6 次 / 右眼 5 次）——人的眼睛不可能单闭几十秒。
两者的区别只有一个：**读数下降的快慢**。俯视眯眼是一帧一帧**慢慢滑**下去的
（`0.99 → 0.66 → 0.38 → 0.16 → 0.06`，跨了好几秒），而有意单闭是眼皮**"啪"一下合上**
（一帧从 0.9 掉到 0.1 以下）。

### 修法①：单闭判据加「起手」两道门（正因为有了它，保持时间才敢缩短）

- **睁得稳**：闭之前那只眼必须连续"明确睁着"（> 0.65）**≥ 600ms**。
- **闭得快**：从「最后一次明确睁着」到「第一次深闭（< 0.30）」必须 **≤ 350ms**。
- 落在回差带里、慢慢眯下去的读数，两道门都过不去；没触发也会留痕（写明卡在哪一条）。
- 顺手修掉 v5.30 的一个日志 bug：右眼闭时 `eyeL/eyeR` 印反了，现在按眼别打印。

拿这两道门回测 v5.30 那 11 次误触：**全部被拒**（它们的起手是几秒的缓慢下滑）；
而对**有意单闭**几乎无损 —— 那是"睁得好好的 → 一下合上 → 保持"。

### 修法②：保持时间可选（用户说 1 秒太长）

设置页新增「单闭保持时间」：**0.4 / 0.6（默认）/ 0.8 / 1.0 秒**。默认从 1.0 秒收到 **0.6 秒**；
挡误触靠的是上面的「起手」判据，而不是拖长时间。

### 修法③：一次调整多少音量可选

设置页新增「每次调整多少音量」：**1 / 2（默认）/ 3 / 5 档**（1 档 = 按一次音量键；
小米 13 上音量索引 0~150、一档 = 10）。按档位逐次调用系统音量接口，用的就是系统自己的步长。

### 修法④：顺手抓到并修掉的第二处「近距离俯视误触」（头部通道，基准线重建期）

同一份日志里 22:05:32.718 还有一次**下滑**，同样是近距离俯视档（阈值 2.5°），但证据显示那不是你的动作：

```
22:05:32.239 MARK face-back lost=100534ms baselineInvalidated=true      ← 脸消失 100 秒后回来
22:05:32.718 nodDown triggered ... light=yes(-1.5→-5.1)° ... travel=-
   ctx: -159ms|-1.2  -89ms|-1.2  0ms|-1.9      ← 原始俯仰只动了 0.7°
22:05:33.251 GazeDiag: pitch=10.2 base=15.7    ← 基准线一度算成 15.7°（真实值约 10.5°）
```

脸回来后的头几帧角度读数本身是脏的，45 帧中位数基准线一度算成 15.7°，于是 `原始 − 基准`
凭空差出 5° → 在 2.5° 的近距离俯视档直接触发。**修法**：基准线被作废（丢脸回来 / recalibrate /
reset）后，攒够 **12 帧（≈1.1 秒）**才允许头部触发；期间所有动作判定停摆。日志里能看到
`baseline settled (pitchCount=12) — head triggers re-armed`，诊断行新增 `baselineSettling=true/false`。
正常换应用进目标 App 时服务会用 `seedBaseline` 预填窗口，所以这条门只在"基准线刚被作废"之后生效。

### 未改动

阈值表 / 方向仲裁（v5.8 那套）/ 远距离行为 / 30cm 静止硬锁定 / 眨眼档位 / 其余设置项：
一个字节未改。这次只动了「单闭通道的起手判据 + 两个新设置项」和「基准线重建期禁触发」这一条门。

### 22:03:44.974 那次点头（只记录，未改）

```
22:03:44.974 nodDown triggered (boosted, dist=near posture=down threshold=2.5 light=yes(0.4→-5.5) strong=yes) latency=396ms travel=-
   ctx: -201ms|-3.5  -108ms|-2.9  0ms|-5.2
```

这一条是**头部通道**，而且读数自洽：原始俯仰确实在 400ms 内往下走了约 5.6°（0.4° → -5.2°），
轻通道报的 5.5° 与之一致。也就是说：**在 30cm 俯视档（阈值 2.5°）下，一次"往下看几度"的姿势
变化就足以触发下滑** —— 这是 v5.27 你要的那种灵敏度本身带来的取舍。如果这个也想收，办法是把
`NEAR_DOWN_NOD_BOOST` 从 0.42 提到 0.53（阈值 2.5° → 3.2°），属灵敏度取舍，**等你说一声再改**。
（另：这条和上面那条的诊断行里 `travel=-`，是已记录的残留问题 —— 判定域切换时位移判据会跳过。）

---

## [5.30] - 2026-09-16

用户要求（原话）：「在 5.29 的基础上，新增单闭右眼，调高音量，单闭左眼，降低音量，再新增个
反方向的开关，让用户自己选择是单闭左眼增加还是降低，是单闭右眼增加还是降低，**不要动其他的
功能**。这个判定应该会很明显，会有一个非常明显的动作，**单闭眼睛 1 秒以上**，才触发这个功能，
这样就能降低误触。」

### 新增：单眼闭眼 → 音量加 / 减

- **判据**：一只眼读数 < 闭眼阈值，**同时另一只眼 > 睁眼阈值（0.70）**，并且该状态
  **连续保持 ≥ 1000ms**。阈值直接沿用用户挑的眨眼灵敏度（默认 0.55 / 0.70），
  **不按距离放松** —— 单闭是「完全闭上」的动作（读数掉到 0.1 以下），原始阈值更严，
  正是防误触要的方向。
- **默认方向**：右眼闭 = 调高、左眼闭 = 调低。
- **反方向的开关（用户点名要的）**：设置页新增两个**互相独立**的开关
  「左眼闭：调高音量」「右眼闭：调高音量」，各自反向，默认分别为「关 / 开」。
- **一次单闭只调一档**：触发后闩锁，必须**睁眼之后再闭**才算下一次 —— 一直闭着眼不会
  把音量一路推到 0。
- **两只眼同时闭（正常眨眼 / 眯眼）不算单闭**：整段作废。所以这条通道和「眨眼翻页」
  互相排斥，谁也不会吃掉对方的事件（连眨 3 次翻页照常，不会被当成单闭调音量）。
- **短于 1 秒的单闭不触发，但留痕**：`I/Wink: wink 左眼 只闭了 620ms（< 1000ms）→ 不触发 …`。
- **不占用防误触冷却**：这是控制指令（与「张嘴点击」同类），不走 `GlobalTriggerGate` ——
  既不会被冷却挡住，也不会把翻页冷却掉。
- **不依赖无障碍 / Shizuku**：音量由 `AudioManager` 直接调 `STREAM_MUSIC`（抖音走的就是
  这条流）。为此新增 normal 级权限 `MODIFY_AUDIO_SETTINGS`（安装时自动授予、不弹窗）。

### 新增日志（这一版的可核对字段）

- 触发（唯一的真值行）：
  `I/Wink: wink 右眼 held=1015ms eyeL=0.94 eyeR=0.03 min=0.03 other=0.94 thr=0.55 dist=mid/far -> volume UP 5->6/15 applied=true pitch=10.4° pitchNow=9.8° base=4.7° chin=0.310 faceRatio=0.52`
  —— 闭了多久、两只眼各读到多少、当时姿势、音量从几变到几（`applied=false` = 系统拒了这次调节）。
- 够久但另一只眼没睁着：
  `I/Wink: wink 左眼 保持 1204ms 但另一只眼没睁着（other=0.62 需要 > 0.70）→ 不触发`。
- 每 3 秒的 `GazeDiag` 行新增一组字段：
  `winkVol=on winkDir=L=down/R=up wink=L=0ms R=1240ms thr=0.55 fired=3 last=右眼 1240ms winkSteps=3`。
- 设置页「实时数值」新增一行：**两只眼各自已经保持的单闭时长** —— 看着数字涨到 1000ms
  就能确认判定生效，也能立刻分辨是哪只眼被读成闭着。
- `tools/capture-loop.ps1` 的过滤标签加上 `Wink:V`（否则新的日志行不会被抓进验证日志）。

### 未改动（用户要求「不要动其他的功能」）

`BlinkDetector` / `HeadPoseDetector` / 参考点通道 / 方向仲裁（v5.8 那套）/ 阈值表 /
30cm 静止硬锁定 / 眨眼档位 / 已有设置项 **一个字节都没有改**：单闭判定放在新文件
`WinkDetector.kt` 里，只吃同一帧的左右眼读数；服务侧只新增了「喂帧 + 调音量 + 日志」，
以及在各处 `blinkDetector?.reset()` 旁边同步清一次单闭状态（遮挡 / 硬锁定 / 换应用 /
重绑，共 7 处）。

### 已知风险（本版先装到机器上，用实测判据）

v5.28 的日志里，**低头看屏幕时 ML Kit 会把一只眼读成闭着**：`minEye=0.90/0.17`、
`0.83/0.05`、`0.99/0.26`、`0.94/0.26`、`0.78/0.26`，时长 845 / 869 / 1035 / 1098 /
1130 / 1187 / 1719 / 3103ms。这些时长与「有意单闭 ≥1000ms」是**重叠**的，形态
（一只眼低、另一只眼高）也一样。v5.29 把这类读数从**眨眼**里清掉了；本版的单闭判据在
形态上无法把它们与有意单闭分开，所以**必须在真机上验证低头时会不会误调音量**。

`I/Wink` 行里的 `pitch=` / `chin=` / `held=` / `dist=` 就是为这次核对准备的：如果误调全部
出现在低头段（pitch/chin 明显偏大），下一版就把「低头姿态」作为额外条件叠加上去，
或要求更深的闭合读数。反过来，如果误调是零星的，就保持现状。

---

## [5.29] - 2026-09-16

用户复测 v5.28：「**21点00分28秒左右，我人没动，误触了，之后还有一次误触**；左右滑还好，没啥问题。」

日志把两次误触都抓到了，是**两个不同的通道**，各自取证后分别修：

### ① 眨眼通道：俯视时"只闭了一只眼"被当成眨眼（21:00:26.741 那次 UP）

`
21:00:19.015 blink #14 closure=307ms  minEye=0.07/0.13  run=1/3   ← 真眨眼：两只都低
21:00:20.103 blink #15 closure=198ms  minEye=0.01/0.02  run=2/3
21:00:26.137 blink #19 closure=190ms  minEye=0.89/0.24  run=2/3   ← 只有右眼低 ✗
21:00:26.738 blink #20 closure=385ms  minEye=0.90/0.17  run=3/3 → A11y: swipe UP ✗
`

用户人没动，但**眨眼通道**在 7 秒里凑够了 3 次"眨眼"→ 翻页。原因：判定口径是
「**任一只眼**低于阈值就算闭眼」，而近距离俯视时 ML Kit 经常只把**一只**眼睛读成半闭
（诊断行里 `eyeL=0.83 eyeR=0.05`、`eyeL=0.14 eyeR=0.64` 反复出现）。

**修法**：改成「**两只眼睛都低于阈值**才算闭眼」（只有一只眼可用时才只看那一只）。
两类数据分得干净：用户有意的眨眼是 `0.02/0.03`、`0.03/0.09`、`0.04/0.03`、`0.06/0.11`、
`0.08/0.28`；被误计数的那一批是 `0.90/0.17`、`0.83/0.05`、`0.99/0.26`、`0.94/0.26`、`0.78/0.26`。

另外加一条**时长上限**：一次眨眼超过 **900ms** 不算眨眼。实测有意眨眼是 139~758ms，
而误计数的那批是 845 / 869 / 1035 / 1098 / 1130 / 1187 / 1719 / **3103**ms —— 那是
"眼睛一直被读成闭着"，不是眨眼。日志会打 `closure ignored: <N>ms > 900ms`。

### ② 俯仰通道：闭眼那一帧被当成了"渐进动作"的证据（21:00:29.848 那次 DOWN）

`
ctx … -123ms|-0.4 → -72ms|-3.5 → 0ms|-5.4      ← 静止两秒后，单帧跳 3.1°、再跳 1.9°
21:00:29.768 nodDown candidate rejected: pitch=-2.9° reason=eyes-unreliable   ← 这一帧已被拒
21:00:29.848 nodDown triggered: pitch=-4.9° light=yes(-0.5→-4.9) strong=yes
`

用户人没动，是**闭眼让 ML Kit 的姿态跳了一下**（这次跳变恰好还叠上了低头方向的位移）。
本该拦住它的是跳变确认（单帧跳变要再撑一帧），但**前一帧虽然被 `eyes-unreliable` 拒了，
却仍然被记成 `previousFramePitch`** → 跳变确认看到"前一帧已经越阈值" → 判定为**渐进动作** →
零延迟放行 ✗。这是 v5.27 的强候选豁免带出来的副作用。

**修法**：**闭眼期的帧不作证据** —— `eyes-unreliable` 被拒的帧不再更新
`previousFramePitch` / 速度样本。于是下一帧必须自己站稳：跳变确认会等一帧，
而这一帧之后姿态已经跳回去了（`below-threshold`），动作自然消失。
对真实动作的代价是**最多多等一帧**（63~116ms）。

### 未动

阈值表（点头 2.5° / 仰头 3.8° / 扭头 20°）、30cm 静止硬锁定、静止锁定、距离档、
晃动判据、手机运动闸门、回中锁定、v5.28 的四条新判据（扭头摆幅 / 扭头强候选豁免 /
位移判据 / 突然性窗口）、远距离全部行为。

### 已知残留（留给下一版）

- `travel=-`：位移判据在"判定域在两帧之间切换"时会跳过（轻通道与基线域切换）。
  21:00:29 那次触发就是 `travel=-`，所以它没被位移判据拦住 —— 这次靠闭眼证据拦住。
  下一步可以给两个域各存一份起点值，让位移判据永远生效。
- 眨眼通道单次「闭眼 900ms 上限」是新的；如果用户的有意眨眼偶尔更长（比如困倦时），
  需要按日志再调。

---
## [5.28] - 2026-09-16

用户复测 v5.27（原话）：

> 「这一版好像没啥问题……就是你看日志，**47分30秒左右，我左右扭，但是被判定成了上滑**，
> **近距离俯视，左右滑有点问题**，上下滑仰头点头暂时不动，先把这个近距离俯视左右给优化一下；
> 另外，再优化一下**晃动的时候产生的误触**，近距离俯视最容易发生误触，优化一下算法；
> 而**点头和扭头的判定是突然性的**，就是突然快速的点头，而不是慢慢的晃动，
> 所以加一个判定方式吧，**不是快速扭头点头的时候，就不触发**。」

三件事，全部按 v5.27 的实机日志取证后改，且**全部只在近距离档生效**（远距离那一套不动）。

### ① 扭头被判定成上滑：俯仰侧根本没看见那次扭头

日志（20:47:30 附近，正是用户点名的那一段）：

`
20:47:32.016 yaw 11.47° base 0.22° delta -11.3°          ← 开始往左扭头
20:47:32.088 tiltUp candidate rejected: pitch=6.1° reason=yaw-dominant-arbitration ✓ 让位成功
20:47:32.138 左 turn triggered yaw=23.2° latency=121ms   ← LEFT ✓
20:47:37.097 pitch 19.1° yaw -29.77°                     ← 用户往右扭头，偏航已 -29.8°
20:47:37.115 tiltUp triggered: pitch=9.3°  ctx 0ms yaw=1.5 ← 俯仰侧只看到 1.5° ✗
20:47:37.119 turnR candidate rejected: yaw=29.5° reason=eyes-unreliable
20:47:37.124 A11y: swipe UP                                ← 想要 RIGHT，得到 UP ✗
`

同一帧里**扭头通道看到 29.5°**、**俯仰通道只看到 1.5°** —— 因为俯仰侧的仲裁读的是
「有符号偏航 = 原始偏航 − 基准线」，而那一刻偏航基准线刚被重学/重置，于是差值塌成 1.5°，
`isYawMovingNow` 的三条判据（速度、占优、幅度）全部不成立 → 不让位 → 俯仰直接触发。

**修法**：新增 `yaw-swing-arbitration` —— 用**原始偏航**在 500ms 内的**摆幅**判定
（与基准线无关，所以基准线怎么漂都不影响）。同一场 52 次触发的统计：

- 真扭头：原始偏航摆幅 **25~59°**
- 真点头/仰头：**1.7~10.4°**

取 **12°**：真扭头全部命中，真点头全部不命中。而且这条放在 onset 声明**之前**，
扭头期间俯仰通道连动作都不开始记 —— 否则扭头一结束，那个"已经越阈值"的残留姿势
会立刻补一次上下滑（20:47:42 那次就是）。

### ② 扭头本身被"闭眼"挡住：25 次扭头候选里 12 次死在眼睛上

`eyes-unreliable` 是"眼部关键点质量掉下来"的信号，而**扭头会把眼皮挤成半闭**
（20:47:37.020 那一帧 `eyeL=0.18`）。这和 v5.27 在俯仰侧修掉的是同一个毛病。

**修法**：扭头通道同样给**强扭头**（越阈值 + 2.0°）豁免这条门 —— 判据与俯仰侧完全一致。
照常调用 `eyeUnreliable()` 维护恢复期，只是不再据此否决强扭头。

### ③ 晃动误触：有一类触发是"头根本没动"

这一场近距离档里有几次触发的原始轨迹是这样的（`ctx` 是触发前 1.2 秒的逐帧读数）：

`
20:46:58 nodDown triggered  ctx 0.0 → -1.3   （位移 1.3°）
20:47:22 nodDown triggered  ctx 0.7 → 0.1    （位移 0.6°）
20:47:24 tiltUp  triggered  ctx 5.1 → 5.8    （位移 0.7°）
`

共同形状：头部姿势**本来就已经越过阈值**（相对滞后的基准线），一次轻微晃动就让"越阈值"成立。
**修法**：新增 `no-travel` 位移判据 —— 判定域里从**运动起点**（onset 那一刻的上一帧取值）
到当前必须真的走过 `0.5 × 当帧阈值`。真实动作起点在 0.4×阈值、终点在阈值之上，
天然走过 0.6×阈值，不受影响；只剩噪声位移的候选被清掉。

⚠️ 位移必须在**同一个域**里比（轻通道是"相对运动起点的位移"，其余是"相对基线量"），
所以起点值与当前值都用同一帧算出来的 `signed` —— v5.17 那次"域混用"把方向整体翻过一次，
这里专门做了防呆（域不一致时 `travel` 记 `-` 并跳过判据）。

### ④ 用户要的"突然性"：只认快速动作

用户原话：「点头和扭头的判定是**突然性**的……不是快速扭头点头的时候，就不触发」。

近距离档的「起点 → 第一次越阈值」窗口：俯仰 **300ms**、扭头 **400ms**
（远处仍用原来的 500ms / 900ms）。依据是同一场的实测分布：52 次真实触发的
「onset → 触发」延迟是 **53~249ms**（中位数约 100ms），21 次真实扭头是 **65~217ms**，
所以这两条窗口只砍得掉慢动作。

⚠️ **没有**直接拿"触发延迟"当判据：延迟里包含门控造成的等待 —— v5.27 远距离那两次
575ms / 624ms 其实是**很快**的仰头，只是被闭眼门连等了 2~3 帧。拿它当"慢"的证据会误杀真实动作。

### 未动

点头 2.5° / 仰头 3.8° / 扭头 20° 三个阈值、30cm 静止硬锁定、静止锁定、距离档与滞回、
晃动判据（方向反转）、手机运动闸门、跳变确认、回中锁定（v5.27 版）、眨眼、张嘴通道、
**远距离的全部行为**（上面四条判据都带 `nearDistance` 前置条件）。

### 新增可核对日志

- 触发行：`travel=<位移> yawSwing=<摆幅> shake=<效率> rev=<反转次数>`（俯仰与扭头都有）。
- 被拒行：同样带上 `yawSwing=` / `travel=`，新原因 `yaw-swing-arbitration`、`no-travel`。
- 扭头慢动作：`ignored slow turn: rise <N>ms > <window>ms`。

---
## [5.27] - 2026-09-16

用户复测 v5.26：

> 「现在好点了，但是**触发还是慢了，而且还是有误触**，是不是有点太敏感了，就是**仰头后，
> 还是会延迟翻页**，要解决掉这个问题，**其余的不要动**，专门攻破这个问题，
> 就是**近距离俯视的点头仰头，不要误触，而且还要快速触发，而且判定还要准确**。」

本版只动这一件事：**近距离俯视的点头/仰头**。四改，全部有 v5.26 实机日志的对应证据。

### ① 先取证：不是"识别慢"，是三道门把**已经做出来**的动作拦住了

v5.26 验证日志（20:29:00~20:31:05，近距离俯视）里被拒候选的分布：

`
tiltUp  ref-veto-up 89 | eyes-unreliable 58 | below-threshold 17 | jump-confirm 11 | hold-not-met 2 | speed-gate 2 | slow-rise 2
nodDown slow-rise 16 | eyes-unreliable 15 | speed-gate 15 | below-threshold 9 | hold-not-met 8 | jump-confirm 7 | ref-veto-down 5
`

最刺眼的是 20:30:40.5~41.2 —— 用户做了一次 **3.5° → 17.2° 的连续仰头**（约 330ms，速度
0.04~0.08°/ms），**一帧都没放行**：

`
20:30:40.573 tiltUp candidate rejected: pitch=3.5°   reason=below-threshold
20:30:40.665 tiltUp candidate rejected: pitch=10.9°  reason=ref-veto-up
20:30:40.759 tiltUp candidate rejected: pitch=14.6°  reason=eyes-unreliable
20:30:40.865 tiltUp candidate rejected: pitch=16.5°  reason=eyes-unreliable
20:30:40.933 tiltUp candidate rejected: pitch=17.2°  reason=eyes-unreliable
20:30:41.005 tiltUp candidate rejected: pitch=14.5°  reason=eyes-unreliable
20:30:41.092 tiltUp candidate rejected: pitch=13.3°  reason=ref-veto-up
20:30:42.551 nodDown triggered light=yes(2.1→-3.6)  ← 回摆末段被当成点头，又翻一页
`

用户说的「仰头后还是会延迟翻页 / 有误触 / 判定不准」，这一小段三样全占了。
延迟的中位数也印证：tiltUp 的 `latency` 中位数 **270ms**，最长 **524ms**。

### ② 根因一：证人否决看的是**原始位移**，而它在起手瞬间必然 ≈0

v5.25/v5.26 的仰头确认要求 `refRelNoseDy ≤ −0.015`。但参考点通道的**参考值是在头部静止时
更新的** —— 动作**起手**那一刻位移必然接近 0，于是每次起手都先被否掉一帧。

自相矛盾的直接证据（同一毫秒）：`

20:30:40.664 would-trigger(rel) tiltUp ref: rel dy=-0.054 opt=ok      ← 证人明确说"仰头方向"
20:30:40.665 tiltUp candidate rejected: pitch=10.9° reason=ref-veto-up ← 却被同一个证人否决
`

**修法**：否决改看**证人自己确认的结论** `ReferencePointDetector.relVerdict`
（只有它自己走到 `opt=ok` —— 越阈值、非跳变、在动作窗内 —— 才算"有结论"），
并且要求位移明显越过它**自己的**阈值（`relThresholdNow`）。起手帧与动作窗外的噪声不再参与。
实测这一条覆盖了 89 次误否里的绝大多数。

### ③ 根因二：仰头本身会被判成"闭眼"

`eyeDip` 是"眼部关键点质量掉下来"的信号。而**仰头时眼皮会被挤成半闭** —— 上面那 4 帧
连续 `eyes-unreliable` 就是这么来的；再叠上 `eyeUnreliable` 的 50ms 恢复期，
仰头整个过程都被压住。

**修法**：`eyes-unreliable` 只对**边缘候选**生效 —— 幅度已经越过"阈值 + 2.0°"（[STRONG_MARGIN_DEG]）
的强候选不再被它否决（恢复期计时照旧）。安全性依据：唯一已知的单帧跳变噪声是 **3.5°**
（v5.14 那次眨眼误触），而近距离俯视的强候选门槛是点头 2.5+2.0=**4.5°**、仰头 3.8+2.0=**5.8°**，
都在它之上。

### ④ 根因三：保持时间/最低速度对"已经做出来的动作"没有信息量

`hold-not-met`(10 次)、`light-confirm`、`speed-gate`(17 次) 都是为**幅度刚好压在阈值上**的候选
防噪声设计的。强候选已经明确越过阈值，再等它们只会白等一帧（63~116ms）。

**修法**：强候选免等这三道门。晃动判据、手机运动闸门、跳变确认**三道门一个都没去掉**
（跳变确认仍然保证单帧跳变最多多撑一帧）。

### ⑤ 顺带修：回中锁定的"400ms 无条件兜底"

v5.2 的兜底是"锁满 400ms 就无条件解锁"，**完全不看头在哪、也不看还在不在动**：
仰头触发后 400ms 头还在 16° 高位就已经解锁，随后"把头放回来"的那次 18° 下行回摆
穿过中性区时又把 150ms 计时刷满 → 回摆末段被判成一次新点头（正是 20:30:42.551 那次）。

**修法**：解除必须先满足**不在动**（速度 < 当帧速度门限），再看
「回到中性区并稳定 150ms」**或**「头就停在自己当前的姿势上（`|signed − settledPitch| ≤ 中性区`，
这条是姿势偏置的出口，避免 v5.1 那种"点头全失效"）」，并保留 **3 秒硬上限**（只需"停住"）。
实测这次的 400ms 兜底在 400ms 就解锁了（`recenter lock released by timeout (400ms)`），
新逻辑下同一时刻头还在 15° 以上、且仍在动，锁不会解除。

### 未动

30cm 静止硬锁定、静止锁定、距离档、方向仲裁（v5.8）、晃动判据、手机运动闸门、跳变确认、
眨眼通道（3 次）、张嘴通道、扭头通道、灵敏度阈值表本身（点头 2.5° / 仰头 3.8° 未改）、
用户设置（反转俯仰＝关）—— 全部原样。

### 新增可核对日志

- 触发行：`strong=yes/no`（这次是不是靠强候选放行的）。
- 被拒行：`strong=0/1` + `refV=<证人结论>/<位移> thr=<证人阈值>`（判据一眼可核）。
- 回中锁定解除行：`released, head settled at own posture (pitch=… settled=… neutral=… 1234ms)` /
  `released by hard cap (3000ms)` / `released, back to neutral (…ms)`。

---
## [5.26] - 2026-09-16

用户复测 v5.25：

> 「灵敏度没啥问题，但是**我仰头有时候会判定下拉，又会判定成上滑，判定有问题**；
> 而且这个灵敏度虽然可以，但是**会有点容易误触**。点头和仰头你单独写个判定的方式，
> 比如**我真要下滑，我就用快速点头，而不是慢慢的点头** —— 有时候慢慢的点头还是会被误判。」

### ① 「仰头被判成下拉」的取证：用参考点通道当独立证人

逐个核对 26 次触发（把"鼻子相对眼睛"的走向当证人）：

`
20:23:01 nodDown light=yes(16.5→-12.6) lat=807ms  relN=-0.029  ← 证人相反 ✗
20:23:06 nodDown light=yes(13.9→-6.9)  lat=198ms  relN=-0.024  ← 证人相反 ✗
`

**17 次点头里有 4 次证人方向相反**，而那正是用户说的"仰头被判成下拉"。
看 light=yes(16.5→-12.6) 就明白了：位移通道的**参考点过期**了
（起点还停在 +16.5，而用户已经换完姿势），于是 **29° 的"换姿势"被当成一次点头** ✗。

### ② 用户给出的解法是对的：只认可**快速**动作

那些误判事件的延迟都很大（**807ms / 588ms / 544ms**）—— 一眼可见是慢动作。
**慢动作期间基准线与参考点会跟着跑，方向就会飘**；快速动作一瞬间完成，它们来不及漂。

**修复（两条）**：

1. **点头也要求"够快"**：[LIGHT_NOD_FAST_VELOCITY] 从 0.009 拉回 **0.020**
   （= 标准快通道门槛）。慢点头一律不再触发 —— 手势因此变得**可学习**：
   「**快速点头 = 下滑，快速仰头 = 上滑**」。
2. **轻通道加位移上限** [LIGHT_MAX_DISPLACEMENT_DEG] = 12°：超过就不走轻通道，
   于是"换姿势"那种 29° 的巨大位移再也冒充不了轻点头。
3. **证人否决改成双向对称**：近距离时，仰头要求 refRelNoseDy ≤ −0.015（v5.25 已有），
   现在**低头也要求证人没有明确反对**（refRelNoseDy 不能 ≤ −0.015）。
   实测这条正好挡住上面那 4 次"仰头被判成下拉"。

### 未动

30cm 静止硬锁定、静止锁定、跳变确认、手机运动闸门、距离档、方向仲裁（v5.8）、
眨眼 4 次、反转俯仰方向、阈值表（低头 2.5° / 仰头 3.8°）—— 全部原样。
灵敏度本身没动（用户说"没啥问题"），只改了"哪些动作算数"。

---

---

## [5.25] - 2026-09-16

用户复测 v5.24：「仰头比之前好点了，但是**还是不够灵敏**，我的角度还是需要大一点，
而且**我仰头了，要等个差不多 0.4-0.5 秒才触发**；我身体一动，就会误触。」

### ① 仰头为什么慢：阈值太高（不是识别慢）

实机 16 次仰头的延迟统计：**中位数 202ms，其中 3 次 ≥500ms** —— 正好对上"等半秒"。
仰头的起点在 onset（2.4°）附近慢慢抬，要涨到 **6.0°** 才放行，于是从起点到触发要 200~760ms。

**修复**：仰头方向在「近距离 + 俯视」时也开一档 —— [NEAR_LOOKUP_BOOST] = 0.63 → **3.8°**，
把这段行程砍掉近一半。

⚠️ v5.7 曾因「近距离仰视被误触」把仰头增益**单向化**（只压低头方向）。这次敢给仰头也开档，
是因为同时加了下面的参考点确认 —— 当年缺的正是这个。

### ② 逐关键点数据挑出了仰头的干净判据：**鼻子相对眼睛**

24 条标注对照（16 次仰头 / 8 次点头）的方向一致率：

| 判据 | 仰头（16 次） | 点头（8 次） |
|---|---|---|
| 绝对鼻子位移 | 一致 11 / 弱 3 / **相反 2** | 一致 0 / 弱 6 / **相反 2** |
| 绝对下巴位移 | 一致 12 / 弱 2 / **相反 2** | 一致 2 / 弱 4 / **相反 2** |
| **鼻子相对眼睛** | **一致 14 / 弱 2 / 相反 0** ✓✓ | 一致 0 / 弱 6 / 相反 2 |
| 下巴相对眼睛 | 一致 14 / 弱 0 / **相反 2** | 一致 4 / 弱 4 / 相反 0 |

**"鼻子相对眼睛"在 16 次真实仰头里 0 次反例**，幅度中位数 0.065、绝大多数 ≥0.028。
它是**平移无关**的：身体动一下只会让整张脸平移，不改变这个相对量。

**修复**：新增 [TILT_REF_CONFIRM] = 0.015 —— 仰头候选必须满足
`relNoseDy ≤ −0.015` 才放行（仅近距离生效，远距离不受影响）。
这条正好打掉用户说的「我身体一动就会误触」：身体平移不改变相对量，因此进不来。

### 未动

30cm 静止硬锁定、静止锁定、跳变确认、手机运动闸门、距离档、方向仲裁（v5.8）、
眨眼 4 次、反转俯仰方向开关、低头方向的 2.5° 档 —— 全部原样。

---

---

## [5.24] - 2026-09-16

**v5.23 的 51 次标注对照数据，给出了"参考点位移能不能替代角度"的定量答案：**

| 标注事件 | 轨道 A（绝对位移） | 轨道 B（平移无关） |
|---|---|---|
| **点头 31 次** | 一致 8 / 无信息 19 / **相反 4** | **一致 21 / 无信息 4 / 相反 6** ✓ |
| **仰头 20 次** | 一致 3 / 无信息 14 / **相反 3** | 一致 9 / 无信息 2 / **相反 9** ✗ |

结论有两条，必须分开说：

1. **"点头"这一半，用户的思路成立** ✓：平移无关轨道在 31 次真实点头里 21 次方向一致、
   只有 4 次"无信息"，是一个可用的判据。
2. **"仰头"这一半目前不可用** ✗：9 对 9，等于掷硬币。**所以本版不能按原计划把主判据
   整体切到位移通道** —— 那会把仰头搞坏，比现在更差。

### 本版（v5.24）因此只做一件事：把仰头这一半的信号找出来

新增**关键点明细日志**（`abs a=… b=… | rel a=… b=…`，a = 鼻子、b = 下巴）：

- 合并成一个 `dy` 之后，"鼻子和下巴哪个更可靠"这类信息被平均掉了；
- 分开记录之后，一次实机动作就能判定：是**下巴（用户对仰头的描述）**在仰头时才有干净的
  竖直位移，还是**仰头这个动作本身就不产生竖直平移**（而是透视缩短，需要另一种度量）。

行为零变化：参考点通道仍然**只观测不接管**，触发权仍在 pitch 通道（= 用户满意的 v5.19 灵敏度）。
30cm 硬锁定、静止锁定、跳变确认、手机运动闸门、距离档、方向仲裁、眨眼 4 次 —— 全部未动。

---

---

## [5.23] - 2026-09-16

**v5.22 首轮实机数据给出的两个结论**（26 条对照记录）：

### ① 用户的符号约定**实测成立** ✓

pitch 通道真实触发的那一刻，位移通道的读数与动作方向一致：

`
19:01:53.566  pitchCh=-9.0°(点头)  →  noseDy=+0.086 chinDy=+0.065   脸往下移 ✓
19:01:56.050  pitchCh=+13.0°(仰头) →  noseDy=-0.036 chinDy=-0.034   脸往上移 ✓
`

两个关键点（鼻子/下巴）**同向** ✓，所以"下移 = 点头、上移 = 仰头"可以用。

### ② 但**绝对位移**的噪声底与真实动作一样大 ✗

4.5% 阈值下 `would-trigger` 达到 **54 次/分钟**，而真实仰头那一次位移只有 **0.035**
（低于阈值）。根因很直接：**脸在画面里的绝对位置会被"手机本身在动"带着走** ——
抬一下手、换个姿势都会让整个脸平移，于是"位移"里混进了手机平移。

### 修法：加一条**平移无关**的轨道，两条一起观测

- **轨道 A（绝对位移）**：`noseNormY` / `chinNormY`，就是用户描述的那个量。
- **轨道 B（平移无关）**：`noseRelEye` / `chinRelEye` = 关键点相对**眼睛中心**的
  归一化距离。手机平移时它**不变**，只有头部真的俯仰（透视缩短）才变 ——
  同一思路，但把"手机在动"这个污染源去掉了。阈值 2.5%（比 A 小，因为读数本来就小）。

两条轨道的 `dy` / `thr` / `spd` / `would` 计数 / 丢弃原因**全部打进日志**，
用同一次实机动作做对照，谁的信噪比高就用谁。

### 仍然只观测、不接管触发

触发权仍在 pitch 通道手里（本版行为与 v5.21 完全一致，零风险）。
下一步：按实测对照数据选定轨道与阈值 → v5.24 切主判据。

---

---

## [5.22] - 2026-09-16

用户提出**换掉"硬算角度"**，改用**参考点位移**：

> 「用一个参考点，来计算我点头还是仰头。标准的仰头就是我的下巴会往上移动几厘米，
> 点头就是鼻子会往下移动几厘米。做一个参考点，**不能硬算**。」

### 做法：新通道完整实现，但本版**只观测、不接管触发**

**为什么分两步**：符号（图像坐标里"鼻子下移"是哪一侧）与阈值必须用**实测数据**确认，
不能猜 —— v5.17 / v5.18 两轮"全部乱了"都是猜符号造成的。所以本版把参考点通道
**完整跑起来、每帧计算、全程打日志**，而**触发权仍归原有 pitch 通道**，
现有可用行为零风险；拿到实测符号与阈值后，v5.23 再把它切成主判据。

### 实现（[ReferencePointDetector] + 分析器新增关键点归一化坐标）

1. FaceGazeAnalyzer 新增三个量（都归一化到**脸框内**）：
   `noseNormY = (noseY − boxTop) / boxHeight`、`chinNormY`（`MOUTH_BOTTOM`）、
   `eyeNormY`（眼睛中心，作不动参照）。除以脸框高度就是用户说的"距离归一化"。
2. **参考点**只在头部稳定时更新（连续 3 帧峰峰值 < 1.2% 脸高），
   所以它是"这次动作的起点"，不会像 45 帧中位数基线那样滞后 —— 这正是 v5.16~v5.21
   反复出问题的根源。
3. **位移**：`noseDy = noseNormY − refNose`、`chinDy = chinNormY − refChin`，
   单位是**脸高比例**（1.0 = 一个脸高）。同一动作下两个关键点同向，判定用二者均值
   `faceDy`；若反向（关键点抖动）则退回只信鼻子。
4. **方向**：图像坐标 Y 向下为正 —— 鼻子/下巴**下移** = 点头、**上移** = 仰头，
   与用户描述一致；`invert`（设置页「反转俯仰方向」）打开时两个方向**一起**反转。
5. **阈值**：默认 4.5% 脸高；近距离俯视时放宽到 **3.2%**（用户要求：只放宽上下方向）。
   另有一道时间窗（700ms 内从 onset 涨到阈值）、速度门限、以及单帧台阶上限
   （一帧跳超 6% 脸高判为关键点抖动）。
6. **日志**（用户要求格式）：
   - 诊断行新增 `ref noseDy=… chinDy=… dy=… thr=… spd=… would=… opt=…`；
   - 每次 pitch 通道真实触发时，紧跟一行 `RefPoint: nodDown ref: …`，
     用来对照"真实点头在位移通道上长什么样"；
   - 位移通道"本来会触发"时打 `would-trigger …`（400ms 节流）。

### 未动的东西

30cm 静止硬锁定、静止锁定、跳变确认、手机运动闸门（陀螺仪+加速度计）、距离档、
俯视判据、阈值表、方向仲裁（v5.8）、眨眼 4 次档、用户自己拨的「反转俯仰方向」——
全部保持 v5.21 原样。

---

---

## [5.21] - 2026-09-16

用户提出的思路（这一版就是照它做的）：

> 「控制开关里有个设置叫**反转俯仰方向**。你先别管是上滑还是下拉，只要调好灵敏度，
> **我自己开这个开关**。我觉得 **5.19 的那个灵敏度最合适**，你抄过来。我们一个一个的解决，先解决这个再说。」

### 做法：把 v5.19 的判据抄回来（撤销 v5.20 的回退）

`git checkout 28ee911 -- HeadPoseDetector.kt GazeCameraService.kt` —— 恢复轻通道的
**运动起点判向**（`settledPitch` + 同域相减 `signedPitch - settledPitch`），
也就是 v5.18 日志里"7 次点头 7 次下滑、3.3~6.6° 就触发"的那套灵敏度。

**方向问题从此交给用户的「反转俯仰方向」开关**：那套判据的方向是**跟着运动走**的
（位移的符号），所以：

- 开关拨到任意一侧，**点头与仰头会一起反过来**，不会出现"两个方向各自为政"；
- 用户自己拨一次就能得到自己期望的对应关系，不需要我们猜。

实机确认：用户**已经自己把该开关拨到 `headPoseInvertPitch = false`**（原为 true）。

### 顺带修一个副作用

在设置页拨开关时，app 会把**整份配置**存一遍，把之前用 adb 设好的
`blinkTriggerCount = 4` 覆盖回了 3。已重新设为 4（用户想用 3 次在设置页一点即可）。

### 保留不变

静止锁定、跳变确认（v5.15）、手机自身运动闸门（陀螺仪 + 加速度计）、
距离档与俯视判据、阈值表（近距俯视低头 2.5°）、眨眼 4 次档 —— 全部未动。

---

---

## [5.20] - 2026-09-16

**按用户明确要求回退**：检测逻辑回退到 v5.16（`git checkout a63ee54 -- HeadPoseDetector.kt GazeCameraService.kt`），
只保留 v5.19 的**眨眼 4 次档**。用户的话是「先不回退…不试了，立刻回退到 v5.16（并把眨眼 4 次档一起带上）」。

### 回退掉了什么

v5.17 / v5.18 的**轻通道位移判向**（`settledPitch` + `light=起点→终点`）整体撤销。
它是 v5.17 引入、v5.18 修掉域混用 bug 之后的版本，在 v5.18 的实测里方向是对的：

`
04:32:41.100 nodDown light=yes(8.5→-3.4) → swipe DOWN（上一个视频）✓   ← 7 次点头 7 次下滑
`

但用户仍然表示"点头变成了上滑"，并选择回退。**记录在此，供以后参考**。

### 回退前先把证据固定下来（避免以后重复排查）

1. **手势几何是对的**：`AdaptiveSwipe.pathFor` 里
   `UP -> 0.70→0.30`（手指往上 = 下一个）、`DOWN -> 0.30→0.70`（手指往下 = 上一个）；
   `SwipeInjector` 的注释也写明 `DOWN` = previous item。所以 app 发"下滑"时确实是"上一个视频"。
2. **上滑只有一个来源：眨眼**。`GazeCameraService` 里眨眼触发写死 `SwipeDirection.UP`。
   v5.18 日志里全场唯一一次上滑，前面紧跟 3 连眨：

   `
   04:33:13.259  blink #9  gap=4603ms run=1/3
   04:33:13.609  blink #10 gap=350ms  run=2/3
   04:33:14.193  blink #11 gap=583ms  run=3/3
   04:33:14.200  swipe UP
   `

   **"点头时看到跳到下一个视频"最可能就是它**：点头时眼皮会动，凑够 3 连眨就发出一次上滑。
3. v5.16 本身也存在**基准线滞后导致小幅度判反方向**的问题（用户当时原话就是
   「轻轻点头，没有下滑，倒是变成了上滑」），回退后该现象可能重现 —— 这是本次回退的已知代价，
   在得到用户同意后明确接受。

### 已知保留项

- **眨眼 4 次档**（v5.19 引入）保留，且设备已设为 4 次 —— 这是"不由自主 3 连眨"的直接对策。
- 静止锁定、跳变确认（v5.15）、手机自身运动闸门（陀螺仪 + 加速度计）、
  距离档与俯视判据（基准俯仰角）、阈值表（近距俯视低头 2.5°）**全部保留**。

### 回退点

`e5daedd` v5.12 / `a63ee54` v5.16 / `ef25e44` v5.18 / `28ee911` v5.19 —— 随时可切换。

---

---

## [5.19] - 2026-09-16

用户复测 v5.18：**「这个点头触发我很喜欢，基本很灵敏」**，但同时说「和之前一样，点头变成了上滑」，
以及「静止不动还会误触」。建议回退到问题最小的那一版。

### 先把日志证据摊开：v5.18 的方向其实是**对的**

`
04:32:41.100 nodDown light=yes(8.5→-3.4) → swipe DOWN（上一个视频）✓
04:32:44.501 nodDown light=yes(5.7→-6.6) → swipe DOWN ✓
04:32:47.713 nodDown light=yes(6.3→-5.8) → swipe DOWN ✓
04:32:50.002 nodDown light=yes(6.3→-5.3) → swipe DOWN ✓
04:32:53.586 nodDown light=yes(4.1→-3.9) → swipe DOWN ✓
04:32:56.572 nodDown light=yes(5.9→-3.3) → swipe DOWN ✓
04:32:58.700 nodDown light=yes(5.9→-4.6) → swipe DOWN ✓
`

**7 次点头、7 次下滑，方向与灵敏度都对**（3.3~6.6° 就触发）。手势几何也核对过：
`SwipeInjector` 里 `DOWN` 的注释就是 *Finger travels down the screen: previous item* ✓。

### 而全场唯一那次上滑，是**不由自主连眨 3 次**触发的

`
04:33:13.259  blink #9  gap=4603ms run=1/3
04:33:13.609  blink #10 gap=350ms  run=2/3
04:33:14.193  blink #11 gap=583ms  run=3/3   ← 3 连眨凑够门槛
04:33:14.200  swipe UP                        ← 眨眼按设计就是上滑
`

这就是用户说的「静止不动还会误触」—— 眨眼触发在服务里写死是 `SwipeDirection.UP`，
所以它永远是上滑。而门槛恰好是 3 次，用户"控制不了"的连续眨眼正是 3 连眨。

**修复**：眨眼次数新增 **4 次**档（设置页可选，`SUPPORTED_COUNTS` 与三处 `coerceIn` 同步放开到 4），
并已把设备设成 4 次。这样"不由自主的 3 连眨"不再触发，而**有意的**眨眼只要眨 4 下即可 ——
沿用"把门槛交回用户本意"这条在 v5.10 被验证有效的做法。

### 关于回退

用户提出回退到"问题最小的那一版"。本版**没有回退**，理由是日志显示 v5.18 的点头方向与
灵敏度正是用户满意的部分，回退会把它一起丢掉；而唯一的"误触"来源（不由自主的连续眨眼）
已经用 4 次门槛处理。git 里保留了完整回退点（`e5daedd` v5.12、`a63ee54` v5.16 …），
一条命令即可切换。

---

## [5.18] - 2026-09-16

用户复测 v5.17：**「全部乱了」** —— 轻点头全变上拉、连续点头也是、走路方向乱、
静止 30 秒更是"人都没动就疯狂上拉"。

### 这是我 v5.17 的一个实现错误（一行），不是方案问题

```kotlin
// v5.17 初版（错）
val lightDelta = pitchDeg - settledPitch      // ✗ 域混用
val signedLight = if (invertPitch) -lightDelta else lightDelta
```

`pitchDeg` 是**原始俯仰角**，而 `settledPitch` 是用 `signedPitch` 记下来的**有符号值**
（已经过 `invertPitch` 翻转、并且扣掉了基准线）。两者相减等于：

1. 把方向**整体翻了一次**（用户 `invertPitch=true`）→ 轻点头全变上拉；
2. 还叠上了一个基准线偏移，而这个偏移本身在抖动 → 差值的量级远大于真实位移
   → 阈值被频繁顶穿 → **静止时"疯狂上拉"**。

**修复**：位移必须在**同一个域**里算 —— 两个量都用有符号值：

```kotlin
val signedLight = signedPitch - settledPitch   // ✓ 同域相减
```

修完之后的语义是干净的：`settledPitch` 是动作起点（有符号域），
`signedLight` 就是"这次移动了多少、往哪个方向"，负值 = 低头方向 → nodDown。

触发日志会打出 `light=yes(起点→终点)`，例如 `light=yes(0.3→-2.8)` 表示从 +0.3 移动到 −2.8，
方向与幅度可以直接在日志里核对，不用再靠感觉判断。

---

## [5.17] - 2026-09-16

用户复测 v5.16：

> 「②**轻轻点头，没有下滑，倒是变成了上滑**，这个有问题」
> 「④连续点头 5 秒没啥问题，但是角度要大一点」
> 「⑤走路触发的概率太小了，只成功了两次……我走路的时候尝试过眨眼，但是没触发眨眼」
> 「⑥30 秒，还是误触了两次」

### ① 主修：轻点头被判成上滑 —— 根因是**滞后的基准线**

```
04:18:23.300  rawPitch=11.28°  base=5.12°  → signed=-6.2° → nodDown（下滑）✓
04:18:25.316  rawPitch=2.82°   base=9.52°  → signed=+6.7° → tiltUp（上滑）✗
```

同一个方向的位移，因为 `base` 在 2 秒内从 5.12 跳到 9.52（中途丢过一次脸、基线重学）
就判成了相反方向。`baselineDeg` 是 **45 帧中位数、本身在追用户的姿势**，
而轻点头的位移只有 5~8° —— 与滞后的基线一比，**起点落在基线哪一侧**就决定了方向，
所以**小幅度动作必然判反**。这也解释了 ⑤ 走路阶段那 8 次"上滑"（很可能就是用户的点头）。

**修复**：轻通道（近距离 + 俯视）改用**运动起点的稳定值** `settledPitch` 作参考点：

- `settledPitch` 只在头部稳定时更新（连续 3 帧峰峰值 < 1.0°），所以它天然是"这次动作的起点"，
  不会滞后好几度；
- 位移 = 当前值 − 稳定值，**方向 = 位移符号、幅度 = 位移大小**。

于是"点头 = 向低头方向移动 2.5°"变成一句能兑现的话，与基线漂到哪无关。
只作用于轻通道：远距离、仰头方向、回中锁定、方向仲裁、晃动判据一律照旧用相对基线的量。

### ② ⑥ 的两次误触：抖动 + 单帧跳变骗过了跳变确认

```
04:21:01.987  pitch: … -0.2  -2.1  -0.4  -2.5  **-12.5**
```

前一帧 −2.5° 恰好已在阈值附近（那一帧被晃动判据拦下了），于是"前一帧已经在动"成立、
跳变确认没有要求再撑一帧，接着一帧跳到 −12.5° 就放行了。

**修复**：跳变确认再加一条"**台阶限制**"——相邻两帧的位移超过 **4°** 就不算渐进动作。
实测轻点头每帧只走 1~2°、有意仰头每帧 2~5°，所以不会误伤正常动作；
代价只是"极快的大幅动作"也要多撑一帧（约 90ms）。

### ③ 走路时眨眼不触发：很可能是冷却被走路误触吃掉了（推测，待验证）

查日志：走路阶段眨眼**本身是正常识别的**（`blink #15~#23`、闭眼 184~506ms），
但 `run=1/3` 始终没凑够 —— 因为**全局冷却是 2 秒**，而走路阶段那些误触（上滑）
不停地重置冷却，眨眼触发被一起吞掉了。① 把走路误触压下去之后应该会自然缓解，
所以本版**不动眨眼逻辑**（间隔窗口仍是 200~1500ms、需要 3 次），留待实测确认。

---

## [5.16] - 2026-09-16

用户复测 v5.15：

> 「②90 秒俯视不动，已经很完美了，没有误触发，**别动了**」
> 「④轻轻点头还是有问题……我轻轻点头然后触发了，然后我等了 2 秒再点头，不触发，
> 得要更大的角度才能触发，而且触发有延迟，**要等 0.5 秒**」
> 「⑥小幅度轻轻晃动，误触了两次好像，还是一次，问题不大」
> 「⑧我走路的时候疯狂点头，但是**只触发了一次**，然后就再也不触发了，还不如第一版」

②的成功是这一版**刻意不动**的部分，改动全部集中在"放行真实动作"这一侧。

### ① `eyes-unreliable` 把真实动作挡了 126 次 → 窗口 120ms 收到 **50ms**

v5.14 加的"闭眼后姿态不可信期"是这一轮最大的问题。日志统计：

```
eyes-unreliable 拒绝 126 次（tiltUp 98 + nodDown 28），且全是真实动作：
  tiltUp  candidate rejected: pitch=25.2° reason=eyes-unreliable
  tiltUp  candidate rejected: pitch=17.5° reason=eyes-unreliable
  nodDown candidate rejected: pitch=-3.2° reason=eyes-unreliable   ← 用户的轻点头
```

原因是 30cm 处"某只眼睛跌破阈值"极其频繁（日志里几乎每秒都有），120ms 的窗口把头部判定
压制了近三成时间 —— 用户说的「**要等 0.5 秒**」就是这么来的（在等这个窗口过期）。

**修复**：窗口 120ms → **50ms**（略小于一帧 63~116ms），效果是**只跳过闭眼那一帧本身**。
它后面那些帧交给 v5.15 的**跳变确认**处理 —— 实测那次眨眼误触正是一帧从 +0.1 跳到 −3.5，
跳变确认足以拦住，所以不需要 120ms 这么长的窗口。

### ② 晃动判据从「路径效率」换成「方向反转次数」→ 走路时的点头不再被误杀

v5.15 里 19 次真实点头被 `reason=shake` 拒掉（幅度 −6.4~−9.3°、速度 0.05~0.08°/ms），
全部集中在走路阶段 —— 用户「走路时疯狂点头只触发了一次」。

根因是**判据选错**：走路时头部俯仰是**真实的周期性摆动（10~25°）**，
于是任何窗口里都含摆动 → 路径效率必然低 → 一律判成晃动。这不是调参能解决的。

**新判据**数"方向反转"，因为两者的**频率**差得很远：

```
走路（约 2Hz，帧间隔 ~90ms）：约 5~6 帧才反转一次 → 6 帧窗口内 ≤1 次  → 放行
有意点头：单调推进，几乎没有反转                  → 0~1 次           → 放行
手抖 / 高频摇晃（3~5Hz）：1~2 帧就反转一次        → 3~6 次           → 拦下
```

低频摇摆手机那种（v5.14 ⑥）由陀螺仪闸门负责 —— 那是"手机在转"，比数头部反转更直接。
路径效率仍然会算出来打进日志（`shake=`），只是不再用于判决。

### ③ 轻点头速度门槛 0.013 → **0.009**

日志里用户"很轻地试"的那几次速度只有 **0.006~0.011°/ms**，落在 0.013 之下，
于是掉进"保持"通道被拒（`reason=hold-not-met` / `speed-gate`）。
单帧跳变现在由跳变确认负责拦，所以速度门槛可以放得更松，让"轻轻一点"也能过。

### ④ 刻意没动的部分

② 之所以完美，靠的是**静止锁定 + 跳变确认 + 手机自身运动闸门**这三道，
本版一律未改；距离档（0.46/0.40 回滞）、俯视判据（基准俯仰角）、
阈值表（近距俯视低头 2.5°）也都保持原样。

---

## [5.15] - 2026-09-16

用户复测 v5.14：

> 「②**误触了 4 次以上，说实话不如上一版**」「④轻轻点头还是不触发，不是不触发，是**概率触发**，
> 我触发了一次，要等 0.5 秒后才能触发点头」「⑥轻轻晃，我能接受的晃动下，还是误触了两次」
> 「⑧我模拟日常走路，触发是会触发点头，能用，但是会**误触**，可能是角度的问题，你看日志」

### ① 主修：区分「渐进动作」与「单帧跳变」（这次是数据指路，不再猜判据）

日志里两种形状一眼可分：

```
真实轻点头：… -0.2  0.0  0.3  -2.0  -2.8  -2.4  -2.7  -3.4   ← 越阈值那帧的前一帧已经在动
误触      ：… -0.5  0.4  -3.1                              ← 前一帧还在 +0.4，一帧跳过去
```

**真实动作是渐进的**：越过阈值那一帧的**前一帧已经在向同一方向移动**；
而噪声、眨眼造成的跳变是"前一帧还在 0 附近、一帧跳过去"。这与 v5.14 修的那个
眨眼误触（浅闭后 107ms 的一帧 −3.5°）是同一类。

**修复**：越阈值时检查**前一帧**（250ms 内）是否已达 onset 门限（阈值×0.4）：

- **渐进动作 → 零延迟**，照旧立刻触发，手感完全不变；
- **跳变型 → 要求再撑过一帧**（本帧 ≥ 阈值、下一帧仍 ≥ 阈值才放行）。
  单帧跳变下一帧必然掉回阈值以下，被 `below-threshold` 清掉，**永远不会触发**。

⚠️ **这不是 v5.9 那个被撤销的全局确认窗口**。v5.9 要求"最近 3 帧至少 2 帧越过
**完整阈值**"，于是轻点头被二次抬高门槛，用户反馈"要更大角度、要等 0.5 秒"。
这次只针对**跳变型**（渐进型完全不受影响），代价只有跳变动作多一帧（约 90ms）。

### ② 「概率触发、要等 0.5 秒」：晃动过滤器在误伤连续点头

日志里 25 次点头候选被 `reason=shake` 拦掉，其中不少是真实动作：

```
nodDown rejected: pitch=-4.6° speed=0.0443°/ms reason=shake (shake=0.05)
nodDown rejected: pitch=-9.3° speed=0.0651°/ms reason=shake (shake=0.31)
```

这些集中在走路阶段（04:00:35~04:00:43）—— 那里晃动过滤器其实**拦对了**
（走路时头部俯仰本来就是周期性摆动，见下）。但连续点头时它也会误伤，
所以本版把它的判定与新增的跳变确认分开：跳变确认负责"一帧的假信号"，
路径效率负责"来回摆的真晃动"，两者不再互相盖住。

### ③ 诊断修正：v5.14 的 accel/gyro 整场都是 0.00，白瞎了一整轮数据

原因是 `recentPeak()` 的窗口只有 **1000ms**，而 `GazeDiag` 每 **3 秒**才打一行 ——
于是诊断行几乎永远读到 0，**走路、晃手机时全都看不见**。v5.15 把窗口放宽到 5000ms
（必须 ≥ 诊断间隔），并把 `PhoneMotion` 也加进抓取标签（上一版它的日志被过滤掉了，
所以"闸门到底有没有生效"都看不出来）。

### ④ 已知仍未解决：走路时的误触（诚实记录）

走路时**头部俯仰本身就会摆动 10~20°**（实测 tiltUp 触发在 8.5~20.5°），
这已经远高于仰头阈值 6.0°，而且它是**真实的头部运动**，不是噪声 ——
摄像头看到的信号与"有意仰头"几乎一致。目前只能靠**路径效率**（周期性摆动）部分挡住，
所以走路时仍会有误触。要真正解决得引入头部之外的信息（例如步态周期检测），
留给后续版本，本版不擅自扩大改动范围。

---

## [5.14] - 2026-09-16

用户复测 v5.13：

> 「②30cm 俯视倒是没啥问题，不过触发了一次，**触发的时候我不受控制的自己眨眼了几次**」
> 「④**轻轻点头还是不触发**，必须还是要稍微动作大一点……我希望近距离俯视的情况下，
> 点头幅度**再小一点点**就完美了」
> 「⑥急停好像没啥问题」「⑦轻轻晃动的话，**误触会多得多**……我要你的做的就是，
> **小幅度的晃动不影响就行**，要是大幅度的晃动误触那也没招」

### ① 用户报的那次误触，日志给出了完整因果链 —— 是眨眼导致的姿态跳变

```
03:44:00.020  I/Blink: closure rejected: 63ms frames=1/2 minEye=0.98/0.14     ← 单帧浅闭
03:44:00.127  I/HeadPose: nodDown triggered pitch=-3.5° speed=0.037°/ms
              ctx: … -99ms|0.1 … 0ms|-3.5                                      ← 一帧从 +0.1 跳到 -3.5
```

用户"不受控制地眨眼"的判断是对的，而且机制很具体：**ML Kit 的头部姿态是靠关键点回归的**，
眼睛闭上时眼部关键点消失，那一帧的姿态估计会跳。注意元凶是 `frames=1/2` 的**一帧浅闭**
——它永远不会被确认成"眨眼"，所以只盯 `eyesClosed` 是抓不到的。

**修复**：新增 `eyeDip`（本帧有眼睛跌破闭眼阈值，**含单帧浅闭**）+ 之后 **120ms** 的
"姿态不可信期"，期间俯仰/偏航候选一律不接受（理由 `eyes-unreliable`）。120ms ≈ 一帧多，
足够跨过跳变那一帧，又短到不影响真实动作。实测相邻帧间隔 63~116ms，
而被挡的那次发生在浅闭后约 107ms，正好落在窗口里。

### ② 阈值表统一：修掉一个一直漏着的方向泄漏 + 点头再降一档

#### 修 bug：v5.4 的俯视增益是**双向**的，偷偷把仰头阈值从 6.0° 压到 4.5°

实机日志（v5.13，用户设定灵敏度 6.0°）：

```
tiltUp triggered: pitch=8.4° speed=0.0369°/ms threshold=4.5° dist=near
诊断分组：th=4.5 thUp=2.3 nodBoost=1.00 downGazeActive=true   ← 337 帧里有 133 帧处于此状态
```

`nodDownGazeBoost = 0.75` 是 v5.4 引入的"俯视补偿"，它乘在**共用**的俯仰阈值上，
所以仰头方向也吃到了 0.75 → 阈值 4.5°。**这与 v5.7 修掉的是同一个错误**
（当年只改了"近距离增益"那一条，这条基于**相对基准线偏移**的一直漏着）。
"被动仰视 4.5~6° 就触发"正是它。

同时它还与 v5.12 的近距离俯视档**叠乘**，使低头阈值在那条状态开关时于
**2.25° 与 3.0° 之间来回跳**（133/337 帧处于该状态）—— 手感时灵时不灵。

**修复**：俯视增益只作用于**低头**方向，且**不与近距离档叠乘**（近距离档用的是可靠判据
"基准俯仰角"，已经把俯视算进去了）。修完之后的完整阈值表（灵敏度设 6.0°）：

| 条件 | 低头 | 仰头 |
|---|---|---|
| 远距离 | 4.5°（俯视补偿生效）/ 6.0° | **6.0°**（修复前是 4.5°/6.0°） |
| 近距离，平视 | 4.08° | 6.0° |
| **近距离 + 俯视** | **2.5°**（稳定值，修复前在 2.25°/3.0° 之间跳） | 6.0° |

#### 点头再降一档：3.0° → **2.5°**

用户要"再小一点点"。v5.12 那一版是 3.0°，而日志里他的轻点头落在 3.0~4.4°，
3.0° 还是卡在门上。现在 **2.5°**，并且是**稳定**的（不再随 v5.4 那条状态抖动）。

### ③ 轻晃误触：陀螺仪才是对的信号（加速度计看不见"摇"）

v5.13 只用直线加速度，实机复测证明**抓不到用户的轻晃**：整场会话线性加速度峰值
最高只到 **2.3 m/s²**、`lurch detected` **一行都没有**，而用户报告"轻晃误触多得多"。

原因是**手晃手机主要是"转"而不是"平移"**：绕手腕转动时传感器的直线加速度可以很小，
角速度却很大。而这恰恰能把两件事分开：

| | 头 | 手机 |
|---|---|---|
| **点头** | 绕脖子转 | 基本不动 → 角速度 ≈ 0 |
| **手晃 / 地铁摇摆 / 急刹** | 被动跟着 | **自己在转** → 角速度 0.6~10 rad/s |

**修复**：加 `TYPE_GYROSCOPE`，角速度 ≥ **0.6 rad/s**（≈35°/秒）即判定"手机在动"，
500ms 内不接受俯仰/偏航候选；加速度门限同步从 3.5 降到 **2.5 m/s²**。
两个峰值都打进诊断行（`accel=` / `gyro=`）与拒绝日志，下一版要调门限有实测数字可依。

顺带说明为什么"路径效率"抓不到这种轻晃：用户的"晃"是**低频摇摆**（日志里俯仰平滑地
±5° 来回走，每半周期都像一次单向动作），效率判据自然不成立 —— 这也解释了
v5.12 那次晃动误触为什么没被拦住。

---

## [5.13] - 2026-09-16

用户复测 v5.12：

> 「2，没有误触；4，**点头都成功了**，但是出现了有一个阶段，我疯狂点头没有触发，然后又等了 2 秒左右
> 才正常触发；6，眨眼三次我试了好像 4 次，成功了三次，失败了一次，能接受；7，晃动测试……
> 还行，只误触发了一次。我的想法是**允许轻轻的摇晃不触发**，但如果遇到那种**急停**
> —— 比如我走在街上看着手机突然停下来，或者地铁上、公交车上**急刹车**时，不要误触。」

本版修两件事。

### ① 「疯狂点头却没反应」：轻点头在被速度门槛挡死

日志坐实了原因 —— 他的轻点头速度只有 **0.014~0.018°/ms**，而"快通道"的门槛是 **0.02**：

```
03:33:39.430 nodDown rejected: pitch=-3.2° speed=0.0179°/ms reason=hold-not-met
03:34:02.575 nodDown rejected: pitch=-2.6° speed=0.0183°/ms reason=below-threshold
03:34:07.626 nodDown rejected: pitch=-3.5° speed=0.0161°/ms reason=hold-not-met
03:34:07.983 nodDown triggered: pitch=-4.7° speed=0.0290°/ms   ← 等它自己变快才成
```

够不到 0.02 就落到"保持"通道，而那条通道要求**保持 150ms 且速度 ≥ 速度门限** ——
近距离速度门限是 `0.012×2 = 0.024`，**比 0.02 还高**，所以那条路**实际上永远走不通**。
净效果是"不够快的点头一律无效"，而轻点头天然就慢。

**修复**：新增**轻点头通道**（[LIGHT_NOD_FAST_VELOCITY] = 0.013），条件与 ×0.50 增益
**完全一致**（近距离 + 俯视 + 低头），所以"阈值压到 3.0°"与"速度门槛降到 0.013"
永远同时生效，不会只松一半。远距离与仰头方向一概不受影响。

通道还要求**多撑过一帧**（60ms，实测帧间隔 63~116ms ⇒ 等价于"下一帧仍须在阈值之上"），
所以单帧跳变依旧进不来 —— 这正是 v5.9 那次全局确认窗口想做的事，
区别是现在只作用在这一条通道上，代价只有一帧，不拖慢其他动作。

### ② 「急停 / 急刹车不要误触」：用加速度计把"头在转"和"人和手机一起顿"分开

**先说明为什么摄像头分不出来**：急刹车时身体前倾，头部俯仰角会变化 5~15°，而且那一下
**是单向的**（不是来回抖），所以 v5.12 的路径效率判据挡不住 —— 实测那次晃动误触就是
`tiltUp 8.1°`，效率判据没触发。脸占比也不够干净（该次末 400ms ΔFR=0.04，
而有意动作也能到 0.04~0.06，重叠）。

但物理上有干净的分界：

| | 头 | 手机 |
|---|---|---|
| **点头** | 绕脖子转 | **几乎不动**（手持只有小抖动） |
| **急停 / 急刹 / 被撞** | 被动前倾 | **有明确的大幅直线加速度** |

所以新增 `PhoneMotionMonitor`：读**加速度计**（优先 `TYPE_LINEAR_ACCELERATION`，
没有就退回 `TYPE_ACCELEROMETER` 并用低通估计重力再减掉），线性加速度 ≥ **3.5 m/s²**
即判定"手机被顿了一下"，之后 **600ms** 内所有俯仰/偏航候选一律不接受（理由 `phone-motion`）。

门限的依据：坐着/躺着刷 **0~0.5 m/s²**；走路手持峰值 **1.5~3**（用户明确说这种轻晃"可以接受"）；
急刹车/突然停下/被撞 **4~15**。所以走路不会误伤，只有真正的顿挫才拦。

而且是**失败开放**：传感器不可用时 `isMoving()` 恒为 false，绝不因为读不到传感器
就把用户的手势一起废掉。峰值打到诊断行（`accel=`），于是"地铁上、走路时到底读到多少"
可以直接看日志 —— 下一版要调门限有实测数字可依。

---

## [5.12] - 2026-09-16

用户复测 v5.11：

> 「阶段 2，没有误触；阶段 4，**还是需要稍微用点角度**，我希望在俯视的状态下能**轻轻**地触发点头；
> 阶段 6，**我眼睛眨烂了，只触发了一次**，我甚至都怀疑触发的那一次是不是误触；阶段 7 没啥问题。
> 另外希望**专门针对近距离俯视点头**做优化……识别到我是近距离还是俯视，点头就稍微灵敏点，
> 而且还要**过滤掉晃动**（地铁上近距离俯视产生晃动然后误触）」

### ① 眨眼：v5.11 的"加帧数"必须撤销

**证据**：v5.11 那一整场会话里 `I/Blink:` **一行都没有** —— 也就是说 30cm 时**没有任何一次眨眼**
达到过判定条件。阶段② 的"0 误触"是**眨眼检测被废掉**换来的，不是真的防住了。

根因是帧数要求：实测帧间隔 63~116ms（**约 11fps**），加一帧就等于要求多闭眼约 90ms，
而一次正常眨眼总共才 150~250ms。v5.11 用了 3 帧 ≈ 270ms，于是用户的**有意眨眼**全被挡掉。

**修复**：近距离**只降阈值、不加帧数**（帧数要求恒等于用户设定值）。
这本来就是我给用户承诺的做法，v5.11 擅自改成 3 帧，教训与 v5.9/v5.10 完全一致：
**凡是会连带挡住真实动作的手段，都不能用来防误触。**

**并新增闭眼记录**：每一次抵达阈值的闭眼都有留痕，**包括被拒的**：

```
I/Blink: blink #12 closure=280ms minEye=0.04/0.07 below=0.30 needFrames=2 gap=940ms run=2/3 dist=near
I/Blink: closure rejected: 90ms frames=1/2 minEye=0.38/0.41 below=0.41 dist=near
```

v5.11 那次把用户的眨眼挡掉了却什么都看不到，只能靠用户说"眼睛眨烂了"才发现问题。
下一版要精调眨眼（有意 vs 不由自主），直接量这两行即可。

### ② 近距离俯视点头专用档：阈值 4.1° → **3.0°**

#### 证据：用户的"轻点头"正好卡在阈值上

```
03:20:02.275 nodDown rejected: pitch=-3.7° threshold=4.1° reason=below-threshold
03:20:05.152 nodDown rejected: pitch=-3.9° threshold=4.1° reason=below-threshold
03:21:20.565 nodDown rejected: pitch=-4.1° threshold=4.1° reason=below-threshold
03:21:20.464 nodDown rejected: pitch=-6.0° threshold=4.1° reason=hold-not-met (speed=0.0196<0.02)
```

用户要的"轻点头"幅度就是 **3.7~4.4°**，而 v5.6 那一档的阈值是 4.1° —— 卡在门上，
所以"有时候好使、有时候不好使"。

#### 俯视判据：改用**基准俯仰角**（v5.6 用 chinRatio 失败，这次换了信号）

`baselineDeg`（静止时人脸相对摄像头的绝对俯仰中位数）实测：

```
30cm 俯视   base = 7.8 ~ 14.4
50cm 平视   base = 0 ~ 2
```

分离干净，而且**不需要任何新传感器或新代码** —— 它本来就是基线学习中位数的输出。
v5.6 用几何比例 `|下巴Y−眼中心Y|/脸高` 失败是因为那个量主要在反映**距离**；
基准俯仰角反映的是**摄像头与脸的相对角度**，物理上直接对应"手机在脸下方、眼睛往下看"。

#### 增益分三档（仰头方向一律不动，v5.7 的原则保留）

| 条件 | 系数 | 阈值（灵敏度设 6.0°） |
|---|---|---|
| 近距离 + 俯视 + 低头 | ×0.50 | **3.0°** ← 本版新增 |
| 近距离 + 低头 | ×0.68 | 4.1°（v5.6） |
| 其他（含所有远距离、仰头方向） | ×1.00 | 6.0°（不变） |

远距离与仰头方向**完全不受影响** —— 该档要求「近距 + 俯视」两条同时成立。

### ③ 晃动过滤：路径效率

阈值压到 3.0° 之后必须有东西挡住晃动（用户明确点名"地铁上近距离俯视晃动然后误触"）。
现有防线（静止锁定/速度门限）都不适用：晃动**不是静止**，所以静止锁定反而失效。

新判据是**路径效率** = `|净位移| / Σ|相邻差值|`：

```
轻点头   0 → 1.0 → 2.5 → 3.5 → 4.2 → 4.2    路径 4.2   净位移 4.2   效率 1.00  ✅ 放行
晃动   -1.5 → 1.2 → -1.0 → 2.8 → -1.2 → 3.2  路径 17.1  净位移 4.7   效率 0.27  ⛔ 拦下
```

窗口 6 帧（约 0.5~0.7 秒，正好覆盖一次轻点头加它前面那段静止），效率低于 0.45 即判为晃动。
之所以不用"数方向反转次数"：效率把幅度与次数合成一个量，少一个要调的参数，
对低频大幅晃动（反转不多但来回走得很远）同样有效。

效率值挂到诊断行（`shake=`）与候选拒绝日志上，于是"地铁上到底算不算晃"可以直接读日志判断。

---

## [5.11] - 2026-09-16

用户复测 v5.10 的反馈：

> 「阶段 2 跳了 1 次」（v5.9 是 4 次）「阶段 4 确实丝滑了，但是**还是有需要角度大点才能点头触发下滑**」
> 「**不是有意的眨眼，是有时候人类的眼睛，会这样频繁的眨眼，控制不了**」

阶段② 的 4 次已经压到 1 次、手感也回来了；本版处理剩下的两件事。

### ① 方向仲裁：「偏航占优」第一次真的比较了两个轴的大小

#### 证据

用户明确在点头的那几秒里，**7 次连续的点头全部被拒**，最后不得不用 −17.3° 的大幅点头才成功：

```
03:10:35.990 nodDown candidate rejected: pitch=-7.7°  reason=yaw-dominant-arbitration
03:10:36.065 nodDown candidate rejected: pitch=-12.2° reason=yaw-dominant-arbitration
03:10:36.395 nodDown candidate rejected: pitch=-10.4° reason=yaw-dominant-arbitration
03:10:36.491 nodDown candidate rejected: pitch=-8.1°  reason=yaw-dominant-arbitration
03:10:38.802 nodDown candidate rejected: pitch=-4.7°  reason=yaw-dominant-arbitration
03:10:38.915 nodDown candidate rejected: pitch=-13.8° reason=yaw-dominant-arbitration
03:10:38.985 nodDown candidate rejected: pitch=-15.2° reason=yaw-dominant-arbitration
03:10:39.062 nodDown triggered                      pitch=-17.3°   ← 加大力气才成
```

阶段④ 里俯仰候选被这条规则拒掉 **39 次**（点头 19、仰头 18），是**最大的一类拒绝**。

#### 根因

v5.8 的仲裁判据**只有一条绝对速度门限**：

```kotlin
yawVelocity >= MIN_YAW_VELOCITY * ARBITRATION_VELOCITY_FRACTION   // 0.015 × 0.4 = 0.006°/ms
```

`0.006°/ms` 是 6°/秒，而 30cm 处偏航读数的**正常抖动**（±1~3°）在实测约 11fps 下折算出来
就是 **0.01~0.03°/ms**，轻松越线。函数叫「偏航占优」，可它从来没有比较过偏航和俯仰的大小。

#### 修复：三条判据缺一不可

1. **偏航确实在动** —— 保留 v5.8 的原始判据（0.006°/ms）；
2. **偏航强过俯仰**（新增）—— 这才是"占优"的字面含义。实测：真实扭头 0.13~0.25°/ms，
   扭头带出的俯仰分量约 0.02°/ms；点头时俯仰 0.04~0.13°/ms、偏航抖动 0.01~0.02°/ms。
   **速度的比较在两个方向上都有鉴别力**；
3. **头部确实偏到轴外**（新增）—— 至少偏到扭头阈值的 30%（用户设 20° → 6°）。
   真实扭头实测 23~45°，而点头时偏航抖动只有 ±1~3°。

⚠️ 第 3 条是**闸门**，不是 v5.8 文档里警告过的"幅度兜底判据"：那次的错误是拿幅度**替代**
速度判据，结果把合格扭头挡掉了。这里是"速度已成立之后，再要求幅度也成立"，两条同时满足
才让位 —— 真正的扭头照样让位，姿态抖动不再误伤点头。

### ② 眨眼的"近距离收紧"终于真正生效（v5.3 设计、一直空转）

#### 证据

`BlinkDetector` 一直用**自己**的 `faceRatio >= 0.55` 判断近距离，而用户 30cm 实测只有
**0.49~0.51** —— 于是收紧**从未生效**，一直在跑中距档（诊断行 `blinkBelow=0.41 blinkFrames=2`）。
而 v5.10 的实测已经坐实：静止 90 秒里的误触**全部**是 `lastTrigger=blink`。

#### 修复

把这个判断改成读 **v5.9 那个统一的距离档**（带 EMA + 回差，由服务每帧同步），
于是 30cm 时终于进入近距离档。

**闭眼帧数：设计值是 4 帧，本版取 3 帧。** 依据是这次终于量到了真实帧率——
实机 ctx 日志里相邻帧间隔 63~116ms（**约 11fps**），4 帧 ≈ 要求连续闭眼 **360ms 以上**，
已经超出一次正常眨眼的总时长，很可能把用户**有意的**眨眼触发一起挡掉。
本项目刚在 v5.10 因为「连带拖慢真实动作」撤过一次改动，不能重犯。3 帧 ≈ 270ms：
足够挡掉短促的自然眨眼，又给有意眨眼留一档余量。

### ③ 新增逐次眨眼诊断

每次**被计数的眨眼**都留痕，这是判断「有意眨眼 vs 不由自主的眨眼」唯一的可靠依据：

```
I/Blink: blink #12 closure=280ms minEye=0.04/0.07 below=0.30 needFrames=3 gap=940ms run=2/3 dist=near
```

字段：闭眼持续多久、左右眼各自最深读到多少、当前生效阈值与帧数要求、与上一次的间隔、累计到几次。
下一版要再调眨眼，直接看这一行就能定量决定，不用再猜。

---

## [5.10] - 2026-09-16

用户按同一流程（阶段②静止 90 秒 / 阶段④正常刷 / 阶段⑥故意动作）复测 v5.9，反馈：

> 「第二，误触了 4 次，应该不是眨眼，我眨眼设置的是触发 3 次才能翻页」「第四，点头需要更大的角度
> 才能触发，不像之前轻轻点头就能触发」「第六，仰头和低头倒是成功了，只不过需要很大的角度才能触发，
> 而且我仰头和点头要等一会，大概 0.5 秒，不像之前那么丝滑了」

结论：**v5.9 的「触发确认窗口」必须撤销**，同时找到了误触的**真正来源**（眨眼）。

### ① 撤销 v5.9 的触发确认窗口（手感回归 v5.8）

撤销依据来自这次带标记的实测（`v59-verify.log`）：

| 阶段 | 头部越阈值被确认窗口挡掉的次数 | 误触次数 |
|---|---|---|
| ② 静止 90 秒（30cm 俯视） | **0** | 3~4（**全部是眨眼**） |
| ④ 正常刷 | 21 | — |
| ⑥ 故意动作 | 16 | — |

也就是说：确认窗口在**静止阶段一次都没派上用场**（因为误触根本不在头部链路上），
却在正常使用中挡掉了 **37 次**越阈值 —— 用户感到的"要更大的角度""要等 0.5 秒""不丝滑"
正是它造成的。轻快的点头往往只跨一帧阈值，被它整个吃掉。

**这已经是本项目第三次踩同一个坑**（v5.5 的幅度放大、v5.7 的单向增益、现在的确认窗口）：
**凡是会连带拖慢真实动作的手段，都不能用来防误触。** 顺序必须是
「先确定误触来自哪个检测器 → 再只动那一条链路」。

### ② 误触的真正来源：眨眼（这是本次最重要的发现）

阶段② 那 90 秒里，头部路径的越阈值次数是 **0**，而所有翻页的诊断行都是
`lastTrigger=blink`：

```
02:58:47  lastTrigger=blink   ← 静止
02:58:54  lastTrigger=blink
02:59:35  lastTrigger=blink
blinks 计数：35 → 49（90 秒内自然眨眼 14 次）
blinkBelow=0.41 blinkFrames=2 faceRatio=0.49~0.51 distMode=near
```

两个叠加的原因：

1. **用户设置里存的是 2 次，不是 3 次**。用户明确说「我眨眼设置的是触发 3 次才能翻页」，
   而设备上的 `blinkTriggerCount=2`（UI 代码本身没有 bug，`rbBlink3 → 3` 的映射是对的，
   是值被存成了 2）。`needBlinks=2` 意味着**任意两次自然眨眼间隔落在 0.2~1.5 秒内就翻页**，
   而自然眨眼约 9~15 次/分钟，成对出现非常常见 —— 静止 90 秒误触 3~4 次完全对得上。
2. **近距离的眨眼收紧没有生效**：`BlinkDetector` 自己用 `faceRatio >= 0.55` 判断近距离，
   而用户 30cm 实测是 0.49~0.51，所以用的是中距档 `blinkBelow=0.41 / 2 帧`，
   而设计中的近距离档是 `0.30 / 4 帧`。

**本版处理**：把设备上的 `blinkTriggerCount` 改成 **3**（这正是用户以为的设置，零风险、
且不影响任何有意眨眼的手感）。

**未处理（留给下一版，附风险分析）**：把 `BlinkDetector` 也接到 v5.9 那个统一的距离档上，
让 30cm 真正进入 `0.30 / 4 帧`。之所以先不动：4 帧在实测帧率（约 17fps）下等于要求
闭眼 **235ms** 以上，可能把用户**主要的**有意眨眼触发也一起挡掉 —— 这正是①刚刚踩过的坑。
先用「2 次→3 次」这一条零风险的改动看效果，不够再动它，并优先只降阈值（0.41→0.34）
而不是加帧数。

### ③ 保留 v5.9 的距离档统一（这条是对的）

`v59-verify.log` 里距离档切换只剩 **4 次/8 分钟**（v5.9 之前是逐帧乱跳）：

```
02:57:53  distance tier: mid/far -> near (faceRatio raw=0.53 smoothed=0.47) -> boost=×2.00 speedGate=0.0240°/ms staticRange=3.0° nodDownThreshold=4.1°
03:01:15  distance tier: near -> mid/far (raw=0.33 smoothed=0.39) -> boost=×1.35 …
```

所以「点头省力增益」在 30cm 时**稳定生效**（`nodDownThreshold=4.1°`），不再时有时无。

---

## [5.9] - 2026-09-16

用户实测反馈：**30cm 俯视状态下还是会误触，太灵敏了**，并且明确了方向 ——
**误触是「上滑 / 跳到下一个视频」（tiltUp）**。

本版做两件事，并**新增逐帧上下文日志**（这是本版最重要的产出，见 ③）。

### ① 距离档统一 + 滞回：修掉「灵敏度按近距离给、防护按中距离算」的空档

#### 实机数据（4 分钟带标记采集，v5.9 之前）

用户 30cm 俯视时的 `faceRatio` 稳定落在 **0.45~0.55**，而代码里三处「近距离」判据
用了**两个不同的门槛**：

| 判据 | 门槛 | 用户 30cm 时实际状态 |
|---|---|---|
| 点头省力增益 0.68（阈值 6.0°→4.1°） | `faceRatio >= 0.50` | ✅ 生效（但逐帧抖动） |
| 速度门限 ×2 / 静止峰峰值 ×2 | `faceRatio >= 0.55` | ❌ 只吃到 ×1.35 |
| 30cm 静止硬锁定 | `faceRatio >= 0.55` | ❌ **从未生效** |
| 眨眼收紧（0.407→0.30、2→4 帧） | `faceRatio >= 0.55` | ❌ 没生效（日志 `blinkBelow=0.41 blinkFrames=2`） |

于是 0.50~0.55 这一段成了「**最灵敏、防护最弱**」的空档，而且这条线正好落在用户的
抖动带里，导致 `nodBoost` 在 0.68/1.00、`distMode` 在 near/far 之间**逐帧乱跳**：

```
02:42:25  FR=0.50  distMode=far   pitchTh=6.0°   ← 抖一下变 far，增益没了
02:42:28  FR=0.49  distMode=near  pitchThUp=4.1° ← 再抖一下变 near
02:44:33  FR=0.51  distMode=near  nodBoost=0.68  gate=0.0243
02:44:36  FR=0.51  distMode=near  nodBoost=1.00  gate=0.0243
```

> 顺带查明：**「30cm 静止硬锁定」在全部历史日志的 700+ 条诊断行里只生效过 2 次**
> （且那两次 `faceRatio=0.66/0.67`，是更近的距离）。也就是说它在用户真实的 30cm
> 使用中**从来没有启动过** —— 门槛 0.55 高于用户实际的脸占比。本版**未改动它的判据**
> （按用户要求"不要动 30cm 静止防误触"），只把发现记录在此，留给下一版用数据决定。

**修复**：只保留**一个**真值 `nearDistance`，由 `NEAR_ENTER_RATIO=0.46` /
`NEAR_EXIT_RATIO=0.40` 回差 + `faceRatio` 的 EMA 平滑共同决定，点头增益、速度门限、
静止峰峰值全部读它。0.46 略低于用户实测区间下沿，保证 30cm 稳定判为近距离；
50cm（0.37~0.41）与 40cm（约 0.42~0.45）**回不到近距离档**，所以远距离手感不变。

### ② 触发确认窗口：单帧跳变不再能翻页

#### 为什么不能靠调阈值解决

实测数据摆在一起看，**幅度和速度这两个瞬时量都已经没有鉴别力**：

```
误触（俯视 30cm，用户未主动动作）：幅度 6.0~7.8°，速度 0.021~0.048°/ms
有意仰头                        ：幅度 7.7~9.7°，速度 0.019~0.048°/ms
```

两者**完全重叠**。按数据反推：若把近距离速度门限提到 0.036°/ms（静止锁定时 ×1.5 的值），
**74% 的真实仰头会被一起挡掉** —— 这正是本项目在 v5.5/v5.7 反复踩过的坑
（"用错工具会把真实动作一起挡掉"）。

真正有鉴别力的是**时间形状**：检测噪声与「手机在手里抖一下」只能维持一帧就回到原位，
真实动作至少跨两帧。

**修复**：阈值必须在**最近 3 帧里至少 2 帧**被越过才允许触发（`CONFIRM_NEEDED=2`）。

- 用「2 of 3」而不是「连续 2 帧」：真实动作在阈值附近上下浮动（实测 ±1.5°）时
  仍然能凑够 2 帧，而孤立尖峰永远凑不够；
- 代价只有**一帧延迟**（实测帧间隔 47~66ms），快通道仍然立刻触发、
  不做 `requiredHoldMs` 那种长保持，所以"动作要快"的手感不变。

### ③ 新增逐帧上下文日志（排查手段的根本改进）

之前只有 3 秒一条的 `GazeDiag` 汇总行，**误触发生前那几百毫秒到底发生了什么完全看不到**，
所以此前几轮排查只能靠猜。本版起：

- **每次触发**都打印前约 1.2 秒的 `(dt, pitch, yaw, faceRatio)` 原始读数：
  ```
  tiltUp triggered: pitch=8.8° ... — ctx 24f: -235ms|2.0/0.4/0.32 -167ms|4.8/1.1/0.32 0ms|8.8/3.3/0.33
  ```
  「单帧尖峰」是孤立的一个大值，「平滑上升」是一串递增的值，日志里一眼可辨。
- **被确认窗口丢掉的帧**打印 `DISCARDED ...`（含同样的上下文），这是验证本版修复的
  主要依据。
- 人脸消失 ≥400ms 后回来打一条 `MARK face-back lost=…ms`：用户按
  「手挡摄像头 3 秒」做阶段标记时，标记不再可能认错（上一轮采集就是因为只有 3 秒采样，
  整段时间轴没法对齐，白跑一遍）。
- 距离档切换打一条 `distance tier: …`，含切换后实际生效的 boost / speedGate /
  staticRange / 点头阈值。

### 验证

`v59-verify.log`（后台断线自动重连抓取，`tools\capture-loop.ps1`）。已确认：
DISCARDED 行大量出现且上下文里都是「一帧跳到阈值以上」，而 `triggered` 明显变少。

---

## [5.8] - 2026-09-16

用户实测反馈：**近距离俯视时左右扭头不好使，会变成上下滑**；远距离俯视正常。
用户自己给的猜测是「近距离灵敏度调太高了，扭头信号被误判成上下滑」——**方向完全正确**。

本版只修这一个问题，其余一律未动。

### 根因：三个独立缺陷叠在一起

#### ① 扭头阈值在近距离被放大到 2 倍（主因）

`distanceBoost()` 在 `faceRatio >= 0.45` 时返回 **2.0**，而 `turnThresholdNow()` 乘了它：

```kotlin
private fun turnThresholdNow(distanceBoost: Float) = turnThresholdDeg * distanceBoost
```

于是用户设的「中 **20°**」在 30cm 处实际生效值是 **40°**。实机日志正好抓在门限上：

```
01:57:37.093 turn triggered yaw=40.5° latency=102ms (fast)
GazeDiag: faceRatio=0.51 dist=中 ... yawTh=40.0° speedGate=0.0240°/ms
```

而用户的扭头幅度就是 40~52° —— **刚好卡在门上，小一点就完全不触发**。

这和 v5.5 在俯仰轴上修掉的**是同一个错误**：当年也想用"放大幅度阈值"来挡近距离噪声，
后来证明那是错的方法（噪声有幅度但没有速度，该管它的是速度门限）。
俯仰轴 v5.5 改过之后没有回头改偏航轴，这条一直留到现在。

**修复**：`turnThresholdNow()` 不再乘距离系数，**两个轴统一**为
「幅度阈值 = 用户设定值」。距离缩放只作用于速度门限与静止门限。
远距离行为完全不变（系数本来就是 1.0）。

#### ② `slow-rise` 把正常的扭头判死（症状：第一下打不中）

扭头的 `turnMotionWindowMs` 是共用的 600ms，而**这个窗口对扭头先天太紧**：

- 俯仰起点容易定准 —— 点头一开始俯仰角就在变；
- 扭头起点常常**先有一段几乎不动的准备**（脖子先转、脸还没跟上），这段被算进
  rise，于是正常的一次扭头会超窗，被当成"慢慢蹭"直接判死。

实机日志里这是**最多的一类扭头拒绝**，且症状正好是用户说的「第一下打不中」：

```
turnL candidate rejected: yaw=54.4° speed=0.1496°/ms threshold=20.0° reason=slow-rise
turnL candidate rejected: yaw=39.4° speed=0.1116°/ms reason=slow-rise   // 连续 8 帧
```

54° 的扭头 + 0.1496°/ms 的速度（远高于 0.015 的速度门限）显然是一次真实动作。
**"够不够快"该由速度门限定义，不是由这个窗口定义** —— 又是"用错工具"。
**修复**：`turnMotionWindowMs` 默认 600 → **900ms**。

#### ③ 偏航在转动时，俯仰会抢先触发（这才是"变成上下滑"的直接原因）

真人在俯视姿态下扭头，头部不会纯绕 Y 轴旋转 —— 脖子带着一个俯仰分量。
实测这个分量是 **±3~±10°**，而近距离点头阈值被单向增益压到 **4.1°**（灵敏度 6°），
所以扭头必然先把俯仰判定打穿。而 `onHeadPose` 里**点头是先于扭头评估的**：

```
02:00:25.848 nodDown triggered pitch=-12.0°
02:00:25.850 turnR candidate rejected: yaw=23.5° ... reason=below-threshold
```

远距离俯仰阈值是 6.0°，同样的分量常常够不到，所以**远距离正常** —— 与用户观察完全一致。

**修复：方向仲裁。** 判据**只看角速度**，因为这是两个轴之间唯一干净的分界：

- 真正的扭头是绕 Y 轴转动 → **偏航角速度必然明显**；
- 真正的点头是绕 X 轴转动 → **偏航角速度几乎为零**；
- 而"扭头带出的俯仰分量"在偏航轴上一定带着那个转动速度。

`|偏航速度| >= MIN_YAW_VELOCITY × 0.4`（0.006°/ms）时，点头/仰头一律让位；
反向也对称地做了（偏航候选让位给俯仰）。门限由**实机 149 个扭头样本**标定：
真实扭头的偏航速度中位数 0.0126、p90 0.158、max 0.248，而静止/保持姿态时接近 0。

### 三次实机迭代（每一轮都由日志推翻上一轮的假设）

这一版是三次"改 → 装机 → 抓日志"循环的结果，中间两个错误值得记下来：

1. **第一版仲裁用了幅度兜底判据**（`yaw >= 阈值×0.6`）→ 反而**把合格的扭头挡掉了**：
   `yaw=30.7° speed=0.2481°/ms reason=pitch-dominant-arbitration`。
   原因很直白：扭头时偏航幅度本来就大，拿幅度当判据等于把"我正在扭头"误判成
   "俯仰更强"。**幅度在仲裁里没有鉴别力。**
2. **第二版仲裁读的是上一帧的偏航**。俯仰先于偏航评估，所以俯仰侧看到的偏航速度
   还停在上一个采样点，导致自己的耦合检测失灵。改为 `yawVelocityWith(本帧读数)` 后，
   日志立刻出现成对事件，说明修复方向对：
   ```
   02:05:27.413 nodDown candidate rejected: pitch=-9.9° reason=yaw-dominant-arbitration
   02:05:27.415 turn triggered yaw=28.5° latency=114ms (fast)
   ```

### 实机结果（v5.8 最终版，用户按测试序列操作 40 秒）

| 事件 | 次数 |
| --- | --- |
| **turn triggered（左 7 + 右 6）** | **13** |
| nodDown triggered | 4 |
| tiltUp triggered | 5 |
| 俯仰候选被"偏航主导"正确否决 | 20 |

扭头全部成功触发，且每次扭头期间的俯仰候选都被仲裁挡下。

### 新增诊断

- **扭头候选拒绝日志**（以前扭头**完全没有候选日志**，只有成功触发才打，
  这正是"近距离扭头不好使"查不出来的原因）：
  ```
  turnL candidate rejected: yaw=43.6° pitch=0.6° speed=0.0000°/ms gate=0.0203°/ms
      pitchSpeed=0.0000°/ms threshold=20.0° dist=far posture=flat reason=hold-not-met
  ```
- 诊断行新增 `yawNow` / `pitchNow` / `pitchYielded`。

### 保留

- **30cm 静止防误触**：逻辑未改动。
- **近距离俯视点头增益（v5.6）**：保留（`pitchTh=4.1°` 仍在生效）。
- **v5.7 仰视单向增益**：保留（实机 `tiltUp triggered: pitch=7.6° threshold=6.0°`）。
- **v5.7 息屏唤醒加固**：保留。
- 眨眼、张嘴点击、自适应滑动、滑动柔度、全局冷却、全局翻页开关全部未动。
- 远距离点头/仰头与扭头手感与 v5.7 一致。

### 验证状态

| 项目 | 状态 | 依据 |
| --- | --- | --- |
| 近距离扭头稳定触发 | ✅ 实机 | 13 次 turn triggered（左 7 / 右 6），`yawTh=20.0°` |
| 不再变成上下滑 | ✅ 实机 | 20 次俯仰候选被 `yaw-dominant-arbitration` 正确否决，且与 turn 触发成对出现 |
| 近距离点头增益未坏 | ✅ 实机 | `nodDown triggered (boosted, dist=near threshold=4.1°)` |
| 仰视单向增益未坏 | ✅ 实机 | `tiltUp triggered: pitch=7.6° threshold=6.0°` |
| 远距离扭头未受影响 | ✅ 实机 | `dist=far` 下 `yawTh=27.0°` 正常触发 |
| 30cm 静止防误触不回归 | ⏳ 待用户确认 | 逻辑未改动 |
| 长时间使用手感 | ⏳ 待用户确认 | 需要日常使用反馈 |

---

## [5.7] - 2026-09-16

本版只做两件事：修掉用户实测反馈的两个问题。**没有顺手改任何其他稳定逻辑**
（用户明确要求「一步一步来，别修炸」）。

### 修复一：近距离仰视误触

**现象**：约 30cm 近距离时仰视会误触，同样距离平视不会；远距离正常。

**根因**：v5.6 的近距离点头增益（×0.68）被乘在了**共用的俯仰阈值**上，
于是**仰头方向也一起吃到了这个系数**。用户灵敏度设为 8° 时，近距离仰头只要
**5.4°** 就触发；设为 6°（灵敏度「高」）时只要 **4.1°**。

从上一版的真机日志里把这个座位挖出来了（触发与检测器状态按时间轴关联）：

| 触发 | pitch | 当时 faceRatio | 当时 pitchTh |
| --- | --- | --- | --- |
| tiltUp | **4.2°** | 0.54（近） | **4.1°** |
| tiltUp | 6.0° / 6.1° / 6.3° / 6.6° / 6.7° | 0.47~0.51（近） | **6.0°** |
| tiltUp | 9.1° / 11.6°（用户**主动**做的） | ~0.30（远） | 8.0° |

整段日志里 **45 次 tiltUp vs 14 次 nodDown**，比例本身就说明了问题：
被动仰视（顺着脖子往后靠、抬头看远处）幅度就在 4~7°，而用户主动仰头是 9~11°。

**修复**：把增益改成**严格单向**——`nearNodDownBoost(signedPitch)` 只在
**低头方向**（`signedPitch < 0`）生效，抬头方向回到用户设定值。

这样正好满足用户的要求「被动仰视不触发，主动仰头才触发」：
被动仰视（4~7°）够不到 8°，主动仰头（9~11°）照样过。

> 为什么不能用"抬高仰头阈值"来修：那会把**主动**仰头一起挡掉。单向增益
> 才能在保住「近距离点头更省力」的同时不动仰头的手感。

**未改动**：速度门限（`speedGate`）本身对两个方向一视同仁，静止锁定、遮挡抑制、
回中锁定、30cm 静止硬锁定全部未动。**30cm 静止防误触逻辑零改动。**

**新增诊断**：`pitchTh`（当前方向）与 **`pitchThUp`（仰头方向）** 同时打出来，
近距离下看到 `pitchTh=5.4° pitchThUp=8.0°` 就说明单向增益生效。
仰头触发也改成完整判据行（用户要求的格式）：

```
tiltUp triggered: pitch=6.2° speed=0.0260°/ms threshold=8.0° dist=far boost=1.00 latency=108ms (fast)
tiltUp candidate rejected: pitch=6.0° speed=0.0090°/ms gate=0.0162°/ms threshold=8.0° dist=near boost=1.00 reason=speed-gate
```

顺带修掉一个**会误导排查**的日志缺陷：两个方向的候选拒绝行原本都打
`nodDown candidate rejected`，排查仰头问题时会把仰头候选误读成点头。现在按方向打
`nodDown` / `tiltUp`，并附带 `dist` 与 `boost`。

### 修复二：隔一会再打开抖音又不触发

用户反馈 v5.5/v5.6 修过的老问题在 v5.6 之后又复现。这次补的是**排查手段**与
**两个真实的死角**。

#### 死角 A：整条恢复链都挂在同一个可能出错的布尔值上

```kotlin
private fun shouldAnalyze(): Boolean =
    screenActive && (AppStateManager.targetActive || AppStateManager.forceActive)
```

`targetActive` 一旦错成 `false`，相机、看门狗、liveness 探针**全部被这一个条件挡住**，
没有任何一条路径会去纠正它。而且 `checkBlind()` 治不了这个方向——它治的是
「前台判不出来」（fail-open），不是「判错了」（fail-closed 到错误的一边）。

**修复**：新增独立的复核 [AppStateManager.fixContradiction]，**不信任自己记的前台**，
直接向无障碍服务再问一次"现在活动的窗口是谁"。若它明确是目标应用、而我们却认为不是，
且持续 2 秒，就强制按"允许翻页"重算一次。判据要求"明确"：读不到窗口时不做任何事，
避免把正常的省电待机误判成故障。

日志：`contradiction: active window is com.ss.android.ugc.aweme (allowed) but
targetActive=false for 2100ms — forcing an immediate re-evaluation`。

#### 死角 B：相机"绑着却没帧"可以无限循环，永不升级

`checkFrames()` 每秒重绑一次，但它**每次都会把 `lastFrameAtMs` 和计时清零**，于是
「重绑 → 宽限期 → 重判 → 再重绑」可以一直循环。如果坏的是 CameraX 那一层
（而不是绑定关系），重绑一万次也没用——只有丢掉 provider 重新 `getInstance` 才能治好。

**修复**：新增**独立于重绑**的计时 `staleBeganAtMs`（只在**真的收到帧**时清零，
见 `onFrame`），持续 12 秒无画面就丢弃 provider 重新获取（与人工点通知里的「重启」等价），
日志 `hard resync #N`。

#### 死角 C：无障碍强制重绑只做一次

`ensureAccessibilityBound` 在实例缺失时只做一次 `repairIfNeeded()`，之后就一直
"重试中"，从日志上看像是已经处理过了。现在改为按**持续缺失时长**升级：
超过 10 秒仍拿不到实例就 `forceRebind`（关掉再打开，等价于用户手动下拉状态栏的效果），
并累计 `a11yForceRebinds` 供诊断。

#### 排查手段：把"最关键的那个分叉"变成可读的日志

这套失效最难自查的地方在于**分不清是"系统没把窗口事件发给我们"还是"事件到了我们判错了"**。
只看 `foreground=` 是分不出来的——那是我们对事件的**理解**，不是事件本身。

所以新增两个计数（在 `onAccessibilityEvent` 里**无条件**记账，包括被过滤掉的 systemui 事件）：

- `a11yEvents` —— 无障碍窗口事件累计次数；
- `a11yEventAgoMs` —— 距上一次窗口事件多久。

再加上 `hardResyncs` / `noFrameForMs` / `a11yForceRebinds`，`GazeSelfCheck` 现在能
在一行里直接指出卡在哪一环。用户要求的标记也补齐了：

```
GazeDiag: foreground change: com.miui.home -> com.ss.android.ugc.aweme (target=true reason=change)
GazeCameraService: pipeline resync (app-switch): forcing rebind (cameraBound=true wasStale=true)
GazeDiag: rebind reason=user-present -> ACTION_USER_PRESENT full restart
```

### 保留

- **30cm 静止防误触**：逻辑未改动，只新增了诊断字段。近距离静止硬锁定仍在
  `faceRatio >= 0.55`、1.2 秒稳定判定、二次确认。
- **近距离俯视点头增益（v5.6）**：保留，且把 `pitchTh=5.4°`（远距离 8.0°）的真机数据
  写进了验证清单。
- 眨眼、点头、仰头、左右扭头、张嘴点击、自适应滑动、滑动柔度、全局冷却、全局翻页开关
  全部未动。远距离点头/仰头手感与 v5.6 一致。

> ⚠️ 本版的两处修复**尚未完成完整实机回归**（开发期间手机无线调试断连）。
> 已完成的验证见下方"验证状态"一节；标为「待实测」的项需要用户跑一遍。

### 验证状态

| 项目 | 状态 | 依据 |
| --- | --- | --- |
| 近距离仰头阈值不再被压低 | ✅ 已由代码与字节码确认 | `nearDownNodBoost` 已不存在；`nearNodDownBoost(signedPitch)` 只对 `< 0` 生效 |
| 编译 | ✅ 全量重编通过 | `compileDebugKotlin --rerun-tasks`，字节码含 `pitchThresholdUpDeg` 等新符号 |
| 近距离仰视不误触 | ⏳ 待实测 | 需要手机在线抓日志确认 `pitchThUp=8.0°` 且 4~7° 的 tiltUp 被 `below-threshold` 拒绝 |
| 主动仰头仍然触发 | ⏳ 待实测 | 9~11° 应照常触发 |
| 近距离点头仍然省力 | ⏳ 待实测 | `pitchTh=5.4°`、触发日志含 `boosted` |
| 30cm 静止防误触不回归 | ⏳ 待实测 | 逻辑未改动 |
| 重开抖音自动恢复 | ⏳ 待实测 | 需要按用户要求做「打开抖音 → 息屏 3~5 分钟 → 亮屏解锁 → 打开抖音 → 尝试翻页」的反复挂测 |

---

## [5.6] - 2026-09-16

### 新增：距离/姿态判定，近距离点头更省力

用户反馈「近距离俯视刷抖音时脖子很累」，并给出很直观的设想：**脸大 = 近距离，
下巴占比大 = 俯视**。按这个思路实现了绝对几何判据：

- **距离**：脸高占画面比例（`faceRatio`）
- **姿态**：`|下巴Y − 眼中心Y| / 脸高`（`chinRatio`）——低头时相机从上方拍脸，
  下巴在透视上被拉长，占比变大

用绝对几何而不是"相对基准线的俯仰偏移"，因为后者会被基准线的自适应吃掉：
用户一直俯视时基准线早就移过去了，相对偏移趋近 0 —— **实测 v5.5 的 `downGaze` 全程为 0，
正是这个原因**。

### ⚠️ 但姿态判据经实机标定后**没有启用**，而且原因值得记录

让用户按「平视 / 俯视 / 近距离俯视 / 回平视」各保持 20 秒，按时间轴对齐后得到：

| 时段 | faceRatio | chinRatio |
| --- | --- | --- |
| 远距 | 0.37 ~ 0.41 | **0.335 ~ 0.368** |
| 近距（第一段） | 0.51 ~ 0.66 | **0.368 ~ 0.421** |
| 近距（第二段） | 0.61 ~ 0.65 | **0.405 ~ 0.432** |

两个结论：

1. **几何推算的门限（0.68）在实测中从未达到**——那样写等于功能不存在；
2. 更重要的是：**`chinRatio` 主要在反映距离，而不是姿态**（近距比远距高约 0.06，
   而"俯视"本身只带来很小的额外变化）。区分度不足以驱动灵敏度开关。

**所以改成只用距离驱动**（`faceRatio` 是干净、可靠、已验证的信号）。
这同时满足用户的两条硬要求——「近距离更轻松」与「远距离俯视不要改错」——
因为远距离本就不在增益范围内。

- 近距离（`faceRatio ≥ 0.50`，实测约 30~40cm）点头阈值压到 **0.68 倍**；
- 远距离完全不变；
- 只压**幅度**阈值：速度门限、静止锁定、近距离静止硬锁定一律不动，
  所以"噪声有幅度没有速度"这道关卡照旧，**30cm 静止防误触未被改动**。

诊断行新增：`chinRatio`（原始俯视几何）、`chinMed`（滑动中位数，供后续标定）、
`dist=near/far`、`posture=down/flat`、`nodBoost`、`nodBoostActive`、`speedGate`。
触发时打印：`nodDown triggered (boosted, dist=near posture=flat threshold=4.1°)`。

> 俯视几何的代码与诊断**保留**，等拿到更干净的分组数据（同一距离下只改变姿态）再考虑启用。
> 现在**绝不**把区分度不足的信号接到灵敏度开关上。

### 修复：屏幕常亮时偶尔仍失效（v5.5 遗留）

两处"确认无事"式的静默跳过：

1. **`ensurePipelineForActive()` 在 `cameraBound == true` 时什么都不做**
   （只打印 `camera already bound`）。于是"相机的确有句柄、但已经很久没有帧"这种
   卡死状态会被反复确认成正常，永远不自愈。现在改为检查**实际是否还有帧**
   （`wasStale`），只要没画面就强制重绑，不再信任这个标志位。
2. **liveness 探针第一次发现卡死时也走同一条路径**，因此同样被"确认无事"吃掉一轮，
   用户感受到的就是"偶尔失效好几秒"。现在第一次就直接强恢复。

另外 `ensurePipelineForActive` 现在会在开始时记录"之前是否已经无帧"，顺序上必须在
重置计时之前判断——反了就永远看不出卡死。

### 保留

- **30cm 静止防误触逻辑未改动**（用户已验证有效）。
- v5.5 的成果全部保留：幅度阈值不膨胀、注入失败自动重连、全链路自检日志。

### 实测证据（发布前的真机运行，不是构造场景）

近距离增益确实生效——32 个诊断样本里 `pitchTh` 从远距离的 **8.0° 掉到 5.4°**
（= 8 × 0.68），手头灵敏度设为 6° 时则是 **6.0 → 4.1°**：

```
GazeDiag: faceRatio=0.60 dist=中 ... pitchTh=5.4° yawTh=27.0° ...
          chinRatio=0.405 chinMed=0.404 nodBoost=1.00 nodBoostActive=true
          speedGate=0.0162°/ms
HeadPose: tiltUp triggered pitch=9.1° latency=95ms (fast, v=0.054°/ms)
```

注意同一行里 `speedGate=0.0162°/ms` 是**距离缩放后**的速度门限——增益只压幅度、
速度门限照旧按距离走，这正是「30cm 静止防误触不回归」的机制保证。

屏幕常亮失效修复也在真机上抓到了现场（v5.5 会在这里静默跳过）：

```
GazeCameraService: pipeline resync (enter): forcing rebind (cameraBound=true wasStale=true)
GazeCameraService: camera released: enter: forced rebind
```

### 修正：两个自己造的诊断字段缺陷（同版本内修掉）

1. **`nodBoost` 打的是常量而不是实际生效值**。诊断行原本打 `nodDownGazeBoost`
   （俯视增益，默认 0.75 的对象），距离增益生效时它仍显示 `1.00`，
   而 `pitchTh` 其实已经乘过 0.68 —— **字段名与实际不符，排查时会误判成功能没生效**。
   现在 `pitchThresholdNow()` 把实际乘上去的系数记进 `lastAppliedNodBoost` 再打出来，
   保证 `nodBoost` 与 `pitchTh` 永远自洽。
2. **诊断行出现了两个 `dist=` 键**：原有的 `dist=近/中/远` 与新加的 `dist=near/far`
   语义不同（前者是握持距离档，后者是增益触发条件），同名字段会看错。
   新字段改名 **`distMode=near/far`**。

> 教训：诊断字段本身也是要 review 的代码。名字含糊或与实现脱节，比没有日志更危险——
> 它会主动把人引向错误的结论。

---

## [5.5] - 2026-09-15

### 修复：仰头/点头阈值被两层系数放大，导致「点头费劲」

这是用户两条抱怨（**近距离点头费劲**、**俯视时点头难受**）的**共同根因**。v5.4 的公式是：

```
阈值 = 用户设定值 × 静止锁定系数 × 距离系数
```

两个系数叠在一起，实测日志直接抓到（`GazeSelfCheck` + 诊断行）：

```
faceRatio=0.32 dist=远 staticLock=false  →  pitchTh=8.0°   yawTh=20.0°
faceRatio=0.32 dist=远 staticLock=true   →  pitchTh=12.0°  yawTh=30.0°   ← 静止锁定 ×1.5
```

也就是说：
- **30cm 时**距离系数 ×2，点头阈值 6° → 12°，要走两倍幅度；
- **静止时**静止锁定又 ×1.5，阈值再涨一半；
- 用户设定的「扭头 中 20°」实际生效值是 **30°**。

**关键认识**：抑制静止噪声**根本不需要放大幅度阈值**。噪声**有幅度但没有速度**，
[最低速度门限]已经在拦它。幅度阈值放大只会连带把真实动作一起挡掉——它不区分
"这是噪声还是动作"。

**修复**：把幅度阈值上的两层系数全部摘掉，改为

```
俯仰阈值 = 用户设定值 × 俯视增益        （8° 就是 8°，6° 就是 6°）
偏航阈值 = 用户设定值 × 距离系数        （"中 20°" 就是 20°）
```

而 [staticLockFactor] **改用到速度门限上**——这才是它该管的地方：静止时要求动作更快，
而不是要求幅度更大。真实动作的速度是噪声的十几倍，所以这条对正常点头/仰头几乎零成本。

距离缩放仍然作用于**所有防误触参数**（静止峰峰值门限、速度门限、偏航阈值），
所以用户已验证有效的「30cm 静止防误触」**一道防线没少**，只是不再靠"让动作更难做"来防误触。

诊断行新增 `pitchTh` / `yawTh` / `downGazeActive` / `nodBoost`，阈值是否被意外放大一眼可见。

### 优化：俯视状态下点头更轻松

俯视时头部本来就低着，再往下点的**可用行程**比平视时短，所以同样幅度的动作只能产生
更小的角度变化。现在检测到俯视姿态后，把点头阈值压到 **0.75 倍**（略大于六折的力度即可触发）。

- 只压**幅度**阈值：速度门限与静止锁定一律不动 → "动作要快"这条门槛没放松，
  噪声依然过不来（噪声有幅度没有速度）。
- 离开俯视 6 秒后自动恢复 1.0（避免刷视频时头一抬一低就反复切换灵敏度）。
- 日志：`posture: looking down detected (...) -> downGaze active, nod sensitivity boosted, threshold adjusted to 6.0°`
- 诊断字段：`downGazeActive` / `nodBoost`。

### 修复：息屏再开后需下拉状态栏（第三条路径）

v5.4 修了"恢复没被触发"和"恢复被错误标志挡住"，这一版找到**第三条**——在注入链上：

`AccessibilityBootstrap.repairIfNeeded()` 的第一句是
`if (GazeAccessibilityService.isConnected()) return`，而 `isConnected()` 只是
`instance != null`。**实例存在 ≠ 连接可用**：长时间息屏后无障碍连接可能已经失效，
但 `instance` 仍是旧引用。那种状态下：

- `repairIfNeeded()` 认为"已连接"，**什么都不做**；
- `dispatchGesture` 注入失败，只记一行日志就结束；
- 服务在跑、通知正常、摄像头也在分析，**但手势根本到不了目标 App**。

这正是"下拉状态栏就好了"的成因——下拉会产生一批窗口事件，让系统重新激活那个失效的连接。

修复：
- 新增 `AccessibilityBootstrap.forceRebind()`：**无条件**把无障碍条目关掉再打开，
  不等用户去下拉（`repairIfNeeded` 因为会提前返回，做不到这件事）。
- 新增**注入失败反馈回路**：连续 2 次注入失败就自动强制重连
  （`injFailures` 出现在自检日志里），成功一次即清零，避免偶发失败累积成误判。
- 亮屏恢复时也会检查一次无障碍绑定。

### 新增：全链路自检日志（便于定位偶发失效）

每 30 秒（以及亮屏时）打**一行**，把所有可能卡住的环节列全：

```
GazeSelfCheck: periodic powerInteractive=true screenActive=true running=true
  shouldAnalyze=true analyzing=true targetActive=true forceActive=true foreground=...
  cameraProvider=true cameraBound=true framesAgoMs=34 stale=false graceMs=0
  restartAttempts=0 probeFailures=0 rebinding=false a11yEnabled=true a11yConnected=true
  swipeReady=true backend=无障碍服务 injFailures=0 occlRemainMs=0 hardLock=false detectorArmed=true
```

判读方法：
| 现象 | 含义 |
| --- | --- |
| `powerInteractive=false` 而 `screenActive=true` | 电源状态错了（自校正未生效） |
| `a11yConnected=false` | 无障碍断开，注入必定失败 |
| `swipeReady=false` | 注入后端不可用 |
| `stale=true` 且 `cameraProvider=true` | 相机句柄在但没画面，需重绑 |
| `detectorArmed=false` | 摄像头在跑但检测器没被喂数据 |

---

## [5.4] - 2026-09-15

### 修复：息屏久了再打开抖音，偶尔仍需下拉状态栏（v5.3 只解决了一半）

v5.3 让恢复不再依赖「包名变化」，但**恢复本身可能被一个错误的标志挡住**。这一版找到两个死角：

**死角一：`screenActive` 会被错误的标志锁死。**
它只在收到 `ACTION_SCREEN_ON` / `ACTION_SCREEN_OFF` 广播时更新。HyperOS 省电策略下这些
广播**可能被丢掉**——一旦丢了，标志就永久停在错误值上：

```
广播丢失 → screenActive 停在 false
        → shouldAnalyze() 永远返回 false
        → 什么都不分析，而且 liveness 探针 / 看门狗 / 强恢复全被这一个条件挡住
        → 用户看到的："功能死了，下拉一下状态栏又活了"
```

修复：新增 **`syncPowerState()` 电源状态自校正**，每 800ms 用系统自己的
`PowerManager.isInteractive` 与标志比对，发现不一致就纠正并触发一次亮屏恢复。
`isInteractive` 是真相来源，不受广播丢失影响。
日志：`power state correction: screenActive=... but isInteractive=... — missed broadcast`

**死角二：`setScreenActive()` 和 `updateCameraState()` 都会"静默跳过"。**
前者有 `if (screenActive == active) return`；后者只在 `!cameraBound` 时才重绑。
于是"亮屏时标志已经是 true"或"相机栈坏了但标志还是 true"这两种情况，什么都不会发生。

修复：
- 亮屏时即使标志已为 true 也强制重新校准一次；
- 新增 **`onScreenAwake()`**：监听 `ACTION_SCREEN_ON` 与 **`ACTION_USER_PRESENT`**
  （解锁事件——亮屏但锁屏时相机拍到的是锁屏画面，恢复放在解锁后更准），
  无条件做一次**完整重建**：作废全部检测器与抑制状态 → 丢弃 provider 重新获取 →
  失败按 **500 / 1000 / 2000ms** 退避重试。
- 日志：`ACTION_USER_PRESENT -> proactive reactivation requested (...)`、
  `Periodic check: foreground=xxx, cameraRunning=false, rebind requested`。

> 附带修掉一个隐私问题：如果息屏广播丢了，相机原本会在**屏幕已熄灭的情况下继续运行**；
> 电源自校正现在也会把这种情况纠正回来。

### 优化：近距离（30cm）残余误触

v5.3 已经做了眨眼阈值收紧、静止锁定、速度门限、姿势偏置校正。这一版补齐剩下两处**不对称**：

- **扭头阈值现在也按距离放大**（v5.3 只放大了俯仰）。近距离下横向噪声同样被放大成十几度，
  而扭头阈值一直是固定的 20°，于是"手在脸旁晃一下"就够到。现在与俯仰共用同一套距离系数，
  近距离阈值变为 40°：真实扭头（实测 20~45°）仍够得到，噪声被挡在外面。
- **距离系数每帧只算一次**，俯仰与偏航共用同一份，保证两条轴行为一致（以前各算各的）。

诊断能力同步加强（便于定位残余误触的来源）：
- `lastTrigger=blink/nod/tilt/turnL/turnR` —— 上一次翻页是谁触发的
- `blinkBelow` / `blinkFrames` —— 眨眼判定**当前实际生效**的阈值与连续帧要求
- `staticHardLock` / `staticRange` / `staticLock` / `downGaze` / `biasRecenter`

> 近距离静止硬锁定（v5.3 引入）已满足本版要求：`faceRatio ≥ 0.55` 且连续 1.2 秒内
> 俯仰/偏航/眼睛开合度都无实质变化即停判。判据用「稳定时长」而非「方差」——
> 语义直观、不易被单个离群点影响，用户也能自行验证（"静止 1.2 秒后就不会误触"）。

### 新增：俯视姿态自动识别与补偿

用户大多数时间是**俯视**看手机（手机在下方、眼睛向下看），此时俯仰角长期偏负。
基准线若不跟着走，这个偏负的偏移会被当成"一直在低头"，白白吃掉阈值余量：
往下动一点立刻越界（误触），往上一动却要走很远（不灵敏）。

修复：把 v5.2 的「姿势偏置自动校正」扩展为显式的俯视检测。两条路径共用同一套动作，
只是触发所需的持续时间不同：

| 场景 | 条件 | 时长 |
| --- | --- | --- |
| **俯视姿态** | 相对基准线偏负 ≥ 3° | **2 秒** |
| 一般姿势偏置 | 偏离越过死区、未到阈值 | 1.5 秒 |

两者都要求「越过死区、但始终没到阈值」——到了阈值就是动作，绝不能把动作当成姿势。
判据基于**相对基准线**的偏移而非绝对角度，所以倒着拿手机、躺着看同样适用。
只在俯仰轴生效，不碰扭头与眨眼。

日志：`posture: looking down detected (held 2100ms, relative -6.2°), baseline gradually shifted to -14.8°`
诊断字段：`downGaze=N`（俯视补偿次数）、`biasRecenter=N`（一般姿势校正次数）。

---

## [5.3] - 2026-09-15

### 修复：切后台一段时间后，重开目标应用必须下拉状态栏才生效（长期遗留问题）

**根因**：`AppStateManager.commitPending()` 里有一句 `if (!force && packageName == foregroundPackage) return`，
导致整条恢复链**只在「前台包名发生变化」时**才会触发。于是当状态里记录的前台已经是抖音、
或这次切换压根没被观察到时：包名没变 → 服务收不到通知 → 相机不重绑 → 功能静默失效。

**下拉状态栏之所以"有效"**，是因为它人为制造了两次包名变化（`com.android.systemui` ↔ 抖音），
等于替我们触发了两次通知。这也解释了它为什么"有时出现有时不出现"，以及为什么跨了多个
版本都没根治——一直在修恢复动作本身，没修"恢复根本没被触发"这个前提。

**修复**：新增**主动存活探针**，由 `AppStateManager` 每 800ms 轮询驱动，不再依赖窗口事件。
三级递进恢复：
1. 应分析却无帧 → 强恢复（重绑相机）；
2. 连续多次仍无帧 → 丢弃 provider 重新获取（相机被其它进程占用时必需）；
3. 再不行 → 重建整条服务管线。

服务实例不存在时探针会直接把它拉起来。日志：`liveness probe: ... -> recovering`。

### 优化：30cm 近距离静止误触

**实测数据定位**：用户截图显示 `俯仰角 -2.08° / 基准线 -2.08°`（偏差 0°），
但 `眨眼累计 86 / 已触发 63` —— 偏差为 0 排除了点头，**误触来自眨眼检测**。

原因：ML Kit 的 `eyeOpenProbability` 是**绝对**读数、不随脸的大小变化，但离得越近，
睫毛 / 镜片反光 / 轻微眯眼造成的遮挡越明显，读数越容易跌破阈值。

**修复**（三道防线）：

1. **眨眼判定按距离收紧**：近距离时同时降低闭眼阈值（0.55 → 中距 0.41 → 近距 0.30）
   并提高连续帧要求（2 → 4 → 6）。只做一件不够——阈值降太多会漏掉真实眨眼，而真实眨眼
   在近距离下持续帧数本来就更多，所以提高帧数对真实眨眼几乎无损。
2. **近距离静止硬锁定**：`faceRatio ≥ 0.55` 且连续 1.2 秒内俯仰/偏航/眼睛开合度都无实质
   变化 → 判定"人没在动、画面在抖"，关掉全部翻页判定，直到出现明显动作。
   判据用"稳定时长"而非"方差"，语义直观、可自行验证。
3. 保留并复用 v5.2 的头部静止锁定与姿势偏置自动校正。

### 新增：触发来源诊断

诊断行新增 `lastTrigger=blink/nod/tilt/turnL/turnR`、`blinkBelow`、`blinkFrames`。
排查"静止疯狂误触"时这是第一个要看的东西——只看触发次数无法区分来源。

---

## [5.2] - 2026-09-15

### 修复：点头翻页完全失效（v5.1 引入的严重回归）

**根因**：v5.0 引入的**回中锁定**只在「角度回到中性区（`±阈值×0.4` = ±2.4°）并稳定 150ms」
时解除。而人的自然姿势常常长期偏离基准线（实测：`俯仰角 -2.85° / 基准线 3.69°`，偏差 6.5°），
**永远进不了中性区** → 锁永不解除 → 反向的点头被无限期屏蔽。

日志直接证实：`recenter lock released by timeout (400ms) — posture offset kept it out of the neutral zone`
——即中性区条件**从未生效过**，而当时并没有兜底。

**修复**：
- 加 **400ms 最长锁定兜底**（回正动作只要几百毫秒），同时这也是最初的设计意图
  （"回中抑制期 300–500ms"）；
- 让锁在头部**静止时**也参与解除判断（v5.1 把解除逻辑放在 onset 判断之后，静止时锁不释放）。

### 修复：速度采样错位

`evaluatePitch` / `evaluateYaw` 在放行路径上没有更新速度采样点，导致下一帧算出速度 0
而被最低速度门限误杀。统一收敛为「单一 reject 原因」结构，并新增诊断：

```
nodDown candidate rejected: pitch=-5.2° speed=0.0031°/ms gate=0.0120°/ms threshold=6.0° reason=speed-gate
```

拒绝原因：`below-onset` / `recenter-lock` / `below-threshold` / `slow-rise` / `hold-not-met` / `speed-gate`。

### 优化：姿势偏置自动校正（30cm 误触的根因）

实测偏差 6.5° 正好贴在 ±6° 阈值上——姿势**缓慢**变化（靠椅背、凑近屏幕）时，滑动窗口中位数
基准线跟不上，偏差被永久保留，白吃掉阈值余量。
新增：偏差越过死区、始终没到阈值、**且持续 1.5 秒**时，判定为「换了姿势」而不是动作，
把基准线渐进挪过去。判据干净：有意动作 500ms 内就结束或已触发，能维持 1.5 秒的只可能是姿势。

另外把**最低速度门限也按距离缩放**（50cm ×1 / 30cm ×2）：只抬高幅度门限挡不住偶尔偏大的
噪声样本，抬高速度门限才能区分开（噪声没有速度）。

---

## [5.1] - 2026-09-15

### 删除：头控音量功能

按要求整段移除（`HeadVolumeDetector.kt`、`AudioVolumeController.kt`、配置项、UI、字符串、
`AudioManager` 调用、诊断字段），全文扫描确认零残留，APK 资源表亦无残留。

> 实测澄清：该功能开关一直为 `false`（日志中 `vol=头控音量已关闭` 贯穿全程），
> 代码路径从未执行，因此删除它**不影响**翻页相关问题的表现。

### 修复：抖音等全屏播放器忽略注入的滑动手势

**发现**：日志显示 `GazeA11y: manual swipe ... ok=true` 但页面纹丝不动，且前台已正确识别为
抖音——手势发出去了、App 不响应。

**根因**：`dispatchGesture` 的**一条直线**相当于"一个采样点从起点直接跳到终点"，
而多数 App 的滚动手势识别（VelocityTracker + touch slop）期望多次坐标更新，
只看得到一次跳变就会忽略它。

**修复**：纵向滑动补 **6 个均匀分布的中间点**，让系统按真实手指轨迹派发多次 MOVE 事件。
已用**截屏像素对比**离线验证生效（设置页 ScrollView 被真实滚动，截图 MD5 改变）。

### 加强：近距离防误触

近距离静止门限与速度门限同步放大；手挡脸的多重遮挡判据（`face lost` / `mouth missing` /
`face size jump`）保留。

---

## [5.0] - 2026-09-14

### 修复：抖音不接受整屏滑动（参数化实测标定）

用新加的**参数化自测通道**（`am broadcast ... --ef from --ef to --el duration`）逐组试：

| 参数 | 抖音反应 |
| --- | --- |
| 90% / 130ms | 无效 |
| 60% / 400ms | 无效 |
| 45% / 350ms | 无效 |
| 50% / 700ms | 无效 |
| **39% / 150ms** | **有效，连试 3 次全部成功** |

结论：抖音认的是**短促快滑**，幅度过大反而被忽略（不是"越长越好"）。
抖音档因此从 `0.90→0.10 / 130ms` 改为 `0.70→0.30 / 150ms`。

### 新增：仰头回中锁定

修「仰头触发上滑后，把头放回正常位置的过程被判成点头，于是又下滑」——
仰头时俯仰角为正，回正会经过 0 继续变负，天然长得像一次反向动作。

### 新增：最低速度门限

噪声**有幅度但没有速度**：真实头部动作几十毫秒就能走完 6~20°，噪声做不到。
实测真实动作 0.037~0.136°/ms，门限 0.012°/ms，对正常动作零影响。

### 修复：张嘴检测基准被污染

实测抓到 `mouthBase=0.114`（正常闭嘴 0.205~0.23），一旦坏样本进了基准窗口就被永久带偏，
检测器**一直以为用户在张嘴** → 表现为"功能要开关一次才能用"。现在加合理性检查、
4 秒卡死自愈、遮挡后重学。

---

## [4.9] - 2026-09-14

- 修复偏航触发延迟打印出天文数字（`clearTurn()` 之后才计算延迟）。
- 新增**人脸丢失状态机**：脸短暂消失又回来 → 作废基准线 + 抑制。
  这是"手放脸上拿开就乱滑"的主要路径（手盖住整张脸时人脸是直接丢失的，
  而 v4.8 只判"有人脸但读不出嘴关键点"，漏掉了它）。
- **距离自适应**：静止门限按脸占比缩放（近 30cm ×2 / 中 50cm ×1.35）。
- 遮挡判据扩到三条：`face lost` / `mouth missing` / `face size jump`。

## [4.8] - 2026-09-14

- **速度驱动触发**：用相邻两帧角度差算角速度，超过门限（俯仰 0.02°/ms、偏航 0.03°/ms）
  就**立即触发**，不再等"保持时间"。实测仰头延迟从 211~329ms 降到 **71~207ms**。
  根因：无论动作多快都必须再等一整个 holdMs，而"动作要快"已由 motionWindowMs 把关，
  再叠一层保持时间属于重复约束。
- 保持时间改为 `min(holdMs, motionWindowMs)`，默认 200ms → 150ms。
- 新增静止锁定、遮挡抑制、脸丢失即作废基准线（150ms，以前 3 秒）。

## [4.7] - 2026-09-14

- 新增**全局使用翻页**开关（桌面 / 任意应用可触发，代价是摄像头常开）。
- 新增头控音量（默认关闭，v5.1 已删除）。
- 修复「切回目标应用后翻页失效、要手动滑一下才恢复」的第一版：引入强恢复 + 持续重试 +
  重绑宽限期。*（v5.3 才彻底解决，见上。）*

## [4.6] - 2026-09-14

- 张嘴从"暂停本 App"改为**点击屏幕中央**（交给目标 App 响应，抖音里即暂停/播放），
  并**不再冻结 App 自身**：张嘴之后眨眼、点头、扭头照常工作。
- 新增**自定义滑动柔度**滑块（列表类 10%–50% 屏高，默认 26%）+ 恢复默认按钮。

## [4.5] - 2026-09-14

- 新增**上下滑动自适应**：抖音整屏视频、微博/小红书/知乎/B站列表、其他应用各一套
  幅度与时长参数（全部是屏幕高度比例，不写死像素）。
- 新增张嘴检测（`MouthOpenDetector`）：嘴到鼻底距离 ÷ 脸高，并按本人闭嘴水平自动校准；
  必须"闭嘴后再次张嘴"才算下一次。

## [4.4] - 2026-09-14

- 新增**左右扭头滑动**（偏航轴，与俯仰轴完全独立计时；前置摄像头镜像已在检测器里翻正）。
- 新增连续点头双击（v4.5 起被"张嘴"取代，v5.1 全部移除）。

## [4.3] - 2026-09-14

- 新增**全局防误触冷却**（`GlobalTriggerGate`）：眨眼与点头**共用同一个冷却计时器**，
  闸门卡在所有触发路径的唯一汇合点 `fireSwipe()`，从根上消除"一次动作翻好几页"
  以及上下滑交替触发。
- 冷却开关 + 500–5000ms 时长滑块，SharedPreferences 持久化，设置页实时显示倒计时。

---

## 更早版本

- **v4.2 及以前**：眨眼翻页、点头/仰头翻页、按需启停省电、目标 App 白名单、
  Shizuku / 无障碍双路径手势注入、权限记忆、相机自愈看门狗。
