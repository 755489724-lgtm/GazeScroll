# GazeScroll v5.3

> 用**前置摄像头**检测眨眼、点头、扭头，自动翻页 —— 刷视频不用手。

本次是一个**以修 bug 为主**的版本，重点是解决一个跨了多个版本、一直没根治的老问题。

---

## 本版重点

### 🐛 修复：切后台一段时间后，重开目标应用必须下拉状态栏才生效

这是长期反馈的"老毛病"，v5.3 找到了**具体的代码死角**，不是玄学。

**根因**：`AppStateManager.commitPending()` 里有一句

```kotlin
if (!force && packageName == foregroundPackage) return
```

整条恢复链**只在「前台包名发生变化」时**才会触发。于是当状态里记的前台已经是抖音
（或这次切换压根没被观察到）时：包名没变 → 服务收不到通知 → **相机不重绑 → 功能静默失效**。

**下拉状态栏之所以"有效"**，是因为它人为制造了两次包名变化
（`com.android.systemui` ↔ 抖音），等于替我们触发了两次通知。

这也解释了它为什么"有时出现有时不出现"，以及为什么之前几版都没治好 ——
一直在修恢复动作本身，没修"恢复根本没被触发"这个前提。

**修复**：新增**主动存活探针**，由 `AppStateManager` 每 800ms 轮询驱动，不再依赖窗口事件。
三级递进恢复：强恢复（重绑相机）→ 丢弃 provider 重新获取 → 重建整条服务管线。
服务实例不存在时探针直接把它拉起来。

### 🐛 优化：30cm 近距离静止误触

用户截图数据 `俯仰角 -2.08° / 基准线 -2.08°`（偏差 0°）+ `眨眼累计 86 / 已触发 63`
直接定位了原因：偏差为 0 排除了点头，**误触来自眨眼检测**。

ML Kit 的 `eyeOpenProbability` 是**绝对**读数、不随脸的大小变化，但离得越近，
睫毛 / 镜片反光 / 轻微眯眼的遮挡越明显，读数越容易跌破阈值。

三道防线：

1. **眨眼判定按距离收紧** —— 近距离时同时降低闭眼阈值（0.55 → 0.41 → 0.30）
   并提高连续帧要求（2 → 4 → 6）。只做一件不够：阈值降太多会漏掉真实眨眼，
   而真实眨眼在近距离下持续帧数本来就更多，所以提高帧数对真实眨眼几乎无损。
2. **近距离静止硬锁定** —— `faceRatio ≥ 0.55` 且连续 1.2 秒内俯仰/偏航/眼睛开合度
   都无实质变化 → 关掉全部判定，直到出现明显动作。
3. 复用 v5.2 的头部静止锁定与姿势偏置自动校正。

### 🔍 新增：触发来源诊断

诊断行新增 `lastTrigger=blink/nod/tilt/turnL/turnR`、`blinkBelow`、`blinkFrames`。
排查误触时这是第一个要看的东西 —— 只看触发次数无法区分来源。

---

## 完整功能列表

### 触发方式
- **眨眼翻页** —— 连眨 1 / 2 / 3 次可配（默认 2 次），灵敏度三档（闭眼阈值 0.60 / 0.55 / 0.45）
- **点头翻页** —— 向下翻页 / 上一个视频
- **仰头上滑** —— 向上翻页 / 下一个视频
- **左右扭头滑动** —— 左 / 右滑，灵敏度三档（14° / 20° / 28°）
- **张嘴点击屏幕中央** —— 抖音等全屏播放器里即暂停 / 播放视频；**不会暂停 App 自身**
- **头控音量** —— 已删除（v5.1）

### 滑动自适应
- **按前台应用自动切换**：
  | 应用 | 滑动距离 | 时长 |
  | --- | --- | --- |
  | 抖音 / 快手等整屏视频 | 39% 屏高 | 150ms |
  | 微博 / 小红书 / 知乎 / B站等列表 | 可调，默认 26% | 420ms |
  | 其他 | 40% | 300ms |
- **自定义滑动柔度滑块** —— 列表类 10%–50% 屏高，每档 2%，附「恢复默认」
- 参数全部是屏幕高度比例，不写死像素

### 防误触
- **全局冷却** —— 眨眼与点头共用同一个计时器，500–5000ms 可调（默认 1500ms）
- **遮挡抑制** —— `face lost` / `mouth missing` / `face size jump` 三判据
- **静止锁定** —— 人不动时自动抬高阈值
- **距离自适应** —— 静止门限与速度门限按脸占比缩放
- **回中锁定** —— 仰头回正不被判成点头（带 400ms 兜底）
- **最低速度门限** —— 噪声有幅度但没有速度
- **姿势偏置自动校正** —— 慢速姿势变化会更新基准线，不再吃掉阈值余量
- **近距离静止硬锁定** —— 30cm 静止 1.2 秒后彻底停判

### 运行模式
- **节能模式 / 白名单** —— 只在勾选的目标应用里生效，切走自动释放相机
- **全局使用翻页** —— 桌面 / 任意应用都能翻页（代价是摄像头常开）
- **三级功耗模型** —— 运行 / 温机（保留相机丢帧）/ 释放
- **主动存活探针** —— 相机卡死自动恢复，不依赖窗口事件

### 设置页
- 全部触发方式与灵敏度可调，改完即时生效并持久化
- **实时数值诊断面板**：人脸占比、距离档、俯仰/偏航角与基准线、张嘴比例与基准、
  当前滑动配置、冷却倒计时、静止/回中锁定状态、上一次触发来源

---

## 安装

1. 下载 `gazescroll-5.3-debug.apk` 并安装（允许「安装未知来源应用」）
2. 按 README 里的 ADB 步骤完成一次授权（Shizuku 或无障碍开启 `WRITE_SECURE_SETTINGS`）
3. 打开 App 授予摄像头与通知权限，勾选要生效的应用即可

> APK 是 **debug 签名**，仅供测试使用。

---

## 从 v4.x / v5.0 升级

直接覆盖安装即可，用户设置会保留并自动迁移。需要注意的历史变更：

- v4.4 的「连续点头双击」、v4.7 的「头控音量」已先后移除
- v4.5 的「张嘴暂停 App」在 v4.6 改为「张嘴点击屏幕中央」
- 抖音滑动参数在 v5.0 由 80%/130ms 改为 39%/150ms（旧参数在抖音里完全无效）

---

## 验证方式

本版两条修复都有实机证据：

- **唤醒问题**：`liveness probe` 日志 + 探针在正常状态下不误报（不产生多余重绑）
- **30cm 误触**：诊断行可读到 `blinkBelow=0.30 blinkFrames=6 staticHardLock=true`（近距离时）

排查任何触发问题，先看诊断行的 `lastTrigger` 与 `* candidate rejected` 的 `reason` 字段。
